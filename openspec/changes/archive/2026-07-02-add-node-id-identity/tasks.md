## 1. Migration + schema snapshot

- [x] 1.1 Create `yascheduler/infra/persistence/sql/migrations/002_add_node_id.sql` with content `ALTER TABLE yascheduler_nodes ADD COLUMN node_id SERIAL PRIMARY KEY;`. Verify: file is a single valid statement; `ALTER TABLE ... ADD COLUMN node_id SERIAL PRIMARY KEY` is valid on a populated table (PG creates `yascheduler_nodes_node_id_seq`, backfills existing rows, adds PK).
- [x] 1.2 In `yascheduler/infra/persistence/sql/schema.sql`, bump the `last_migration` CONSTANT in the DO block from `'001'` to `'002'` (line 8). Verify: the CONSTANT is the single edit point.
- [x] 1.3 In `schema.sql`, update the `CREATE TABLE IF NOT EXISTS yascheduler_nodes` snapshot to add `node_id SERIAL PRIMARY KEY` as the FIRST column (before `ip VARCHAR(15) UNIQUE`); keep `ip VARCHAR(15) UNIQUE` and all other columns unchanged. Verify: snapshot DDL matches what a fresh DB would have after the migration; `ip UNIQUE` is retained.
- [x] 1.4 Confirm the migration edit-procedure unit test (if one exists scanning `migrations/` for `prefix_id` uniqueness and asserting `schema.sql` CONSTANT == latest migration `prefix_id`) still passes with `'002'` and the new file. If no such test exists, do NOT add one (out of scope). Verify: `uv run pytest -m unit` for migration-runner tests passes.

## 2. Domain types — `NodeId`, `NewNode`, `Node`

- [x] 2.1 In `yascheduler/domain/model.py`, add `NodeId` as `@dataclass(frozen=True)` with field `value: int`; `__post_init__` that raises `ValueError` when `value <= 0`; `__str__` returning `str(self.value)`. Add GRACE `START_CONTRACT: NodeId` block (PURPOSE/INPUTS/OUTPUTS/SIDE_EFFECTS/LINKS). Verify: `NodeId(5)` ok; `NodeId(0)` and `NodeId(-1)` raise; `str(NodeId(5)) == "5"`; `NodeId(5) == 5` is False; `hash(NodeId(5))` works.
- [x] 2.2 In `yascheduler/domain/model.py`, add `NewNode` as `@dataclass(frozen=True)` with fields `ip: str`, `ncpus: int`, `enabled: bool = True`, `cloud: str | None = None`, `username: str = "root"`, `port: int = 22` (mirrors `Node`'s non-id fields with identical defaults; NO `node_id`). Add GRACE `START_CONTRACT: NewNode`. Verify: `NewNode(ip="x", ncpus=4)` constructs with the defaults; no `node_id` attribute.
- [x] 2.3 In `yascheduler/domain/model.py`, change the existing `Node` dataclass: add `node_id: NodeId` as the FIRST field (before `ip`); keep `ip: str`, `ncpus: int`, `enabled: bool = True`, `cloud: str | None = None`, `username: str = "root"`, `port: int = 22`. Field order is dataclass-valid (3 non-default fields first, then defaulted). Add a GRACE `START_CONTRACT: Node` update note (now post-persistence, carries `NodeId`). Verify: `Node(node_id=NodeId(1), ip="x", ncpus=4)` constructs; `node.node_id` is a `NodeId`.
- [x] 2.4 Export `NewNode` and `NodeId` from `yascheduler/domain/__init__.py` (and ensure they are importable from `yascheduler.domain.model`). Update the `START_MODULE_MAP`/`START_CHANGE_SUMMARY` in `model.py` and the domain facade. Verify: `from yascheduler.domain import NewNode, NodeId` and `from yascheduler.domain.model import NewNode, NodeId` both resolve.

## 3. `NodeRepository` port

- [x] 3.1 In `yascheduler/domain/ports.py` `NodeRepository` Protocol: rename `add(node: Node) -> None` → `insert(new_node: NewNode) -> Node`; add `get_by_id(node_id: NodeId) -> Node | None`. Keep `update`, `get(ip)`, `add_tmp`, `enable`, `disable`, `remove`, `list_enabled`, `list_disabled`, `list_all`, `get_by_ips`, `count_by_status` unchanged. Add TYPE_CHECKING imports for `NewNode`, `NodeId`. Update the `START_MODULE_MAP`/`START_CHANGE_SUMMARY`/`START_CONTRACT: NodeRepository` (if present). Verify: Protocol imports cleanly; `get_by_id` and `insert` signatures match the spec.

## 4. SQL queries

- [x] 4.1 Create `yascheduler/infra/persistence/sql/node/get_by_id.sql`: `SELECT node_id, ip, ncpus, enabled, cloud, username, port FROM yascheduler_nodes WHERE node_id = :node_id;`. Verify: pg8000 binds `:node_id`.
- [x] 4.2 In `yascheduler/infra/persistence/sql/node/insert.sql`, add `RETURNING node_id` to the INSERT statement. Verify: the statement is `INSERT INTO yascheduler_nodes (ip, ncpus, enabled, cloud, username, port) VALUES (:ip, :ncpus, :enabled, :cloud, :username, :port) RETURNING node_id;`.
- [x] 4.3 Add `node_id` as the first column to the SELECT lists of `node/get_by_ip.sql`, `node/list_all.sql`, `node/get_by_ips.sql`, `node/list_enabled.sql`, `node/list_disabled.sql`. Verify: each SELECT lists `node_id, ip, ncpus, enabled, cloud, username, port`.
- [x] 4.4 In `node/list_all.sql`, append `ORDER BY node_id`. Verify: the file selects all columns and orders by `node_id`.
- [x] 4.5 Leave `node/update.sql`, `enable.sql`, `disable.sql`, `remove.sql`, `insert_tmp.sql`, `count_by_cloud.sql`, `count_by_status.sql` UNCHANGED. Verify: grep shows no `node_id` added to those files.

## 5. `PostgresNodeRepository` impl

- [x] 5.1 In `yascheduler/infra/persistence/postgres.py`, rename `add` → `insert(self, new_node: NewNode) -> Node`. Run `load_query("node/insert")` with `new_node`'s fields; read `rows[0]["node_id"]`; return a `Node` built from `_row_to_node(rows[0])` (which now includes node_id). Update the `START_CONTRACT: insert` (PURPOSE: persist a NewNode, return the persisted Node with generated NodeId; LINKS: node/insert.sql RETURNING node_id, _row_to_node). Verify: returns a `Node` carrying a `NodeId` matching the DB-generated value.
- [x] 5.2 Add `get_by_id(self, node_id: NodeId) -> Node | None`: `rows = await self._run(load_query("node/get_by_id"), node_id=node_id.value)`; return `None` if empty else `_row_to_node(rows[0])`. NOTE the `.value` unwrap (pg8000 cannot adapt a dataclass). Add `START_CONTRACT: get_by_id` (LINKS: node/get_by_id.sql). Verify: `get_by_id(NodeId(5))` passes `5` as the SQL param; returns `Node` or `None`.
- [x] 5.3 Update `_row_to_node` to read `node_id` and wrap `NodeId(int(row["node_id"]))` — `Node(node_id=NodeId(int(row["node_id"])), ip=row["ip"], ncpus=row.get("ncpus") or 0, enabled=bool(row.get("enabled", False)), cloud=row.get("cloud"), username=row.get("username", "root"), port=row.get("port", 22))`. Update the `START_CONTRACT: _row_to_node` INPUTS to include `node_id`. Verify: a row `{"node_id": 7, ...}` maps to `Node(node_id=NodeId(7), ...)`.
- [x] 5.4 Leave `update`, `enable`, `disable`, `remove`, `add_tmp`, `list_enabled`/`list_disabled` (post-filter), `get_by_ips`, `count_by_*` unchanged. Verify: grep confirms no `WHERE node_id` introduced into those; `add_tmp` still returns `rows[0]["ip"]`.

## 6. `CloudProvisioner.allocate` → `NewNode`

- [x] 6.1 In `yascheduler/domain/ports.py` `CloudProvisioner` Protocol, change `allocate(provider: str) -> Node` → `allocate(provider: str) -> NewNode`. Update the requirement text comment if present. Verify: Protocol type-checks.
- [x] 6.2 In `yascheduler/infra/cloud/manager.py` `_setup_vm`, change the final `return Node(...)` to `return NewNode(...)` (drop the non-existent node_id; keep `ip=ip_addr, ncpus=ncpus, enabled=True, cloud=adapter.name, username=config.username, port=22`). Update the `START_CONTRACT: _setup_vm` OUTPUTS to `NewNode`. Verify: `_setup_vm` returns a `NewNode`.
- [x] 6.3 In `CloudProvisionerImpl.allocate`, update the return type annotation/contract to `NewNode` (the body returns `_setup_vm`'s result). Update the `START_CONTRACT: allocate` OUTPUTS. Verify: `allocate` returns `NewNode`.
- [x] 6.4 In `yascheduler/application/allocate_task.py` (call site ~line 381), change `await uow.nodes.add(node)` → `node = await uow.nodes.insert(node)` where `node` is the `NewNode` returned by `provisioner.allocate(...)`. Capture the returned persisted `Node` if a downstream use needs it; otherwise discard. Update the local variable type comment. Verify: the cloud node is persisted and the `Node` carries a `NodeId`.
- [x] 6.5 Audit `orchestrator.py` and `deallocate_nodes.py` for how they consume `allocate`'s return; they hold a `NewNode` now. Where they need persisted identity, they obtain it via `insert`'s return (allocate_task is the persist site) or read. If they only forward the object to `insert`, no change beyond the type. Update any type annotations from `Node` → `NewNode` on the relevant variables. Verify: `uv run pytest -m unit` passes; mypy/zuban if run.
- [x] 6.6 Update unit/e2e tests that mock `CloudProvisioner.allocate` to return a `NewNode` (not a `Node`): grep `tests/` for `allocate` mocks and `provisioner.allocate`. Verify: `uv run pytest -m unit` passes.

## 7. `yanodes` — display `node_id`

- [x] 7.1 In `yascheduler/entrypoints/cli/show_nodes.py`, add `node_id: NodeId` to the `_NodeView` frozen dataclass (first field, identity first). Import `NodeId` from `yascheduler.domain`. Verify: `_NodeView` carries `node_id`.
- [x] 7.2 In `_fetch_nodes_view`, set `node_id=node.node_id` when building each `_NodeView`. Verify: each row carries the node's `NodeId`.
- [x] 7.3 In `_render_nodes_table`, add `NODE_ID` as the FIRST column header and the first cell `str(row.node_id)` (no display transformation). Verify: header is `NODE_ID, IP, PORT, NCPUS, ENABLED, CLOUD, TASK_ID, LABEL`; NODE_ID cell renders the bare int.
- [x] 7.4 In `_render_nodes_json`, add `"node_id": r.node_id.value` to each object (before `"ip"`). Verify: JSON includes `"node_id": <int>`; serializes without error (uses `.value`).
- [x] 7.5 Bump `show_nodes.py` `VERSION` and `START_CHANGE_SUMMARY` (LAST_CHANGE: add NODE_ID column/JSON field / add-node-id-identity). Verify: `uv run pytest -m unit` for show_nodes passes.

## 8. `yasetnode` — accept node_id positional

- [x] 8.1 In `yascheduler/entrypoints/cli/manage_node.py`, add a frozen dataclass `NodeTarget` with `node_id: NodeId | None` and `host_spec: HostSpec | None` (exactly one set). Import `NodeId`. Verify: dataclass is frozen; construction works.
- [x] 8.2 Add `_parse_node_target(s: str) -> NodeTarget` argparse type: if `s.isdigit()` → `NodeTarget(node_id=NodeId(int(s)), host_spec=None)`; else `NodeTarget(node_id=None, host_spec=_parse_host_spec(s))`. Keep `_parse_host_spec` UNCHANGED. Add `START_CONTRACT: _parse_node_target`. Verify: `_parse_node_target("5")` → `NodeTarget(node_id=NodeId(5), host_spec=None)`; `_parse_node_target("10.0.0.1")` → host_spec path; `_parse_node_target("-5")` falls through to `_parse_host_spec` (rejects).
- [x] 8.3 Change the positional `host` argument's `type=` from `_parse_host_spec` to `_parse_node_target` in `_parse_node_args`. The `args.host` is now a `NodeTarget`. Verify: parser produces `args.host: NodeTarget`.
- [x] 8.4 In `_manage_node_async`, branch the validation `already_there` check: if `args.host.node_id is not None` → `already_there = await uow.nodes.get_by_id(args.host.node_id) is not None`; else (host_spec path) → `already_there = await uow.nodes.get(args.host.host_spec.host) is not None` (current behavior). Verify: both branches resolve presence correctly.
- [x] 8.5 Add the add-by-id rejection: after `parse_args`, if `args.host.node_id is not None and not (args.remove_soft or args.remove_hard)` → call `parser.error("a node cannot be added by id; provide a host like user@host[:port][~ncpus]")` (exit 2). Place near the existing `--skip-setup × remove` `parser.error`. Verify: `yasetnode 5` exits 2 with the message; `yasetnode 5 --remove-soft` does NOT hit this branch.
- [x] 8.6 Refactor `_remove_node_hard` and `_remove_node_soft` to accept the `NodeTarget` (or the resolved `Node` + ip) instead of `HostSpec`. On the node_id path: resolve `node = await uow.nodes.get_by_id(args.host.node_id)` (in the dispatch UoW), then use `node.ip` for `tasks.list_ids_by_ip_and_status(node.ip, TaskStatus.RUNNING)` and `nodes.disable(node.ip)` / `nodes.remove(node.ip)`. On the host_spec path: use `spec.host_spec.host` as the ip (current behavior). Keep the ip-keyed mutators unchanged. Verify: `yasetnode 5 --remove-soft` resolves the node by id and removes/disables by ip; `yasetnode 1.2.3.4 --remove-soft` unchanged.
- [x] 8.7 Pass `NodeTarget` (and the resolved node/ip) through `_add_node` on the host_spec path only (add-by-id is already rejected in 8.5). `_add_node` uses `spec.host_spec.host` for SSH connect and `NewNode(ip=spec.host_spec.host, port=spec.host_spec.port, username=..., ncpus=..., enabled=True)` then `node = await uow.nodes.insert(new_node)`. The add success message stays EXACTLY `Added host to yascheduler: {host}:{port}` (unchanged per the frozen `cli-commands` spec — do NOT add `node_id` to the output). Verify: `yasetnode 1.2.3.4` adds via `insert(NewNode(...))` and prints the unchanged success message.
- [x] 8.8 Update the success-message `{host}` sourcing: on the node_id remove path, `{host}` is `node.ip` (resolved from `get_by_id`); on the host path, `{host}` is `HostSpec.host`. Verify: `yasetnode 5 --remove-hard` prints `Removed host from yascheduler: <node.ip>`.
- [x] 8.9 Bump `manage_node.py` `VERSION` and `START_CHANGE_SUMMARY` (LAST_CHANGE: positional accepts node_id via _parse_node_target/NodeTarget; add→insert / add-node-id-identity). Verify: `uv run pytest -m unit` for manage_node passes.

## 9. Tests

- [x] 9.1 Unit: `tests/unit/test_persistence_adapter.py` (or the relevant node-repo unit test) — update `Node` constructions to include `node_id=NodeId(...)` for DB-read assertions; update `add` calls to `insert(NewNode(...))` and assert the returned `Node` carries a `NodeId`. Add `test_get_by_id_returns_node` and `test_get_by_id_missing_returns_none`. Verify: `uv run pytest -m unit` passes.
- [x] 9.2 Integration: `tests/integration/test_persistence_adapter.py` — add/adjust: insert a `NewNode`, assert `get_by_id(NodeId(n))` returns a `Node` with matching fields + `node_id`; assert `list_all()` returns rows ordered by `node_id`; assert `get_by_id(NodeId(99999))` is `None`. Verify: `uv run pytest -m integration` passes (testcontainer Postgres applies the new migration).
- [x] 9.3 Integration: add/adjust a migration test confirming `002_add_node_id.sql` applies on a legacy-style DB (no node_id column) and backfills existing rows with sequential `node_id` SERIAL values; fresh DB (snapshot has node_id) is seeded to `'002'` and skips the migration. Verify: `uv run pytest -m integration` migration tests pass.
- [x] 9.4 Unit: `tests/unit/test_domain_ports.py` (or model test) — add `NodeId` value-object tests: `__post_init__` rejects ≤0; `__str__` renders bare int; `== int` is False; hashable. Add `NewNode` has-no-node_id test. Verify: `uv run pytest -m unit` passes.
- [x] 9.5 Unit: `tests/unit/test_cloud_provisioner_impl.py` (and `test_cloud_alloc_session_lifecycle.py`) — update `allocate` mocks to return `NewNode`; update `allocate_task` tests to assert `nodes.insert` is called with a `NewNode` and returns a `Node`. Verify: `uv run pytest -m unit` passes.
- [x] 9.6 Unit: `tests/unit/test_show_nodes.py` (if present) — update `_NodeView`/table/JSON assertions to expect `NODE_ID` first column and `"node_id"` JSON field. Verify: `uv run pytest -m unit` passes.
- [x] 9.7 Unit: add a `manage_node` parser test for `_parse_node_target`: digit → `NodeTarget(node_id=NodeId(n))`; non-digit → host_spec; `"0"` raises (via `NodeId(0)`); add-by-id `parser.error` (exit 2); remove-by-id resolves via `get_by_id`. Verify: `uv run pytest -m unit` passes.
- [x] 9.8 E2E: `tests/e2e/` — if any e2e asserts node table/JSON output or exercises `yasetnode` add/remove, update expectations for `node_id` display and the `insert`/`NewNode` path. Verify: `uv run pytest -m e2e` passes.

## 10. GRACE-lite knowledge graph + final validation

- [x] 10.1 Update `docs/knowledge-graph.xml`: in `M-DOMAIN-MODEL` `<annotations>` add `type-NodeId PURPOSE="Node primary-key value object (frozen dataclass wrapping int)"` and `type-NewNode PURPOSE="Pre-persistence node record (no node_id)"`; update the existing `class-Node` annotation PURPOSE to mention it now carries `node_id: NodeId`. In `M-DOMAIN-PORTS` update `NodeRepository`/`CloudProvisioner` annotations if they list methods (insert replaces add; allocate returns NewNode). Verify: `python3 scripts/grace_check.py` passes.
- [x] 10.2 Run static checks: `uv run zuban check`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run lint-imports`. Verify: all pass.
- [x] 10.3 Run spec validation: `openspec validate --all --json`. Verify: exit 0.
- [x] 10.4 Run GRACE-lite validation: `python3 scripts/grace_check.py --json`. Verify: exit 0.
- [x] 10.5 Run the test matrix: `uv run pytest -m unit`, `uv run pytest -m integration`, `uv run pytest -m e2e`. Verify: all pass.