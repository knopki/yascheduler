# Tasks — relocate-daemon-launchers-to-cli

Implementation checklist. Each task is scoped to ≤2 hours. Follow the order;
later tasks depend on earlier ones. Reference `design.md` for the how and
`specs/package-facades/spec.md` for the what.

## 1. Move `daemon_systemd.py` into `entrypoints/cli/`

- [x] 1.1 Create `yascheduler/entrypoints/cli/daemon_systemd.py` as a verbatim copy of `yascheduler/entrypoints/daemon/daemon_systemd.py` (preserve the full file: the `if __name__ == "__main__":` block, the `from yascheduler.infra.cli import daemonize` / `from yascheduler.shared import LOG_FILE` imports, the `MODULE_CONTRACT`/`MODULE_MAP`/`CHANGE_SUMMARY`). Do NOT touch the imports — they are already absolute facade paths.
- [x] 1.2 In `yascheduler/entrypoints/cli/daemon_systemd.py`, update the `# FILE:` header path to `yascheduler/entrypoints/cli/daemon_systemd.py`. Bump `VERSION` (1.7.0 → 1.8.0). Promote the existing `LAST_CHANGE` line (v1.7.0 `relocate-daemon-launchers`) to `PREVIOUS_CHANGE`; add a new `LAST_CHANGE` entry: `v1.8.0 - Relocated into yascheduler/entrypoints/cli/ subpackage (relocate-daemon-launchers-to-cli); the entrypoints/daemon/ subpackage was liquidated and the launcher is now a sibling of init/show_nodes/submit/manage_node`. The `MODULE_CONTRACT PURPOSE`/`SCOPE`/`DEPENDS: M-CLI-COMMANDS, M-SHARED`/`LINKS: M-CLI-COMMANDS, M-SHARED` and the `MODULE_MAP` stay unchanged.
- [x] 1.3 Delete `yascheduler/entrypoints/daemon/daemon_systemd.py`.

## 2. Move `daemon_sysv.py` into `entrypoints/cli/`

- [x] 2.1 Create `yascheduler/entrypoints/cli/daemon_sysv.py` as a verbatim copy of `yascheduler/entrypoints/daemon/daemon_sysv.py` (preserve the full file: `start_daemon`, the argparse `__main__` block, the `import argparse`/`os`/`daemon`/`from daemon import pidfile` imports, the `from yascheduler.infra.cli import daemonize` / `from yascheduler.shared import LOG_FILE, PID_FILE` imports, the `MODULE_CONTRACT`/`MODULE_MAP`/`CHANGE_SUMMARY`). Do NOT touch the imports — they are already absolute facade paths.
- [x] 2.2 In `yascheduler/entrypoints/cli/daemon_sysv.py`, update the `# FILE:` header path to `yascheduler/entrypoints/cli/daemon_sysv.py`. Bump `VERSION` (1.7.0 → 1.8.0). Promote the existing `LAST_CHANGE` line (v1.7.0 `relocate-daemon-launchers`) to `PREVIOUS_CHANGE`; add a new `LAST_CHANGE` entry: `v1.8.0 - Relocated into yascheduler/entrypoints/cli/ subpackage (relocate-daemon-launchers-to-cli); the entrypoints/daemon/ subpackage was liquidated and the launcher is now a sibling of init/show_nodes/submit/manage_node`. The `MODULE_CONTRACT PURPOSE`/`SCOPE`/`DEPENDS: M-CLI-COMMANDS, M-SHARED`/`LINKS: M-CLI-COMMANDS, M-SHARED`, the `MODULE_MAP` (`start_daemon - ...`), and the `start_daemon` body (incl. `working_directory=os.path.dirname(__file__)`) stay unchanged. The `MODULE_CONTRACT DEPENDS` and `LINKS` still reference `M-CLI-COMMANDS, M-SHARED` (the launcher still imports `yascheduler.infra.cli.daemonize` and `yascheduler.shared.{LOG_FILE,PID_FILE}`).
- [x] 2.3 Delete `yascheduler/entrypoints/daemon/daemon_sysv.py`.

## 3. Liquidate the `entrypoints/daemon/` subpackage

- [x] 3.1 Delete `yascheduler/entrypoints/daemon/__init__.py`.
- [x] 3.2 Remove the now-empty `yascheduler/entrypoints/daemon/` directory (confirm it contains no files other than the deleted `__init__.py`, `daemon_systemd.py`, `daemon_sysv.py`, and the `__pycache__/` dir; remove the `__pycache__/` dir too if present).

## 4. Update `entrypoints/cli/__init__.py` facade (declarative)

- [x] 4.1 In `yascheduler/entrypoints/cli/__init__.py`, bump `VERSION` (1.3.0 → 1.4.0). Revise the `MODULE_CONTRACT PURPOSE` to mention the daemon launchers alongside the CLI commands: `PURPOSE: Subpackage facade for the init, show_nodes, submit, manage_node CLI entry points and the daemon_systemd/daemon_sysv daemon launchers.` Revise `SCOPE` to: `SCOPE: no re-exports — init, show_nodes, submit, manage_node are invoked by console_script; daemon_systemd and daemon_sysv are invoked by path from service templates (via %YASCHEDULER_DAEMON_FILE% substitution produced by yainit). None are imported across layers.` Add `M-DAEMON-SYSTEMD` and `M-DAEMON-SYSV` to the `LINKS` (after `M-ENTRYPOINTS-CLI-MANAGE-NODE`). Do NOT add `__all__` (the facade currently has none — only a docstring). Do NOT add any re-export.
- [x] 4.2 Update the module docstring to: `"""Init, show_nodes, submit, manage_node CLI entry point and daemon_systemd/daemon_sysv daemon launcher subpackage facade."""`
- [x] 4.3 Promote the existing `LAST_CHANGE` (v1.3.0 `manage_node` sibling) to `PREVIOUS_CHANGE`; add a new `LAST_CHANGE`: `v1.4.0 - daemon_systemd.py and daemon_sysv.py now sibling residents (moved from entrypoints/daemon/ in relocate-daemon-launchers-to-cli); the entrypoints/daemon/ subpackage was liquidated; no re-exports added.`

## 5. Update `entrypoints/__init__.py` facade (declarative)

- [x] 5.1 In `yascheduler/entrypoints/__init__.py`, bump `VERSION` (2.0.0 → 2.1.0). Revise the `START_CHANGE_SUMMARY LAST_CHANGE` entry: from `v2.0.0 - client.py and aiida_plugin.py flat residents; daemon/ subpackage resident (daemon_systemd.py, daemon_sysv.py relocated from package root in relocate-daemon-launchers); only di.py and infra/cli/ remain deferred for follow-up.` → `v2.1.0 - client.py and aiida_plugin.py flat residents; daemon_systemd.py and daemon_sysv.py now residents of entrypoints/cli/ (moved from entrypoints/daemon/ in relocate-daemon-launchers-to-cli; the entrypoints/daemon/ subpackage was liquidated); only di.py and infra/cli/ remain deferred for follow-up.` Move the existing v2.0.0 line to `PREVIOUS_CHANGE`. The `MODULE_CONTRACT PURPOSE`/`SCOPE`/`DEPENDS`/`LINKS` and the `MODULE_MAP` stay unchanged (the facade's public surface is unchanged; only the resident-set commentary in `CHANGE_SUMMARY` is updated).

## 6. Update `entrypoints/cli/init.py` (yainit path rewrites)

- [x] 6.1 In `yascheduler/entrypoints/cli/init.py`, in `_init_systemd` (L103): rewrite `daemon_file = install_path / "entrypoints/daemon/daemon_systemd.py"` → `daemon_file = install_path / "entrypoints/cli/daemon_systemd.py"`. Leave the `install_path = Path(__file__).parent.parent.parent` computation (L60) unchanged — it still resolves to the `yascheduler/` package dir, which is the correct prefix for the new subpackage path. The `%YASCHEDULER_DAEMON_FILE%` substitution into the systemd unit file template (`yascheduler/data/yascheduler.service`, `ExecStart=/usr/bin/python3 %YASCHEDULER_DAEMON_FILE%`) now produces the new absolute path.
- [x] 6.2 In `yascheduler/entrypoints/cli/init.py`, in `_init_sysv` (L127): rewrite `daemon_file = install_path / "entrypoints/daemon/daemon_sysv.py"` → `daemon_file = install_path / "entrypoints/cli/daemon_sysv.py"`. The `%YASCHEDULER_DAEMON_FILE%` substitution into the SysV init.d template (`yascheduler/data/yascheduler.sh`, `yascheduler=%YASCHEDULER_DAEMON_FILE%`) now produces the new absolute path.
- [x] 6.3 Bump `VERSION` in the `# FILE:` header of `yascheduler/entrypoints/cli/init.py` (1.0.0 → 1.1.0) and add a `START_CHANGE_SUMMARY` entry: `LAST_CHANGE` describing the daemon-file path computation update (`entrypoints/daemon/daemon_*.py` → `entrypoints/cli/daemon_*.py` to track the relocated launchers). `PREVIOUS_CHANGE` stays as the v1.0.0 `relocate-init-command` entry. The `MODULE_CONTRACT PURPOSE`/`SCOPE`/`DEPENDS`/`LINKS` stay unchanged (the installer still writes service unit files and creates the DB schema; no new dependencies).

## 7. Knowledge graph

- [x] 7.1 In `docs/knowledge-graph.xml`, update `M-DAEMON-SYSTEMD <path>` (L117) from `yascheduler/entrypoints/daemon/daemon_systemd.py` → `yascheduler/entrypoints/cli/daemon_systemd.py`. The `<depends>M-CLI-COMMANDS, M-SHARED</depends>` stays unchanged. The empty `<annotations>` block stays.
- [x] 7.2 In `docs/knowledge-graph.xml`, update `M-DAEMON-SYSV <path>` (L125) from `yascheduler/entrypoints/daemon/daemon_sysv.py` → `yascheduler/entrypoints/cli/daemon_sysv.py`. The `<depends>M-CLI-COMMANDS, M-SHARED</depends>` stays unchanged. The `<fn-start_daemon PURPOSE="SysV daemon entry point with PID file management" />` annotation stays.
- [x] 7.3 In `docs/knowledge-graph.xml`, delete the entire `M-ENTRYPOINTS-DAEMON` module element (L132-138). Per GRACE-lite "module removed → drop M- entry" rule. Verify no `CrossLink` or `DF-*` entry references `M-ENTRYPOINTS-DAEMON` (verified: the ID appears only in its own block and in `yascheduler/entrypoints/daemon/__init__.py:7` LINKS, which is being deleted in task 3.1). The `DF-DAEMON-START` (L929: `M-DAEMON-SYSTEMD / M-DAEMON-SYSV -> M-CLI-COMMANDS`) stays unchanged — it references the launcher M-IDs (which are stable across the relocation), not the subpackage facade.
- [x] 7.4 Run `rg -n "M-ENTRYPOINTS-DAEMON\|entrypoints/daemon\|entrypoints\.daemon" docs/knowledge-graph.xml` and confirm zero hits (the subpackage facade element is gone, the launcher `<path>` values are updated). The `DF-DAEMON-START` line (referencing `M-DAEMON-SYSTEMD` / `M-DAEMON-SYSV` by M-ID, not by path) is the only remaining `DAEMON`-family reference and it is correct.

## 8. Spec delta

- [x] 8.1 The spec delta at `openspec/changes/relocate-daemon-launchers-to-cli/specs/package-facades/spec.md` is already authored (batch 3 of the propose flow). Re-read it and confirm it matches the design's D6 commitment: R1 enumeration line drops `yascheduler.entrypoints.daemon`; R1 scenarios reproduced verbatim against the current main spec (the in-flight `relocate-check-status-command` / `relocate-manage-node-command` own the scenario resident-list edits; this change's delta does NOT touch them); "Entrypoints layer facade" daemon-launcher prose paths + "Daemon launchers are not re-exported" scenario paths updated to `entrypoints/cli/daemon_*.py`; "Outside-layer-set exemptions" "Daemon launchers are layer-checked after migration" scenario module paths updated to `yascheduler.entrypoints.cli.daemon_systemd` / `.daemon_sysv`. No edits needed unless a discrepancy is found.
- [x] 8.2 Run `openspec validate --changes "relocate-daemon-launchers-to-cli" --json` and confirm `valid: true` with zero `issues`.

## 9. Verification

- [x] 9.1 `uv run pytest -m unit` — all unit tests pass, including `tests/unit/test_cli_init.py::TestServiceInstall::test_overwrites_existing_systemd_unit` (asserts `"daemon_systemd.py" in content` — basename preserved by the move) and `::test_overwrites_existing_sysv_script` (asserts `"daemon_sysv.py" in content` — basename preserved). Confirm no test asserts the full `entrypoints/daemon/` path (verified pre-implementation; if any is found, update it to `entrypoints/cli/daemon_*.py`).
- [x] 9.2 `uv run zuban check` — passes.
- [x] 9.3 `uv run ruff check .` and `uv run ruff format --check .` — pass.
- [x] 9.4 `uv run lint-imports` — passes. Confirm `yascheduler.entrypoints.cli.daemon_systemd` and `yascheduler.entrypoints.cli.daemon_sysv` are in layer 1 (`entrypoints`) and import downward to `yascheduler.infra.cli` (layer 2) and `yascheduler.shared` (layer 5) — trivially R3-compliant. Confirm the `forbidden: shared → config` contract still passes. Confirm no `infra → entrypoints` reverse import was introduced.
- [x] 9.5 `python3 scripts/grace_check.py` — exit 0. Confirm: updated `M-DAEMON-SYSTEMD`/`M-DAEMON-SYSV` `<path>` values valid, `M-ENTRYPOINTS-DAEMON` element removed (no dangling `LINKS:` references to it), no dangling `LINKS:` references to the deleted `entrypoints/daemon/__init__.py` path, the moved `daemon_*.py` file headers and CHANGE_SUMMARY entries valid, the updated `entrypoints/cli/__init__.py` / `entrypoints/__init__.py` / `init.py` contracts valid.
- [x] 9.6 `openspec validate --all --json` — passes (the change is valid; main specs are not yet synced — that happens at archive).
- [x] 9.7 Import smoke check: `python -c "import yascheduler.entrypoints.cli.daemon_systemd; import yascheduler.entrypoints.cli.daemon_sysv; print('import OK')"` — confirm the new module paths load (the `daemon_systemd` module's `__main__` block is not executed on import; the `daemon_sysv` module's `start_daemon` is defined at module level and imports cleanly). Also confirm `from yascheduler import Yascheduler` still resolves (unaffected) and `from yascheduler.entrypoints import Yascheduler` still resolves (unaffected).
- [x] 9.8 init.py path smoke check: run `python -c "from pathlib import Path; install_path = Path('yascheduler/entrypoints/cli/init.py').resolve().parent.parent.parent; p1 = install_path / 'entrypoints/cli/daemon_systemd.py'; p2 = install_path / 'entrypoints/cli/daemon_sysv.py'; assert p1.is_file() and p2.is_file(), (p1, p2); print('paths OK:', p1, p2)"` from the repo root — confirm both computed paths point at files that exist after the move.
- [x] 9.9 Stale-reference sweep: `rg -n "entrypoints/daemon|entrypoints\.daemon" --glob '!openspec/changes/archive/**' --glob '!*.egg-info/**' --glob '!openspec/changes/relocate-daemon-launchers-to-cli/**'` — confirm zero references to the old subpackage path in non-archived, non-egg-info, non-this-change files. (Acceptable hits: archived OpenSpec change proposals, this change's own propositional artifacts, and the pre-sync live `openspec/specs/package-facades/spec.md` lines that the delta replaces at sync/archive time — but those hits should be in `openspec/specs/package-facades/spec.md` which is touched only at archive; if any other live source/spec file references the old path, fix it.)
- [x] 9.10 Confirm the `entrypoints/daemon/` directory no longer exists: `test ! -d yascheduler/entrypoints/daemon && echo "directory removed" || echo "STILL EXISTS"`.
- [x] 9.11 Confirm no remaining `yascheduler.entrypoints.daemon` import path anywhere in the repo: `rg -n "yascheduler\.entrypoints\.daemon\b" --glob '!openspec/changes/**' --glob '!*.egg-info/**'` returns no hits.
- [x] 9.12 Sequencing note for archive: this change MUST be archived AFTER `relocate-check-status-command` and `relocate-manage-node-command` (per design D6), so its R1 delta is regenerated against the post-merge main spec. Record this as a note in the archive step (do not block implementation on it).