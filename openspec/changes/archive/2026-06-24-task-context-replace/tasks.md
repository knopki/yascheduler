## 1. Compat shim — re-export `Unpack`

- [x] 1.1 In `yascheduler/shared/compat.py`, add a version-branched `Unpack` re-export mirroring the existing `Self`/`ParamSpec` pattern: `if sys.version_info < (3, 11): from typing_extensions import Unpack` / `else: from typing import Unpack`. Add `"Unpack"` to `__all__`.
- [x] 1.2 In `yascheduler/shared/compat.py`, update `MODULE_MAP`: add a line `Unpack - Unpack type re-exported from typing_extensions for older Python versions` near the `Self`/`ParamSpec` lines. Update `START_MODULE_CONTRACT` `SCOPE` to mention `Unpack` alongside `Self` and `ParamSpec`.
- [x] 1.3 In `yascheduler/shared/compat.py`, bump `VERSION` (`1.6.0` → `1.7.0`) and add a `START_CHANGE_SUMMARY` entry: `LAST_CHANGE: v1.7.0 - Re-export Unpack (PEP 692) with version branch (task-context-replace).`, shift the existing `LAST_CHANGE` to `PREVIOUS_CHANGE`.
- [x] 1.4 In `yascheduler/shared/__init__.py`, add `Unpack` to the re-export from `.compat` (mirror the existing `Self`/`ParamSpec` re-export on the line `from .compat import ParamSpec, Self`). Update the `MODULE_MAP` lines mentioning `Self`/`ParamSpec` to mention `Unpack` too. Bump `VERSION` (`1.6.0` → `1.7.0`) and add a `START_CHANGE_SUMMARY` entry: `LAST_CHANGE: v1.7.0 - Re-export Unpack from .compat (task-context-replace).`, shifting the existing `LAST_CHANGE` to `PREVIOUS_CHANGE`. (If `shared/__init__.py` does not carry a VERSION/CHANGE_SUMMARY, skip the bump — but verify first via `rg -n "^# VERSION:|^# START_CHANGE_SUMMARY" yascheduler/shared/__init__.py`.)

## 2. Domain entity — `TaskContext.replace` + `TaskContextOverrides` + GRACE markup

- [x] 2.1 In `yascheduler/domain/model.py`, add the import `from typing import TypedDict` (stdlib; placed with the other `typing` imports near line 32) and add `Self, Unpack` to the import from `yascheduler.shared` (or add a new `from yascheduler.shared import Self, Unpack` line if no such import exists yet — check current imports first).
- [x] 2.2 In `yascheduler/domain/model.py`, define `class TaskContextOverrides(TypedDict, total=False):` with exactly 4 fields: `remote_folder: str | None`, `local_folder: str | None`, `error: str | None`, `extra: dict[str, object]`. Place it immediately above the `TaskContext` class definition (after `ProcessResult`, before `@dataclass(frozen=True) class TaskContext:`). Do NOT include `engine`, `webhook_url`, or `webhook_custom_params` — YAGNI.
- [x] 2.3 In `yascheduler/domain/model.py`, add the `TaskContext.replace` method inside `TaskContext` (after `from_metadata`, before the class ends). Signature: `def replace(self, **overrides: Unpack[TaskContextOverrides]) -> Self:`. Body: `return replace(self, **overrides)`. The bare `replace` resolves to the module-level `dataclasses.replace` import (lexical scope), not the method — no recursion.
- [x] 2.4 Add a `START_CONTRACT: TaskContext.replace` / `END_CONTRACT: TaskContext.replace` block above the method. Copy the shape of the `Task.record_event` contract: PURPOSE `Typed copy-with returning a new TaskContext`, INPUTS `{ **overrides: Unpack[TaskContextOverrides] - subset of fields to override }`, OUTPUTS `{ Self - new TaskContext instance with overrides applied }`, SIDE_EFFECTS `None`, LINKS `M-DOMAIN-MODEL`. Add a one-line note inside the contract: the bare `replace` in the body is the module-level `dataclasses.replace`, not the method (lexical scoping).
- [x] 2.5 In `yascheduler/domain/model.py`, update the `MODULE_MAP` (line 14): change the `TaskContext - Typed task metadata with arbitrary extras` line to `TaskContext - Typed task metadata with arbitrary extras; .replace() typed copy-with` and add a new line `TaskContextOverrides - TypedDict (total=False) of overridable TaskContext fields: remote_folder, local_folder, error, extra`.
- [x] 2.6 In `yascheduler/domain/model.py`, bump `VERSION` in the header (line 2): `1.11.0` → `1.12.0`. Update `START_CHANGE_SUMMARY` (lines 22-25): new `LAST_CHANGE: v1.12.0 - Add TaskContext.replace typed copy-with + TaskContextOverrides TypedDict (task-context-replace).`, shift existing `LAST_CHANGE` to `PREVIOUS_CHANGE`.

## 3. Migrate call sites to `TaskContext.replace`

- [x] 3.1 In `yascheduler/application/submit_task.py`, line 90: change `context = replace(task.context, remote_folder=remote_folder)` → `context = task.context.replace(remote_folder=remote_folder)`. The subsequent chain on line 91 (`task = task.with_context(context).with_event(...)`) stays.
- [x] 3.2 In `yascheduler/application/submit_task.py`, after the migration in 3.1, the `from dataclasses import replace` import (line 22) becomes dead — `replace` is no longer used as a bare function call anywhere in this file (the remaining `task.context.replace(...)` is a method call, not the imported function). Remove the import. Verify with `rg -n "(?<!\.)replace\(" yascheduler/application/submit_task.py` (negative lookbehind excludes `.replace(` method calls) — it should return no matches before removing. As a safety net, `uv run ruff check` (step 7.1) will flag F401 if the import is accidentally kept.
- [x] 3.3 In `yascheduler/application/submit_task.py`, bump `VERSION` (`1.2.0` → `1.3.0`) and add a `START_CHANGE_SUMMARY` entry: `LAST_CHANGE: v1.3.0 - Migrate replace(task.context, ...) to task.context.replace(...); drop dead dataclasses.replace import (task-context-replace).`, shift existing `LAST_CHANGE` to `PREVIOUS_CHANGE`.
- [x] 3.4 In `yascheduler/application/consume_task.py`, lines 98-103: change the multi-line `updated_context = replace(task.context, local_folder=..., remote_folder=..., extra={**...})` → `updated_context = task.context.replace(local_folder=..., remote_folder=..., extra={**...})`. Keep the same argument values and line structure; only the function-call target changes from `replace(` to `task.context.replace(`.
- [x] 3.5 In `yascheduler/application/consume_task.py`, after the migration in 3.4, the `from dataclasses import replace` import (line 26) becomes dead — `replace` is no longer used as a bare function call anywhere in this file (the remaining `task.context.replace(...)` is a method call). Remove the import. Verify with `rg -n "(?<!\.)replace\(" yascheduler/application/consume_task.py` (negative lookbehind excludes `.replace(` method calls) — it should return no matches before removing. As a safety net, `uv run ruff check` (step 7.1) will flag F401 if the import is accidentally kept.
- [x] 3.6 In `yascheduler/application/consume_task.py`, bump `VERSION` (`5.2.0` → `5.3.0`) and add a `START_CHANGE_SUMMARY` entry: `LAST_CHANGE: v5.3.0 - Migrate replace(task.context, ...) to task.context.replace(...); drop dead dataclasses.replace import (task-context-replace).`, shift existing `LAST_CHANGE` to `PREVIOUS_CHANGE`.
- [x] 3.7 In `yascheduler/domain/model.py`, line 237 (inside `Task.fail`): change `context=replace(self.context, error=reason),` → `context=self.context.replace(error=reason),`. The outer `replace(self, status=TaskStatus.DONE, context=...)` stays as `dataclasses.replace` (Task-level field replacement).
- [x] 3.8 In `yascheduler/domain/model.py`, line 259 (inside `Task.reject`): change `context=replace(self.context, error=reason),` → `context=self.context.replace(error=reason),`. The outer `replace(self, status=TaskStatus.DONE, context=...)` stays as `dataclasses.replace`.
- [x] 3.9 Verify `from dataclasses import ... replace` import in `model.py` (line 30) STAYS — it is still used by `Task.allocate_to`, `mark_running`, `complete`, `fail` (outer), `reject` (outer), `record_event`, `with_context`, `pull_events`, and `ConnectedMachine.occupy`/`release`. Do NOT remove it.

## 4. Knowledge graph annotations

- [x] 4.1 In `docs/knowledge-graph.xml`, in the `M-DOMAIN-MODEL` element (around line 218), add to its `<annotations>` block: `<fn-replace PURPOSE="Typed copy-with returning new TaskContext" />` (use the `fn-` prefix per GRACE-lite rules; place it near the `class-TaskContext` annotation) and `<type-TaskContextOverrides PURPOSE="TypedDict (total=False) of overridable TaskContext fields: remote_folder, local_folder, error, extra" />` (use the `type-` prefix). Do NOT touch `M-DOMAIN-MODEL <depends>` or `<path>`.
- [x] 4.2 In `docs/knowledge-graph.xml`, in the `M-SHARED` element (around line 103), add to its `<annotations>` block: `<type-Unpack PURPOSE="Unpack re-exported from typing_extensions for older Python versions" />` (use the `type-` prefix, matching the existing `<type-Self>` and `<type-ParamSpec>` entries on lines 110-111). Do NOT touch `M-SHARED <depends>` or `<path>`.

## 5. Unit tests — `TaskContext.replace` suite + drift-lock

- [x] 5.1 In `tests/unit/test_domain_model.py`, add a `TestTaskContextReplace` test class. Use `TaskContext` construction consistent with the existing test file's style. Construct a base `TaskContext(engine="fleur", remote_folder=None, local_folder=None, extra={"k0": "v0"})` for assertions. Import `TaskContextOverrides` from `yascheduler.domain.model` for the drift-lock test.
- [x] 5.2 Add test `test_replace_single_field_override`: `ctx = TaskContext(engine="fleur")`; `new = ctx.replace(remote_folder="/r/new")`; assert `new.remote_folder == "/r/new"` and `new.engine == "fleur"` and all other fields (`local_folder`, `webhook_url`, `webhook_custom_params`, `error`, `extra`) equal to `ctx`'s fields.
- [x] 5.3 Add test `test_replace_multi_field_override`: `ctx = TaskContext(engine="fleur")`; `new = ctx.replace(local_folder="/l", remote_folder="/r", extra={"k": "v"})`; assert `new.local_folder == "/l"`, `new.remote_folder == "/r"`, `new.extra == {"k": "v"}`, and non-overridden fields (`engine`, `webhook_url`, `webhook_custom_params`, `error`) preserved.
- [x] 5.4 Add test `test_replace_leaves_original_unchanged`: `ctx = TaskContext(engine="fleur", error=None)`; `new = ctx.replace(error="boom")`; assert `new.error == "boom"` and `ctx.error is None` (original unchanged — frozen dataclass).
- [x] 5.5 Add test `test_replace_no_overrides_returns_equal_copy`: `ctx = TaskContext(engine="fleur", remote_folder="/r")`; `new = ctx.replace()`; assert `new == ctx` and `new is not ctx` (equal but not identical).
- [x] 5.6 Add test `test_replace_error_field_override_chains_into_fail`: build a RUNNING task (via `task.allocate_to(ip).mark_running()`), then `result = task.fail("disk full")`. Assert `result.status == TaskStatus.DONE` and `result.context.error == "disk full"`. This verifies the migrated `Task.fail` internal (model.py:237 now uses `self.context.replace(error=reason)`) still produces the correct final context — the `error` field override flows through the new method end-to-end. (The standalone `error`-field override behavior is already covered by 5.4; this test adds the integration assurance that `Task.fail`'s internal migration is behavior-preserving.)
- [x] 5.7 Add test `test_taskcontext_overrides_keys_match_audited_usage` (the drift-lock test): `assert set(TaskContextOverrides.__annotations__) == {"remote_folder", "local_folder", "error", "extra"}`. This catches both directions of drift: a new call site overriding an unlisted field (type checker also catches this), and a TypedDict field with no call site (only this test catches it).
- [x] 5.8 Add test `test_replace_chains_through_with_context`: `task = Task(task_id=1, label="x", context=TaskContext(engine="fleur"))`; `new_ctx = task.context.replace(remote_folder="/r")`; `new_task = task.with_context(new_ctx)`; assert `new_task.context is new_ctx` and `new_task.context.remote_folder == "/r"`. This verifies the fluent chain at both layers (`TaskContext.replace` feeding `Task.with_context`).

## 6. Verify call-site migrations preserve behavior

- [x] 6.1 Run `uv run pytest -m unit tests/unit/test_application_use_cases.py -q` — confirm all existing submit_task and consume_task tests pass unchanged. Pay attention to assertions on `inserted_arg.context.extra`, `saved_arg.context.remote_folder`, `saved_task.context.error` — these are behavioral and should stay green.
- [x] 6.2 Run `uv run pytest -m unit tests/unit/test_application_orchestrator.py -q` — confirm the orchestrator tests pass (orchestrator's `_task_consumer_consumer` uses `task.fail("node is gone").with_event(TaskAbandoned, ...)`; the internal `Task.fail` migration in step 3.7 is behavior-preserving, so assertions on `saved_task.context.error` stay green).
- [x] 6.3 Run `uv run pytest -m unit tests/unit/test_domain_model.py -q` — confirm the new `TestTaskContextReplace` suite passes and no existing domain-model test regressed (especially the existing `TestTaskWithContext` suite from the prior change, and any `Task.fail`/`reject` tests that assert `context.error`).

## 7. Static checks + GRACE + OpenSpec validation

- [x] 7.1 `uv run ruff check .` — confirm no lint errors (especially the new method, the TypedDict, the migrated call sites, and the removed imports in submit_task/consume_task).
- [x] 7.2 `uv run ruff format --check .` — confirm no formatting violations; if violations, run `uv run ruff format .` and verify only the touched files are reformatted.
- [x] 7.3 `uv run zuban check` — confirm static analysis passes, especially `Unpack[TaskContextOverrides]` type resolution and the `Self` return type.
- [x] 7.4 `uv run lint-imports` — confirm the import-layer contract passes (the new `from yascheduler.shared import Self, Unpack` in `model.py` is intra-shared-kernel, no layer violation; `from typing import TypedDict` is stdlib).
- [x] 7.5 `python3 scripts/grace_check.py` — confirm knowledge-graph XML + source MODULE_CONTRACT checks pass, including: the new `<fn-replace>` and `<type-TaskContextOverrides>` annotations in `M-DOMAIN-MODEL`, the new `<type-Unpack>` annotation in `M-SHARED`, the updated `model.py` MODULE_MAP/CHANGE_SUMMARY/VERSION, the updated `compat.py` MODULE_MAP/CHANGE_SUMMARY/VERSION, and the `TaskContext.replace` START_CONTRACT block.
- [x] 7.6 `openspec validate --all --json` — confirm all specs validate (the delta specs in `openspec/changes/task-context-replace/specs/` and the live specs in `openspec/specs/`). Must exit 0.
- [x] 7.7 Stale-reference sweep: run `rg -nU "replace\([^)]*\.context|\.context\.replace\("` (multiline so the multi-line `task.context.replace(...)` in consume_task.py:98-103 is matched). Confirm the only remaining `replace(...context...)` occurrences in `yascheduler/` are: `model.py:237` and `model.py:259` now reading `self.context.replace(error=reason)` (migrated), `consume_task.py:98` reading `task.context.replace(...)` (migrated), `submit_task.py:90` reading `task.context.replace(...)` (migrated). No raw `replace(task.context, ...)` or `replace(self.context, ...)` should remain outside `model.py`'s `Task.with_context` (which uses `replace(self, context=context)` — Task-level, not TaskContext-level, and stays).

## 8. Full unit suite + final smoke

- [x] 8.1 `uv run pytest -m unit -q` — confirm the full unit suite passes (domain, application, orchestrator, persistence-mocked, config, cli, di).
- [x] 8.2 Smoke-check the new method: `python3 -c "from yascheduler.domain.model import TaskContext; c = TaskContext(engine='fleur'); r = c.replace(remote_folder='/r'); assert r.remote_folder == '/r' and c.remote_folder is None and r.engine == 'fleur'; print('TaskContext.replace OK')"`.
- [x] 8.3 Confirm `from yascheduler import Yascheduler` still imports cleanly (public API unaffected) — `python3 -c "from yascheduler import Yascheduler; print('public API OK')"`.
- [x] 8.4 Confirm the compat shim works on this runtime: `python3 -c "from yascheduler.shared import Self, Unpack; print('compat shim OK', Unpack.__name__ if hasattr(Unpack, '__name__') else Unpack)"`.