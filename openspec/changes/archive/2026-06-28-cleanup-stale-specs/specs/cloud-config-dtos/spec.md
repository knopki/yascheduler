## REMOVED Requirements

### Requirement: Cloud config DTOs relocated to infra
**Reason**: Merged into the new `cloud-config` capability's `Cloud config DTOs relocated to infra` requirement. Content preserved verbatim.
**Migration**: Read `cloud-config` — `Cloud config DTOs relocated to infra` requirement with all scenarios.