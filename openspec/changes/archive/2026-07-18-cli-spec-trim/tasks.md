## Common rules for every code-touching task

Every code-touching task below obeys these invariants. They exist because a
prior attempt at this change was discarded specifically for violating them.

- **GRACE fields are a closed set.** Allowed fields: `PURPOSE`, `SCOPE`,
  `INVARIANTS`, `USECASES`, `DEPENDENCIES`, `RATIONALE`, `KEYWORDS`,
  `REQUIRES`, `ENSURES`. No invented fields. Specifically: no `SHALL NOT:`
  pseudo-field, no `EFFECTS:`, no `EXAMPLES:`, no `NOTES:`, no free-form
  labels. The spec's removed `SHALL NOT` sentences do NOT become a `SHALL NOT:`
  contract field — they become an `INVARIANTS` entry stating the positive
  contract, or a `RATIONALE` Q/A if the rationale is the valuable part.
- **`RATIONALE` is Q/A format only**, answering "why is this entity shaped
  this way?". It is NOT a junk drawer for arbitrary prose, NOT a place to
  restate `PURPOSE`, NOT a place to dump the trimmed spec text. One Q and one
  A per item, multi-item allowed when there are distinct reasons.
- **`PURPOSE` answers WHY, not WHAT.** "Submit an AiiDA task via DI" is WHAT
  and fails. "Ensure an AiiDA submission script becomes a tracked task whose
  identity is observable to the AiiDA plugin's `int(stdout.strip())` parser"
  is WHY and passes.
- **Every `CLASS_*` / `FUNC_*` / `METHOD_*` region encloses the FULL entity.**
  For a class: the `@dataclass(...)` decorator (if any), the `class` line, the
  docstring, every field, every `__init__` line, every `self.<attr>` assignment,
  through the trailing blank line before the next region marker. For a
  function: the decorator (if any), the `def`/`async def` line, the entire
  body, the trailing blank line. A region that closes before its entity ends
  (e.g. wrapping only the contract comment) is a defect.
- **Comment-only diff.** No code logic, signature, decorator choice, docstring
  semantics, or import changes. Edits are `# region`/`# endregion` marker
  insertion and contract-field enrichment inside the marker block.

## 1. Apply the cli spec delta

- [x] 1.1 Apply the 25 MODIFIED requirements from `openspec/changes/cli-spec-trim/specs/cli/spec.md` to `openspec/specs/cli/spec.md`, replacing each original requirement block in place. Preserve requirement header text exactly (whitespace-insensitive match) so OpenSpec recognizes the MODIFIED operation. Headers to match (in spec order): `Shared argparse helpers`, `Shared daemon core for entry points`, `CLI commands call use cases via DI`, `yasubmit parses flags via argparse`, `yasubmit validates script content in the body`, `yasubmit preserves AiiDA stdout compatibility`, `Daemon launcher argparse and defaults`, `Daemon and CLI exit-code contract`, `yanodes lists nodes and their running tasks`, `yanodes parses flags via argparse`, `yanodes default table output format`, `yanodes --json output format`, `yanodes joins nodes to running tasks in memory`, `--json is the machine-readable CLI output convention`, `yasetnode parses host grammar via argparse type`, `yasetnode parses flags via argparse`, `yasetnode output channels and verbatim success messages`, `yasetnode positional discriminates node_id from host`, `yasetnode gateway lifecycle and resource safety`, `yasetnode dispatches add and remove paths`, `yastatus queries task status`, `yastatus default output format (AiiDA compatibility)`, `yastatus parses flags via argparse`, `yastatus --json output format`, `yastatus view mode connects via SSH with correct node params`.
- [x] 1.2 Remove the 4 duplicated per-command exit-code requirements from `openspec/specs/cli/spec.md` (per the REMOVED section of the delta): `yasubmit exit code contract`, `yanodes exit code contract`, `yasetnode exit code contract`, `yastatus exit code contract`. Each is fully subsumed by the shared `Daemon and CLI exit-code contract` requirement.
- [x] 1.3 Confirm the trimmed main spec contains zero `SHALL NOT` / `shall not` instances in requirement bodies (all 11 from the original spec have been relocated). Confirm every observable behavioral scenario (`#### Scenario:` count) is preserved: pre 111 → post 107 (107 = 111 − 4 single-scenario duplicates removed).
- [x] 1.4 `openspec validate --all --json` passes (exit 0). The change validates AND the trimmed main spec validates.

## 2. submit.py — wrap missing entities + enrich existing regions

Each new `CLASS_*` / `FUNC_*` region opens one line above the entity and closes one line below the entity body, enclosing the FULL entity per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 2.1 Add `# region CLASS_EngineNotDefinedError` ... `# endregion CLASS_EngineNotDefinedError` enclosing the FULL class block (class line, docstring, `__init__`, `super().__init__` call, trailing blank). `PURPOSE` (WHY: signal that a submission script lacks an `ENGINE` declaration so the submit path surfaces the gap rather than silently failing later). `ENSURES` (message is the literal `"Script has not defined an engine"`).
- [x] 2.2 Add `# region CLASS_EngineNotSupportedError` ... `# endregion CLASS_EngineNotSupportedError` enclosing the FULL class block (class line, docstring, `__init__` with `self.engine_name`, `super().__init__`, trailing blank). `PURPOSE` (WHY: tell the operator which engine name was rejected so they can fix the script or the config without grepping logs). `ENSURES` (stores `engine_name`; message is `f"Engine {engine_name} is not supported"`).
- [x] 2.3 Add `# region FUNC__parse_submit_args` ... `# endregion FUNC__parse_submit_args` enclosing the FULL function (def line → body → trailing blank). `PURPOSE` (WHY: declare the yasubmit argparse grammar — `prog="yasubmit"`, one positional `script` validated by `existing_path`, plus the shared `--config` / `--log-level` flags — so the AiiDA plugin's command shape stays stable). `ENSURES` (returns a `Namespace` whose `script` is a `Path` to an existing file or argparse exits 2 before this function returns).
- [x] 2.4 Add `# region FUNC__parse_script_metadata` ... `# endregion FUNC__parse_script_metadata` enclosing the FULL function. `PURPOSE` (WHY: extract `key=value` metadata lines from an AiiDA submission script so the rest of the submit path can read them as a dict). `INVARIANTS` (lines without exactly one `=` are silently skipped — this matches the AiiDA script convention which permits non-metadata lines like comments or shebangs; no exception is raised on malformed lines).
- [x] 2.5 Add `# region FUNC__read_input_files` ... `# endregion FUNC__read_input_files` enclosing the FULL function. `PURPOSE` (WHY: read each engine-declared input file into the task metadata dict so the orchestrator has all inputs locally before upload). `INVARIANTS` (text files are stored as UTF-8 strings; binary files that fail UTF-8 decode are stored base64-encoded under the same key).
- [x] 2.6 Add `# region FUNC__build_metadata` ... `# endregion FUNC__build_metadata` enclosing the FULL function. `PURPOSE` (WHY: assemble the task metadata dict — `local_folder`, engine input files, and webhook fields when applicable — so the submit use case receives a single ready payload). `INVARIANTS` (`local_folder` is always present; engine input files are present only when the engine is known; webhook fields are present only when `PARENT` is in the script AND `config.local.webhook_url` is set).
- [x] 2.7 Enrich existing `FUNC__submit_async` and `FUNC_submit` regions: tighten `PURPOSE` to WHY (current text is acceptable; ensure it does not slide into WHAT). Add `INVARIANTS` to `FUNC__submit_async` capturing: (a) success path writes exactly `str(task_id)` to stdout (no decoration — the AiiDA plugin parses `int(stdout.strip())`); (b) failure path writes `Error: <message>` to stderr and exits 1; (c) argparse failures (exit 2) propagate because `SystemExit` is not an `Exception` subclass and is not caught by the body-level `except Exception`. Add `RATIONALE` Q/A to `FUNC__submit_async`: "Q: Why is there no `--json` / output-mode flag? A: The AiiDA scheduler plugin parses `int(stdout.strip())` of the subprocess; the success output is therefore fixed to `str(task_id)` and cannot be decorated." Add `RATIONALE` Q/A to `FUNC_submit`: "Q: Why `asyncio.run` directly instead of a shared `@to_sync` helper? A: CLI entry points have no async caller; the sync↔async boundary is the boundary the process model already wants."
- [x] 2.8 Verify `uv run ruff check yascheduler/entrypoints/cli/submit.py` and `uv run ruff format --check yascheduler/entrypoints/cli/submit.py` pass; `uv run pytest -m unit tests/unit/` for any test importing `yascheduler.entrypoints.cli.submit` is green.

## 3. show_nodes.py — wrap missing entities + enrich existing regions

Each new region encloses the FULL entity per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 3.1 Add `# region CLASS__NodeView` ... `# endregion CLASS__NodeView` enclosing the FULL dataclass block — the `@dataclass(frozen=True)` decorator, the `class _NodeView:` line, the docstring, every field, the trailing blank line. `PURPOSE` (WHY: project a `Node` plus its optional single running `Task` into a flat, frozen view object so filtering and rendering operate on a single value without re-joining per row). `INVARIANTS` (`task_id` and `label` are both `None` when free, both set when busy — the one-RUNNING-task-per-node invariant encoded in the type; frozen so it is safe to pass to renderers without defensive copies).
- [x] 3.2 Add `# region FUNC__parse_nodes_args` ... `# endregion FUNC__parse_nodes_args` enclosing the FULL function. `PURPOSE` (WHY: declare the yanodes argparse grammar — `prog="yanodes"`, the `--json` renderer selector, the four subset-selector filters, and the `--cloud` / `--no-cloud` mutex group — so the flag matrix is observable in one place). `ENSURES` (returns a `Namespace` whose `cloud` and `no_cloud` are never both active; argparse exits 2 on `--cloud X --no-cloud` before this function returns).
- [x] 3.3 Add `# region FUNC__filter_rows` ... `# endregion FUNC__filter_rows` enclosing the FULL function. `PURPOSE` (WHY: apply the active CLI filters as a conjunctive predicate list so any combination of `--enabled` / `--disabled` / `--busy` / `--free` / `--cloud` / `--no-cloud` composes by AND). `INVARIANTS` (subset-selector pairs `--enabled`/`--disabled` and `--busy`/`--free` are no-ops when BOTH are set, matching the spec's "selects all nodes" rule; `--no-cloud` only adds a predicate when `--cloud` was not given, mirroring the argparse mutex).
- [x] 3.4 Enrich existing `FUNC__fetch_nodes_view`, `FUNC__render_nodes_table`, `FUNC__render_nodes_json`, `FUNC__show_nodes_async`, `FUNC_show_nodes`: tighten `PURPOSE` to WHY where it has slipped to WHAT. Add `INVARIANTS` to `FUNC__fetch_nodes_view` capturing the single-UoW + `tasks_by_node_id` dict pattern. Add `RATIONALE` Q/A to `FUNC__render_nodes_json`: "Q: Why is `node_id` serialized via `.value` but other fields are emitted as-is? A: a `NodeId` dataclass is not JSON-serializable; `.value` unwraps the bare int that JSON accepts." Add `RATIONALE` Q/A to `FUNC__render_nodes_table`: "Q: Why is the table rendered with stdlib f-string width specifiers rather than `rich` or `tabulate`? A: the CLI surface stays dependency-free for `yascheduler` operators who install only the daemon's runtime deps."
- [x] 3.5 Verify `uv run ruff check yascheduler/entrypoints/cli/show_nodes.py` and `uv run ruff format --check yascheduler/entrypoints/cli/show_nodes.py` pass; `uv run pytest -m unit tests/unit/` for any test importing `yascheduler.entrypoints.cli.show_nodes` is green.

## 4. check_status.py — wrap missing entities + enrich existing regions

Each new region encloses the FULL entity per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 4.1 Add `# region FUNC__parse_status_args` ... `# endregion FUNC__parse_status_args` enclosing the FULL function. `PURPOSE` (WHY: declare the yastatus argparse grammar — `prog="yastatus"`, the `-j` filter, the three-renderer mutex group, and the `-o` convergence modifier — so the flag matrix is observable in one place). `ENSURES` (returns a `Namespace` whose `convergence` flag is never active without `view`; argparse exits 2 on `-v -i`, and the body-level `parser.error` exits 2 on `-o` without `-v`).
- [x] 4.2 Add `# region FUNC__query_tasks` ... `# endregion FUNC__query_tasks` enclosing the FULL function. `PURPOSE` (WHY: pick the right task query based on whether the operator passed `-j` so the renderer always receives a task list). `INVARIANTS` (`-j` wraps each int to `TaskId` before crossing into the repository, matching the facade-side marshalling pattern; without `-j` the default query is `{RUNNING, TO_DO}` — `DONE` is excluded so the AiiDA plugin's default view stays useful).
- [x] 4.3 Add `# region FUNC__download_convergence_snippet` ... `# endregion FUNC__download_convergence_snippet` enclosing the FULL function. `PURPOSE` (WHY: fetch a remote CRYSTAL `OUTPUT` file via SFTP to a local temp path so `_parse_convergence` can parse it without monkey-patching pycrystal's I/O). `ENSURES` (returns `True` on successful SFTP transfer; returns `False` on `OSError` so the caller can skip convergence display rather than crash).
- [x] 4.4 Add `# region FUNC__parse_convergence` ... `# endregion FUNC__parse_convergence` enclosing the FULL function. `PURPOSE` (WHY: convert a CRYSTAL output file into the human-readable convergence + optgeom text block the operator expects). `INVARIANTS` (returns the `CRYSTOUT_Error` message verbatim on parse failure instead of raising — the verbose renderer needs to print SOMETHING for a corrupt file). `SCOPE` (numerical formatting of optgeom cycles; NOT: file fetching or session lifecycle).
- [x] 4.5 Enrich existing `FUNC__render_default`, `FUNC__render_info`, `FUNC__render_json`, `FUNC__display_remote_output`, `FUNC__render_view`, `FUNC__check_status_async`, `FUNC_check_status`: tighten `PURPOSE` to WHY where slipped. Add `RATIONALE` Q/A to `FUNC__render_default`: "Q: Why is the output a bare `<task_id>   <STATUS>` line with no header or footer? A: the AiiDA scheduler plugin's joblist parser does `for job_id, status in job.split()` against `_MAP_STATUS_YASCHEDULER`; any header/footer/summary decoration would surface as phantom jobs in the plugin." Add `INVARIANTS` to `FUNC__display_remote_output` capturing: (a) connects via `repository.connect(node=node, ...)` reading login user / port / jump-leg parameters from the `Node`, never passing `jump_host` / `jump_username` as separate arguments; (b) prints `NO ALLOCATED HOSTNAME`, `CAN'T CONNECT`, `OUTDATED TASK, SKIPPING` sentinel lines verbatim — the verbose mode consumers depend on them. Add `INVARIANTS` to `FUNC__render_view`: (a) creates the convergence snippet temp file ONCE and reuses it across all RUNNING tasks; (b) unlinks the snippet in a `finally` clause so no leak occurs on success, exception, or early-return paths.
- [x] 4.6 Verify `uv run ruff check yascheduler/entrypoints/cli/check_status.py` and `uv run ruff format --check yascheduler/entrypoints/cli/check_status.py` pass; `uv run pytest -m unit tests/unit/` for any test importing `yascheduler.entrypoints.cli.check_status` is green.

## 5. manage_node.py — wrap missing entities + enrich existing regions

Each new region encloses the FULL entity per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 5.1 Add `# region CLASS_MalformedHostSpecError` ... `# endregion CLASS_MalformedHostSpecError` enclosing the FULL class block (class line, docstring, `__init__` with `self.raw`, `super().__init__`, trailing blank). `PURPOSE` (WHY: signal that the yasetnode positional failed the host-grammar parser so argparse surfaces a clean usage error at exit 2 rather than a Python traceback). `ENSURES` (subclass of `argparse.ArgumentTypeError` so argparse converts it to exit 2; stores `raw` for callers; message is `f"malformed host spec: {raw!r}"`).
- [x] 5.2 Add `# region CLASS_NodeAlreadyInDBError` ... `# endregion CLASS_NodeAlreadyInDBError` enclosing the FULL class block. `PURPOSE` (WHY: stop the add path when a node with the same hostname already exists so the operator fixes the input rather than creating a silent duplicate). `ENSURES` (subclass of `ValueError`; stores `host`; message is `f"Host already in DB: {host}"`).
- [x] 5.3 Add `# region CLASS_NodeIdNotInDBError` ... `# endregion CLASS_NodeIdNotInDBError` enclosing the FULL class block. `PURPOSE` (WHY: signal that a remove-by-id target does not exist so the operator can re-list nodes via `yanodes` to find the right id). `ENSURES` (subclass of `ValueError`; stores `node_id`; message is `f"Node ID not in DB: {node_id}"`).
- [x] 5.4 Add `# region CLASS_HostNotInDBError` ... `# endregion CLASS_HostNotInDBError` enclosing the FULL class block. `PURPOSE` (WHY: signal that a remove-by-hostname target does not exist so the operator distinguishes "typo in hostname" from "node already gone"). `ENSURES` (subclass of `ValueError`; stores `host`; message is `f"Host NOT in DB: {host}"`).
- [x] 5.5 Add `# region CLASS_HostSpec` ... `# endregion CLASS_HostSpec` enclosing the FULL dataclass block — `@dataclass(frozen=True)`, `class HostSpec:`, docstring, all four fields (`host`, `username`, `port`, `ncpus`), trailing blank. `PURPOSE` (WHY: carry the parsed host grammar as a single frozen value across the add path so parser, validator, and the `_add_node` helper share one typed representation). `INVARIANTS` (frozen; `username` is `None` when the grammar omitted `user@`; `ncpus` is `None` when absent OR when `~0` was given — the unlimited sentinel; `port` is in `1..65535`).
- [x] 5.6 Add `# region CLASS_NodeTarget` ... `# endregion CLASS_NodeTarget` enclosing the FULL dataclass block — `@dataclass(frozen=True)`, `class NodeTarget:`, docstring, both fields, trailing blank. `PURPOSE` (WHY: discriminate a `node_id` target from a `host_spec` target at the type level so the dispatch logic cannot accidentally confuse the two). `INVARIANTS` (exactly one of `node_id` / `host_spec` is non-`None` — the parser guarantees this; frozen).
- [x] 5.7 Add `# region FUNC__parse_node_target` ... `# endregion FUNC__parse_node_target` enclosing the FULL function. `PURPOSE` (WHY: route a yasetnode positional to either a `node_id` or a `HostSpec` so the rest of `manage_node` knows which path it is on). `INVARIANTS` (the discriminator is `s.isdigit()`: pure-digit input is treated as `node_id`; everything else is parsed as a host spec — safe because IPv4 literals contain `.`, IPv6 must be bracketed `[...]`, and FQDNs contain `.` or letters, so none of them can collide with a pure-digit form).
- [x] 5.8 Enrich existing `FUNC__parse_host_spec`, `FUNC__parse_node_args`, `FUNC__remove_node_hard`, `FUNC__remove_node_soft`, `FUNC__add_node`, `FUNC__resolve_and_validate_node`, `FUNC__manage_node_async`, `FUNC_manage_node`: tighten `PURPOSE` to WHY where slipped. Add `INVARIANTS` to `FUNC__add_node` capturing: (a) inserts the row FIRST with `enabled=False` to obtain a `node_id` before any SSH work; (b) reads `jump_host` / `jump_username` / `jump_port` from `config.remote` (with sensible defaults) and stamps them on the `NewNode`, never passing them as separate arguments to `repository.connect` — the node carries them; (c) on connect failure, best-effort removes the tmp row so no orphan `enabled=FALSE` row survives; (d) `repository.disconnect(tmp.node_id)` is called in a `finally` clause regardless of outcome. Add `RATIONALE` Q/A to `FUNC__resolve_and_validate_node`: "Q: Why are the not-in-DB errors split into `NodeIdNotInDBError` and `HostNotInDBError`? A: the operator's remediation differs — a wrong id suggests re-running `yanodes` to find the right one, while a wrong hostname suggests a typo or a node that was already removed."
- [x] 5.9 Verify `uv run ruff check yascheduler/entrypoints/cli/manage_node.py` and `uv run ruff format --check yascheduler/entrypoints/cli/manage_node.py` pass; `uv run pytest -m unit tests/unit/` for any test importing `yascheduler.entrypoints.cli.manage_node` is green.

## 6. Shared infra — enrich existing regions (args.py, daemon_common.py, daemon launchers, init.py, package facade)

These files already have all public/non-trivial entities wrapped — this group only enriches existing regions with `INVARIANTS` / `RATIONALE` / `SCOPE` absorbing what leaves the spec. Each existing region continues to enclose the FULL entity (no boundary change). Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 6.1 `args.py` — enrich `FUNC_existing_path`: add `RATIONALE` Q/A "Q: Why is this the single source of the `not a file: <path>` message? A: every CLI command and daemon launcher that takes `--config` shares this validator, so the exit-2 message is consistent across the console-script surface." Enrich `FUNC_add_log_level_arg`: add `INVARIANTS` capturing (a) `choices` is the explicit five-element list `["DEBUG","INFO","WARNING","ERROR","CRITICAL"]` resolved via `logging.getLevelName` (no private `logging._levelToName` API); (b) the caller MUST ensure the `short` flag does not collide with another short flag on the same parser — `daemon_sysv` MUST NOT pass `short="-l"` because it registers `-l` as the alias for `--log-file`. Tighten `MODULE_CONTRACT` PURPOSE to WHY if it has slipped to WHAT.
- [x] 6.2 `daemon_common.py` — enrich `FUNC_configure_logger`: add `INVARIANTS` capturing (a) configures the ROOT logger, NOT `yascheduler` or any third-party logger namespace; (b) does NOT call `logging.basicConfig` (handlers and level are wired explicitly); (c) always adds `StreamHandler(sys.stderr)`; (d) adds `FileHandler(log_file)` only when `log_file is not None`; (e) both handlers share a single `LogFormatter` instance; (f) suppresses `backoff` and `asyncssh` loggers to `ERROR`; (g) calls `logging.captureWarnings(True)`. Enrich `FUNC_run_daemon`: add `INVARIANTS` capturing (a) the `logger` parameter is used ONLY for the signal-handler messages inside this function and is NOT forwarded to `make_daemon`; (b) `orch.start()` is awaited inside a `try` whose `finally` always awaits `orch.stop()` so background-job cleanup and HTTP-session close run even when startup raises. Tighten `MODULE_CONTRACT` PURPOSE to WHY if slipped.
- [x] 6.3 `daemonize.py` — enrich `FUNC_daemonize`: add `INVARIANTS` capturing (a) builds its own argparse parser via the shared helpers and calls `run_daemon` directly (no `python-daemon` — runs in the foreground); (b) the entry point itself does NOT register SIGTERM/SIGINT handlers — `run_daemon` does that on the running loop; (c) the `try/except Exception` block prints `Error: <exception>` and exits 1; argparse's `SystemExit(2)` propagates because `SystemExit` is not an `Exception` subclass. Add `RATIONALE` Q/A "Q: Why does `daemonize` keep the `-l`/`--log-level` short flag when `daemon_systemd` does not? A: backward compatibility — the pre-refactor `yascheduler` command supported `-l DEBUG`, and operators' muscle memory + shell scripts depend on it; `daemon_systemd` is new and has no such legacy."
- [x] 6.4 `daemon_systemd.py` — enrich `FUNC_main`: add `INVARIANTS` mirroring `daemonize` (foreground, no `python-daemon`, no own signal handlers, exit-code contract). Tighten `MODULE_CONTRACT` PURPOSE to WHY if slipped.
- [x] 6.5 `daemon_sysv.py` — enrich `FUNC_main`: add `INVARIANTS` capturing (a) wraps execution in `daemon.DaemonContext` with `working_directory="/"` and `umask=0o002`; (b) `configure_logger` is called INSIDE the `DaemonContext` so the `FileHandler` opens the file in the daemon's post-double-fork context; (c) `-p`/`--pid-file` defaults to `PID_FILE`; (d) `-l` aliases `--log-file` (NOT `--log-level`) for compatibility with the installed `yascheduler.sh` init script; (e) `--config` and `--log-level` are long-only (no `-l` collision). Add `RATIONALE` Q/A "Q: Why is `working_directory="/"` and not `os.path.dirname(__file__)`? A: the previous `__file__`-relative CWD made relative paths resolve against an unreadable directory once the daemon detached; the python-daemon default `/` is the system-daemon convention." Add `RATIONALE` Q/A "Q: Why does `configure_logger` run inside the `DaemonContext`? A: the `FileHandler` opens the file in the daemon's post-double-fork context, so the open file descriptor is the one the daemon actually writes to."
- [x] 6.6 `init.py` — enrich existing `FUNC__init_systemd`, `FUNC__init_sysv`, `FUNC__init_schema`, `FUNC_init` regions: tighten `PURPOSE` to WHY where slipped. Add `INVARIANTS` to `FUNC_init` capturing (a) `--schema` and `--daemon` are subset selectors, not mutually exclusive — both flags → service install AND schema+migrations; neither → same as both (the historical default); (b) the systemd-vs-sysv detection is `/run/systemd/system` directory presence; (c) the body-level `try/except Exception` prints `Error: <message>` and exits 1; `SystemExit` (raised by `sys.exit` and argparse) propagates because it is not an `Exception` subclass. Add `RATIONALE` Q/A to `FUNC_init`: "Q: Why does `yainit` skip DI and call `apply_schema` / `apply_migrations` directly? A: bootstrapping — the database may not yet exist or may not yet have the schema that DI's repository adapters assume; `yainit` is the operation that brings it into existence."
- [x] 6.7 `__init__.py` (subpackage facade) — keep `MODULE_CONTRACT` as is (already minimal and WHY-shaped); no entity-level regions needed (the file declares no entities).
- [x] 6.8 Verify `uv run ruff check yascheduler/entrypoints/cli/` and `uv run ruff format --check yascheduler/entrypoints/cli/` pass on all shared-infra files; `uv run pytest -m unit tests/unit/` for any test importing `yascheduler.entrypoints.cli.args`, `yascheduler.entrypoints.cli.daemon_common`, `yascheduler.entrypoints.cli.daemonize`, `yascheduler.entrypoints.cli.daemon_systemd`, `yascheduler.entrypoints.cli.daemon_sysv`, `yascheduler.entrypoints.cli.init` is green.

## 7. End-to-end verify

- [x] 7.1 Manual scan: every `# region CLASS_*`, `FUNC_*`, `METHOD_*`, `BLOCK_*`, and `MODULE_CONTRACT` has a paired `# endregion` and wraps the entire entity (no orphaned trailing code outside the region; no region closes before its entity ends; no entity outside a region except trivial one-liner private helpers per the GRACE proportional rule).
- [x] 7.2 Manual scan: no invented GRACE field names appear anywhere in `yascheduler/entrypoints/cli/*.py` — only `PURPOSE` / `SCOPE` / `INVARIANTS` / `USECASES` / `DEPENDENCIES` / `RATIONALE` / `KEYWORDS` / `REQUIRES` / `ENSURES`. Specifically, NO `SHALL NOT:` field anywhere.
- [x] 7.3 Manual scan: every `PURPOSE` field answers WHY, not WHAT. Spot-check every region in: `submit.py`, `show_nodes.py`, `check_status.py`, `manage_node.py`, `args.py`, `daemon_common.py`, `daemonize.py`, `daemon_systemd.py`, `daemon_sysv.py`, `init.py`.
- [x] 7.4 Manual scan: every `RATIONALE` field is in Q/A format ("Q: ... A: ..."). No `RATIONALE` block contains free-form prose that should be in `PURPOSE` / `INVARIANTS` / `SCOPE`.
- [x] 7.5 `openspec validate --all --json` passes (exit 0); the trimmed `cli` spec validates AND the change `cli-spec-trim` validates AND no other spec regresses.
- [x] 7.6 `uv run pytest -m unit` — all unit tests pass (no behavior changed).
- [x] 7.7 `uv run pytest -m integration` — all integration tests pass (assume Docker running).
- [x] 7.8 `uv run ruff check .` and `uv run ruff format --check .` pass on all changed files.
- [x] 7.9 `uv run lint-imports` passes (no new imports introduced; markup-only edits).
- [x] 7.10 Confirm no public-surface change: no CLI command, console_script, INI config, DB schema, public API, or log-format change in the diff. The diff is `# region`/`# endregion` markup + comment-field enrichment + spec text trim + 4 requirement removals only.
