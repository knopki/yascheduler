## Common rules for every code-touching task

Every code-touching task below obeys these invariants. They exist because a
prior attempt at a similar change was discarded specifically for violating
them.

- **GRACE fields are a closed set.** Allowed fields: `PURPOSE`, `SCOPE`,
  `INVARIANTS`, `USECASES`, `DEPENDENCIES`, `RATIONALE`, `KEYWORDS`,
  `REQUIRES`, `ENSURES`. No invented fields. Specifically: no `SHALL NOT:`
  pseudo-field, no `EFFECTS:`, no `EXAMPLES:`, no `NOTES:`, no `RAISES:`,
  no free-form labels. The spec's removed `SHALL NOT` sentences do NOT become
  a `SHALL NOT:` contract field — they become an `INVARIANTS` entry stating
  the positive contract, or a `RATIONALE` Q/A if the rationale is the
  valuable part.
- **`RATIONALE` is Q/A format only**, answering "why is this entity shaped
  this way?". It is NOT a junk drawer for arbitrary prose, NOT a place to
  restate `PURPOSE`, NOT a place to dump the trimmed spec text verbatim. One
  Q and one A per item; multi-item allowed when there are distinct reasons.
- **`PURPOSE` answers WHY, not WHAT.** "Persist task mutations to the DB"
  is WHAT and fails. "Make the orchestrator's task-state mutations durable
  across restarts so an in-flight task survives a daemon crash and a
  missing-row precondition surfaces as a typed error instead of silent data
  loss" is WHY and passes. If the existing `PURPOSE` already answers WHY,
  leave it — do not churn for churn's sake.
- **Every `CLASS_*` / `FUNC_*` / `METHOD_*` region encloses the FULL entity.**
  For a class: the `class` line (and any `@dataclass(...)` decorator), the
  docstring, every field, every `__init__` line, every `self.<attr>`
  assignment, through the trailing blank line before the next region
  marker. For a function: the decorator (if any), the `def` / `async def`
  line, the entire body, the trailing blank line. A region that closes
  before its entity ends (e.g. wrapping only the contract comment block) is
  a defect. The contract comment block (`# PURPOSE:`, `# INVARIANTS:`, etc.)
  sits INSIDE the region, ABOVE the entity's first line; the `# region`
  marker opens the block, the contract fields follow, then the entity, then
  `# endregion`. Nesting is allowed: `METHOD_*` and inner `BLOCK_*` regions
  live INSIDE the enclosing `CLASS_*` region; the `CLASS_*` `# endregion`
  comes after the last nested `# endregion`. Reference: the existing
  `CLASS_PostgresTaskRepository` already wraps its 9 nested `METHOD_*`
  regions correctly in `yascheduler/infra/persistence/postgres.py` — use
  that as the shape.
- **Comment-only diff.** No code logic, signature, decorator choice,
  docstring semantics, or import changes. Edits are `# region` /
  `# endregion` marker insertion and contract-field enrichment inside the
  marker block. Module docstrings (the first `"""..."""` after `# endregion
MODULE_CONTRACT`) are NOT touched.
- **Scope boundary for `yascheduler/infra/persistence/`.** This change
  touches ONLY the four files: `exceptions.py`, `postgres_uow.py`,
  `postgres.py`, `sql_loader.py`. The other modules in the same package
  (`__init__.py`, `db_config.py`, `migration_base.py`,
  `postgres_migrations.py`, `postgres_schema.py`) are owned by other in-flight
  trim changes (`config-value-objects-spec-trim`, `db-migrations-spec-trim`,
  `postgres-schema-apply-spec-trim`) or already have tight `MODULE_CONTRACT`
  regions; they are out of scope here.

## 1. Apply the postgres-persistence spec delta

- [x] 1.1 Apply the 5 MODIFIED requirements from `openspec/changes/postgres-persistence-spec-trim/specs/postgres-persistence/spec.md` to `openspec/specs/postgres-persistence/spec.md`, replacing each original requirement block in place. Preserve requirement header text exactly (whitespace-insensitive match) so OpenSpec recognizes the MODIFIED operation. Headers to match (in spec order): `SQL file layout`, `PostgresUnitOfWork transactional boundaries`, `Task repository write semantics`, `PostgresNodeRepository implements NodeRepository`, `All repository methods avoid blocking the event loop`.
- [x] 1.2 Confirm the trimmed main spec contains zero invented `SHALL NOT` / "is removed" / "are NOT on NewTask" / "are NOT bound" / "are REMOVED" / "is deleted" enumerations of absent code in requirement bodies and scenarios (all 12 enumerated in `proposal.md` Why § 1 are gone). Confirm every observable behavioral scenario (`#### Scenario:` count) is preserved: pre 13 → post 13. Confirm the schema DDL paragraph and the migration-files paragraph in `SQL file layout` are gone (their content lives in `postgres-schema-apply` and `db-migrations` specs). Confirm the parameter-by-parameter `save` / `insert` / row-mapping binding narrative in `Task repository write semantics` is gone. Confirm the per-method node row-mapping / `update` SET / `list_*` filter narrative in `PostgresNodeRepository implements NodeRepository` is gone.
- [x] 1.3 `openspec validate --all --json` passes (exit 0). The change validates AND the trimmed main spec validates AND no other spec regresses.

## 2. yascheduler/infra/persistence/exceptions.py — wrap CLASS_UnitOfWorkNotInitializedError + enrich MODULE_CONTRACT

The new region opens one line above the entity and closes one line below the entity body, enclosing the FULL entity per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 2.1 Add `# region CLASS_UnitOfWorkNotInitializedError` ... `# endregion CLASS_UnitOfWorkNotInitializedError` enclosing the FULL class block — the `class UnitOfWorkNotInitializedError(RuntimeError):` line, the docstring, and the trailing blank line. The contract comment block (`# PURPOSE:`, `# INVARIANTS:`) sits INSIDE the new region, above the `class` line. `PURPOSE` (WHY: surface a programming-error precondition violation — using `tasks` / `nodes` / `commit` / `rollback` before `async with` — as a typed error so callers do not silently hit `None`-attribute access on an uninitialized UoW). `INVARIANTS` (subclass of `RuntimeError` so `isinstance(exc, RuntimeError)` is `True`; sibling of `TaskRowNotFoundError` — both signal programming-error contract violations, not transient DB failures; carries no extra state — the message is the only payload).
- [x] 2.2 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text "Signal persistence-layer contract violations (missing row, uninitialized UoW) with typed exceptions so callers distinguish programming errors from recoverable failures without depending on opaque pg8000 exceptions." answers WHY — keep). No further enrichment needed.
- [x] 2.3 Verify `uv run ruff check yascheduler/infra/persistence/exceptions.py` and `uv run ruff format --check yascheduler/infra/persistence/exceptions.py` pass; `uv run pytest -m unit` for any test importing `yascheduler.infra.persistence.exceptions` is green.

## 3. yascheduler/infra/persistence/sql_loader.py — enrich FUNC_load_query + tighten MODULE_CONTRACT

Only the existing regions are enriched. No new region added. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 3.1 Enrich the existing `FUNC_load_query`: tighten `PURPOSE` to WHY if slipped (current text "Provide idempotent SQL query access so repositories and schema utilities get SQL strings without duplicating path resolution or disk I/O per call." answers WHY — keep). Add `INVARIANTS` (`@cache`d on `name` — the same `name` returns the same string instance on every call; reads from `_SQL_DIR = Path(__file__).parent / "sql"`, scoped to this package's bundled SQL directory — never reads from the CWD or an absolute user path; raises `FileNotFoundError` if `name.sql` is absent — the call site never receives an empty string). Add `ENSURES` (the returned string is the exact contents of `_SQL_DIR / f"{name}.sql"`; the same `name` returns the same string on every call).
- [x] 3.2 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text "Load named .sql query files once with caching so persistence repositories work with string SQL without duplicating path resolution or disk I/O per call." answers WHY — keep). No further enrichment needed.
- [x] 3.3 Verify `uv run ruff check yascheduler/infra/persistence/sql_loader.py` and `uv run ruff format --check yascheduler/infra/persistence/sql_loader.py` pass; `uv run pytest -m unit` for any test importing `yascheduler.infra.persistence.sql_loader` is green.

## 4. yascheduler/infra/persistence/postgres_uow.py — enrich MODULE_CONTRACT, CLASS_PostgresUnitOfWork, and 7 METHOD_* regions

Existing regions are kept. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 4.1 Enrich the existing `MODULE_CONTRACT`: tighten `PURPOSE` to WHY if slipped (current text "Bound repository access to a single database transaction and dispatch collected domain events on commit, so the orchestrator's writes are atomic and side-effects are delivered exactly once." answers WHY — keep). Add `INVARIANTS` (every repository access requires entering the `async with` context first — accessing `tasks` / `nodes` / `commit` / `rollback` before `__aenter__` raises `UnitOfWorkNotInitializedError`; the `ThreadPoolExecutor` is single-worker, serializing all DB calls so a single UoW never issues concurrent queries on the same connection; repositories share the same `pg8000.Connection` instance so all writes participate in the same transaction).
- [x] 4.2 Enrich the existing `CLASS_PostgresUnitOfWork`: tighten `PURPOSE` to WHY if slipped (current text "Wrap repository access in a single transaction per request and dispatch collected domain events on commit, so the orchestrator's writes are atomic and side-effects fire exactly once." answers WHY — keep). Add `INVARIANTS` (the `_executor` is a `ThreadPoolExecutor(max_workers=1)` — single-worker, owned by the UoW, shutdown on `__aexit__`; `_saved_tasks` collects tasks saved within the transaction for event dispatch on `commit` / `publish_events`; `_conn` / `_tasks` / `_nodes` are `None` until `__aenter__` sets them and reset to `None` on `__aexit__`; the `tasks` / `nodes` properties and `commit` / `rollback` methods route through `_require_conn` / `None`-checks that raise `UnitOfWorkNotInitializedError` before any DB call). Add `RATIONALE` Q/A — Q: why does `PostgresUnitOfWork` use a single-worker `ThreadPoolExecutor` instead of running pg8000 calls inline or sharing the application-wide executor? A: pg8000's native `Connection` is synchronous and not thread-safe for concurrent use; a single-worker executor serializes all DB calls from one UoW so the same connection is never driven from two threads at once. Owning the executor on the UoW (not the application) lets `__aexit__` shut it down alongside the connection, so a closed UoW cannot leak a thread-pool slot.
- [x] 4.3 Enrich the existing `METHOD___aenter__`: tighten `PURPOSE` to WHY if slipped (current text "Open a connection, start a transaction, and expose repositories so callers read/write within the same transactional boundary." is borderline WHAT — replace with a WHY statement such as "Open a transactional boundary around the orchestrator's unit of work so every repository write within the `async with` block participates in the same BEGIN/COMMIT cycle"); keep `ENSURES`; add `INVARIANTS` (opens the `pg8000.Connection` from `self._config` via `_create_connection` on the executor; issues `BEGIN` on the same connection; wires `PostgresTaskRepository(self._conn, self._executor, self._saved_tasks)` and `PostgresNodeRepository(self._conn, self._executor)` so both repositories share the connection; on any `BaseException` during setup, closes the connection and shuts down the executor before re-raising — never leaves a half-wired UoW).
- [x] 4.4 Enrich the existing `METHOD___aexit__`: tighten `PURPOSE` to WHY if slipped (current text "Rollback on error, close the connection, and release the thread-pool so resources never leak regardless of the exit path." answers WHY — keep). Add `INVARIANTS` (when `exc_type is not None`, calls `rollback()` before closing the connection so partial writes are discarded; closes the connection via `_run_sync(self._conn.close)` on the executor; shuts down the executor with `wait=False` so pending tasks are cancelled, not awaited; sets `_conn = None` so a subsequent property access raises `UnitOfWorkNotInitializedError`; returns `False` — exceptions propagate to the caller).
- [x] 4.5 Enrich the existing `METHOD_commit`: tighten `PURPOSE` to WHY if slipped (current text "Persist all repository writes and deliver domain event side-effects atomically — commit fails = no writes + no events." answers WHY — keep); add `INVARIANTS` (calls `conn.run("COMMIT")` on the executor before dispatching events — if COMMIT raises, no events are dispatched and the exception propagates; the trailing `publish_events` call is wrapped in `try/except Exception` logged at `exception` level — a dispatch failure does not undo the COMMIT; `_require_conn` raises `UnitOfWorkNotInitializedError` if the UoW was not entered).
- [x] 4.6 Enrich the existing `METHOD_rollback`: tighten `PURPOSE` to WHY if slipped (current text "Discard all uncommitted writes and collected events so a failed unit of work leaves the database in its pre-transaction state." answers WHY — keep); add `INVARIANTS` (calls `conn.run("ROLLBACK")` on the executor; clears `_saved_tasks` so a subsequent `publish_events` does not redispatch events from a rolled-back unit; `_require_conn` raises `UnitOfWorkNotInitializedError` if the UoW was not entered).
- [x] 4.7 Enrich the existing `METHOD_collect_events`: tighten `PURPOSE` to WHY if slipped (current text "Gather domain events emitted by saved aggregates so they can be dispatched after commit, ensuring side-effects match persisted state." answers WHY — keep); add `INVARIANTS` (reads `task.events` from every task in `_saved_tasks`, then clears `_saved_tasks` — calling `collect_events` twice in a row yields `[]` the second time; events are NOT cleared from the in-memory `Task` instances — only the UoW's collection is drained).
- [x] 4.8 Enrich the existing `METHOD_publish_events`: tighten `PURPOSE` to WHY if slipped (current text "Dispatch collected domain events through the message bus so registered handlers (webhooks, logging) react after a successful commit." answers WHY — keep); add `INVARIANTS` (calls `collect_events` then `self._bus.dispatch(events)`; the trailing `self._saved_tasks.clear()` is a defensive double-clear after `collect_events` already cleared it — no-op in the normal path; if `_bus.dispatch` raises, the exception propagates — the caller (`commit`) catches it).
- [x] 4.9 Enrich the existing `METHOD__create_connection`: tighten `PURPOSE` to WHY if slipped (current text "Bootstrap a pg8000 connection from the frozen config so the UoW can start a transaction against the configured database." answers WHY — keep); add `INVARIANTS` (synchronous — called on the executor from `__aenter__`; constructs a `pg8000.native.Connection` with `user` / `host` / `database` / `port` / `password` from `self._config` — no DSN string, no SSL kwargs, no timeouts; the connection is in pg8000's default autocommit-off mode until `__aenter__` issues `BEGIN`).
- [x] 4.10 Verify `uv run ruff check yascheduler/infra/persistence/postgres_uow.py` and `uv run ruff format --check yascheduler/infra/persistence/postgres_uow.py` pass; `uv run pytest -m unit` and `uv run pytest -m integration` for any test importing `yascheduler.infra.persistence.postgres_uow` is green.

## 5. yascheduler/infra/persistence/postgres.py — wrap CLASS__PgRepository + enrich MODULE_CONTRACT, 2 CLASS_* repositories, and 16 METHOD_* regions

The new `CLASS__PgRepository` region opens one line above the `_PgRepository` class and closes one line below its body, enclosing the FULL class per the Common rules. The nested `METHOD__run` region stays INSIDE the new `CLASS_*` region (closing order: inner `METHOD` `# endregion` first, then the outer `CLASS` `# endregion`). Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 5.1 Add `# region CLASS__PgRepository` ... `# endregion CLASS__PgRepository` enclosing the FULL class block — the `class _PgRepository:` line, the docstring, the `__init__(self, conn, executor)` method (signature + body), and the nested `METHOD__run` region (already exists — leave its content untouched), and the trailing blank line. The contract comment block (`# PURPOSE:`, `# INVARIANTS:`) sits INSIDE the new region, above the `class _PgRepository:` line. `PURPOSE` (WHY: hold the shared `pg8000.Connection` and `ThreadPoolExecutor` so the two public repositories derive a single `_run(sql, **params)` helper without re-declaring the connection / executor wiring). `INVARIANTS` (private base — not exported in `__all__`; `conn` and `executor` are stored as `_conn` / `_executor` and never re-assigned after `__init__`; `_run` is the sole SQL-execution path — every public repository method routes through it).
- [x] 5.2 Enrich the existing `METHOD__run` inside `CLASS__PgRepository`: tighten `PURPOSE` to WHY if slipped (current text "Offload synchronous pg8000 calls to a thread-pool so async callers never block the event loop during SQL execution." answers WHY — keep); add `INVARIANTS` (offloads the inner `_fn` closure to `asyncio.get_running_loop().run_in_executor(self._executor, _fn)`; `self._conn.run(sql, **params)` returns either `None`, a list of tuples, or a list — `rows or []` normalizes to `[]`; column names are read from `self._conn.columns` (a list of dicts with a `"name"` key) — when `columns` is `None` (e.g. a DDL statement), the row mapping produces `[]`). Add `ENSURES` (returns a `list[dict[str, Any]]` keyed by column name; an empty list when the SQL returns no rows).
- [x] 5.3 Enrich the existing `CLASS_PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Persist and load tasks via PostgreSQL so the orchestrator's task lifecycle (submit, allocate, run, complete) survives restarts without coupling to pg8000." answers WHY — keep); add `INVARIANTS` (subclass of `_PgRepository`; carries an optional `_saved_tasks: list[Task] | None` injected by the UoW — when `None`, `save` does NOT track for event collection; when set, `save` appends to it for `publish_events`; the public surface is exactly `get`, `save`, `update_status`, `insert`, `list_by_status`, `list_by_jobs`, `list_ids_by_node_id_and_status`, `count_by_status`, `_row_to_task` — there is no `update_meta` method (dead path removed); passes `task_id.value` (the bare `int`) as the SQL param for `task_id` — pg8000 cannot adapt a `TaskId` dataclass; binds `webhook_custom_params` and `extra` as Python `dict` values directly — pg8000 adapts `dict` to JSONB natively, no `json.dumps` at the call site). Add `RATIONALE` Q/A — Q1: why does `save` pass `task_id.value` (the bare `int`) instead of the `TaskId` instance? A1: pg8000 cannot adapt a `TaskId` dataclass to a SQL parameter — passing the bare `int` avoids a `TypeError` at the pg8000 adapter layer and keeps the boundary explicit (the row-mapping inverse `TaskId(int(row["task_id"]))` runs on read). Q2: why does `save` bind `webhook_custom_params` / `extra` as `dict` directly without `json.dumps`? A2: pg8000 adapts Python `dict` to JSONB natively; calling `json.dumps` would double-encode the value into a JSON string and break the column's JSONB contract.
- [x] 5.4 Enrich the existing `METHOD_get` on `PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Retrieve a persisted task so the orchestrator can inspect its state before deciding the next lifecycle step (allocate, retry, complete)." answers WHY — keep); add `INVARIANTS` (passes `task_id.value` as the SQL param; returns `None` when the row is absent — does NOT raise `TaskRowNotFoundError` (that's reserved for `save` / `update_status`); routes the row through `_row_to_task`).
- [x] 5.5 Enrich the existing `METHOD_save` on `PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Persist task mutations (status, allocation, error) so the latest state is durable — raises TaskRowNotFoundError to prevent silent data loss when the expected row is missing." answers WHY — keep); keep `ENSURES`; add `INVARIANTS` (binds exactly 11 SQL params — 10 SET columns (`title` (the DB column name for the domain `label` field), `engine`, `remote_folder`, `local_folder`, `webhook_url`, `error`, `webhook_custom_params` (dict), `extra` (dict), `status` (the `task.status.name` string), `allocated_node_id` (bound as `node_id=task.allocated_node_id.value` when set, else `None`)) plus the WHERE key `task_id` (`task_id.value`); the SQL SET clause sets all 10 columns above — no `ip` (the column is dropped), no `updated_at` (the `BEFORE UPDATE` trigger sets it), no `created_at` (immutable on UPDATE), no `task_id` (the WHERE key, not a SET target); the SQL RETURNING yields only `task_id` — the in-memory `Task` is NOT refreshed; `updated_at` is observable on the next `get` via the trigger). Add `RATIONALE` Q/A — Q: why does `save` not refresh the in-memory `Task`'s `updated_at` from the UPDATE's RETURNING? A: the SQL `update_by_id.sql` RETURNING clause yields only `task_id`; the `BEFORE UPDATE` trigger stamps `updated_at`, so a re-read via `get` would observe the new value. Refreshing in `save` would require either widening the RETURNING clause (a SELECT round-trip per save) or re-fetching the row — neither is needed because the orchestrator does not act on `updated_at` immediately after `save`.
- [x] 5.6 Enrich the existing `METHOD_update_status` on `PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Advance a task's lifecycle status atomically so concurrent observers see a consistent state and missing rows are detected via TaskRowNotFoundError." answers WHY — keep); add `INVARIANTS` (binds exactly 2 SQL params — `task_id` (`.value`) and `status` (`status.name`); the SQL SET clause sets only `status`; the SQL RETURNING yields `task_id`; raises `TaskRowNotFoundError(task_id)` when 0 rows match — same precondition contract as `save`).
- [x] 5.7 Enrich the existing `METHOD_list_ids_by_node_id_and_status` on `PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Find tasks on a specific node in a given state so the orchestrator can decide whether to drain, decommission, or re-provision that node." answers WHY — keep); add `INVARIANTS` (binds `node_id.value` and `status.name`; constructs `TaskId(int(row["task_id"]))` for every row; returns `[]` when no rows match — does NOT raise).
- [x] 5.8 Enrich the existing `METHOD_insert` on `PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Accept a new task submission so the orchestrator tracks it through its lifecycle — the returned Task carries a TaskCreated event for cross-layer notification." answers WHY — keep); add `INVARIANTS` (passes 7 kwargs to `_run` — `title` (`new_task.label`), `engine`, `remote_folder` (constant `None` — `NewTask` carries no `remote_folder`; pg8000 silently ignores the unused kwarg), `local_folder`, `webhook_url`, `webhook_custom_params` (dict), `extra` (dict); the SQL `insert.sql` references only 6 of them (`title`, `engine`, `local_folder`, `webhook_url`, `webhook_custom_params`, `extra`) — `remote_folder` is passed but unused by the INSERT column list; does NOT bind `allocated_node_id` or `status` — the DB column defaults handle them (`allocated_node_id` defaults to `NULL`, `status` defaults to `'TO_DO'` per the schema); does NOT bind `created_at` / `updated_at` — the DB `DEFAULT NOW()` populates them on insert and reads them back via RETURNING; the SQL RETURNING yields the full task column set; routes the row through `_row_to_task` and wraps the result in `materialize_task(...)` to attach `TaskCreated`; does NOT import `TaskCreated` directly — `materialize_task` (in the domain layer) owns event construction). Add `RATIONALE` Q/A — Q1: why does `insert` ignore `NewTask.task_id`? A1: `NewTask` is the submission DTO — it carries no `task_id` because the DB generates it via `GENERATED ALWAYS AS IDENTITY`; reading it back via RETURNING avoids a second `get` round-trip. Q2: why does `insert` not import `TaskCreated` directly? A2: `materialize_task` (in the domain layer) owns event construction so the infra layer never depends on the domain's event types directly — the boundary stays at the `materialize_task` seam, and the domain is free to evolve the event shape without an infra-layer import chain.
- [x] 5.9 Enrich the existing `METHOD_list_by_status` on `PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Poll for tasks matching a given state set so the orchestrator's main loop picks up the next batch of work to process." answers WHY — keep); add `INVARIANTS` (binds `statuses` (a list of `s.name` strings) and `lim` (an `int | None`); routes every row through `_row_to_task`; returns `[]` when no rows match — does NOT raise).
- [x] 5.10 Enrich the existing `METHOD_list_by_jobs` on `PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Batch-load tasks by explicit IDs so the client facade returns task details without issuing N+1 queries." answers WHY — keep); add `INVARIANTS` (binds `task_ids` (a list of `tid.value` ints — pg8000 adapts a Python list to a PostgreSQL array for `IN = ANY(:task_ids)`); routes every row through `_row_to_task`; returns `[]` when the input list is empty or no rows match — does NOT raise).
- [x] 5.11 Enrich the existing `METHOD_count_by_status` on `PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Report per-status task tallies so dashboards, health checks, and CLI commands reflect the scheduler's workload without scanning all rows." answers WHY — keep); add `INVARIANTS` (binds no params — the SQL takes none; constructs `{TaskStatus[row["status"]]: row["count"] for row in rows}` — the row's `status` is the enum label string, looked up by name; returns an empty `Mapping` when the table is empty).
- [x] 5.12 Enrich the existing `METHOD__row_to_task` on `PostgresTaskRepository`: tighten `PURPOSE` to WHY if slipped (current text "Deserialize raw DB rows into domain Task entities so the application layer works with typed model objects, not raw dicts or manual JSON parsing." answers WHY — keep); add `INVARIANTS` (static method; reads `task_id`, `title` (mapped to the `Task.label` field), `engine`, `remote_folder`, `local_folder`, `webhook_url`, `error`, `webhook_custom_params`, `extra`, `status`, `allocated_node_id`, `created_at`, `updated_at` from the row; constructs `TaskId(int(row["task_id"]))`; constructs `NodeId(int(row["allocated_node_id"]))` when the value is not `None`, else `allocated_node_id=None`; constructs `TaskStatus[row["status"]]` (name lookup — the row's `status` is the enum label string); reads `created_at` / `updated_at` as `datetime` (pg8000 returns `datetime` for `TIMESTAMPTZ`); reads `webhook_custom_params` / `extra` as Python `dict` — when pg8000 returns them as a `str`, the mapping `json.loads` them (defensive — pg8000's JSONB adaptation normally returns `dict`, but a `str` fallback path is preserved); always sets `events=()` — events are transient, the DB has no events column; does NOT read a `metadata` column (the column is dropped); does NOT construct a `TaskContext` (the value object is removed)). Add `RATIONALE` Q/A — Q: why does `_row_to_task` always set `events=()` even though `insert` returns a `Task` with a `TaskCreated` event? A: the DB has no `events` column — events are transient and live only in memory. `insert` routes the row through `_row_to_task` (which sets `events=()`) and then wraps the result in `materialize_task(...)` (which attaches `TaskCreated`). Every other read path (`get`, `list_by_status`, `list_by_jobs`) returns the row as-is with empty events; only `insert` post-processes the row to attach the event.
- [x] 5.13 Enrich the existing `CLASS_PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "Persist and load compute nodes via PostgreSQL so the orchestrator's node pool (enabled, disabled, cloud-provisioned) survives restarts without coupling to pg8000." answers WHY — keep); add `INVARIANTS` (subclass of `_PgRepository`; carries no extra state — the constructor is the inherited `_PgRepository.__init__`; the public surface is exactly `get_by_id`, `get_by_ids`, `list_all`, `list_enabled`, `list_disabled`, `insert`, `update`, `enable`, `disable`, `remove`, `count_by_cloud`, `count_by_status`, `_row_to_node` — there is no `get(ip)` method, no `get_by_ips(ips)` method, no `add_tmp` method (all removed); node lookups use `get_by_id` / `get_by_ids` only; the tmp-node reservation flow uses `insert` with a `NewNode(enabled=False)`; passes `node_id.value` (the bare `int`) as the SQL param for `node_id` — pg8000 cannot adapt a `NodeId` dataclass; passes `[n.value for n in node_ids]` as the SQL param for `get_by_ids` — pg8000 adapts a Python list to a PostgreSQL array for `= ANY(:node_ids)`). Add `RATIONALE` Q/A — Q1: why were `get(ip)`, `get_by_ips`, and `add_tmp` removed? A1: node identity is `NodeId`-keyed everywhere — the hostname is mutable (a tmp-node starts with `hostname=""` and is flipped to the real VM hostname by `update`), so a hostname lookup would return a transient value. `get_by_id` is the stable lookup. The tmp-reservation flow uses `insert(NewNode(cloud=..., enabled=False))` to reserve a row, then `update` stamps the real hostname — there is no separate `add_tmp` path. Q2: why does `count_by_status.sql` use `COUNT(node_id)` instead of `COUNT(*)` or `COUNT(hostname)`? A2: `COUNT(node_id)` counts non-NULL `node_id` values (the primary key, never NULL) — semantically equivalent to `COUNT(*)` here, but explicit about what is being counted; `COUNT(hostname)` would under-count tmp rows (`hostname=""` is non-NULL but the field is sometimes excluded by the prior `list_disabled` filter narrative — `COUNT(node_id)` keeps the count unambiguous.
- [x] 5.14 Enrich the existing `METHOD_list_all` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "Return the entire node inventory so management commands and CLI can enumerate, audit, or export all known nodes." answers WHY — keep); add `INVARIANTS` (binds no params; the SQL includes `ORDER BY node_id` so rows arrive in ascending identity order; returns ALL rows regardless of `enabled` or `hostname` — including tmp rows with `hostname=""` — because the allocator counts tmp rows toward `max_nodes` capacity; routes every row through `_row_to_node`). Add `RATIONALE` Q/A — Q: why does `list_all` return tmp rows (`hostname=""`, `enabled=FALSE`) instead of filtering them out? A: the cloud allocator counts every node row — including in-flight tmp reservations — toward the per-cloud `max_nodes` capacity limit; hiding tmp rows from `list_all` would let the allocator over-provision past `max_nodes`. The CLI / management consumers that need to filter tmp rows use `list_disabled` (which excludes them via `WHERE hostname <> ''`).
- [x] 5.15 Enrich the existing `METHOD_list_enabled` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "List nodes available for scheduling so the allocator picks from active candidates without scanning disabled nodes." answers WHY — keep); add `INVARIANTS` (binds no params; the SQL filters `WHERE enabled = TRUE` — there is no Python post-filter; by the `hostname == ""` IFF `enabled = FALSE` invariant (the tmp-row state), no enabled row has `hostname == ""`, so the prior `"." in r["hostname"]` filter was dead and is removed; routes every row through `_row_to_node`).
- [x] 5.16 Enrich the existing `METHOD_list_disabled` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "List nodes that are disabled but have a known hostname so operators can review or re-enable previously active nodes without seeing transient tmp rows." answers WHY — keep); add `INVARIANTS` (binds no params; the SQL filters `WHERE enabled = FALSE AND hostname <> ''` — tmp rows (`hostname=""`) are excluded at the DB layer, not in Python; routes every row through `_row_to_node`).
- [x] 5.17 Enrich the existing `METHOD_insert` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "Register a new compute node (SSH machine or cloud instance) so the orchestrator can track and allocate it via its assigned NodeId." answers WHY — keep); add `INVARIANTS` (binds exactly 11 SQL params — `hostname`, `ncpus`, `enabled`, `cloud`, `username`, `port`, `jump_host`, `jump_port`, `jump_username`, `external_id`, `status` (`new_node.status.value` — the enum's value, not name), all in the INSERT column list and the VALUES clause; reads `node_id` / `created_at` / `updated_at` back via RETURNING; when called with `NewNode(cloud=..., enabled=False)` (the tmp-reservation path, with `hostname=""` and `ncpus=0` defaults from `NewNode`), inserts a row with `hostname=""`, `enabled=FALSE`, the given `cloud`, and `username` / `port` from the `NewNode` defaults (`"root"`, `22`); the SQL RETURNING yields `node_id`, `created_at`, `updated_at`; merges the RETURNING row with the input `NewNode`'s field values into a synthetic row dict so `_row_to_node` sees the full row). Add `RATIONALE` Q/A — Q: why does `insert` merge the RETURNING row with the input `NewNode`'s field values? A: the SQL RETURNING yields only the DB-generated columns (`node_id`, `created_at`, `updated_at`); the rest of the `Node` fields (hostname, ncpus, enabled, cloud, username, port, jump_*, external_id, status) are the same as the input `NewNode`. Rather than re-`SELECT` the row by `node_id`, `insert` constructs a synthetic row dict that overlays the RETURNING row with the input values — `_row_to_node` then materializes a `Node` carrying both the DB-generated and the input fields in one pass.
- [x] 5.18 Enrich the existing `METHOD_get_by_id` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "Retrieve a specific node by ID so the orchestrator can inspect its state before scheduling or decommissioning it." answers WHY — keep); add `INVARIANTS` (binds `node_id.value` as the SQL param; returns `None` when the row is absent — does NOT raise; routes the row through `_row_to_node`).
- [x] 5.19 Enrich the existing `METHOD_get_by_ids` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "Batch-load nodes by ID list so the orchestrator resolves many node references in one query instead of N round-trips." answers WHY — keep); add `INVARIANTS` (binds `node_ids` — a list of `n.value` ints — pg8000 adapts a Python list to a PostgreSQL array for `= ANY(:node_ids)`; an empty input list runs the SQL with an empty array and returns an empty dict; constructs the result as `{NodeId(int(row["node_id"])): _row_to_node(row) for row in rows}` — keyed by `NodeId`, not by bare `int`).
- [x] 5.20 Enrich the existing `METHOD_update` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "Persist node mutations (hostname, status, cloud properties) so the orchestrator's view of each node remains current across restarts." answers WHY — keep); add `INVARIANTS` (binds exactly 12 SQL params — `node_id` (`node.node_id.value` as the WHERE key) plus `hostname`, `ncpus`, `enabled`, `cloud`, `username`, `port`, `jump_host`, `jump_port`, `jump_username`, `external_id`, `status` (`node.status.value`); the SQL SET clause sets all 11 mutable fields including `hostname` — the V1 cloud-allocation lifecycle relies on `update` to flip the tmp row's `hostname` from `""` (the `NewNode` default) to the real VM hostname in a single `UPDATE`; the SQL has no RETURNING clause — `update` returns `None`). Add `RATIONALE` Q/A — Q: why does `update`'s SET clause include `hostname` even though `hostname` is also set at `insert` time? A: the tmp-node reservation flow inserts a row with `hostname=""`, then `clouds.allocate` calls `update` to flip the `hostname` to the real VM hostname in a single `UPDATE`; an `update` without `hostname` in SET would leave cloud nodes unreachable after daemon restart (the SSH hostname never lands on the row) and excluded from `list_disabled`'s `WHERE hostname <> ''` filter (a VM leak — the row stays tmp-forever). Keeping `hostname` in the SET clause is the single-edit seam for the V1 lifecycle.
- [x] 5.21 Enrich the existing `METHOD_enable` / `METHOD_disable` / `METHOD_remove` on `PostgresNodeRepository`: tighten each `PURPOSE` to WHY if slipped (current texts answer WHY — keep); add `INVARIANTS` to each (each binds exactly one SQL param — `node_id.value`; the SQL filters `WHERE node_id = :node_id`; each returns `None` — no RETURNING clause; `enable` sets `enabled = TRUE`, `disable` sets `enabled = FALSE`, `remove` deletes the row; each is a no-op when the `node_id` does not match a row — the SQL affects 0 rows silently, no exception is raised). One shared `RATIONALE` Q/A on `METHOD_enable` is enough (the others mirror it); add it only there if helpful, otherwise omit.
- [x] 5.22 Enrich the existing `METHOD_count_by_cloud` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "Report per-provider node tallies so cost dashboards and capacity planning see distribution across cloud backends." answers WHY — keep); add `INVARIANTS` (binds no params; constructs `{row["cloud"]: row["count"] for row in rows}` — the row's `cloud` is a nullable string; a NULL `cloud` becomes `None` in the dict; returns an empty `Mapping` when the table is empty).
- [x] 5.23 Enrich the existing `METHOD_count_by_status` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "Report enabled/disabled node counts so operators gauge cluster capacity at a glance." answers WHY — keep); add `INVARIANTS` (binds no params; the SQL uses `COUNT(node_id)` (not `COUNT(*)` or `COUNT(hostname)`); constructs `{bool(row["enabled"]): row["count"] for row in rows}` — the row's `enabled` is a PostgreSQL boolean, cast to Python `bool`; returns an empty `Mapping` when the table is empty).
- [x] 5.24 Enrich the existing `METHOD__row_to_node` on `PostgresNodeRepository`: tighten `PURPOSE` to WHY if slipped (current text "Deserialize raw DB rows into domain Node entities so the application layer works with typed model objects, not raw dicts." answers WHY — keep); add `INVARIANTS` (static method; reads `node_id`, `hostname`, `ncpus`, `enabled`, `cloud`, `username`, `port`, `created_at`, `updated_at`, `jump_host`, `jump_port`, `jump_username`, `external_id`, `status` from the row; constructs `NodeId(int(row["node_id"]))` for every row; reads `hostname` unchanged — `""` is a valid `str` (the tmp-row state) and the mapping works without special-casing; constructs `NodeStatus[row["status"]]` (name lookup — the row's `status` is the enum label string); reads `created_at` / `updated_at` as `datetime` (pg8000 returns `datetime` for `TIMESTAMPTZ`); uses `.get(..., default)` for the `username` / `port` / `jump_*` fields so a row missing those columns (e.g. a synthetic row from `insert`'s merge) still maps cleanly; `ncpus` is read via `.get` so a `NULL` becomes `None`).
- [x] 5.25 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text "Bridge domain repository ports to PostgreSQL so the orchestrator persists and loads tasks and nodes transactionally without coupling domain logic to pg8000 or SQL details." answers WHY — keep). Add `INVARIANTS` (every repository method is `async` and routes synchronous pg8000 calls through `asyncio.get_running_loop().run_in_executor(self._executor, _fn)`; the package owns two public repository classes — `PostgresTaskRepository` and `PostgresNodeRepository` — and one private base — `_PgRepository`; `__all__` exports only the two public classes). No further enrichment needed.
- [x] 5.26 Verify `uv run ruff check yascheduler/infra/persistence/postgres.py` and `uv run ruff format --check yascheduler/infra/persistence/postgres.py` pass; `uv run pytest -m unit` and `uv run pytest -m integration` for any test importing `yascheduler.infra.persistence.postgres` is green.

## 6. End-to-end verify

- [x] 6.1 Manual scan: every `# region CLASS_*`, `FUNC_*`, `METHOD_*`, `BLOCK_*`, and `MODULE_CONTRACT` in `yascheduler/infra/persistence/exceptions.py`, `yascheduler/infra/persistence/postgres_uow.py`, `yascheduler/infra/persistence/postgres.py`, and `yascheduler/infra/persistence/sql_loader.py` has a paired `# endregion` and wraps the entire entity. No orphaned trailing code outside the region; no region closes before its entity ends. `CLASS_PostgresUnitOfWork` and `CLASS_PostgresTaskRepository` correctly enclose their nested `METHOD_*` regions (the `CLASS_*` `# endregion` comes after the last nested `METHOD_*` `# endregion`); `CLASS__PgRepository` is newly added and correctly encloses the pre-existing nested `METHOD__run` (closing order: inner `METHOD__run` `# endregion` first, then the outer `CLASS__PgRepository` `# endregion`); `CLASS_UnitOfWorkNotInitializedError` is newly added and wraps the full class block.
- [x] 6.2 Manual scan: no invented GRACE field names anywhere in the touched files — only `PURPOSE` / `SCOPE` / `INVARIANTS` / `USECASES` / `DEPENDENCIES` / `RATIONALE` / `KEYWORDS` / `REQUIRES` / `ENSURES`. Specifically, NO `SHALL NOT:` field, NO `RAISES:` field, NO `EFFECTS:` field, NO `EXAMPLES:` field, NO `NOTES:` field anywhere.
- [x] 6.3 Manual scan: every `PURPOSE` field answers WHY, not WHAT. Spot-check `MODULE_CONTRACT` and every `CLASS_*` / `METHOD_*` / `FUNC_*` region in: `exceptions.py`, `postgres_uow.py`, `postgres.py`, `sql_loader.py`. Where the existing `PURPOSE` already answers WHY, leave it.
- [x] 6.4 Manual scan: every `RATIONALE` field is in Q/A format ("Q: ... A: ..."). No `RATIONALE` block contains free-form prose that should be in `PURPOSE` / `INVARIANTS` / `SCOPE`. Specifically, the "pg8000 cannot adapt a `TaskId` dataclass" narrative lives as a Q/A in `CLASS_PostgresTaskRepository.RATIONALE`; the "trigger sets `updated_at`" narrative lives as a Q/A in `METHOD_save.RATIONALE`; the "`NewTask.task_id` is ignored" narrative lives as a Q/A in `METHOD_insert.RATIONALE` (task repo); the "events-always-empty" narrative lives as a Q/A in `METHOD__row_to_task.RATIONALE`; the "tmp rows counted toward `max_nodes`" narrative lives as a Q/A in `METHOD_list_all.RATIONALE`; the "V1 lifecycle stamps hostname via `update`" narrative lives as a Q/A in `METHOD_update.RATIONALE`; the "RETURNING-merge avoids re-SELECT" narrative lives as a Q/A in `METHOD_insert.RATIONALE` (node repo); the "single-worker executor" narrative lives as a Q/A in `CLASS_PostgresUnitOfWork.RATIONALE`.
- [x] 6.5 `openspec validate --all --json` passes (exit 0); the trimmed `postgres-persistence` spec validates AND the change `postgres-persistence-spec-trim` validates AND no other spec regresses.
- [x] 6.6 `uv run pytest -m unit` — all unit tests pass (no behavior changed; the existing 13 scenarios in `tests/unit/test_persistence_*` already assert them).
- [x] 6.7 `uv run pytest -m integration` — all integration tests pass (DB-touching scenarios in `tests/integration/test_persistence_*` already assert the row-mapping and `TaskRowNotFoundError` contract).
- [x] 6.8 `uv run ruff check .` and `uv run ruff format --check .` pass on all changed files.
- [x] 6.9 `uv run lint-imports` passes (no new imports introduced; markup-only edits).
- [x] 6.10 Confirm no public-surface change: no CLI command, console_script, INI config key, DB schema, public API, or log-format change in the diff. The diff is `# region` / `# endregion` markup + comment-field enrichment + spec text trim only.
