## Common rules for every code-touching task

Every code-touching task below obeys these invariants. They exist because a
prior attempt at this change was discarded specifically for violating them.

- **GRACE fields are a closed set.** Allowed fields: `PURPOSE`, `SCOPE`,
  `INVARIANTS`, `USECASES`, `DEPENDENCIES`, `RATIONALE`, `KEYWORDS`,
  `REQUIRES`, `ENSURES`. No invented fields. Specifically: no `SHALL NOT:`
  pseudo-field, no `EFFECTS:`, no `EXAMPLES:`, no `NOTES:`, no `RAISES:`,
  no free-form labels. The spec's removed `SHALL NOT` sentences do NOT
  become a `SHALL NOT:` contract field — they become an `INVARIANTS` entry
  stating the positive contract, or a `RATIONALE` Q/A if the rationale is
  the valuable part.
- **`RATIONALE` is Q/A format only**, answering "why is this entity shaped
  this way?". It is NOT a junk drawer for arbitrary prose, NOT a place to
  restate `PURPOSE`, NOT a place to dump the trimmed spec text. One Q and
  one A per item, multi-item allowed when there are distinct reasons.
- **`PURPOSE` answers WHY, not WHAT.** "Async factory creating a fully-wired
  Orchestrator" is WHAT and fails. "Hand the daemon entry point a single
  ready Orchestrator wired with UoW, SSH, cloud, and event-bus collaborators
  so the entry point owns only the lifecycle (start/stop)" is WHY and
  passes.
- **Every `CLASS_*` / `FUNC_*` / `METHOD_*` region encloses the FULL entity.**
  For a class: the `@dataclass(...)` / other decorator (if any), the `class`
  line, the docstring, every field, every `__init__` line, every
  `self.<attr>` assignment, through the trailing blank line before the next
  region marker. For a function: the decorator (if any), the `def` /
  `async def` line, the entire body, the trailing blank line. A region that
  closes before its entity ends (e.g. wrapping only the contract comment) is
  a defect. Nesting is allowed: `METHOD_*` and inner `BLOCK_*` regions live
  INSIDE the enclosing `CLASS_*` region; the `CLASS_*` `# endregion` comes
  after the last nested `# endregion`.
- **Comment-only diff.** No code logic, signature, decorator choice, docstring
  semantics, or import changes. Edits are `# region` / `# endregion` marker
  insertion and contract-field enrichment inside the marker block. The
  existing long internal `# Single SSHMachineRepository for the production
  # path ...` commentary inside `make_daemon` stays as-is (it is non-region
  narrative inside a region and explains the non-obvious shared-repository
  contract).

## 1. Apply the dependency-injection spec delta

- [x] 1.1 Apply the 6 MODIFIED requirements from `openspec/changes/dependency-injection-spec-trim/specs/dependency-injection/spec.md` to `openspec/specs/dependency-injection/spec.md`, replacing each original requirement block in place. Preserve requirement header text exactly (whitespace-insensitive match) so OpenSpec recognizes the MODIFIED operation. Headers to match (in spec order): `make_daemon factory`, `make_cli_deps factory`, `DI factories in yascheduler.entrypoints.di`, `Each factory creates only needed dependencies`, `Yascheduler deps_factory test seam`, `make_daemon shares one SSHMachineRepository on the production path`.
- [x] 1.2 Confirm the trimmed main spec contains zero `SHALL NOT` / `shall not` instances in requirement bodies (all 5 enumerated in `proposal.md` Why § 1 are gone; the only remaining `SHALL NOT` / `NOT` language lives inside Gherkin scenario bodies where it expresses observable behavior — e.g. "does NOT import a `DB` facade", "SHALL NOT open a second connection", "no SSH connections or cloud providers are instantiated", "WITHOUT any `machine_operations=` keyword argument AND WITHOUT any `log=` keyword argument"). Confirm every observable behavioral scenario (`#### Scenario:` count) is preserved: pre 20 → post 20.
- [x] 1.3 `openspec validate --all --json` passes (exit 0). The change validates AND the trimmed main spec validates AND no other spec regresses (currently 20 specs + `cli-spec-trim`, `cloud-spec-trim`, `config-value-objects-spec-trim`).

## 2. di.py — enrich existing regions

Every existing region in `di.py` is kept (no boundary change). Each region continues to enclose the FULL entity per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 2.1 Enrich `MODULE_CONTRACT`: tighten `PURPOSE` from the current WHAT ("Assemble all domain, application, and infrastructure collaborators into ready-to-use entry point objects (daemon Orchestrator, CLI dependencies).") to a WHY statement such as "Hand each entry point only the collaborators its use cases need, so the daemon wire-up stays separate from the CLI wire-up and the entry point owns lifecycle alone.". Add `INVARIANTS` capturing: (a) no DB-facade class is imported — persistence is wired only via `PostgresUnitOfWork` and the repository ports (`TaskRepository`, `NodeRepository`); (b) no `typing.cast` bridges the domain `CloudConfig` Protocol and the infra `ConfigCloud` Union — the concrete `ConfigCloud*` DTOs explicitly inherit the Protocol, so `list[ConfigCloud]` is assignable to `Sequence[CloudConfig]` (covariance + inheritance) without a cast; (c) `make_daemon` accepts no `log` parameter and threads no logger into collaborators — each collaborator module binds its own logger via `get_logger("M-...")` at module top; (d) the module follows the `layers` contract (R3) — imports flow `entrypoints → infra → application → domain`. Extend `SCOPE` with an explicit `NOT:` exclusion: `NOT: DB-facade class, typing.cast bridges, logger threading into collaborators, imports from yascheduler.infra.clouds.* subpaths or any dissolved remote_machine/ subpath`.
- [x] 2.2 Enrich `CLASS_CLIDeps`: tighten `PURPOSE` from the current WHAT ("Lightweight dependency container for CLI submit operations — holds engines and a UoW factory.") to a WHY statement such as "Carry the minimum collaborators a CLI command needs (engine registry + UoW factory) so a CLI invocation can submit or query tasks without paying the daemon's SSH/cloud/event-bus wire-up cost.". Add `INVARIANTS` (frozen-shape dataclass with exactly two fields; carries no `aiohttp.ClientSession`, no `SSHMachineRepository`, no cloud adapters; the bundled `uow_factory` builds a `PostgresUnitOfWork` whose `MessageBus` has no registered webhook handlers — webhook delivery is a daemon-only concern). Add `RATIONALE` Q/A — Q: why does `CLIDeps` bundle a `uow_factory` callable instead of a ready `AbstractUnitOfWork` instance? A: each CLI query needs a fresh UoW so its transaction boundary is its own; a callable lets the facade open + close the UoW per call without leaking a handle across calls.
- [x] 2.3 Enrich `METHOD_submit`: tighten `PURPOSE` from the current WHAT ("Submit a new task via the submit_task use case, returning the generated TaskId.") to a WHY statement such as "Forward label, metadata, engine, and the two bundled collaborators into `submit_task` so the CLI call site stays one line and the boundary between CLI and use case stays explicit.". Add `INVARIANTS` (delegates positionally to `submit_task(label, metadata, engine_name, self.engines, self.uow_factory)`; the bundled `engines` and `uow_factory` are the sole collaborators passed — no extras are constructed inside `submit`).
- [x] 2.4 Enrich `FUNC__setup_domain_events`: tighten `PURPOSE` from the current WHAT ("Create a MessageBus, an aiohttp client session, and register webhook handlers for all domain event types.") to a WHY statement such as "Stand up the daemon's event-bus backbone (a `MessageBus` plus a single shared `aiohttp.ClientSession` with webhook handlers registered for every task-lifecycle event) so domain events emitted by the use-case layer are delivered to operator-configured webhooks without each use case re-wiring its own bus.". Add `INVARIANTS` (registers `webhook_handler` (bound to the shared session via `functools.partial`) for exactly five event types — `TaskCreated`, `TaskAllocated`, `TaskCompleted`, `TaskFailed`, `TaskAbandoned`; the `aiohttp.ClientSession` carries a uniform `total=30` seconds timeout; returns `(bus, http)` so the caller owns the session lifecycle and can close it on shutdown). Add `RATIONALE` Q/A — Q: why is the webhook handler bound to one shared `aiohttp.ClientSession` instead of one session per webhook delivery? A: an `aiohttp.ClientSession` owns a connection pool; reusing one across all webhook deliveries keeps the daemon's outbound connection count bounded and lets the pool keep-alive across events.
- [x] 2.5 Enrich `FUNC_make_daemon`: tighten `PURPOSE` from the current WHAT ("Async factory creating a fully-wired Orchestrator with all daemon dependencies (UoW, SSH, cloud, event bus).") to a WHY statement such as "Hand the daemon entry point one ready `Orchestrator` wired with UoW + SSH + cloud + event bus + collaborators so `run_daemon` owns only the start/stop lifecycle and the wire-up boundary stays in one place.". Add `INVARIANTS` listing only conditions that ALWAYS hold (regardless of which branch runs): (a) exactly one `SSHMachineRepository` is constructed per call, with NO arguments; (b) exactly one instance each of `TaskDeployer()`, `OutputDownloader()`, `OccupancyChecker()` is constructed, all with NO arguments — collaborators bind their own module-local loggers via `get_logger("M-...")` at module top, no logger is threaded from the composition root; (c) the single `SSHMachineRepository` is always injected into `Orchestrator.repository`; (d) the three collaborator instances are always injected into `Orchestrator` via the `task_deployer=` / `output_downloader=` / `occupancy_checker=` keyword arguments; (e) `make_daemon` accepts no `log` parameter; (f) on any exception inside the body, `await http.close()` runs before the exception propagates so the aiohttp session is never leaked. Add `ENSURES` for the branch-specific postconditions the scenarios assert: (a) on the `clouds is None` branch, `CloudProvisionerImpl(...)` is invoked with `machine_repository=` (plus `adapters`, `configs`, `local_config`, `remote_config`, `engines`) and WITHOUT any `machine_operations=` (or other operations-side) keyword argument AND WITHOUT any `log=` keyword argument; (b) on the `clouds is None` branch, the SAME `SSHMachineRepository` instance is injected into both `CloudProvisionerImpl.machine_repository` and `Orchestrator.repository` (`is`); (c) on the `clouds is not None` branch, the caller-supplied `clouds` is wired into `Orchestrator.clouds` unchanged — `CloudProvisionerImpl` is NOT constructed inside this call, and the orchestrator's `repository` is the freshly-constructed `SSHMachineRepository()` (NOT the repository the caller built into `clouds`). (The DB-facade and `typing.cast` prohibitions are stated once in the `MODULE_CONTRACT` `INVARIANTS` and are not restated here.) Add `RATIONALE` Q/A: Q1 — "Why is exactly one `SSHMachineRepository` shared between the cloud provisioner and the orchestrator on the `clouds is None` path?" A1 — "So connections opened during cloud allocation (`_setup_vm`) are visible to the orchestrator (no double-connect for the same node) and reaped at shutdown by `Orchestrator.stop()` → `repository.disconnect_all()`. Two repositories would mean the orchestrator never sees cloud-setup connections and the daemon would leak them on shutdown.". Q2 — "Why does `make_daemon` refuse to accept or thread a `log` parameter?" A2 — "Each collaborator module (`TaskDeployer`, `OutputDownloader`, `OccupancyChecker`, `CloudProvisionerImpl`, `SSHMachineRepository`) binds its own logger via `get_logger("M-...")` at module top. Threading a single logger from the composition root would couple every collaborator to the entry point's logging configuration and re-create the pre-refactor tangle where every class carried a `log` parameter through its constructor.".
- [x] 2.6 Enrich `FUNC_make_cli_deps`: tighten `PURPOSE` from the current WHAT ("Sync factory creating lightweight CLIDeps for CLI commands (no SSH/cloud dependencies).") to a WHY statement such as "Build a `CLIDeps` with only the engine registry and a UoW factory so a CLI invocation pays neither the SSH-pool nor the cloud-adapter nor the event-bus wire-up cost.". Add `INVARIANTS` (sync — `make_cli_deps` is a plain `def`, not `async def`; constructs a fresh `MessageBus` with no registered handlers — webhook delivery is a daemon-only concern; the bundled UoW factory builds a `PostgresUnitOfWork` parameterized by `config.db` and the (empty) bus; returns a `CLIDeps` whose `engines` is `config.engines`).
- [x] 2.7 Verify `uv run ruff check yascheduler/entrypoints/di.py` and `uv run ruff format --check yascheduler/entrypoints/di.py` pass; `uv run pytest -m unit tests/unit/test_di.py` is green.

## 3. client.py — wrap CLASS_Yascheduler + FUNC__task_to_dict, enrich existing regions

`client.py` currently declares `class Yascheduler:` with seven nested `METHOD_*` regions but no enclosing `CLASS_*` region — a defect under the GRACE Python rule ("if an entity is annotated by markup, it must always be wrapped in a region"). The `_task_to_dict` projection (a ~40-line non-trivial function) is also unwrapped. Each new region encloses the FULL entity per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 3.1 Add `# region FUNC__task_to_dict` ... `# endregion FUNC__task_to_dict` enclosing the FULL function — the `def _task_to_dict(t: Task, nodes_by_id: dict[NodeId, Node]) -> Mapping[str, Any]:` line, the entire body, and the trailing blank line. `PURPOSE` (WHY: project a `Task` plus its optional allocated `Node` into a flat JSON-serializable mapping so the public client API returns plain dicts to sync callers that cannot see domain value objects like `TaskId` / `NodeId`). `INVARIANTS` (`task_id` is emitted as `t.task_id.value` (the bare `int`) so callers reading the public mapping never see a `TaskId` dataclass; `metadata` is built engine-first, then conditionally augments with `remote_folder` / `local_folder` / `webhook_url` / `webhook_custom_params` / `error` only when the field is non-`None` / non-empty; `t.extra` entries are merged last and NEVER overwrite an existing metadata key — engine-declared keys win; `node` is `None` in the mapping when the task has no allocated node OR when the node is missing from `nodes_by_id`).
- [x] 3.2 Add `# region CLASS_Yascheduler` opening ONE line above the `class Yascheduler:` declaration (i.e. immediately after the `# endregion FUNC__task_to_dict` line and the separating blank line) and closing AFTER the last nested `METHOD_queue_get_task` `# endregion` line at the end of the file. The seven existing `METHOD_*` regions (`METHOD___init__`, `METHOD_queue_submit_task_async`, `METHOD_queue_submit_task`, `METHOD_queue_get_tasks_async`, `METHOD_queue_get_tasks`, `METHOD_queue_get_task_async`, `METHOD_queue_get_task`) stay nested INSIDE the new `CLASS_Yascheduler` — DO NOT move, rename, or close them early; the `CLASS_Yascheduler` `# endregion` comes AFTER the last nested `METHOD_*` `# endregion`. `PURPOSE` (WHY: give sync and async callers a single facade for submitting and querying tasks so they do not have to know about the use-case layer, the UoW factory, or the int↔`TaskId` / int↔`TaskStatus` marshalling — the facade owns all three). `INVARIANTS` (the facade is the SOLE int↔`TaskId` boundary: `queue_get_tasks_async` wraps every `int` job id to `TaskId` before crossing into the `query_tasks` use case and unwraps `TaskId` back to `int` inside `_task_to_dict`; the public surface preserves `jobs: list[int]` and returns mappings whose `task_id` is `int`; `STATUS_TO_DO` / `STATUS_RUNNING` / `STATUS_DONE` class attrs expose the integer status values so AiiDA-plugin and CLI callers compare against `int`, not the `TaskStatus` enum; the `**_` kwargs sink in `__init__` absorbs legacy keyword args without error so older callers do not break on construction).
- [x] 3.3 Enrich `METHOD___init__`: tighten `PURPOSE` from the current WHAT ("Initialize the Yascheduler client with config path and optional DI factory.") to a WHY statement such as "Bind the facade to a parsed `Config` and (optionally) a deps factory seam so the rest of the facade can lazily obtain a fresh `CLIDeps` per query without re-parsing config and without a test-hostile module-level singleton.". Add `INVARIANTS` capturing the `deps_factory` seam contract: (a) `deps_factory` is keyword-only — passing it positionally raises `TypeError`; (b) when `deps_factory is None`, the constructor defaults to `make_cli_deps` (the production factory); (c) the factory is NOT invoked at construction time and NOT cached — it is invoked once per `queue_*_async` call as `self._deps_factory(self.config)` to produce a fresh `CLIDeps`; (d) the factory callable is synchronous — its result is used directly, never awaited.
- [x] 3.4 Enrich `METHOD_queue_submit_task_async`: tighten `PURPOSE` from the current WHAT ("Submit a new task asynchronously via CLIDeps.submit, returning the task_id as an int.") to a WHY statement such as "Forward a submit request through the deps seam into the use case and unwrap the resulting `TaskId` to `int` so sync callers see a plain int job id (the public surface stays `int`, the `TaskId` dataclass never escapes the facade).". Add `INVARIANTS` (obtains `deps` via `self._deps_factory(self.config)` (sync call, NOT awaited); awaits `deps.submit(label, dict(metadata), engine_name)`; extracts `.value` from the returned `TaskId` so the public contract returns `int`; the `_webhook_onsubmit` keyword is passed through with a leading underscore to mark it as facade-internal — it is NOT part of the public `queue_submit_task` surface).
- [x] 3.5 Enrich `METHOD_queue_submit_task`: tighten `PURPOSE` from the current WHAT ("Submit a new task synchronously via the async delegate wrapped with to_sync.") to a WHY statement such as "Expose the async submit path to sync callers (AiiDA plugin, REPL) by bridging through `to_sync` so the facade stays sync on its public surface while the use-case layer stays async.". Add `INVARIANTS` (delegates to `to_sync(self.queue_submit_task_async)`; passes `_webhook_onsubmit=webhook_onsubmit` so the public `webhook_onsubmit` kwarg is forwarded under the underscore-prefixed name the async method expects).
- [x] 3.6 Enrich `METHOD_queue_get_tasks_async`: tighten `PURPOSE` from the current WHAT ("Query tasks asynchronously by job IDs or statuses via the query_tasks use case.") to a WHY statement such as "Resolve a list of `int` job ids and `int` status values into typed `TaskId` / `TaskStatus` sequences, run them through the deps-seam query use case, and project each result via `_task_to_dict` so the public surface returns plain mappings and the marshalling boundary stays on the facade.". Add `INVARIANTS` (status ints are validated via `TaskStatus(x)` — an unknown int raises `ValueError` before any DB work; job ints are wrapped to `TaskId` (the facade is the sole int↔`TaskId` boundary); obtains `deps` via `self._deps_factory(self.config)` per call (NOT cached); projects the result via `_task_to_dict` so the public surface is JSON-serializable).
- [x] 3.7 Enrich `METHOD_queue_get_tasks`: tighten `PURPOSE` from the current WHAT ("Query tasks synchronously by job IDs or statuses.") to a WHY statement such as "Bridge the async query path to sync callers via `to_sync` so the public surface stays sync.". Add `INVARIANTS` (delegates to `to_sync(self.queue_get_tasks_async)`; the wrapped call runs in a worker thread when an event loop is already running (AiiDA plugin context), otherwise via a fresh `asyncio.run`).
- [x] 3.8 Enrich `METHOD_queue_get_task_async`: tighten `PURPOSE` from the current WHAT ("Get a single task by ID asynchronously.") to a WHY statement such as "Hand callers a one-task view (or `None`) on top of the list query so they do not have to unwrap `[0]` or handle empty lists themselves.". Add `INVARIANTS` (delegates to `queue_get_tasks_async(jobs=[task_id])`; returns the first result or `None` on an empty list — no exception is raised for unknown ids).
- [x] 3.9 Enrich `METHOD_queue_get_task`: tighten `PURPOSE` from the current WHAT ("Get a single task by ID synchronously.") to a WHY statement such as "Bridge the single-task async path to sync callers via `to_sync`.". Add `INVARIANTS` (delegates to `to_sync(self.queue_get_task_async)`; same loop-handling as `queue_get_tasks`).
- [x] 3.10 Enrich existing `FUNC_to_sync`: tighten `PURPOSE` if it has slipped to WHAT (current text mentions WHY — keep); add `INVARIANTS` (detects a running event loop via `asyncio.get_running_loop()`; on success-path `RuntimeError` (no running loop), runs the coroutine via a fresh `asyncio.run`; on a running loop, offloads to a single-worker `ThreadPoolExecutor` so the calling loop is not blocked; the wrapped function preserves the original signature and metadata via `@wraps`). Add `RATIONALE` Q/A — Q: why does `to_sync` spin up a worker thread when an event loop is already running instead of using `asyncio.run_until_complete`? A: `asyncio.run_until_complete` cannot be called on a running loop; the only ways to block on a coroutine from inside a running loop are (1) `await` it (impossible — the caller is sync) or (2) move the coroutine to a different loop in a different thread and block on its result. The worker-thread path is option (2).
- [x] 3.11 Enrich `MODULE_CONTRACT`: tighten `PURPOSE` if it has slipped to WHAT (current text mentions WHY — keep); add `INVARIANTS` capturing the int↔`TaskId` / int↔`TaskStatus` marshalling boundary: the facade is the sole crossing point; the public surface preserves `int` for ids and `int` for status codes; the `Task` / `Node` / `TaskId` / `NodeId` / `TaskStatus` domain types never escape the facade.
- [x] 3.12 Verify `uv run ruff check yascheduler/entrypoints/client.py` and `uv run ruff format --check yascheduler/entrypoints/client.py` pass; `uv run pytest -m unit tests/unit/test_client_query.py tests/unit/test_characterization.py tests/unit/test_cli_smoke.py` is green.

## 4. End-to-end verify

- [x] 4.1 Manual scan: every `# region CLASS_*`, `FUNC_*`, `METHOD_*`, `BLOCK_*`, and `MODULE_CONTRACT` in `yascheduler/entrypoints/di.py` and `yascheduler/entrypoints/client.py` has a paired `# endregion` and wraps the entire entity (no orphaned trailing code outside the region; no region closes before its entity ends; nested `METHOD_*` regions live INSIDE the enclosing `CLASS_Yascheduler`; the `CLASS_Yascheduler` `# endregion` comes AFTER the last nested `METHOD_*` `# endregion` at end of file).
- [x] 4.2 Manual scan: no invented GRACE field names anywhere in `yascheduler/entrypoints/di.py` and `yascheduler/entrypoints/client.py` — only `PURPOSE` / `SCOPE` / `INVARIANTS` / `USECASES` / `DEPENDENCIES` / `RATIONALE` / `KEYWORDS` / `REQUIRES` / `ENSURES`. Specifically, NO `SHALL NOT:` field, NO `EFFECTS:` field, NO `RAISES:` field, NO `EXAMPLES:` field anywhere.
- [x] 4.3 Manual scan: every `PURPOSE` field answers WHY, not WHAT. Spot-check every region in: `di.py` (`MODULE_CONTRACT`, `CLASS_CLIDeps`, `METHOD_submit`, `FUNC__setup_domain_events`, `FUNC_make_daemon`, `FUNC_make_cli_deps`); `client.py` (`MODULE_CONTRACT`, `FUNC_to_sync`, `FUNC__task_to_dict`, `CLASS_Yascheduler`, `METHOD___init__`, `METHOD_queue_submit_task_async`, `METHOD_queue_submit_task`, `METHOD_queue_get_tasks_async`, `METHOD_queue_get_tasks`, `METHOD_queue_get_task_async`, `METHOD_queue_get_task`).
- [x] 4.4 Manual scan: every `RATIONALE` field is in Q/A format ("Q: ... A: ..."). No `RATIONALE` block contains free-form prose that should be in `PURPOSE` / `INVARIANTS` / `SCOPE`.
- [x] 4.5 `openspec validate --all --json` passes (exit 0); the trimmed `dependency-injection` spec validates AND the change `dependency-injection-spec-trim` validates AND no other spec regresses.
- [x] 4.6 `uv run pytest -m unit` — all unit tests pass (no behavior changed). Specifically `tests/unit/test_di.py`, `tests/unit/test_client_query.py`, `tests/unit/test_characterization.py`, `tests/unit/test_cli_smoke.py`, `tests/unit/test_daemon_common.py`, `tests/unit/test_daemon_common_cleanup.py`.
- [x] 4.7 `uv run ruff check .` and `uv run ruff format --check .` pass on all changed files.
- [x] 4.8 `uv run lint-imports` passes (no new imports introduced; markup-only edits).
- [x] 4.9 Confirm no public-surface change: no CLI command, console_script, INI config, DB schema, public API, factory signature, factory parameter, or log-format change in the diff. The diff is `# region` / `# endregion` markup + comment-field enrichment + spec text trim only.
