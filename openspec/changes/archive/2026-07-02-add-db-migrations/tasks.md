## 1. `Migration` base class + runner module

- [x] 1.1 Create `yascheduler/infra/persistence/migration_base.py` with GRACE-lite `FILE`/`VERSION`/`START_MODULE_CONTRACT`/`START_MODULE_MAP`/`START_CHANGE_SUMMARY` header (PURPOSE: `Migration` base class for `.py` migrations with injected `config`/`conn`/`log` and `begin()`/`commit()` helpers; SCOPE: `Migration` class; DEPENDS: M-INFRA-DB-CONFIG, M-PERSISTENCE; LINKS: M-PERSISTENCE-MIGRATIONS). Add `from __future__ import annotations`; TYPE_CHECKING imports for `pg8000.native.Connection` and `.db_config.PostgresDbConfig`; `import logging`. Define `class Migration` with `__init__(self, config, conn, log)` storing `self.config`/`self.conn`/`self.log`; `begin(self) -> None` calling `self.conn.run("BEGIN")`; `commit(self) -> None` calling `self.conn.run("COMMIT")`; `migrate(self) -> None` raising `NotImplementedError`. Add `START_CONTRACT: Migration` block (PURPOSE/INPUTS/OUTPUTS/SIDE_EFFECTS/LINKS). Verify: `uv run pytest -m unit` passes; `Migration(config, conn, log).begin`/`.commit`/`.migrate` exist; instantiating and calling `migrate()` raises `NotImplementedError`.
- [x] 1.2 Create `yascheduler/infra/persistence/postgres_migrations.py` with GRACE-lite header (PURPOSE: synchronous migration runner — scans `sql/migrations/`, applies pending `.sql`/`.py` migrations in string-sorted `prefix_id` order, each in its own transaction, recording each in `yascheduler_migrations`; SCOPE: `apply_migrations(config)`; DEPENDS: M-INFRA-DB-CONFIG, M-PERSISTENCE-SQLLOADER, M-PERSISTENCE-MIGRATION-BASE; LINKS: M-PERSISTENCE, M-PERSISTENCE-SCHEMA, M-ENTRYPOINTS-CLI-INIT). Imports: `logging`, `inspect`, `importlib.util`, `pathlib.Path`, `pg8000.native.Connection`, `pg8000.exceptions.DatabaseError` (for the "no transaction in progress" catch — confirm the exact exception class pg8000 raises), `.db_config.PostgresDbConfig`, `.migration_base.Migration`. Define the migrations dir constant `_MIGRATIONS_DIR = Path(__file__).parent / "sql" / "migrations"`.
- [x] 1.3 Implement `_prefix_id(filename: Path) -> str`: return the substring of `filename.name` before the first `_`. (Used for filtering and ordering; uniqueness is a unit test's job, not the runner's.)
- [x] 1.4 Implement `_scan_migrations() -> list[Path]`: glob `_MIGRATIONS_DIR.glob("*.sql")` + `_MIGRATIONS_DIR.glob("*.py")`, return sorted by `filename.name` (string sort). Do NOT deduplicate or validate uniqueness here.
- [x] 1.5 Implement `_last_applied(conn: Connection) -> str | None`: run `SELECT MAX(migration_id) FROM yascheduler_migrations`; return `None` if the result row's `MAX` is `NULL` (empty tracker) or the table is absent (defensive — should not happen since `apply_schema` ran first, but return `None` to mean "apply all" rather than crash).
- [x] 1.6 Implement `_pending(last: str | None, files: list[Path]) -> list[Path]`: if `last is None` return all `files`; else return `[f for f in files if _prefix_id(f) > last]` preserving the sorted order.
- [x] 1.7 Implement `_one_migration_subclass(module: module) -> type[Migration]`: use `inspect.getmembers(module, inspect.isclass)`; filter to classes that are subclasses of `Migration` and are defined in this module (`cls.__module__ == module.__name__`) and are not `Migration` itself; if the count is not exactly 1, raise `RuntimeError(f"<file>: expected exactly one Migration subclass, found {count}")`; return the single class.
- [x] 1.8 Implement `_apply_sql_migration(conn, path, prefix_id, log)`: `conn.run("BEGIN")`; `try`: `conn.run(path.read_text())` (multi-statement via Simple Query); `conn.run("INSERT INTO yascheduler_migrations (migration_id) VALUES :p", p=prefix_id)` (confirm pg8000 native named-parameter syntax); `conn.run("COMMIT")`; `except Exception`: best-effort `conn.run("ROLLBACK")` wrapped in `try/except`; `raise`.
- [x] 1.9 Implement `_apply_py_migration(conn, path, prefix_id, config, log)`: `conn.run("BEGIN")`; `try`: load module by file path via `importlib.util.spec_from_file_location(prefix_id, path)` + `module_from_spec` + `exec_module`; `klass = _one_migration_subclass(module)`; `migration = klass(config, conn, log)`; `migration.migrate()`; then the best-effort tracker block: `try`: `conn.run("INSERT INTO yascheduler_migrations ...", p=prefix_id)`; `conn.run("COMMIT")`; `except <no-transaction-in-progress>`: `conn.run("BEGIN")`; retry the `INSERT`; `conn.run("COMMIT")`; outer `except Exception`: best-effort `ROLLBACK`; `raise`. Add a `START_BLOCK_APPLY_PY` / `END_BLOCK_APPLY_PY` and a `START_BLOCK_TRACKER_RECORD` / `END_BLOCK_TRACKER_RECORD` anchor.
- [x] 1.10 Implement `_rollback(conn: Connection) -> None`: `try`: `conn.run("ROLLBACK")`; `except Exception`: `pass` (best-effort; connection may be in a bad state). Add `START_BLOCK_ROLLBACK` / `END_BLOCK_ROLLBACK`.
- [x] 1.11 Implement `apply_migrations(config: PostgresDbConfig) -> None` with `START_CONTRACT` block (PURPOSE: apply pending migrations; INPUTS: config; OUTPUTS: None; SIDE_EFFECTS: runs DDL/DML, writes to yascheduler_migrations, opens/closes a pg8000 connection; LINKS: M-PERSISTENCE-MIGRATIONS, M-PERSISTENCE-SCHEMA). Open `Connection(user=config.user, host=config.host, database=config.database, port=config.port, password=config.password)`; `log = logging.getLogger("yascheduler.infra.persistence.postgres_migrations")`; `try`: `last = _last_applied(conn)`; `files = _scan_migrations()`; `pending = _pending(last, files)`; for each `f` in `pending`: `pid = _prefix_id(f)`; if `f.suffix == ".sql"`: `_apply_sql_migration(conn, f, pid, log)`; else: `_apply_py_migration(conn, f, pid, config, log)`; `log.debug("[postgres_migrations][apply_migrations] applied %s", pid)`; `finally`: `conn.close()`. Add `START_BLOCK_OPEN_CONNECTION`/`END`, `START_BLOCK_READ_LAST`/`END`, `START_BLOCK_APPLY_PENDING`/`END`, `START_BLOCK_CLOSE`/`END` anchors. Verify: `uv run pytest -m unit` passes; module imports cleanly; `apply_migrations` is callable with a `PostgresDbConfig`.
- [x] 1.12 Add `apply_migrations` to the persistence facade `yascheduler/infra/persistence/__init__.py`: `from .postgres_migrations import apply_migrations`; add to `__all__`; update `START_MODULE_MAP` and `START_CHANGE_SUMMARY`. Then add `apply_migrations` to the top-level adapters facade `yascheduler/infra/__init__.py`: update the `from .persistence import ...` line to `from .persistence import PostgresUnitOfWork, apply_migrations, apply_schema`; add `"apply_migrations"` to `__all__`; bump `VERSION` and `START_CHANGE_SUMMARY` (LAST_CHANGE: re-export apply_migrations / add-db-migrations); update `START_MODULE_MAP` to list `apply_migrations`. Verify: `from yascheduler.infra import apply_migrations` resolves; `from yascheduler.infra.persistence import apply_migrations` resolves; `uv run pytest -m unit` passes.

## 2. `schema.sql` snapshot + bootstrap DO block

- [x] 2.1 In `yascheduler/infra/persistence/sql/schema.sql`, add the bootstrap DO block at the TOP (before any `CREATE TABLE`), per design.md. Use `to_regclass('yascheduler_migrations') IS NULL` as the outer guard; declare `last_migration CONSTANT TEXT := '<prefix_id of the first migration>'` (set to the actual prefix_id chosen in §3.1 — for now use a placeholder and update in §3.2); `EXECUTE 'CREATE TABLE yascheduler_migrations (migration_id TEXT PRIMARY KEY, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())'`; inner `IF to_regclass('yascheduler_nodes') IS NULL THEN INSERT INTO yascheduler_migrations (migration_id) VALUES (last_migration); END IF;`. Verify: `apply_schema` on a fresh testcontainer Postgres creates `yascheduler_migrations` with one seeded row; on a legacy DB (with `yascheduler_nodes`, no tracker) creates the tracker empty; on a modern DB (with tracker) is a no-op.
- [x] 2.2 Remove the two `ALTER TABLE yascheduler_nodes ADD COLUMN IF NOT EXISTS username/port` statements from `schema.sql` (they become the first migration in §3.1). Confirm `CREATE TABLE IF NOT EXISTS yascheduler_nodes` already includes `username` and `port` columns (it does in the current file). Verify: `schema.sql` has no `ALTER TABLE` statements; `CREATE TABLE yascheduler_nodes` lists `ip, port, username, ncpus, enabled, cloud`.

## 3. First migration file

- [x] 3.1 Create `yascheduler/infra/persistence/sql/migrations/001_add_username_port.sql` (or another `prefix_id` per the team's chosen convention — the spec does not fix the format, only uniqueness + string-sort). Content: `ALTER TABLE yascheduler_nodes ADD COLUMN IF NOT EXISTS username VARCHAR(255) DEFAULT 'root';` and `ALTER TABLE yascheduler_nodes ADD COLUMN IF NOT EXISTS port INTEGER DEFAULT 22;`. These are idempotent (`IF NOT EXISTS`) because they were originally inline ALTERs; idempotency is a property of these specific statements, not a general migration requirement.
- [x] 3.2 Update the `last_migration` CONSTANT in the `schema.sql` DO block (from §2.1) to the exact `prefix_id` chosen in §3.1 (e.g. `"001"`). Verify: a fresh testcontainer Postgres seeded to this `prefix_id` skips `001_add_username_port.sql` on `apply_migrations` (it is not pending).

## 4. `yainit` wiring

- [x] 4.1 In `yascheduler/entrypoints/cli/init.py`, update `_init_schema(config_path)` to call `apply_migrations(config.db)` after `apply_schema(config.db)`. Import `apply_migrations` from `yascheduler.infra` (the facade). The existing `except DatabaseError` block already catches errors from `apply_schema`; it will also catch `DatabaseError` from `apply_migrations` (same exception type). Bump `init.py` `VERSION` and `START_CHANGE_SUMMARY` (LAST_CHANGE: add apply_migrations call after apply_schema in _init_schema / add-db-migrations). Update the `_init_schema` `START_CONTRACT` SIDE_EFFECTS note to mention migrations. Verify: `uv run pytest -m unit` passes; `yainit --schema` on a fresh testcontainer Postgres applies schema then migrations (no pending migrations after seeding); `yainit --daemon` does NOT call either (unchanged).
- [x] 4.2 Confirm `init()`'s `run_schema` dispatch logic (`run_schema = not args.daemon or args.schema`) is unchanged — `apply_migrations` runs whenever `apply_schema` runs. No new CLI flag. Verify: `yainit` (no flags) → both; `yainit --schema` → both; `yainit --daemon` → neither; `yainit --schema --daemon` → both (equals default).

## 5. Test fixtures

- [x] 5.1 In `tests/integration/conftest.py`, update the `_init_schema` fixture to call `apply_migrations(_db_config)` after `apply_schema(_db_config)`. Import `apply_migrations` from `yascheduler.infra.persistence`. Bump the fixture's `START_CHANGE_SUMMARY` if it has one. Verify: `uv run pytest -m integration` (session fixture applies schema + migrations; on a fresh testcontainer the migrations are no-ops after seeding, but the call exercises the runner).
- [x] 5.2 In `tests/e2e/conftest.py`, update the `_init_schema` fixture identically. Verify: `uv run pytest -m e2e` collection still works; the fixture applies schema + migrations.

## 6. Unit tests

- [x] 6.1 Create `tests/unit/test_migration_runner.py` with `test_prefix_id_extracts_token_before_first_underscore`: assert `_prefix_id(Path("001_add_username_port.sql")) == "001"` and `_prefix_id(Path("20260701120000_add_index.sql")) == "20260701120000"`. Add `START_CONTRACT`/`START_BLOCK` per GRACE-lite if the test is non-trivial.
- [x] 6.2 `test_pending_returns_all_when_last_is_none`: assert `_pending(None, [Path("001_a.sql"), Path("002_b.sql")])` returns both in sorted order.
- [x] 6.3 `test_pending_filters_greater_than_last`: assert `_pending("001", [Path("001_a.sql"), Path("002_b.sql"), Path("010_c.sql")])` returns `[Path("002_b.sql"), Path("010_c.sql")]`.
- [x] 6.4 `test_scan_migrations_sorts_by_filename`: create a temp `migrations/` dir (or mock `_MIGRATIONS_DIR`), populate with `010_c.sql`, `001_a.sql`, `002_b.py`, assert `_scan_migrations()` returns them sorted as `001_a.sql`, `002_b.py`, `010_c.sql`.
- [x] 6.5 `test_one_migration_subclass_finds_exactly_one`: build a temp module with one `Migration` subclass, assert `_one_migration_subclass` returns it. `test_one_migration_subclass_zero_raises`: a module with no subclass raises `RuntimeError`. `test_one_migration_subclass_two_raises`: a module with two subclasses raises `RuntimeError`.
- [x] 6.6 `test_prefix_id_uniqueness_across_migrations`: glob the real `migrations/` dir, extract `prefix_id` from each filename, assert no duplicates (this is the unit-test-enforced uniqueness guard the runner does NOT perform). This test fails if two migration files ever share a `prefix_id`.
- [x] 6.7 `test_schema_sql_last_migration_constant_matches_latest_migration`: parse `schema.sql` text, extract the `last_migration CONSTANT TEXT := '<...>'` value, glob `migrations/`, find the max `prefix_id` by string sort, assert they are equal. This catches the "forgot step 2" edit-procedure drift (design.md Risk #1).

## 7. Integration tests (testcontainers Postgres)

- [x] 7.1 In `tests/integration/test_migrations.py` (new or existing persistence integration test file), add `test_fresh_db_seeds_last_and_skips_migrations`: start a fresh Postgres testcontainer; `apply_schema(config)`; assert `yascheduler_migrations` has one row with `migration_id == <last_migration CONSTANT>`; `apply_migrations(config)`; assert no additional rows were inserted (the seeded migration was not re-applied); assert `yascheduler_nodes` has `username` and `port` columns (from the snapshot CREATE TABLE).
- [x] 7.2 `test_legacy_db_runs_all_migrations`: start a fresh Postgres testcontainer; manually `CREATE TABLE yascheduler_nodes (ip VARCHAR(15) UNIQUE)` (legacy shape, no username/port, no tracker); `apply_schema(config)`; assert `yascheduler_migrations` was created and is EMPTY (no seed, because `yascheduler_nodes` existed); `apply_migrations(config)`; assert `yascheduler_migrations` now has one row with `migration_id == "001"` (or the chosen prefix_id); assert `yascheduler_nodes` now has `username` and `port` columns (added by the migration).
- [x] 7.3 `test_modern_db_skips_bootstrap_and_applies_only_pending`: start a fresh Postgres testcontainer; manually `CREATE TABLE yascheduler_migrations (migration_id TEXT PRIMARY KEY, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())` and `CREATE TABLE yascheduler_nodes (ip VARCHAR(15) UNIQUE)` (simulate a modern DB that already has both the tracker and nodes — DO NOT run `apply_schema`, which would seed); insert a row into `yascheduler_migrations` with `migration_id = "000"` (simulate a DB that has applied up to "000"); `apply_schema(config)` (the DO block `to_regclass('yascheduler_migrations') IS NULL` guard is false → no-op; `CREATE TABLE IF NOT EXISTS yascheduler_nodes` is a no-op); `apply_migrations(config)`; assert only migrations with `prefix_id > "000"` were applied and recorded (in this change, that is `"001"` if "000" < "001"); assert the total row count in `yascheduler_migrations` is 2 (the "000" we inserted + the "001" the runner applied); assert `yascheduler_nodes` now has `username`/`port` columns (added by the "001" migration). This isolates the pending-path: the runner reads `MAX = "000"` and applies everything greater.
- [x] 7.4 `test_py_migration_best_effort_reopen`: create a temp `.py` migration file (in a temp dir pointed at by a testable `_MIGRATIONS_DIR` override, or via monkeypatch) whose `migrate()` calls `self.commit()` then runs a `CREATE TABLE test_reopen (id int)` then returns WITHOUT `self.begin()`; run `apply_migrations`; assert the tracker records the migration AND the `test_reopen` table exists (the migration's data was committed by its own `self.commit()`, and the tracker was recorded in a reopened transaction).
- [x] 7.5 `test_sql_migration_failure_rolls_back_and_not_recorded`: create a temp `.sql` migration that runs a valid statement then an invalid one (e.g. `CREATE TABLE x (id int); CREATE TABLE x (id int);` — the second errors); run `apply_migrations`; assert a `DatabaseError` is raised; assert `yascheduler_migrations` has NO row for that `prefix_id`; assert the first statement's table does NOT exist (transaction rolled back). (Place this and §7.4 temp-migration tests behind a fixture that monkeypatches `_MIGRATIONS_DIR` so they don't pollute the real migrations dir.)

## 8. GRACE-lite markup + knowledge graph

- [x] 8.1 Add `START_CONTRACT` blocks to `apply_migrations`, `_apply_sql_migration`, `_apply_py_migration`, `_one_migration_subclass` in `postgres_migrations.py` (PURPOSE/INPUTS/OUTPUTS/SIDE_EFFECTS/LINKS); add `START_BLOCK_*` anchors for OPEN_CONNECTION, READ_LAST, APPLY_PENDING, APPLY_SQL, APPLY_PY, TRACKER_RECORD, ROLLBACK, CLOSE.
- [x] 8.2 Add `M-PERSISTENCE-MIGRATIONS` and `M-PERSISTENCE-MIGRATION-BASE` entries to `docs/knowledge-graph.xml` (TYPE=CORE_LOGIC, STATUS=planned→implemented); add `<annotations>` for `apply_migrations`, `Migration`, `_one_migration_subclass`; add `<depends>` referencing `M-INFRA-DB-CONFIG`, `M-PERSISTENCE-SQLLOADER`, `M-PERSISTENCE-SCHEMA`; add `<CrossLink from="M-ENTRYPOINTS-CLI-INIT" to="M-PERSISTENCE-MIGRATIONS" relation="calls apply_migrations after apply_schema"/>`. Test modules stay out of the graph.
- [x] 8.3 Update `M-PERSISTENCE-SCHEMA` in the knowledge graph: add a note in its `<purpose>` or via a `<CrossLink>` that `schema.sql` now bootstraps `yascheduler_migrations`; update `<depends>` if needed.

## 9. Static checks + spec validation

- [x] 9.1 `uv run ruff check . && uv run ruff format --check .` pass.
- [x] 9.2 `uv run lint-imports` passes (new `postgres_migrations.py` imports intra-package; `migration_base.py` uses TYPE_CHECKING for `Connection`/`PostgresDbConfig`).
- [x] 9.3 `uv run zuban check` passes.
- [x] 9.4 `python3 scripts/grace_check.py` exits 0 (markup + graph valid after §8 updates).
- [x] 9.5 `openspec validate --all --json` passes (change + three delta specs valid — already passing after specs Round 2; re-run after any task touch).
- [x] 9.6 `uv run pytest -m unit` passes (new unit tests in §6 + existing).
- [x] 9.7 `uv run pytest -m integration` passes (new integration tests in §7 + existing; requires Docker for testcontainers).