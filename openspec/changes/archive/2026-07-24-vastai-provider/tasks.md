## 1. Provider skeleton & exception hierarchy

- [x] 1.1 Create `yascheduler/infra/cloud/providers/vastai.py` with a full GRACE `# region MODULE_CONTRACT` (PURPOSE: VastAI provider lifecycle — SSH key registration, offer search, cheapest selection, instance create/poll, delete; SCOPE: cloud-side lifecycle only, NOT DB/UoW/SSH-setup/allocator; DEPENDENCIES: USES API aiohttp, ConfigCloudVastAI, CloudInitConfig, CloudCreateNodeDTO, SSHKey; KEYWORDS) and the `__all__` exporting `vastai_create_node`, `vastai_delete_node`, `VastAIError`, `VastAIDeleteError`, `VastAINoOffersError`, `VastAIInvalidOfferError`, `VastAIInstanceCreateError`.
- [x] 1.2 (N/A — `aiohttp` is a core dependency, not optional; no guard needed)
- [x] 1.3 Define the VastAI exception hierarchy, each wrapped in a `# region CLASS_<Name>` with PURPOSE: `VastAIError` (root — auth 401/403, HTTP transport, unexpected response shape, SSH-key registration), `VastAIDeleteError`, `VastAINoOffersError`, `VastAIInvalidOfferError`, `VastAIInstanceCreateError` (create failure, polling timeout, terminal status). All subclass `VastAIError`; each carries a free-form `str` message (no custom `__init__`).
- [x] 1.4 Add public-function stubs: `# region FUNC_vastai_create_node` and `# region FUNC_vastai_delete_node` with full contracts (PURPOSE/REQUIRES/ENSURES/INVARIANTS — external_id=instance id, session closed on all paths, idempotent delete) and signatures, bodies `raise NotImplementedError`.
- [x] 1.5 Add helper stubs, each in a `# region FUNC_<name>` with PURPOSE/REQUIRES/ENSURES and signature, body `raise NotImplementedError`: `request`, `ensure_ssh_key`, `search_offers`, `select_cheapest_offer`, `generate_onstart`, `detect_launch_mode`, `wait_until_ready`. All async, taking `session: aiohttp.ClientSession` as the first parameter (plus specific args). None in `__all__`.
- [x] 1.6 Write unit tests (new VastAI test module) asserting: the five exception classes subclass `VastAIError` and `str(e)` carries the message; `VastAIError` is not a `SchedulingError`; all 7 helper stubs exist, are async, not in `__all__`, and raise `NotImplementedError`. Outcome: provider module imports, the exception hierarchy is correct and importable.

## 2. Pure helpers: offer selection, onstart generation, launch mode detection

- [x] 2.1 Implement `select_cheapest_offer` inside its contracted region: take a list of offer dicts (already price-sorted by the query), return the first; raise `VastAINoOffersError` on an empty list; raise `VastAIInvalidOfferError` when the chosen offer is missing its `id` or its `dph_total` exceeds the configured `max_price_per_hr`. Emit a block-marker log on selection.
- [x] 2.2 Implement `detect_launch_mode`: return `"kvm"` when `cfg.image` contains `vastai/kvm`, else `"docker"`. No logging (trivial).
- [x] 2.3 Implement `generate_onstart`: return `cfg.onstart_script` verbatim when non-empty; otherwise assemble a bash script from `CloudInitConfig` — package-manager upgrade when `package_upgrade`, install of `packages`, append `bootcmd` lines — with package-manager detection (apt-get for Debian/Ubuntu-derived images, dnf otherwise). KVM images get a `#!/bin/bash` shebang.
- [x] 2.4 Write unit tests (pure, no HTTP) for: `select_cheapest_offer` (empty→`VastAINoOffersError`, valid→first, missing id→`VastAIInvalidOfferError`, price over limit→`VastAIInvalidOfferError`); `detect_launch_mode` (kvm/docker by image substring); `generate_onstart` (custom verbatim, translation with apt-get, translation with dnf, bootcmd appended, shebang on KVM). Outcome: the three pure helpers behave per spec without any network.

## 3. HTTP layer: request wrapper, SSH key, offer search, readiness polling

- [x] 3.1 Implement `request` (the single HTTP entry point): issue `session.request(...)`, check `resp.status`, parse JSON, raise `VastAIError` on non-2xx or bad shape; redact `api_key` from any error/log. Reused by all HTTP-touching helpers.
- [x] 3.2 Implement `ensure_ssh_key`: GET the registered keys, compare by public key; POST if absent. Emit block-marker logs (presence check, registration) with the key fingerprint (not the key body) as a structured field.
- [x] 3.3 Implement `search_offers`: POST `/bundles/` with the fixed filter body (`gpu_ram` gte min_vram_mb, `num_gpus` eq, `gpu_frac` gte 1.0, `rentable` eq true, `rented` eq false, `dph_total` lte max_price_per_hr, `type` on-demand, `order` dph_total asc, `limit` 20) and return the offers list. Emit a block-marker log with the offer count.
- [x] 3.4 Implement `wait_until_ready`: poll `GET /instances/{id}/` until ready, bounded by `create_node_timeout`; on timeout or terminal status (exited/unknown/offline), best-effort DELETE the known instance id to prevent orphans, then raise `VastAIInstanceCreateError`. Emit a block-marker log per poll (status), on readiness (ssh_host/ssh_port), and on the orphan-cleanup attempt.
- [x] 3.5 Write unit tests with a mocked `aiohttp.ClientSession` (the hetzner mock pattern) for: `request` non-2xx→`VastAIError`, bad JSON→`VastAIError`, 2xx→parsed body, api_key absent from logged fields; `ensure_ssh_key` already-present→no POST, absent→POST; `search_offers` posts the expected filter body and returns offers; `wait_until_ready` ready→returns inst, timeout→best-effort DELETE then `VastAIInstanceCreateError`, terminal→best-effort DELETE then `VastAIInstanceCreateError`. Outcome: the HTTP helpers behave per spec against a mocked session.

## 4. Public callables: vastai_create_node orchestration & vastai_delete_node

- [x] 4.1 Implement `vastai_create_node` inside its contracted region: open `async with aiohttp.ClientSession()` and orchestrate `ensure_ssh_key` → `search_offers` → `select_cheapest_offer` → `generate_onstart` + `detect_launch_mode` → `PUT /asks/{offer.id}/` (image, disk, runtype, label, onstart, vm?) → `wait_until_ready` → build `CloudCreateNodeDTO` (external_id=instance id, hostname=ssh_host, port=ssh_port, username/port/jump_* from cfg). Emit block-marker logs for key/search/select/create/ready. Surface provider exceptions as-is (the provisioner wraps them in `CloudAllocateError`).
- [x] 4.2 Implement `vastai_delete_node` inside its contracted region: open `async with aiohttp.ClientSession()`, `DELETE /instances/{external_id}/`; treat a 404 / already-deleted indication as success (idempotent, no raise); raise `VastAIDeleteError` on any other non-2xx. Emit a block-marker log.
- [x] 4.3 Write unit tests with a mocked session asserting: `vastai_create_node` returns a `CloudCreateNodeDTO` with external_id=instance id (not IP), hostname=ssh_host, port=ssh_port, username/port/jump_* from cfg; the orchestration block-marker logs appear in order (key/search/select/create/ready); api_key is absent from all logged fields; `vastai_delete_node` issues DELETE and returns None on success and on 404, raises `VastAIDeleteError` on other non-2xx. Outcome: the public callables satisfy the cloud adapter contract end-to-end against a mocked session.

## 5. Rewrite shared create-delete test stubs & adapter integration

- [x] 5.1 Rewrite the existing VastAI stubs in the shared cloud-provider create-delete unit test to match the real contract: `vastai_create_node` returns a DTO with external_id=instance id (not IP) and hostname=ssh_host; `vastai_delete_node` accepts `external_id` and is idempotent. Use the mocked-session pattern from slice 4.
- [x] 5.2 Verify `get_vastai_adapter` in `yascheduler/infra/cloud/adapters.py` resolves against the now-importable provider module (no import error) and the adapter registers with `op_limit=1`. Add or adjust a unit test if the existing registration test references the old stubs. Outcome: the shared create-delete tests pass with the real provider contract and the adapter resolves.

## 6. E2E live test & documentation

- [x] 6.1 Add an env-gated e2e test (new VastAI live test module) modeled on `test_hetzner_live.py`: `YASCHEDULER_TEST_VASTAI=1` + `VAST_API_KEY` gate; build a `hetzner_config`-style config with a vastai section; submit two `test_shell` jobs; assert a cloud==vastai node appears in the DB; assert both jobs reach DONE with matching outputs; assert CLOUD_DONE and CLOUD_DELETE log records; poll for idle deallocation (node row removed). Use wider timeouts for GPU cold-start.
- [x] 6.2 Add the strong finally cleanup with a VastAI-native deletion oracle to the e2e: best-effort `vastai_delete_node` every observed instance id, then assert each is gone via a real `GET /instances/{id}/` (404 expected); a survivor fails the test loudly (no silent billing leak).
- [x] 6.3 Rewrite `docs/VASTAI.md` to document the actual adapter behavior: the `[clouds]` config keys (including `onstart_script` and `docker_options`), the cloud-init-to-onstart translation with package-manager detection, KVM/Docker auto-detection by image, on-demand-only pricing, and the env-gated e2e. Outcome: the e2e runs against a real VastAI account when gated, and the docs match the implemented adapter.