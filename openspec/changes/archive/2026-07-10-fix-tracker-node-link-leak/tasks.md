## 1. AllocationTracker shape change

- [x] 1.1 In `yascheduler/application/allocation_tracker.py`: change the internal field `self._on_tasks: set[TaskId]` → `self._entries: dict[TaskId, NodeId | None]` (line 42). Update the `START_MODULE_CONTRACT` PURPOSE and SCOPE to mention the task-to-node link. Update `START_MODULE_MAP` to list `add`, `set_node`, `discard`, `discard_by_node`, `__contains__`. Update `START_CONTRACT: AllocationTracker` INPUTS (constructor takes no args; methods take `task_id`/`node_id`). Bump VERSION and add a `START_CHANGE_SUMMARY` entry. The file has `from __future__ import annotations` (line 19), so all annotations are lazy strings — `NodeId` does NOT need a runtime import; keep it under `TYPE_CHECKING` alongside `TaskId` (add `NodeId` to the existing `if TYPE_CHECKING: from yascheduler.domain.model import TaskId` import). The method bodies store and compare passed values without referencing the `NodeId` name, so no runtime import is needed. Verify: `uv run pytest -m unit tests/unit/test_allocation_tracker.py` (will fail until 1.2; that's expected — RED).

- [x] 1.2 Change `add(self, task_id: TaskId) -> bool` → `add(self, task_id: TaskId, node_id: NodeId | None = None) -> bool` (line 44). Keep the dedup semantics: if `task_id` already in `self._entries`, return `False`; else `self._entries[task_id] = node_id; return True`. The default `node_id=None` preserves the existing `tracker.add(task.task_id)` call at `allocate_task.py:489` without modification. Update the `START_CONTRACT` for `add` (if a separate contract exists) or the class contract INPUTS. Verify: `uv run pytest -m unit tests/unit/test_allocation_tracker.py::TestAllocationTracker::test_add_new_task_returns_true test_add_duplicate_task_returns_false` pass.

- [x] 1.3 Add `set_node(self, task_id: TaskId, node_id: NodeId) -> None`: if `task_id` in `self._entries`, set `self._entries[task_id] = node_id`; else no-op (defensive — see design D4). Add a `START_CONTRACT: set_node` block above the method: PURPOSE "Patch the node link into an existing tracker entry", INPUTS `{ task_id: TaskId, node_id: NodeId }`, OUTPUTS `{ None }`, SIDE_EFFECTS "None — in-memory only. No-op if task_id is not tracked." Verify: a focused unit test `tracker.add(TaskId(42)); tracker.set_node(TaskId(42), NodeId(7)); assert tracker.discard_by_node(NodeId(7)) == 1` passes.

- [x] 1.4 Change `discard(self, task_id: TaskId) -> None` (line 51): `self._on_tasks.discard(task_id)` → `self._entries.pop(task_id, None)`. Semantics unchanged (no-op if not present). Verify: existing `test_discard_tracked_task_removes_it`, `test_discard_untracked_task_is_noop` pass.

- [x] 1.5 Add `discard_by_node(self, node_id: NodeId) -> int`: iterate `self._entries`, collect all `task_id`s whose value equals `node_id`, remove them from `self._entries`, return the count. Add a `START_CONTRACT: discard_by_node` block: PURPOSE "Remove all tracker entries linked to the given node and return the count removed", INPUTS `{ node_id: NodeId }`, OUTPUTS `{ int - count of entries removed }`, SIDE_EFFECTS "None — in-memory only." Verify: focused unit test — add two entries for the same node (corruption case), `discard_by_node` returns 2, both removed; add one entry for a different node, `discard_by_node` for the first node returns 0, the other entry survives.

- [x] 1.6 `__contains__(self, task_id: TaskId) -> bool` (line 54): `task_id in self._on_tasks` → `task_id in self._entries`. Semantics unchanged. Verify: existing `test_containment_check` passes.

## 2. allocate_task: add `set_node` call after tmp-node insert

- [x] 2.1 In `yascheduler/application/allocate_task.py`, inside `allocate_task`'s `START_BLOCK_ALLOCATE_CLOUD_CRITICAL_SECTION` (after line 514 `tmp_node_id = selected.node.node_id` and before line 516 `node = await _allocate_cloud_node(...)`), add `tracker.set_node(task.task_id, tmp_node_id)`. This patches the node link into the tracker entry created by `tracker.add(task.task_id)` at line 489. No `await` between the tmp insert and `set_node` — the `None` window is a single synchronous step. Update the `START_CHANGE_SUMMARY` with the new entry. Verify: `uv run pytest -m unit tests/unit/test_application_use_cases.py` — the cloud-fallback happy path test must assert `tracker.set_node.assert_called_once_with(todo_task.task_id, NodeId(1))` (add this assertion; see 2.2).

- [x] 2.2 In `tests/unit/test_application_use_cases.py`, the cloud-fallback happy path test (`test_allocate_task_cloud_fallback_happy_path`, around line 393): after the existing `tracker.add.assert_called_once_with(todo_task.task_id)` assertion, add `tracker.set_node.assert_called_once_with(todo_task.task_id, NodeId(1))` (the tmp node's `node_id` is `NodeId(1)` per the `cloud_node` fixture at line 365). Verify: the test passes.

- [x] 2.3 In `tests/unit/test_allocate_task_failure_modes.py`, the three failure-mode tests (`test_step1_commit_failure_discards_tracker`, `test_step2_cleanup_failure_still_discards_tracker`, `test_step3_persist_failure_discards_tracker`): `set_node` may or may not have been called depending on where the failure occurred (step1 commit failure → before `set_node`; step2/step3 → after `set_node`). Do NOT assert `set_node` was called in these tests — the failure-path contract only guarantees `tracker.discard(task_id)` in the `finally`. Verify: the three failure-mode tests still pass (they assert `tracker.discard` was called, which is unchanged).

## 3. abandon_node: replace dead lookup with `discard_by_node`

- [x] 3.1 In `yascheduler/application/abandon_node.py`, replace the `START_BLOCK_RELEASE_TASK` block (lines 70-79: the `list_by_status({TO_DO})` read + `matching` filter) and the `START_BLOCK_DISCARD_TRACKER` block (lines 96-106: the `len(matching) == 1` / `len(matching) > 1` branches) with a single call after `START_BLOCK_REMOVE_ROW`: `removed = tracker.discard_by_node(node.node_id)`; if `removed > 1`, log a warning `[abandon_node][AMBIGUOUS_TRACKER] node_id=%s ip=%s count=%d` (preserve the multi-match warning spirit — the count is now over tracker entries, not TO_DO tasks). Remove the `from yascheduler.domain import Node, TaskStatus` → `TaskStatus` import is no longer used (only `Node` remains used in the signature). Update the `START_MODULE_CONTRACT` PURPOSE (remove "release stuck TO_DO task" → "discard tracker entry linked to the node"). Update `START_MODULE_MAP` (remove "discard stuck task's tracker entry" → "discard tracker entry by node via discard_by_node"). Update `START_CONTRACT: abandon_node` INPUTS/SIDE_EFFECTS and the `START_BLOCK_RELEASE_TASK`/`START_BLOCK_DISCARD_TRACKER` anchor names (rename to `START_BLOCK_DISCARD_BY_NODE`). Bump VERSION and `START_CHANGE_SUMMARY`. Verify: `uv run pytest -m unit tests/unit/test_abandon_node.py` (will fail until 3.2; that's expected — RED).

- [x] 3.2 In `tests/unit/test_abandon_node.py`: rewrite the test suite for the new `discard_by_node` mechanism. The `_build_uow` helper no longer needs `todo_tasks` (the DB read is removed) — drop the `todo_tasks` parameter and the `uow.tasks` mock setup. The `_todo_task` helper is no longer needed — remove it. Rewrite the tests:
  - `test_happy_path_vm_deleted_row_removed_tracker_discarded`: assert `clouds.deallocate` called with `node`, `uow.nodes.remove` called with `NodeId(1)`, `tracker.discard_by_node` called with `node.node_id` and returns 1 (mock `tracker.discard_by_node.return_value = 1`).
  - `test_abandon_node_non_cloud_skips_vm_deletion`: assert `clouds.deallocate` is NOT called (unchanged), `uow.nodes.remove` called with `NodeId(1)` (unchanged), but now assert `tracker.discard_by_node` IS called with `node.node_id` — the discard runs regardless of cloud status (the tracker may have an entry for a non-cloud node if it was provisioned, and in practice non-cloud nodes are static and never tracked, but `discard_by_node` is unconditional now). The old test asserted `tracker.discard.assert_not_called()` — this assertion is REPLACED.
  - `test_abandon_node_cloud_delete_failure_does_not_block_db_cleanup`: unchanged (cloud delete raises → logged, DB remove still runs, `discard_by_node` still called).
  - `test_abandon_node_db_remove_failure_reraised`: assert `tracker.discard_by_node` is NOT called (the discard runs AFTER the remove; if remove raises, discard is skipped — matches the new ordering where discard is after the `try/except` around remove... OR keep discard in a `finally`? Decide: the current code discards AFTER the remove block, so if remove raises and re-raises, discard is skipped. This is acceptable — the node row removal failed, the node still exists, the task is still linked; on the next abandon attempt the discard will run. Document this in the test.)
  - `test_abandon_node_no_matching_tracker_entry_no_discard`: `tracker.discard_by_node.return_value = 0` → assert no warning logged, function returns without raising (the 0-count case is a no-op, no warning).
  - `test_abandon_node_multiple_tracker_entries_logs_warning`: `tracker.discard_by_node.return_value = 2` → assert warning logged with `AMBIGUOUS_TRACKER`, `node_id`, `ip`, `count=2`, function returns without raising.
  Verify: `uv run pytest -m unit tests/unit/test_abandon_node.py` passes (GREEN).

## 4. Integration test: restore the dropped tracker-release assertion

- [x] 4.1 In `tests/integration/test_never_connected_node_abandon.py`, the `START_BLOCK_VERIFY_TRACKER_RELEASED` block (lines 228-237): remove the NOTE that explains the dead-code gap, and restore the assertion `assert task_id not in tracker` (the leak is now fixed — `discard_by_node` closes it). The test should now verify that after `abandon_node` runs, the tracker entry for the task is gone. Update the comment to reflect the fix. Verify: `uv run pytest -m integration tests/integration/test_never_connected_node_abandon.py` passes.

## 5. Static checks and spec validation

- [x] 5.1 Run `uv run ruff check .` and `uv run ruff format --check .` — fix any lint/format issues in the modified files.
- [x] 5.2 Run `uv run lint-imports` — verify the `NodeId` import in `allocation_tracker.py` is at runtime (not under `TYPE_CHECKING`), and the `TaskStatus` import is removed from `abandon_node.py` if no longer used.
- [x] 5.3 Run `uv run zuban check` — fix any type errors in the modified files (the `dict[TaskId, NodeId | None]` shape, the `set_node`/`discard_by_node` signatures).
- [x] 5.4 Run `python3 scripts/grace_check.py` — verify GRACE-lite markup is valid (updated MODULE_CONTRACT/MODULE_MAP/CHANGE_SUMMARY in `allocation_tracker.py` and `abandon_node.py`).
- [x] 5.5 Run `openspec validate --all --json` — verify all specs and the change pass validation.
- [x] 5.6 Run `uv run pytest -m unit` — full unit suite passes.
- [x] 5.7 Run `uv run pytest -m integration` — full integration suite passes (including the restored tracker-release assertion).