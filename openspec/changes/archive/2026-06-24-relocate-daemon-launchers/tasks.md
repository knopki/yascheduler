## 1. Move the daemon launchers into entrypoints/daemon/

- [x] 1.1 Create `yascheduler/entrypoints/daemon/__init__.py` as a thin GRACE-lite subpackage facade: `# FILE: yascheduler/entrypoints/daemon/__init__.py`, `# VERSION: 1.0.0`, `START_MODULE_CONTRACT` (PURPOSE: subpackage facade for the daemon launchers; SCOPE: no re-exports — the launchers are invoked by path from service templates, not imported; DEPENDS: none; LINKS: M-ENTRYPOINTS-DAEMON), `START_MODULE_MAP` (empty or "none"), `START_CHANGE_SUMMARY` (LAST_CHANGE: v1.0.0 - Initial subpackage facade for the relocated daemon launchers; the flat residents `daemon_systemd.py` and `daemon_sysv.py` moved here from the package root in `relocate-daemon-launchers`). No `__all__`, no re-exports. A module docstring `"""Daemon launcher subpackage facade."""` is fine.
- [x] 1.2 Move `yascheduler/daemon_systemd.py` → `yascheduler/entrypoints/daemon/daemon_systemd.py` (preserve the full file: the `if __name__ == "__main__":` block, the `from .infra.cli import daemonize` / `from .shared import LOG_FILE` imports, the GRACE-lite MODULE_CONTRACT/MODULE_MAP/CHANGE_SUMMARY). Update the `# FILE:` header path to `yascheduler/entrypoints/daemon/daemon_systemd.py`. Bump `VERSION`. Add a `START_CHANGE_SUMMARY` entry: LAST_CHANGE describing the relocation into the `entrypoints/daemon/` subpackage and the relative→absolute import conversion (PREVIOUS_CHANGE stays as the v1.6.2 shared-kernel-extraction entry). Drop the `# FIXME: move this module to adapters` comment (stale — the move target is `entrypoints`, not `adapters`). The `MODULE_CONTRACT DEPENDS: M-CLI-COMMANDS, M-SHARED` and `LINKS: M-CLI-COMMANDS, M-SHARED` stay unchanged (the launcher still imports `daemonize` from `yascheduler.infra.cli` and `LOG_FILE` from `yascheduler.shared`).
- [x] 1.3 Move `yascheduler/daemon_sysv.py` → `yascheduler/entrypoints/daemon/daemon_sysv.py` (preserve the full file: `start_daemon`, the argparse `__main__` block, the `import argparse`/`os`/`daemon`/`from daemon import pidfile` imports, the `from .infra.cli import daemonize` / `from .shared import LOG_FILE, PID_FILE` imports, the GRACE-lite MODULE_CONTRACT/MODULE_MAP/CHANGE_SUMMARY). Update the `# FILE:` header path to `yascheduler/entrypoints/daemon/daemon_sysv.py`. Bump `VERSION`. Add a `START_CHANGE_SUMMARY` entry: LAST_CHANGE describing the relocation and the relative→absolute import conversion (PREVIOUS_CHANGE stays as the v1.6.2 shared-kernel-extraction entry). Drop the `# FIXME: move this module to adapters` comment. The `MODULE_CONTRACT DEPENDS: M-CLI-COMMANDS, M-SHARED` and `LINKS: M-CLI-COMMANDS, M-SHARED` stay unchanged. The `<fn-start_daemon>` annotation in the MODULE_MAP stays.

## 2. Convert relative imports to absolute facade paths

- [x] 2.1 In `yascheduler/entrypoints/daemon/daemon_systemd.py`, convert the two relative imports inside the `if __name__ == "__main__":` block: `from .infra.cli import daemonize` → `from yascheduler.infra.cli import daemonize`, and `from .shared import LOG_FILE` → `from yascheduler.shared import LOG_FILE`. (Inside the `entrypoints/daemon/` subpackage, `.infra` would resolve to `yascheduler.entrypoints.daemon.infra` — non-existent — and `.shared` to `yascheduler.entrypoints.daemon.shared` — non-existent. Absolute facade paths match the `entrypoints/client.py` convention.)
- [x] 2.2 In `yascheduler/entrypoints/daemon/daemon_sysv.py`, convert the two relative imports: `from .infra.cli import daemonize` → `from yascheduler.infra.cli import daemonize`, and `from .shared import LOG_FILE, PID_FILE` → `from yascheduler.shared import LOG_FILE, PID_FILE`. The `start_daemon` body's `working_directory=os.path.dirname(__file__)` stays unchanged (D6: the post-fork CWD shifts from `yascheduler/` to `yascheduler/entrypoints/daemon/`, but the consumed paths are env-overridable absolute defaults — `YASCHEDULER_CONF_PATH` / `YASCHEDULER_LOG_PATH` / `YASCHEDULER_PID_PATH` — so the CWD change is benign; accept as-is without compensating with `Path(__file__).parent.parent.parent`).

## 3. Update init.py daemon-file path computation

- [x] 3.1 In `yascheduler/infra/cli/init.py`, in `_init_systemd` (L57): rewrite `daemon_file = install_path / "daemon_systemd.py"` → `daemon_file = install_path / "entrypoints/daemon/daemon_systemd.py"`. Leave the `install_path = Path(__file__).parent.parent.parent` computation (L39) unchanged — it still resolves to the `yascheduler/` package dir, which is the correct prefix for the new subpackage path. The `%YASCHEDULER_DAEMON_FILE%` substitution into the systemd unit file template (`yascheduler/data/yascheduler.service`, `ExecStart=/usr/bin/python3 %YASCHEDULER_DAEMON_FILE%`) now produces the new absolute path.
- [x] 3.2 In `yascheduler/infra/cli/init.py`, in `_init_sysv` (L72): rewrite `daemon_file = install_path / "daemon_sysv.py"` → `daemon_file = install_path / "entrypoints/daemon/daemon_sysv.py"`. The `%YASCHEDULER_DAEMON_FILE%` substitution into the SysV init.d template (`yascheduler/data/yascheduler.sh`, `yascheduler=%YASCHEDULER_DAEMON_FILE%`) now produces the new absolute path.
- [x] 3.3 Bump `VERSION` in the `# FILE:` header of `yascheduler/infra/cli/init.py` and add a `START_CHANGE_SUMMARY` entry: LAST_CHANGE describing the daemon-file path computation update (the path prefix `install_path / "daemon_*.py"` → `install_path / "entrypoints/daemon/daemon_*.py"` to track the relocated launchers). PREVIOUS_CHANGE stays as the v2.0.2 rename-adapters-to-infra entry. The `MODULE_CONTRACT` PURPOSE/SCOPE/DEPENDS/LINKS stay unchanged (the installer still writes service unit files and creates the DB schema; no new dependencies).

## 4. Update entrypoints/__init__.py CHANGE_SUMMARY

- [x] 4.1 In `yascheduler/entrypoints/__init__.py`, revise the `START_CHANGE_SUMMARY` LAST_CHANGE entry: from "v1.0.0 - Initial entrypoints layer facade; only client.py resident. di.py, aiida_plugin.py, daemon_*.py, infra/cli/ migrate in follow-up changes." → "v2.0.0 - `client.py` and `aiida_plugin.py` flat residents; `daemon/` subpackage resident (daemon_systemd.py, daemon_sysv.py relocated from package root in `relocate-daemon-launchers`); only `di.py` and `infra/cli/` remain deferred for follow-up." Bump `VERSION` to 2.0.0. Move the existing v1.0.0 line to `PREVIOUS_CHANGE`. Do NOT re-export `start_daemon` or any daemon launcher symbol from the facade — `__all__` stays `["Yascheduler"]` (lazy-publication: the launchers are invoked by path from service templates, not imported across layers). The `MODULE_CONTRACT PURPOSE`/`SCOPE`/`DEPENDS`/`LINKS` and `MODULE_MAP` stay unchanged (the facade's public surface is unchanged; only the resident commentary in CHANGE_SUMMARY is updated).

## 5. Update knowledge graph

- [x] 5.1 In `docs/knowledge-graph.xml`, update `M-DAEMON-SYSTEMD <path>` (L120) from `yascheduler/daemon_systemd.py` → `yascheduler/entrypoints/daemon/daemon_systemd.py`. The `<depends>M-CLI-COMMANDS, M-SHARED</depends>` stays unchanged (the launcher still imports `yascheduler.infra.cli.daemonize` and `yascheduler.shared.LOG_FILE`). The empty `<annotations>` block stays.
- [x] 5.2 In `docs/knowledge-graph.xml`, update `M-DAEMON-SYSV <path>` (L128) from `yascheduler/daemon_sysv.py` → `yascheduler/entrypoints/daemon/daemon_sysv.py`. The `<depends>M-CLI-COMMANDS, M-SHARED</depends>` stays unchanged. The `<fn-start_daemon PURPOSE="SysV daemon entry point with PID file management" />` annotation stays.
- [x] 5.3 Add a new `M-ENTRYPOINTS-DAEMON` module element to `docs/knowledge-graph.xml` (placed adjacent to `M-DAEMON-SYSTEMD` / `M-DAEMON-SYSV` or in the entrypoints-layer section near `M-ENTRYPOINTS`): `NAME="Daemon launcher subpackage facade"`, `TYPE="ENTRY_POINT"`, `STATUS="implemented"`, `<purpose>Subpackage facade for the daemon launchers; re-exports nothing (launchers are invoked by path from service templates, not imported).</purpose>`, `<path>yascheduler/entrypoints/daemon/__init__.py</path>`, `<depends>none</depends>`, empty `<annotations>`. Per GRACE-lite "module added → M- entry" rule; mirrors the existing `M-ENTRYPOINTS` entry for `entrypoints/__init__.py`.
- [x] 5.4 Confirm no `CrossLink` or `DF-*` entry references the old `yascheduler/daemon_*.py` paths. Run `rg -n "daemon_systemd|daemon_sysv" docs/knowledge-graph.xml` and confirm the only hits are the `M-DAEMON-SYSTEMD` / `M-DAEMON-SYSV` `<path>` values (now updated) and the `M-ENTRYPOINTS-DAEMON` entry — no stale edges.

## 6. Rewrite package-facades spec delta (finalize the frozen delta)

- [x] 6.1 Confirm `openspec/changes/relocate-daemon-launchers/specs/package-facades/spec.md` contains 2 MODIFIED requirements with exact header text matching the live spec: "Entrypoints layer facade" and "Outside-layer-set exemptions". Each contains the FULL updated content (header + description + ALL scenarios). Scenarios use `####` (4 hashtags). (This delta was reviewed and frozen in the specs batch; this task is a final confirmation it is unchanged.)
- [x] 6.2 In the "Entrypoints layer facade" requirement: confirm the lazy-publication prose (3rd paragraph) drops `daemon_*.py` from the follow-up sentence (leaving only `di.py` and `infra/cli/*` as pending), adds a new sentence explaining the daemon launchers (`entrypoints/daemon/daemon_systemd.py` and `entrypoints/daemon/daemon_sysv.py`) are NOT re-exported either (invoked by path from service templates via `%YASCHEDULER_DAEMON_FILE%` substitution). Confirm the new scenario "Daemon launchers are not re-exported by the entrypoints facade" is present and attributes `start_daemon` only to `daemon_sysv.py`. Confirm the "Empty facade is valid for future residents" scenario parenthetical drops "daemon launchers" → "(e.g., CLI)".
- [x] 6.3 In the "Outside-layer-set exemptions" requirement: confirm the `yascheduler.daemon_systemd`, `yascheduler.daemon_sysv` bullet (L252 in live spec) is removed. Confirm the "Outside-set modules not flagged for layer direction" scenario enumeration drops both `yascheduler.daemon_systemd` and `yascheduler.daemon_sysv`. Confirm the new scenario "Daemon launchers are layer-checked after migration" is present and worded correctly (the modules now under `yascheduler.entrypoints.daemon` ARE checked for R3 and pass because their imports flow downward).

## 7. Point-edit docs/ARCHITECTURE.md

- [x] 7.1 §1 diagram: in the "COMPOSITION ROOT / OUTSIDE-LAYER-SET" box (L90), delete the two daemon lines at L97-98 (`daemon_systemd.py  Systemd entry point (deferred → entrypoints)` and `daemon_sysv.py     SysV entry point (deferred → entrypoints)`). In the "PRESENTATION (`yascheduler.entrypoints`)" box (L22), add a `daemon/           Systemd + SysV launcher subpackage` line (or equivalent formatting matching the box's existing resident list). The "outside layers contract; may import downward" footer on the COMPOSITION ROOT box stays (it still applies to the remaining outside-set modules: `di.py`, `client.py`, `config/`, `data/`).
- [x] 7.2 §2 Component Reference table: the `entrypoints/` row's resident list — add `daemon/` (systemd + sysv launchers) alongside the existing `client.py` and `aiida_plugin.py` entries. Do NOT add a standalone daemon row (the table has none today; the residents are listed under the `entrypoints/` row).
- [x] 7.3 §4 Project Structure tree: under the `entrypoints/` subtree (which already shows `__init__.py`, `client.py`, `aiida_plugin.py`), add a `daemon/` subpackage line with `__init__.py`, `daemon_systemd.py`, `daemon_sysv.py` as its contents. Remove the `daemon_systemd.py` and `daemon_sysv.py` lines from the package-root section of the tree (they are no longer at the root). Match the tree's existing indentation/formatting convention.
- [x] 7.4 Stale-reference sweep: run `rg -n "daemon_systemd|daemon_sysv" docs/ARCHITECTURE.md` and confirm every remaining hit is consistent with the new state (e.g., inside the `entrypoints/daemon/` subpackage, not at the package root, not described as "deferred"). Fix any straggler.

## 8. Verification

- [x] 8.1 `uv run pytest -m unit` — confirm all unit tests pass (no test references `daemon_*`; the `init.py` path-substitution edit is behavioral but is not exercised by unit tests — a dedicated test is deferred to a separate testing change per the proposal's Out of Scope).
- [x] 8.2 `uv run lint-imports` — confirm the `layers` contract passes: `yascheduler.entrypoints.daemon.daemon_systemd` and `yascheduler.entrypoints.daemon.daemon_sysv` are now in layer 1 (`entrypoints`) and import downward to `yascheduler.infra.cli` (layer 2) and `yascheduler.shared` (layer 5) — trivially R3-compliant. Confirm the `forbidden: shared → config` contract still passes.
- [x] 8.3 `uv run ruff check .` — confirm no lint errors.
- [x] 8.4 `uv run ruff format --check .` — confirm no formatting violations.
- [x] 8.5 `uv run zuban check` — confirm static analysis passes.
- [x] 8.6 `python3 scripts/grace_check.py` — confirm knowledge-graph XML + source MODULE_CONTRACT checks pass (including the updated `M-DAEMON-SYSTEMD`/`M-DAEMON-SYSV` paths, the new `M-ENTRYPOINTS-DAEMON` entry, the moved `daemon_*.py` file headers and CHANGE_SUMMARY entries, the new `entrypoints/daemon/__init__.py` MODULE_CONTRACT, and the updated `entrypoints/__init__.py` / `init.py` contracts).
- [x] 8.7 `openspec validate --all --json` — confirm all specs validate (the delta in `openspec/changes/relocate-daemon-launchers/specs/package-facades/spec.md` and the live specs in `openspec/specs/`).
- [x] 8.8 Import smoke check: `python -c "import yascheduler.entrypoints.daemon.daemon_systemd; import yascheduler.entrypoints.daemon.daemon_sysv; print('import OK')"` — confirm the new module paths load (the `daemon_systemd` module's `__main__` block is not executed on import; the `daemon_sysv` module's `start_daemon` is defined at module level and imports cleanly). Also confirm `from yascheduler import Yascheduler` still resolves (unaffected).
- [x] 8.9 init.py path smoke check: run `python -c "from pathlib import Path; install_path = Path('yascheduler/infra/cli/init.py').resolve().parent.parent.parent; p1 = install_path / 'entrypoints/daemon/daemon_systemd.py'; p2 = install_path / 'entrypoints/daemon/daemon_sysv.py'; assert p1.is_file() and p2.is_file(), (p1, p2); print('paths OK:', p1, p2)"` from the repo root — confirm both computed paths point at files that exist after the move.
- [x] 8.10 Stale-reference sweep: `rg -n "yascheduler\.daemon_systemd\b|yascheduler\.daemon_sysv\b" --glob '!openspec/changes/archive/**' --glob '!*.egg-info/**' --glob '!openspec/changes/relocate-daemon-launchers/**'` — confirm zero references to the old deep paths in non-archived, non-egg-info, non-this-change files. (Acceptable hits: archived OpenSpec change proposals, this change's own propositional artifacts, and the pre-sync live `openspec/specs/package-facades/spec.md` lines that the delta replaces at sync/archive time.)