## REMOVED Requirements

### Requirement: UnitOfWorkNotInitializedError exception class
**Reason**: Merged into the `domain-exceptions` capability as a new `UnitOfWorkNotInitializedError exception class` requirement (under `## ADDED Requirements`). The exception is a `RuntimeError` sibling grouped with the project's exception hierarchy for discoverability; it does not need a standalone spec.
**Migration**: Read `domain-exceptions` — `UnitOfWorkNotInitializedError exception class` requirement with all scenarios.

### Requirement: (any other requirement in this spec)
**Reason**: N/A — this spec had only one requirement.
**Migration**: N/A