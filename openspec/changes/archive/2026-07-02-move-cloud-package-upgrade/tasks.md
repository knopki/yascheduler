# Tasks

## 1. Domain: remove `cloud_package_upgrade` from `LocalSettings`

- [x] 1.1 In `yascheduler/domain/settings.py`, delete the `cloud_package_upgrade: bool = True` field (line ~58, including its `# FIXME: ...` comment) from `LocalSettings`. Bump module `VERSION` (1.2.0 → 1.3.0) and update `START_CHANGE_SUMMARY` (LAST_CHANGE: relocate cloud_package_upgrade to ConfigCloud* DTOs, this change; PREVIOUS_CHANGE: the v1.2.0 entry). No `__post_init__` change needed (the field had no validation). Verify: `uv run pytest -m unit`; `python -c "import dataclasses,yascheduler.domain as d; assert 'cloud_package_upgrade' not in [f.name for f in dataclasses.fields(d.LocalSettings)]"`.

## 2. Infra DTOs: add `package_upgrade` to all four `ConfigCloud*`

- [x] 2.1 In `yascheduler/infra/cloud/cloud_configs.py`, add `package_upgrade: bool = True` to `ConfigCloudAzure`, `ConfigCloudHetzner`, `ConfigCloudUpcloud`, and `ConfigCloudVastAI` (place it next to `idle_tolerance`/`connect_grace` for each DTO). Bump module `VERSION` (1.2.0 → 1.3.0) and update `START_CHANGE_SUMMARY`. Do NOT add the field to the `CloudConfig` Protocol or to `AzureImageReference`. Verify: `python -c "from yascheduler.infra.cloud import ConfigCloudHetzner; assert ConfigCloudHetzner().package_upgrade is True; assert ConfigCloudHetzner(package_upgrade=False).package_upgrade is False"`; same for the other 3 DTOs.

## 3. Config parser: parse `{prefix}_package_upgrade`, drop `[local]` key

- [x] 3.1 In `yascheduler/entrypoints/config_parser.py`, remove the `cloud_package_upgrade=sec.getboolean("cloud_package_upgrade", fallback=True),` line from `_parse_local_section` (line ~616). Bump module `VERSION` and update `START_CHANGE_SUMMARY` (the `[local]` parse is gone; `_local_valid_fields()` introspection drops the key automatically). Verify (covered by unit test in 5.2): a leftover `[local] cloud_package_upgrade = false` now emits a `ConfigWarning` (via `pytest.warns(ConfigWarning)`), and `_local_valid_fields()` no longer lists `cloud_package_upgrade`.
- [x] 3.2 In each of `_parse_azure_section`, `_parse_hetzner_section`, `_parse_upcloud_section`, `_parse_vastai_section`, add `package_upgrade=sec.getboolean(fmt("package_upgrade"), fallback=True)` to the DTO constructor call. Do NOT edit `cloud_valid_fields` or `_CLOUD_FIELD_RULES` — `cloud_valid_fields(prefix)` auto-introspects `dataclasses.fields(dto_cls)`, so `{prefix}_package_upgrade` registers automatically and `_ALL_CLOUD_VALID_FIELDS` follows. Verify: `python -c "from configparser import ConfigParser; from yascheduler.entrypoints.config_parser import parse_clouds; from yascheduler.domain import RemoteDefaults; cp=ConfigParser(); cp.add_section('clouds'); cp['clouds']['hetzner_token']='x'; cp['clouds']['hetzner_package_upgrade']='false'; cp['clouds']['az_tenant_id']='y'; [c.__class__.__name__+':'+str(c.package_upgrade) for c in parse_clouds(cp, RemoteDefaults())]"` → `ConfigCloudHetzner` has `package_upgrade=False`, `ConfigCloudAzure` has `True`.

## 4. Infra cloud manager: re-source `package_upgrade` from the per-cloud config

- [x] 4.1 In `yascheduler/infra/cloud/manager.py`, change `_get_cloud_config_data` signature to `async def _get_cloud_config_data(self, adapter: CloudAdapter, config: ConfigCloud) -> CloudInitConfig` and the body to `package_upgrade=config.package_upgrade`. Update the method's `START_CONTRACT` INPUTS (`config: ConfigCloud - resolved per-cloud config DTO`) and SIDE_EFFECTS/LINKS notes (drop `M-DOMAIN-SETTINGS`, add `M-CLOUD-CONFIGS`). `ConfigCloud` is already importable intra-package (`from .cloud_configs import ConfigCloud`); confirm the import. Bump module `VERSION` and update `START_CHANGE_SUMMARY`.
- [x] 4.2 In `CloudProvisionerImpl.allocate` (manager.py ~line 170), update the single call site to `cloud_config=await self._get_cloud_config_data(adapter, config)` (pass the `config` already resolved at line ~155). No other call sites exist. Verify: `uv run pytest tests/unit/test_cloud_provisioner_impl.py -q` after task 5.1.

## 5. Tests: re-source and add coverage

- [x] 5.1 In `tests/unit/test_cloud_provisioner_impl.py`, rename `test_cloud_config_package_upgrade_sourced_from_local_config` to `test_cloud_config_package_upgrade_sourced_from_per_cloud_config` and rewrite it to pass a real `ConfigCloudHetzner(package_upgrade=False)` as the new `config` arg to `_get_cloud_config_data(adapter, config)`; assert the returned `CloudInitConfig.package_upgrade is False`. Update `test_cloud_config_with_engine_packages` (currently sets `lc.cloud_package_upgrade = True` on a MagicMock) to pass a real DTO (e.g. `ConfigCloudHetzner(package_upgrade=True)`) as `config`. Bump the test file's VERSION + `START_CHANGE_SUMMARY`.
- [x] 5.2 In `tests/unit/test_config.py`, add parser coverage: (a) `[clouds] hetzner_package_upgrade = false` → `ConfigCloudHetzner.package_upgrade is False`; (b) `[clouds]` without the key → `ConfigCloudHetzner.package_upgrade is True` (default); (c) assert the new key does NOT warn — wrap case (a) in `pytest.warns(None)` (or `WarningsChecker`) to confirm `warn_unknown_fields` stays silent on `hetzner_package_upgrade`; (d) `assert "package_upgrade" not in dir(CloudConfig)` (or introspect `typing.get_type_hints(CloudConfig)`) to guard that the field is NOT on the domain Protocol; (e) one assertion that a `[local] cloud_package_upgrade = false` section now emits a `ConfigWarning` (via `pytest.warns(ConfigWarning)`). Bump VERSION + `START_CHANGE_SUMMARY` if the file carries GRACE-lite markup.
- [x] 5.3 In `tests/e2e/test_hetzner_live.py`, move `cloud_package_upgrade = false` out of the `[local]` block into the `[clouds]` block as `hetzner_package_upgrade = false`. Update the file's GRACE-lite comments (module-contract `hetzner_config` description, fixture PURPOSE, the inline comment at ~line 118) and bump VERSION + `START_CHANGE_SUMMARY`. Verify: the e2e config still parses and `config.clouds[0].package_upgrade is False`; `connect_grace` absence invariant unchanged.

## 6. User-facing docs: README.md

- [x] 6.1 In `README.md`, document the new common `[clouds]` key in the "These settings are common to all the providers" list (~lines 257-285, right after `*_idle_tolerance`): add a `*_package_upgrade` entry — "Per provider cloud-init `package_upgrade` flag on freshly-provisioned VMs. Set to `false` to skip the slow `apt-get upgrade` on first boot. _Default_: `true`." Note: the `[local]` section (~lines 122-209) never documented `cloud_package_upgrade` (the prior `add-hetzner-live-e2e` change omitted it), so there is nothing to remove there — only the new `[clouds]` entry is added. Verify: `grep -n 'package_upgrade' README.md` shows exactly one match (the new `*_package_upgrade` bullet); no `cloud_package_upgrade` mention remains.

## 7. GRACE-lite knowledge graph + final validation

- [x] 7.1 Update `docs/knowledge-graph.xml`: in `M-DOMAIN-SETTINGS` `<annotations>`, remove the `cloud_package_upgrade` reference (if present); in `M-CLOUD-CONFIGS` `<annotations>`, add `type-package_upgrade PURPOSE="cloud-init package_upgrade flag, default True"` (or update the DTO annotations). Update `M-CLOUD-MANAGER` `<depends>`/`CrossLink` to reflect `_get_cloud_config_data` now reading `M-CLOUD-CONFIGS` instead of `M-DOMAIN-SETTINGS` (and the `M-CLOUD-PROVISIONER → M-DOMAIN-SETTINGS` CrossLink at ~line 1266 shifts to `M-CLOUD-CONFIGS`). Run `python3 scripts/grace_check.py` (exit 0).
- [x] 7.2 Run the full verification suite: `uv run pytest -m unit`, `uv run pytest -m integration`, `uv run zuban check`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run lint-imports`, `openspec validate --all --json` (all must pass). Run `tests/e2e/test_hetzner_live.py` only if the Hetzner live credentials are configured (`YASCHEDULER_HETZNER_TOKEN` etc.); otherwise note it as deferred.
