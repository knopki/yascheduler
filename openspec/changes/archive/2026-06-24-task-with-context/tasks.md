## 1. Domain entity — Task.with_context + GRACE markup

- [x] 1.1 In `yascheduler/domain/model.py`, add the `Task.with_context` method after `Task.record_event` (before the `Task.with_event` overloads, around line 272). Signature: `def with_context(self, context: TaskContext) -> Task:`. Body: `return replace(self, context=context)`. Single line, no merge, no validation guard. Add a `START_CONTRACT: Task.with_context` / `END_CONTRACT: Task.with_context` block above the method (copy the shape of the `Task.record_event` contract: PURPOSE, INPUTS `{ context: TaskContext - new context to wholesale-replace }`, OUTPUTS `{ Task - new instance with context replaced, all other fields preserved }`, SIDE_EFFECTS `None`, LINKS `M-DOMAIN-MODEL`).
- [x] 1.2 In `yascheduler/domain/model.py`, update the `MODULE_MAP` (line 16): append `with_context` to the `Task - ...` line so it reads `Task - Task entity with allocate_to, mark_running, complete, fail, reject lifecycle, record_event, with_event, with_context, pull_events`.
- [x] 1.3 In `yascheduler/domain/model.py`, bump `VERSION` in the header (line 2): `1.10.0` → `1.11.0`. Update `START_CHANGE_SUMMARY` (lines 22-25): new `LAST_CHANGE: v1.11.0 - Add Task.with_context wholesale context setter (task-with-context).`, shift existing `LAST_CHANGE` to `PREVIOUS_CHANGE`.

## 2. Knowledge graph annotation

- [x] 2.1 In `docs/knowledge-graph.xml`, in the `M-DOMAIN-MODEL` element, add `<fn-with_context PURPOSE="Wholesale-replace task context, returning new Task" />` to the `<annotations>` block. Place it in the `<annotations>` block near the `class-Task` annotation (use the `fn-` prefix per GRACE-lite rules; there is no existing `fn-with_event` sibling — `with_event` is only mentioned inside `class-Task`'s PURPOSE string). Do not touch `M-DOMAIN-MODEL <depends>` or `<path>`.

## 3. Migrate call sites to with_context

- [x] 3.1 In `yascheduler/application/submit_task.py`, line 91: change `task = replace(task, context=context)` → `task = task.with_context(context)`. The preceding `context = replace(task.context, remote_folder=remote_folder)` on line 90 STAYS (the `TaskContext.with` helper is a follow-up change, out of scope). The `with_event` call on line 92 stays. Verify the chain reads `task.with_context(context).with_event(TaskCreated, engine_name=task.context.engine)`.
- [x] 3.2 In `yascheduler/application/consume_task.py`, `_record_finalization_event` (lines 98-114): in the `if sftp_errors:` branch (lines 105-109), DELETE line 107 `updated_context = replace(updated_context, error=error_msg)` (redundant — `.fail(error_msg)` on line 108 sets `context.error` internally via `replace(self.context, error=reason)`). Change line 108 `task = replace(task, context=updated_context).fail(error_msg)` → `task = task.with_context(updated_context).fail(error_msg)`. In the `else:` branch (lines 110-114), change line 111 `task = replace(task, context=updated_context).complete()` → `task = task.with_context(updated_context).complete()`. The `with_event` calls on lines 109 and 112-114 stay. The `updated_context = replace(task.context, local_folder=..., remote_folder=..., extra=...)` construction on lines 98-103 STAYS.
- [x] 3.3 In `yascheduler/application/consume_task.py` and `yascheduler/application/submit_task.py`, after the migrations above, check whether the `from dataclasses import replace` import is still needed. In `consume_task.py`: `replace` is still used at line 98 (`replace(task.context, ...)`), so the import STAYS. In `submit_task.py`: `replace` is still used at line 90 (`replace(task.context, remote_folder=...)`), so the import STAYS. Do NOT remove the import in either file.
- [x] 3.4 In `yascheduler/application/submit_task.py` and `yascheduler/application/consume_task.py`, bump `VERSION` in the `# FILE:` header (submit_task.py `1.1.0` → `1.2.0`, consume_task.py `5.1.0` → `5.2.0`) and add a `START_CHANGE_SUMMARY` entry in each describing the `with_context` migration (and the line-107 cleanup in consume_task.py).

## 4. Unit tests — Task.with_context suite

- [x] 4.1 In `tests/unit/test_domain_model.py`, add a `TestTaskWithContext` test class mirroring the structure of `TestTaskWithEvent` in `tests/unit/test_domain_events.py`. Use `make_task` / `Task` / `TaskContext` construction consistent with the existing test file's style. Construct a base task with a known `TaskContext` and a distinct `new_context` for assertions. The frozen-dataclass `FrozenInstanceError` is already imported in this file (line 40) — reuse it for the immutability assertion.
- [x] 4.2 Add test `test_with_context_replaces_context_wholesale`: assert `task.with_context(new_context).context is new_context` and that all other fields (`task_id`, `label`, `status`, `allocated_ip`, `_events`) are preserved unchanged from the original task.
- [x] 4.3 Add test `test_with_context_preserves_events`: build a task with a recorded event (via `task.record_event(...)` or `task.with_event(...)`), call `with_context(new_context)`, assert the returned task's `_events == original._events` (event tuple preserved).
- [x] 4.4 Add test `test_with_context_leaves_original_unchanged`: call `task.with_context(new_context)`, then assert the original `task.context` is unchanged (frozen dataclass — attempting `task.context = new_context` should raise `FrozenInstanceError`). Assert `original.context is not new_context`.
- [x] 4.5 Add test `test_with_context_no_status_validation`: parameterize over `TaskStatus.TO_DO`, `TaskStatus.RUNNING`, `TaskStatus.DONE` — for each, `task.with_context(new_context)` returns a new Task with the new context and the original status preserved, no error raised. This locks in the no-guard decision (mirrors the `with_context performs no status validation` scenario).
- [x] 4.6 Add test `test_with_context_chains_with_with_event`: `result = task.with_context(new_context).with_event(TaskCreated, engine_name=new_context.engine)`. Assert `result.context is new_context` and `len(result._events) == 1` and the event is a `TaskCreated` with `engine_name == new_context.engine` and `task_id == task.task_id` and `webhook_url == new_context.webhook_url` (base fields populated from the NEW context, not the old — this is the point of replacing context before recording the event).
- [x] 4.7 Add test `test_with_context_chains_with_fail`: build a RUNNING task (via `task.allocate_to(ip).mark_running()`), then `result = task.with_context(new_context).fail("reason")`. Assert `result.status == TaskStatus.DONE`, `result.context.error == "reason"`, and the rest of `new_context` (engine, remote_folder, local_folder, extra) is preserved on `result.context`. This confirms the consume_task error-branch chain works after the line-107 cleanup.
- [x] 4.8 Add test `test_with_context_chains_with_complete`: build a RUNNING task (via `task.allocate_to(ip).mark_running()`), then `result = task.with_context(new_context).complete()`. Assert `result.status == TaskStatus.DONE` and `result.context is new_context` (the consume_task success-branch chain). This closes the testing-unit delta's chaining-with-`complete` coverage.

## 5. Verify call-site migrations preserve behavior

- [x] 5.1 Run `uv run pytest -m unit tests/unit/test_application_use_cases.py -q` — confirm all existing submit_task and consume_task tests pass unchanged. Pay attention to assertions on `inserted_arg.context.extra` (line 137), `saved_arg.context.remote_folder` (lines 144-146), `saved_task.context.error` (lines 205, 616, 662). These are behavioral and should stay green.
- [x] 5.2 Run `uv run pytest -m unit tests/unit/test_application_orchestrator.py -q` — confirm the `saved_task.context.error == "node is gone"` assertion (line 452) still passes (orchestrator's `_task_consumer_consumer` is unaffected — it uses `task.fail("node is gone").with_event(TaskAbandoned, ...)`, no `with_context` migration needed there).
- [x] 5.3 Run `uv run pytest -m unit tests/unit/test_domain_model.py -q` — confirm the new `TestTaskWithContext` suite passes and no existing domain-model test regressed.

## 6. Static checks + GRACE + OpenSpec validation

- [x] 6.1 `uv run ruff check .` — confirm no lint errors (especially the new method and the migrated call sites).
- [x] 6.2 `uv run ruff format --check .` — confirm no formatting violations; if violations, run `uv run ruff format .` and verify only the touched files are reformatted.
- [x] 6.3 `uv run zuban check` — confirm static analysis passes.
- [x] 6.4 `uv run lint-imports` — confirm the import-layer contract passes (no layer change introduced; `with_context` is intra-domain).
- [x] 6.5 `python3 scripts/grace_check.py` — confirm knowledge-graph XML + source MODULE_CONTRACT checks pass, including the new `fn-with_context` annotation in `M-DOMAIN-MODEL`, the updated `model.py` MODULE_MAP/CHANGE_SUMMARY/VERSION, and the `with_context` START_CONTRACT block.
- [x] 6.6 `openspec validate --all --json` — confirm all specs validate (the delta specs in `openspec/changes/task-with-context/specs/` and the live specs in `openspec/specs/`). Must exit 0.
- [x] 6.7 Stale-reference sweep: run `rg -nU "replace\([^)]*context"` (multiline mode so the multi-line `replace(task.context, ...)` in consume_task.py:98-103 is matched). Confirm the only remaining `replace(...context...)` occurrences in `yascheduler/` are: `model.py:237` and `model.py:259` (internal to `fail`/`reject`, out of scope), `consume_task.py:98` (`replace(task.context, ...)` — the `TaskContext` construction that stays), `submit_task.py:90` (`replace(task.context, ...)` — the `TaskContext` construction that stays). No `replace(task, context=...)` (entity-level) should remain outside `model.py`.

## 7. Full unit suite + final smoke

- [x] 7.1 `uv run pytest -m unit -q` — confirm the full unit suite passes (domain, application, orchestrator, persistence-mocked, config, cli, di).
- [x] 7.2 `python3 -c "from yascheduler.domain.model import Task, TaskContext; t = Task(task_id=1, label='x', context=TaskContext(engine='fleur')); nc = TaskContext(engine='cp2k', remote_folder='/r'); r = t.with_context(nc); assert r.context is nc and r.task_id == 1 and r.status == t.status; print('with_context OK')"` — smoke-check the new method loads and behaves.
- [x] 7.3 Confirm `from yascheduler import Yascheduler` still imports cleanly (public API unaffected) — `python3 -c "from yascheduler import Yascheduler; print('public API OK')"`.