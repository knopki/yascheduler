## proposal Round 1 — 2026-06-25

### 🔴 Fixed
- none

### 🟡 Addressed
- **Spec-delta overlap with two in-flight changes is not acknowledged.** The proposal modifies the R1 "Within-package relative imports" requirement and the "Entrypoints layer facade" requirement in `openspec/specs/package-facades/spec.md` (proposal L115-133, L184-193, L227-228). Two in-flight changes already hold `## MODIFIED Requirements` deltas against the SAME R1 requirement in the SAME spec file:
  - `openspec/changes/relocate-check-status-command/specs/package-facades/spec.md` rewrites R1 wholesale (enumeration at its L10-12 still lists `yascheduler.entrypoints.daemon`; edits the `infra/cli/__init__.py` scenario to mention `check_status` and the `entrypoints/cli/__init__.py` scenario to add `check_status`).
  - `openspec/changes/relocate-manage-node-command/specs/package-facades/spec.md` rewrites R1 wholesale (enumeration at its L10-12 still lists `yascheduler.entrypoints.daemon`; edits the same two scenarios for `manage_node`).
  OpenSpec applies `## MODIFIED Requirements` by replacing the entire requirement body, so when this change's delta is eventually generated it will collide textually with both in-flight deltas on R1 unless sequenced. Recommended clarification to add to the proposal: state explicitly that this change MUST be archived AFTER `relocate-check-status-command` and `relocate-manage-node-command` (so its R1 delta is generated against the post-merge main spec and drops the `yascheduler.entrypoints.daemon` line without clobbering the scenario edits), or alternatively restrict this change's R1 delta to ONLY the enumeration line and leave the scenarios untouched. The end-state described by the proposal is correct; only the sequencing/coordination note is missing.

### 🔴 Outstanding
- none

### Notes
- All factual code references in the proposal verified accurate:
  - `yascheduler/entrypoints/daemon/daemon_systemd.py` (28 lines) and `daemon_sysv.py` (52 lines) exist with the described `MODULE_CONTRACT` (DEPENDS/LINKS: `M-CLI-COMMANDS, M-SHARED`), `START_CHANGE_SUMMARY` (LAST_CHANGE v1.7.0 `relocate-daemon-launchers`), and imports (`from yascheduler.infra.cli import daemonize`; `from yascheduler.shared import LOG_FILE` / `LOG_FILE, PID_FILE`). `working_directory=os.path.dirname(__file__)` confirmed at `daemon_sysv.py:38`.
  - `yascheduler/entrypoints/cli/init.py:103` (`daemon_file = install_path / "entrypoints/daemon/daemon_systemd.py"`) and `:127` (sysv variant) and `:60` (`install_path = Path(__file__).parent.parent.parent`) all match verbatim.
  - `yascheduler/entrypoints/cli/__init__.py` PURPOSE lists `init, show_nodes, submit, manage_node`; LINKS lists `M-ENTRYPOINTS-CLI-INIT, M-ENTRYPOINTS-CLI-SHOW-NODES, M-ENTRYPOINTS-CLI-SUBMIT, M-ENTRYPOINTS-CLI-MANAGE-NODE`; VERSION 1.3.0; `__all__` not present (only docstring) — consistent with proposal L65-67.
  - `yascheduler/entrypoints/__init__.py:15` LAST_CHANGE contains the `daemon/ subpackage resident (daemon_systemd.py, daemon_sysv.py relocated from package root in relocate-daemon-launchers)` clause the proposal proposes to revise.
  - `docs/knowledge-graph.xml`: M-DAEMON-SYSTEMD `<path>` at L117, M-DAEMON-SYSV `<path>` at L125, M-ENTRYPOINTS-DAEMON element at L132-138, DF-DAEMON-START at L929 (`M-DAEMON-SYSTEMD / M-DAEMON-SYSV -> M-CLI-COMMANDS`). M-DAEMON-SYSTEMD `<annotations>` empty; M-DAEMON-SYSV carries `<fn-start_daemon ...>`. Confirmed via grep that `M-ENTRYPOINTS-DAEMON` has NO inbound `CrossLink` and NO `DF-*` reference anywhere in the graph (only self-reference at `entrypoints/daemon/__init__.py:7` LINKS, which is being deleted) — so deletion is graph-safe as the proposal claims.
  - `openspec/specs/package-facades/spec.md`: R1 enumeration at L96-98 lists `yascheduler.entrypoints.daemon`; daemon-launcher prose at L186-193; "Daemon launchers are not re-exported by the entrypoints facade" scenario at L207-209; "Daemon launchers are layer-checked after migration" scenario at L283-285 (referencing `yascheduler.entrypoints.daemon.daemon_systemd` / `.daemon_sysv`). All match the proposal's planned edits.
  - `tests/unit/test_cli_init.py:241` (`assert "daemon_systemd.py" in content`) and `:257` (`assert "daemon_sysv.py" in content`) are basename-only. Grep across `tests/` confirms NO assertion references the full `entrypoints/daemon/` path — proposal's "no test rewrite needed" claim holds.
  - `pyproject.toml:[project.scripts]` (L48-54) registers `yainit`, `yanodes`, `yascheduler`, `yasetnode`, `yastatus`, `yasubmit` — NO `daemon_systemd` / `daemon_sysv` entry. Proposal's "invoked by path, not registered" claim holds.
  - `yascheduler/data/yascheduler.service:8` (`ExecStart=/usr/bin/python3 %YASCHEDULER_DAEMON_FILE%`) and `yascheduler/data/yascheduler.sh:27` (`yascheduler=%YASCHEDULER_DAEMON_FILE%`) both carry the placeholder. Proposal's BREAKING (re-install) rationale holds: `yainit` is the sole path-producing step, the launchers are not imported, and a stale unit pointing at the removed `entrypoints/daemon/` path will fail to start.
  - `yascheduler/entrypoints/daemon/__init__.py` SCOPE is "no re-exports — the launchers are invoked by path from service templates, not imported." — matches proposal L53-54.
  - Grep across `docs/` confirms `daemon_systemd` / `daemon_sysv` / `entrypoints/daemon` appear ONLY in `docs/knowledge-graph.xml` (the three M-ENTRYPOINTS-DAEMON-section paths) — proposal's "no docs/ARCHITECTURE.md hits" claim holds.
- GRACE-lite markup edits planned by the proposal are consistent with existing file conventions (FILE header → VERSION → MODULE_CONTRACT → MODULE_MAP → CHANGE_SUMMARY ordering; `M-DAEMON-SYSTEMD`/`M-DAEMON-SYSV` M-IDs added to cli facade LINKS match the existing `M-ENTRYPOINTS-CLI-*` style; CHANGE_SUMMARY PREVIOUS_CHANGE preservation matches the L18 pattern in both launcher files).
- Scope is exactly as the user instructed (move two launchers into `entrypoints/cli/`, liquidate `entrypoints/daemon/`); no extraneous changes. "Out of scope" section correctly defers `daemonize`/`check_status`/`di.py` migrations to the named follow-up changes.
- Minor narrative imprecision (not a defect): proposal L11-13 calls `relocate-init-command`, `relocate-show-nodes-command`, `relocate-submit-command`, `relocate-manage-node-command` "four recent relocation changes" and frames them as established precedent. `relocate-manage-node-command` is still in-flight in `openspec/changes/` (not yet archived), though the codebase already reflects its implementation (`cli/__init__.py` LINKS includes `M-ENTRYPOINTS-CLI-MANAGE-NODE`). Does not affect proposal correctness.

## design Round 1 — 2026-06-25

### 🔴 Fixed
- none

### 🟡 Addressed
- **D6 internal wording tension on R1 delta body vs sequencing.** D6 "Choice" bullet (design.md L279-280) states this change's R1 delta "reproduces the current main-spec scenarios verbatim (so it does not clobber the in-flight edits) and only alters the enumeration line." The D6 "Sequencing note" (design.md L305-312) then states the change "MUST be archived AFTER `relocate-check-status-command` and `relocate-manage-node-command` (so its R1 enumeration-line drop is generated against the post-merge main spec)." These two statements are in tension: "current main-spec scenarios" read at authoring time are PRE-merge (the in-flight changes' scenario edits to `infra/cli/__init__.py` and `entrypoints/cli/__init__.py` resident lists are not yet in the main spec — verified: main spec L101/L107 still list `check_status, daemonize, manage_node` and `show_nodes and submit`, whereas the in-flight deltas rewrite them). OpenSpec applies `## MODIFIED Requirements` by full-body replacement, so archiving this change AFTER the in-flight ones with a delta body frozen at pre-merge scenarios WOULD clobber their scenario edits. The Sequencing note's "generated against the post-merge main spec" is the correct resolution (regenerate the delta body at archive time against the then-current main spec, changing only the enumeration line), but the Choice bullet's "current main-spec scenarios verbatim" phrasing could mislead an implementer into freezing the delta now. Recommend tightening the Choice bullet to say "reproduces the post-merge (post-in-flight-archive) main-spec scenarios verbatim" or "the delta body is regenerated at archive time." The design's intent is sound and resolvable; flagged as clarification, not a blocking defect.

### 🔴 Outstanding
- none

### Notes
- All factual code citations in design.md verified accurate:
  - `daemon_sysv.py:38` `working_directory=os.path.dirname(__file__)` confirmed verbatim.
  - `yascheduler/shared/variables.py:24-26` confirmed env-overridable absolute defaults (`CONFIG_FILE`/`LOG_FILE`/`PID_FILE` via `getenv` with absolute fallbacks). D3's benign-CWD-shift rationale holds.
  - `entrypoints/cli/__init__.py` has NO `__all__` (only docstring at L19) — confirmed. `entrypoints/__init__.py:23` has `__all__ = ["Yascheduler"]`.
  - `entrypoints/cli/init.py:60` (`install_path = Path(__file__).parent.parent.parent`), `:103` (`entrypoints/daemon/daemon_systemd.py`), `:127` (`entrypoints/daemon/daemon_sysv.py`) all match D5 verbatim. `init.py` being itself an `entrypoints/cli/` resident means `.parent.parent.parent` resolves to `yascheduler/` for the new `entrypoints/cli/daemon_*.py` path — D5's "install_path unchanged" claim is correct.
  - `yascheduler/data/yascheduler.service:8` (`ExecStart=/usr/bin/python3 %YASCHEDULER_DAEMON_FILE%`) and `yascheduler/data/yascheduler.sh:27` (`yascheduler=%YASCHEDULER_DAEMON_FILE%`) confirmed — D5's BREAKING rationale holds.
  - `tests/unit/test_cli_init.py:241` (`assert "daemon_systemd.py" in content`) and `:257` (`assert "daemon_sysv.py" in content`) are basename-only; grep across `tests/` finds no full-path assertion. Design's "no test rewrite" stance holds.
  - `docs/knowledge-graph.xml`: `M-DAEMON-SYSTEMD <path>` at L117, `M-DAEMON-SYSV <path>` at L125 (with `<fn-start_daemon>` at L128), `M-ENTRYPOINTS-DAEMON` block at L132-138, `DF-DAEMON-START` at L929 (`M-DAEMON-SYSTEMD / M-DAEMON-SYSV -> M-CLI-COMMANDS`). Grep confirms `M-ENTRYPOINTS-DAEMON` appears ONLY in its own graph block and `entrypoints/daemon/__init__.py:7` (its own LINKS, being deleted) — D7's graph-safe-deletion claim holds.
  - `openspec/specs/package-facades/spec.md`: R1 enumeration at L96-98 includes `yascheduler.entrypoints.daemon`; daemon-launcher prose at L186-193; "not re-exported" scenario at L207-209; "layer-checked after migration" scenario at L283-285 (referencing `yascheduler.entrypoints.daemon.daemon_systemd`/`.daemon_sysv`). All match D6's planned edits.
- The two in-flight changes' `specs/package-facades/spec.md` deltas (`relocate-check-status-command`, `relocate-manage-node-command`) each contain ONLY `### Requirement: Within-package relative imports (R1)` — neither touches the "Entrypoints layer facade" requirement. Both keep `yascheduler.entrypoints.daemon` in their R1 enumeration (their L10-12). D6's claim that the "Entrypoints layer facade" delta is safe to include in full is correct; D6's claim that this change owns the enumeration-line drop (neither in-flight change drops it) is correct.
- Design D4 correctly diverges from (and corrects) proposal L65-67: the proposal asserts `entrypoints/cli/__init__.py`'s "`__all__` stays `["Yascheduler"]`", but that file has NO `__all__`. D4 (design.md L212-216) catches this and clarifies the `__all__ = ["Yascheduler"]` reference belongs to `entrypoints/__init__.py`. This is a design improvement over the proposal, not a contradiction defect. (The proposal Round 1 Notes L19 described this as "consistent with proposal L65-67", which was itself imprecise — the design rectifies it.)
- Every Decision (D1-D7) has a real rationale and ≥1 rejected alternative (D1: 2; D2: 1; D3: 1; D4: 1; D5: 2; D6: 3; D7: 1). No hand-wavy or contradictory decisions.
- Risks/Trade-offs table covers all material risks: BREAKING re-install, spec-delta collision, CWD shift, graph-deletion safety, test-assertion concern, plus two extra (verbatim-move drift, `__all__` confusion). No missing material risk.
- Migration plan order-of-operations (1-9) is implementable in sequence; rollback is correct (revert PR + re-run `yainit` post-rollback); sequencing note (archive after the two in-flight changes) is present and consistent with D6.
- Scope discipline maintained: Non-Goals explicitly exclude `daemonize` reimplementation, `argv`, exit-code contract, service-template changes, `console_script`, `application/`/`domain/`/`infra/*` touches, DB schema, new dependencies, filename renaming. No scope creep beyond proposal.md.
- GRACE-lite markup edits planned (FILE header / VERSION / CHANGE_SUMMARY LAST_CHANGE→PREVIOUS_CHANGE promotion; PURPOSE/SCOPE/LINKS revision; empty-MODULE_MAP preservation) match existing file conventions in both launcher files and both facades.
- D6 addresses the proposal Round 1 🟡 sequencing note (spec-delta overlap with the two in-flight changes); the design's archive-after + enumeration-line-only resolution is the recommended path forward.

## specs Round 1 — 2026-06-25

### 🔴 Fixed
- none

### 🟡 Addressed
- none

### 🔴 Outstanding
- none

### Notes
- Delta file is `openspec/changes/relocate-daemon-launchers-to-cli/specs/package-facades/spec.md` (109 lines). Top header is `## MODIFIED Requirements` (delta L1) — schema-compliant. Three requirements modified, each with `### Requirement:` (3 hashtags) and ≥1 `#### Scenario:` (4 hashtags): "Within-package relative imports (R1)" (delta L3-28, 4 scenarios), "Entrypoints layer facade" (delta L30-76, 5 scenarios), "Outside-layer-set exemptions" (delta L78-109, 5 scenarios). SHALL/MUST normative language preserved verbatim from main spec.
- Verified each delta block against `openspec/specs/package-facades/spec.md` via byte-level `diff` (main-spec L89-115, L167-213, L254-285 extracted and compared). Each diff produces EXACTLY the intended change set and nothing else:
  - **R1**: single diff at the enumeration line — main L96-98 ("…`yascheduler.infra.persistence`,\n`yascheduler.entrypoints.daemon`, `yascheduler.entrypoints.cli`, and all\nother subpackages.") → delta L10-11 ("…`yascheduler.infra.persistence`,\n`yascheduler.entrypoints.cli`, and all other subpackages."). All 4 R1 scenarios reproduced verbatim (no resident-list edits), honoring design D6's "enumeration-line only" restriction so the in-flight `relocate-check-status-command` / `relocate-manage-node-command` R1 scenario edits are not clobbered. Confirmed: in-flight deltas still keep `yascheduler.entrypoints.daemon` in their enumeration (their L11), so this change uniquely owns the enumeration-line drop — D6's ownership claim holds.
  - **"Entrypoints layer facade"**: two diffs, both daemon-launcher path updates only. Delta L50-51 prose: `entrypoints/daemon/daemon_systemd.py` / `entrypoints/daemon/daemon_sysv.py` → `entrypoints/cli/daemon_systemd.py` / `entrypoints/cli/daemon_sysv.py`. Delta L72 scenario ("Daemon launchers are not re-exported by the entrypoints facade"): `start_daemon (from entrypoints/daemon/daemon_sysv.py)` and the `__main__` blocks of both `entrypoints/daemon/daemon_systemd.py` and `entrypoints/daemon/daemon_sysv.py` → all `daemon/` → `cli/`. All other prose and 4 scenarios (Yascheduler re-export, sole public surface, AiiDA plugin, empty-facade) reproduced verbatim. Consistent with proposal L184-193 and design D6.
  - **"Outside-layer-set exemptions"**: single diff at the "Daemon launchers are layer-checked after migration" scenario (delta L109). Module paths `yascheduler.entrypoints.daemon.daemon_systemd` / `.daemon_sysv` → `yascheduler.entrypoints.cli.daemon_systemd` / `.daemon_sysv`, AND the parenthetical "(now under the `yascheduler.entrypoints` layer)" → "(under the `yascheduler.entrypoints` layer)" drops "now" (the launchers were already under the `entrypoints` layer; they only moved subpackages within it — the wording change is correct). All other prose (outside-set list with `config`/`data`/`di`/`client`, shared-no-business-logic clause) and 4 scenarios reproduced verbatim. Consistent with proposal L184-193, L227-228 and design D6.
- No new re-exports claimed anywhere in the delta: the "Daemon launchers are not re-exported" scenario (delta L70-72) still asserts the launchers are NOT re-exported by the facade — consistent with proposal L65-67 and design D4 ("declarative-only" facade edits, `__all__` stays `["Yascheduler"]` for `entrypoints/__init__.py`).
- Scope discipline verified: grep `^### Requirement:` on the delta returns exactly 3 matches (R1, Entrypoints facade, Outside-layer-set exemptions) — none of "Compat shim for yascheduler.client", "Layers contract configuration", "Documented residual edges", "Domain package facade contents", "Extended facade contents", "Documented private-symbol carve-outs", "Broad ignore_imports tradeoff", "Public API stability", "Yascheduler client query method public contract", "Layer direction (R3)", "Shared kernel config-import prohibition", "Cross-package facade imports (R2)", "Package facade as public surface (lazy publication)" appear in the delta. Matches proposal's "Modified Capabilities" list (only `package-facades`).
- Internal consistency: no scenario references the old `entrypoints/daemon/` or `yascheduler.entrypoints.daemon` path while another references the new path — grep across the delta for `entrypoints/daemon` and `entrypoints\.daemon` returns zero hits (clean cutover).
- `openspec validate --changes "relocate-daemon-launchers-to-cli" --json` passes (`valid: true`, `issues: []`).
- Minor cosmetic observations (NOT defects, require no action):
  - The R1 enumeration collapse preserves the original Oxford-comma pattern, producing "`yascheduler.entrypoints.cli`, and all other subpackages" — grammatically slightly awkward as a single-item list followed by `, and`, but it is the faithful verbatim-minus-dropped-item subtraction and matches design D6's "enumeration-line only" prescription.
  - The delta file has no trailing newline at end of file (diff reported "\ No newline at end of file"). No functional impact on OpenSpec parsing.

## tasks Round 1 — 2026-06-25
### 🔴 Fixed
- none

### 🟡 Addressed
- none

### 🔴 Outstanding
- none

### Notes
- All factual citations in tasks.md verified accurate against source:
  - `daemon_systemd.py:3` `VERSION: 1.7.0` (task 1.2 bump 1.7.0→1.8.0 ✓); `:17` `LAST_CHANGE: v1.7.0 ... relocate-daemon-launchers` (task 1.2 promotion source ✓); `:18` `PREVIOUS_CHANGE: v1.6.2 ...` (correctly discarded by the LAST_CHANGE→PREVIOUS_CHANGE roll, matching the file's 2-entry history pattern ✓); `:24-28` `if __name__ == "__main__":` body with `from yascheduler.infra.cli import daemonize` / `from yascheduler.shared import LOG_FILE` (task 1.1 verbatim-preservation list ✓).
  - `daemon_sysv.py:3` `VERSION: 1.7.0` (task 2.2 bump ✓); `:17` `LAST_CHANGE: v1.7.0 ... relocate-daemon-launchers` (task 2.2 promotion source ✓); `:38` `working_directory=os.path.dirname(__file__),` (task 2.2 explicit preservation incl. this line ✓ — matches D3); `:34-42` `start_daemon` body; `:45-52` argparse `__main__` block; imports at `:24-31` (task 2.1 verbatim-preservation list ✓).
  - `entrypoints/cli/__init__.py:2` `VERSION: 1.3.0` (task 4.1 bump 1.3.0→1.4.0 ✓); `:4` PURPOSE, `:5` SCOPE, `:7` LINKS (M-ENTRYPOINTS-CLI-INIT/SHOW-NODES/SUBMIT/MANAGE-NODE), `:15` LAST_CHANGE v1.3.0 manage_node (task 4.1/4.3 sources ✓); `:19` docstring (task 4.2 source ✓); file has NO `__all__` (task 4.1 "Do NOT add `__all__`" correct — follows design D4's correction of the proposal's imprecise L65-67 claim ✓).
  - `entrypoints/__init__.py:2` `VERSION: 2.0.0` (task 5.1 bump 2.0.0→2.1.0 ✓); `:15` `LAST_CHANGE: v2.0.0 - client.py and aiida_plugin.py flat residents; daemon/ subpackage resident (daemon_systemd.py, daemon_sysv.py relocated from package root in relocate-daemon-launchers); only di.py and infra/cli/ remain deferred for follow-up.` (task 5.1 source-verbatim ✓); `:16` PREVIOUS_CHANGE v1.0.0 (will be overwritten by the v2.0.0 promotion — file's 2-entry history pattern ✓); `:23` `__all__ = ["Yascheduler"]` (the design D4 / proposal L66 reference target — unchanged by task 5.1 ✓).
  - `entrypoints/cli/init.py:2` `VERSION: 1.0.0` (task 6.3 bump 1.0.0→1.1.0 ✓); `:60` `install_path = Path(__file__).parent.parent.parent  # yascheduler/` (tasks 6.1/6.2 "leave unchanged" ✓); `:103` `daemon_file = install_path / "entrypoints/daemon/daemon_systemd.py"` (task 6.1 rewrite source ✓); `:127` `daemon_file = install_path / "entrypoints/daemon/daemon_sysv.py"` (task 6.2 rewrite source ✓); `:17-19` single LAST_CHANGE entry, no PREVIOUS_CHANGE (task 6.3 says "PREVIOUS_CHANGE stays as the v1.0.0 relocate-init-command entry" — note: there is currently NO PREVIOUS_CHANGE in this file; task 6.3 implicitly means the existing LAST_CHANGE v1.0.0 becomes PREVIOUS_CHANGE, matching the standard roll pattern — implementation-safe, slightly imprecise wording ✓).
  - `docs/knowledge-graph.xml:117` M-DAEMON-SYSTEMD `<path>yascheduler/entrypoints/daemon/daemon_systemd.py</path>` (task 7.1 ✓); `:118` `<depends>M-CLI-COMMANDS, M-SHARED</depends>`, `:119-120` empty `<annotations>` (task 7.1 "stay unchanged" ✓); `:125` M-DAEMON-SYSV `<path>yascheduler/entrypoints/daemon/daemon_sysv.py</path>` (task 7.2 ✓); `:128` `<fn-start_daemon PURPOSE="SysV daemon entry point with PID file management" />` (task 7.2 ✓); `:132-138` M-ENTRYPOINTS-DAEMON block (task 7.3 delete target ✓); `:929` `<DF-DAEMON-START NAME="Daemon start">M-DAEMON-SYSTEMD / M-DAEMON-SYSV -> M-CLI-COMMANDS</DF-DAEMON-START>` (task 7.3 "stays unchanged" ✓).
  - Grep confirms `M-ENTRYPOINTS-DAEMON` appears ONLY at knowledge-graph.xml:132,138 (its own block) — no `CrossLink` or `DF-*` references it. The only other reference is `yascheduler/entrypoints/daemon/__init__.py:7` LINKS self-reference (being deleted in task 3.1). Task 7.3 "graph-safe" claim and task 7.4 zero-hit verification will both hold ✓.
  - `tests/unit/test_cli_init.py:224` `class TestServiceInstall:`, `:227` `def test_overwrites_existing_systemd_unit`, `:243` `def test_overwrites_existing_sysv_script` (task 9.1 test names ✓); `:241` `assert "daemon_systemd.py" in content` (basename-only ✓); `:257` `assert "daemon_sysv.py" in content` (basename-only ✓).
- Completeness: every Decision D1-D7 and every proposal "What Changes" bullet is covered.
  - D1: tasks 1.3, 2.3, 3.1, 3.2 (delete all three files + dir).
  - D2: tasks 1.1/1.2 (systemd verbatim + markup), 2.1/2.2 (sysv verbatim + markup) — both 1.1/2.1 explicitly enumerate the unchanged substrings (imports, `if __name__`, `MODULE_CONTRACT`/`MODULE_MAP`/`CHANGE_SUMMARY`); 2.2 explicitly preserves `start_daemon` body incl. `working_directory=os.path.dirname(__file__)`.
  - D3: folded into task 2.2 (the explicit `working_directory` preservation line) — matches the user's verification target.
  - D4: tasks 4.1/4.2/4.3 (cli facade: VERSION/PURPOSE/SCOPE/LINKS/docstring/CHANGE_SUMMARY, no `__all__`, no re-export) and task 5.1 (entrypoints facade: VERSION/CHANGE_SUMMARY only; PURPOSE/SCOPE/DEPENDS/LINKS/MODULE_MAP stay).
  - D5: tasks 6.1 (L103), 6.2 (L127), 6.3 (VERSION + CHANGE_SUMMARY) — both 6.1/6.2 explicitly preserve `install_path` (L60).
  - D6: task 8.1 (re-read + confirm delta matches D6), task 8.2 (validate), task 9.12 (archive-after sequencing note). Task 8.1 correctly re-reads the frozen delta without editing it (soft-freeze respected).
  - D7: tasks 7.1 (M-DAEMON-SYSTEMD path), 7.2 (M-DAEMON-SYSV path), 7.3 (delete M-ENTRYPOINTS-DAEMON + DF-DAEMON-START unchanged), 7.4 (rg zero-hit verification).
  - proposal Tests bullet: task 9.1 (basename-only assertions; conditional full-path-assertion rewrite).
  - proposal BREAKING (re-install) flag: source-of-break is the path rewrite in tasks 6.1/6.2 — both present. The break itself is documented in proposal.md/design.md, not re-stated as a task (acceptable per the user's verification spec).
- Granularity: every task is a single-file edit, single regex sweep, or single command — all well under the ≤2-hour limit. No task conflates two independent edits.
- Ordering: 1.x → 2.x (move both launchers before deleting subpackage), 3.x (delete subpackage after both files gone), 4.x/5.x (facade edits independent of move but logically after), 6.x (init.py rewrites; trivially safe even before move since they edit a path string, but ordered after for narrative coherence), 7.x (graph), 8.x (spec delta), 9.x (verification: 9.8 path-existence test after 1.1/2.1, 9.10 dir-gone test after 3.2, 9.9/9.11 stale-reference sweep after all deletes). No ordering defect.
- Verifiability: every task has a concrete done-condition (path string rewritten, file deleted, command exit 0, grep zero hits, etc.).
- Scope discipline: no work outside proposal/design. No `daemonize` reimplementation, no `argv`, no exit-code contract, no service-template changes, no `console_script`, no `application/`/`domain/`/`infra/*` touches, no DB schema, no new dependencies, no filename renames — matches Non-Goals L87-105.
- Checkbox format: every task is `- [ ]` (verified by grep — 27 task lines, all matching). No `- [x]`, no nested-list ambiguity.
- Soft-freeze respect: tasks.md proposes no edits to proposal.md/design.md/specs/. Task 8.1 explicitly re-reads the frozen delta and confirms; task 8.2 runs the validator against it. Tasks 6.1/6.2 implement the path rewrite that is the source of the BREAKING flag (these edit `init.py`, not the frozen artifacts).
- Stale-reference sweep (tasks 9.9/9.11) pre-verified: live `entrypoints/daemon` / `entrypoints.daemon` hits are exactly the files being touched by this change (the three subpackage files, knowledge-graph.xml:117/125/134, init.py:103/127) plus the pre-sync main `openspec/specs/package-facades/spec.md` lines that the delta replaces at archive time. No unanticipated hits in `application/`, `domain/`, `infra/`, `tests/` (other than the basename-only assertions), or other docs. Task 9.9's acceptable-hits clause correctly enumerates these.
