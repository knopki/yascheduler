## REMOVED Requirements

### Requirement: MachineSession port
**Reason**: Merged into the new `ssh-infrastructure` capability's `MachineSession port` requirement. Content preserved verbatim.
**Migration**: Read `ssh-infrastructure` — `MachineSession port` requirement with all scenarios.

### Requirement: SSHMachineSession implements MachineSession
**Reason**: Merged into `ssh-infrastructure`'s `SSHMachineSession implements MachineSession` requirement. Content preserved verbatim.
**Migration**: Read `ssh-infrastructure` — `SSHMachineSession implements MachineSession` requirement.

### Requirement: Session is returned by repository and resolved per-tick by the orchestrator
**Reason**: Merged into `ssh-infrastructure`'s `Session is returned by repository and resolved per-tick by the orchestrator` requirement. Content preserved verbatim.
**Migration**: Read `ssh-infrastructure` — `Session is returned by repository and resolved per-tick by the orchestrator` requirement.