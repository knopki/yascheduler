## 1. Trace records render correctly through the rewritten LogFormatter

- [x] 1.1 Rewrite `yascheduler/shared/log.py`: remove the `YaLogger` class and the `get_logger` factory; retain only `LogFormatter`; add module-level `_NATIVE_KEYS` derived once at import time from a freshly constructed `logging.LogRecord` instance (introspection, no hardcoded literal list); add module-level `_PACKAGE` derived from the top segment of this module's own `__name__` (no hardcoded package literal); implement `LogFormatter.format` with the three-condition trace discriminator (DEBUG level AND non-empty `set(record.__dict__) - _NATIVE_KEYS` AND in-package logger name per `_PACKAGE` gate); render trace records as `[<shortname>][<funcName>]:<lineno> <message> <sorted k=v>` (shortname = `record.name.removeprefix(_PACKAGE + ".")`, fields sorted alphabetically by key, values via `repr()`); render regular records as `<LEVEL> <name>: <message>`; do NOT mutate `record.shortname`; update the file's `START_MODULE_CONTRACT`, `START_MODULE_MAP`, and `START_CHANGE_SUMMARY` to reflect the new exports and the removed symbols
- [x] 1.2 Update `yascheduler/shared/__init__.py`: drop re-exports of `YaLogger` and `get_logger` from `.log`; retain `LogFormatter` (from `.log`) and `Self`, `Unpack`, `StrEnum` (from `.compat`); update the file's `START_MODULE_MAP` and `START_CHANGE_SUMMARY` to reflect the reduced export surface
- [x] 1.3 Rewrite `tests/unit/test_log.py` around `LogFormatter` and the extra-diff discriminator: remove all `YaLogger`/`.trace()`/`get_logger`-specific tests; cover the discriminator's positive case (in-package DEBUG with extra renders the trace layout with sorted `k=v`), the regular-rendering cases (INFO/WARN/ERROR; DEBUG-without-extra; out-of-package DEBUG-with-extra), the alphabetical field ordering, the introspection-derived native-key set (assert it is non-empty and contains `name`/`msg`/`funcName`), the `_PACKAGE` derivation from `__name__`, and the single-formatter-both-handlers wiring contract
- [x] 1.4 Verify outcome: `uv run pytest -m unit tests/unit/test_log.py` passes; `LogFormatter` is the only symbol imported from `yascheduler.shared.log` by the test; no reference to `YaLogger` or `get_logger` remains in the rewritten file

## 2. Package emits trace via stdlib `debug+extra` and binds loggers via `getLogger(__name__)`

- [x] 2.1 Migrate the logger binding in every package module that currently uses `get_logger("M-...")` to `logger = logging.getLogger(__name__)`: `yascheduler/application/orchestrator.py`, `yascheduler/application/allocate_task.py`, `yascheduler/application/deallocate_nodes.py`, `yascheduler/application/abandon_node.py`, `yascheduler/application/query_tasks.py`, `yascheduler/infra/persistence/postgres_uow.py`, `yascheduler/infra/persistence/postgres_schema.py`, `yascheduler/infra/persistence/postgres_migrations.py`, `yascheduler/infra/cloud/manager.py`, `yascheduler/infra/cloud/ssh_keys.py`, `yascheduler/infra/cloud/provider_selection.py`, `yascheduler/infra/cloud/providers/az.py`, `yascheduler/infra/ssh/platform/linux.py`, `yascheduler/infra/ssh/platform/windows.py`, `yascheduler/infra/ssh/repository.py`, `yascheduler/infra/ssh/session.py`, `yascheduler/infra/ssh/operations/occupancy.py`, `yascheduler/infra/notifier/webhook.py`, `yascheduler/entrypoints/cli/show_nodes.py`; in each, replace `from yascheduler.shared import get_logger` with `import logging` (if not already imported), and remove any now-unused `YaLogger` type annotations or imports; update each file's `START_CHANGE_SUMMARY` to note the binding migration
- [x] 2.2 Migrate every `logger.trace(<BLOCK>, <k>=<v>, ...)` callsite in the package to `logger.debug(<BLOCK>, extra={<k>: <v>, ...})`: the positional block marker becomes the debug message text; keyword arguments become the flat `extra` dict (no nested sentinel key, no wrapper function); handle multi-line calls (e.g. `yascheduler/infra/persistence/postgres_migrations.py` `_record_py_tracker`'s split `logger.trace("TRACKER_RECORD", exc=exc,)`); an AST-based transform is the recommended mechanical tool (handles line spanning, trailing commas, nested calls correctly where sed would not); the transform is a one-shot migration aid, not a retained artifact
- [x] 2.3 Confirm that every `extra={...}` callsite introduced by 2.2 uses keys that do not collide with native `LogRecord` attribute names (`name`, `msg`, `funcName`, `levelname`, `levelno`, `lineno`, `module`, `pathname`, `filename`, `args`, `exc_info`, `exc_text`, `stack_info`, `process`, `processName`, `thread`, `threadName`, `taskName`, `created`, `msecs`, `relativeCreated`); the existing block fields (`ip`, `task_id`, `prefix`, `exc`, `hostname`, `cloud`, `node_id`, etc.) are all non-native
- [x] 2.4 Verify outcome: `uv run zuban check` is green; `python -c "import yascheduler"` (and importing each migrated module) succeeds; no occurrence of `get_logger`, `YaLogger`, or `.trace(` remains anywhere under `yascheduler/` (grep-verified); `uv run pytest -m unit tests/unit/test_daemon_common.py` confirms `LogFormatter` is still wired onto both the stderr `StreamHandler` and the file `FileHandler` in `configure_logger`
- [x] 2.5 Logging-hygiene corrections surfaced during implementation review: (a) drop vestigial DEBUG traces that carry no `extra` fields and are not asserted in any test — `query_tasks.EMPTY_DISPATCH`, `postgres_schema.{OPEN_CONNECTION,APPLY_SCHEMA,HANDLE_EXISTING,ROLLBACK,CLOSE}`, `postgres_migrations.{OPEN_CONNECTION,CLOSE}`, `provider_selection.NONE`, `linux_setup_deb_node.UPGRADE` — removing the now-dead module logger binding/import where logging becomes unused (`query_tasks`, `postgres_schema`); (b) revert the spurious version bump and changelog in `entrypoints/cli/daemon_common.py` (it binds no module logger and emits no trace, so nothing was actually migrated there); (c) remove the injected `log: logging.Logger | None` parameter from every `infra/ssh/platform/{linux,windows}.py` function (`deploy_local_files`, `deploy_local_archive`, `deploy_remote_archive`, `linux/windows_deploy_engines`, `log_mpi_version`, `linux_setup_node`, `linux_setup_deb_node`, `windows_setup_node`) and from the `SetupNodeCallable` protocol in `platform/protocol.py`; each module now binds `logger = logging.getLogger(__name__)` and emits directly (dropping `if log:` guards); `linux.py` renames the non-standard `_log` binding to `logger`; `SSHMachineSession.setup_node` drops its `log=logger` kwarg. Internal consistency fix: `linux/windows_deploy_engines` previously forwarded `log` only to `deploy_local_files` (not to the archive deploy helpers), so their DEBUG traces were silently dead — now all deploy helpers trace uniformly via the module logger.

## 3. Logging-discipline guards refocus on the stdlib contract

- [x] 3.1 Rewrite `tests/unit/test_log_scope_discipline.py`: remove `test_no_raw_debug_calls_in_yascheduler` (raw `.debug(` is now the sanctioned trace path), `test_logger_names_are_real_m_ids`, `test_no_setloggerclass_in_yascheduler`, and their synthetic-violation meta-tests (`test_guard_catches_raw_debug_call`, `test_guard_catches_fabricated_m_id`, `test_guard_catches_direct_getlogger_binding`); remove the helper functions used only by the removed guards (`_find_get_logger_calls*`, `_find_debug_calls*`, `_find_setloggerclass_calls*`, `_find_direct_getlogger_bindings*`); retain `test_no_injected_logger_in_collaborator_constructors` unchanged; update the file's `START_MODULE_CONTRACT`, `START_MODULE_MAP`, and `START_CHANGE_SUMMARY`
- [x] 3.2 Add `test_no_extra_key_collision_with_native_attrs` to `tests/unit/test_log_scope_discipline.py`: AST-walk every `extra={...}` literal in `yascheduler/`; derive the native `LogRecord` attribute set by the same introspection as `shared/log.py` (or import `_NATIVE_KEYS` from `yascheduler.shared.log`); fail naming the file, the offending key, and the call if any `extra` key intersects the native set; add a synthetic-violation meta-test asserting the helper catches a fabricated colliding key (e.g. `extra={"funcName": ...}`)
- [x] 3.3 Verify outcome: `uv run pytest -m unit tests/unit/test_log_scope_discipline.py` passes; the two guards (injected-logger + extra-collision) pass on the committed package without external resources

## 4. E2E and unit log-driven tests verify trace via `getMessage` + extra-diff

- [x] 4.1 Migrate the log-assertion helpers in `tests/e2e/test_full_cycle.py` (`_assert_allocation_logs`) off `record.block` / `record.fields` onto `record.getMessage()` (the former block marker is now the message) plus the extra-diff for field values (`{k: getattr(r, k) for k in r.__dict__ if k not in _NATIVE_KEYS}`, importing `_NATIVE_KEYS` from `yascheduler.shared.log`); update the `START_CHANGE_SUMMARY` entries that referenced the `record.block`/`record.fields` migration
- [x] 4.2 Migrate the log-assertion helpers in `tests/e2e/test_hetzner_live.py` (`_assert_cloud_done_log`, `_assert_cloud_delete_log`) off `record.block` / `record.fields` onto `getMessage()` + extra-diff; update the `START_CHANGE_SUMMARY`
- [x] 4.3 Migrate the trace assertions in `tests/unit/test_allocate_task_node_pairing.py` (`TestTryStartOnMachineNodeIdLogging`) off `record.block` / `record.fields` onto `getMessage()` + extra-diff; update the `START_CHANGE_SUMMARY`
- [x] 4.4 Migrate the test-file logger binding in `tests/e2e/test_consume_retry.py` from `get_logger("M-TEST")` to `logging.getLogger(__name__)`; remove the `from yascheduler.shared import get_logger` import
- [x] 4.5 Update the `log_records` fixture docstring in `tests/e2e/conftest.py` to describe `getMessage()` + extra-diff assertions instead of `record.block`/`record.fields`; the fixture mechanism itself (handler on the `"yascheduler"` parent logger at DEBUG) is unchanged because `logging.getLogger(__name__)` still produces `yascheduler.*` descendant names that propagate
- [x] 4.6 Verify outcome: `uv run pytest -m unit,integration,e2e` is green; no occurrence of `record.block` or `record.fields` remains in `tests/` (grep-verified); the `log_records` fixture still captures descendant-emitted records via propagation

## 5. OpenSpec main specs, knowledge graph, and AGENTS.md reflect the stdlib logging contract

- [x] 5.1 Archive the `logging` capability delta into `openspec/specs/logging/spec.md`: apply the REMOVED requirements (`YaLogger project logger class`, `M-ID namespaced logger names`), the ADDED requirement (`Module-local stdlib logger binding`), and the MODIFIED requirement (`LogFormatter renders trace and user-facing records distinctly`) so the main spec matches the change's `specs/logging/spec.md` content
- [x] 5.2 Archive the `testing-unit` capability delta into `openspec/specs/testing-unit/spec.md`: apply the MODIFIED `Logging discipline guard tests` requirement (two guards: injected-logger + extra-collision) so the main spec matches the change's `specs/testing-unit/spec.md` content
- [x] 5.3 Update `docs/knowledge-graph.xml` `M-LOGGING` entry: drop the `fn-trace`, `class-YaLogger`, and `export-get_logger` annotations; retain the `class-LogFormatter` annotation; update `<purpose>` if needed; leave `<depends>` and `<path>` unchanged
- [x] 5.4 Update `AGENTS.md` GRACE-lite logging subsections: rewrite the "Logger binding" guidance to mandate `logging.getLogger(__name__)` (remove the `get_logger`/M-ID-namespaced-logger-names guidance and the `yascheduler.` prefix factory rule); rewrite the "Record contract for tests" guidance to describe `getMessage()` + extra-diff against the introspection-derived native key set (remove the `record.block`/`record.fields` attribute contract); update the "LogFormatter rendering contract" to the `[module][funcName]:lineno msg k=v` trace layout and the regular `LEVEL name: message` layout; update the "M-ID namespaced logger names" subsection to reflect stdlib `__name__` binding (the `yascheduler.` prefix still emerges naturally from `__name__`)
- [x] 5.5 Verify outcome: `openspec validate --all --json` passes (exit 0); `python3 scripts/grace_check.py` passes (exit 0); `uv run ruff check .` and `uv run ruff format --check .` are green; `uv run lint-imports` passes
