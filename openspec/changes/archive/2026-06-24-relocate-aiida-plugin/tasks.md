## 1. Move the AiiDA plugin into entrypoints

- [x] 1.1 Move `yascheduler/aiida_plugin.py` → `yascheduler/entrypoints/aiida_plugin.py` (preserve the full file: both classes `YaschedJobResource`, `YaScheduler`, the `_MAP_STATUS_YASCHEDULER` / `_CMD_PREFIX` constants, all GRACE-lite contracts). Update the `# FILE:` header path to `yascheduler/entrypoints/aiida_plugin.py`. Bump `VERSION` in the header. Add a `START_CHANGE_SUMMARY` entry describing the relocation and the `make_aiida` deletion context. The `MODULE_CONTRACT DEPENDS: none` and `LINKS: M-AIIDA` stay unchanged (the plugin imports only `aiida.*` + stdlib; verified zero yascheduler imports). No internal import edits needed.
- [x] 1.2 Update `yascheduler/entrypoints/__init__.py`: change the comment from "only `client.py` resident; `di.py`, `aiida_plugin.py`, `daemon_*.py`, `infra/cli/` migrate in follow-up changes" → "`client.py` and `aiida_plugin.py` residents; `di.py`, `daemon_*.py`, `infra/cli/` migrate in follow-up changes". Bump `VERSION` and add a `START_CHANGE_SUMMARY` entry. Do NOT re-export `YaScheduler` or `YaschedJobResource` from the facade (lazy-publication policy; plugin discovery is via the entry-point registry, not the facade). `__all__` stays `["Yascheduler"]`.

## 2. Update pyproject.toml entry-point

- [x] 2.1 In `pyproject.toml`, update `[project.entry-points."aiida.schedulers"]`: change `yascheduler = "yascheduler.aiida_plugin:YaScheduler"` → `yascheduler = "yascheduler.entrypoints.aiida_plugin:YaScheduler"`. The entry-point *name* (`yascheduler`) is unchanged. Do NOT touch `[project.scripts]` or `[tool.importlinter]` (the layers contract already lists `yascheduler.entrypoints` as layer 1; the plugin imports only external `aiida.*`, trivially R3-compliant).

## 3. Delete make_aiida from di.py

- [x] 3.1 In `yascheduler/di.py`, delete the `make_aiida` function (L237-238, `def make_aiida(config: Config) -> None: raise NotImplementedError(...)`) and its entire `START_CONTRACT: make_aiida` / `END_CONTRACT: make_aiida` block (L230-236). This contract block is the only place `M-AIIDA` appears in the file — the top-level `MODULE_CONTRACT LINKS` field (L7) does NOT list `M-AIIDA` and needs no separate edit.
- [x] 3.2 In `yascheduler/di.py`, drop `make_aiida` from the `MODULE_CONTRACT SCOPE` field (L5: "make_daemon, make_cli_deps, make_aiida, CLIDeps dataclass" → "make_daemon, make_cli_deps, CLIDeps dataclass") and from the `MODULE_MAP` (L13: delete the `make_aiida - Stub for future AiiDA integration` line).
- [x] 3.3 In `yascheduler/di.py`, drop the "AiiDA" mention from the `MODULE_CONTRACT PURPOSE` (L4: "factories per entry point (daemon, CLI, AiiDA)" → "factories per entry point (daemon, CLI)") to stay consistent with the trimmed `SCOPE`. Add a `START_CHANGE_SUMMARY` entry (bump `VERSION`, new `LAST_CHANGE` describing the `make_aiida` deletion).

## 4. Delete TestMakeAiida from tests/unit/test_di.py

- [x] 4.1 In `tests/unit/test_di.py`, delete `class TestMakeAiida` (L172-183, the test method `test_raises_not_implemented_error` and the class docstring).
- [x] 4.2 In `tests/unit/test_di.py`, drop `make_aiida` from the import on L39: `from yascheduler.di import CLIDeps, make_aiida, make_cli_deps, make_daemon` → `from yascheduler.di import CLIDeps, make_cli_deps, make_daemon`.
- [x] 4.3 In `tests/unit/test_di.py`, update the `MODULE_CONTRACT SCOPE` (L6: drop `make_aiida` from "CLIDeps dataclass, make_cli_deps, make_aiida, make_daemon factories") and the `MODULE_MAP` (L14: delete the `TestMakeAiida - make_aiida stub raises NotImplementedError` line). Bump `VERSION`, add a `START_CHANGE_SUMMARY` entry.

## 5. Update knowledge graph

- [x] 5.1 In `docs/knowledge-graph.xml`, update `M-AIIDA <path>` (L81) from `yascheduler/aiida_plugin.py` → `yascheduler/entrypoints/aiida_plugin.py`. The `<depends>none</depends>` stays unchanged (plugin imports only external `aiida.*` + stdlib). The `<annotations>` block stays unchanged (`class-YaschedJobResource`, `class-YaScheduler`).
- [x] 5.2 In `docs/knowledge-graph.xml`, in `M-DI` (L377-388): delete the `<fn-make_aiida PURPOSE="Stub for future AiiDA integration" />` annotation (L385). Update the `M-DI <purpose>` (L378) to drop the "AiiDA" mention: "factories per entry point: daemon, CLI, AiiDA" → "factories per entry point: daemon, CLI" (keep it consistent with the edited `di.py` PURPOSE). Verify `M-AIIDA` is NOT in `M-DI <depends>` (L380) — it isn't, so no depends edit is needed.
- [x] 5.3 Confirm no `CrossLink` or `DF-*` entry references `M-AIIDA` (the spurious `M-AIIDA → M-CLIENT` CrossLink and `DF-AIIDA-INTEGRATION` were already deleted in `add-entrypoints-layer`). Run `rg -n "M-AIIDA" docs/knowledge-graph.xml` and confirm only the `M-AIIDA` module element and its `<depends>`/`<annotations>` remain — no stale edges.

## 6. Rewrite package-facades spec delta (already drafted — verify + finalize)

- [x] 6.1 Confirm `openspec/changes/relocate-aiida-plugin/specs/package-facades/spec.md` contains 4 MODIFIED requirements with exact header text matching the live spec: "Shared kernel config-import prohibition", "Entrypoints layer facade", "Outside-layer-set exemptions", "Public API stability". Each contains the FULL updated content (header + description + ALL scenarios). Scenarios use `####`. (This delta was reviewed and frozen in the specs batch; this task is a final confirmation it is unchanged.)
- [x] 6.2 In the "Shared kernel config-import prohibition" requirement: confirm the outside-set enumeration prose drops `yascheduler.db` (module removed) and `yascheduler.aiida_plugin` (relocated into the entrypoints layer), and the "legacy DB layer, or the AiiDA plugin is negligible" prose is rewritten to not reference removed/relocated modules.
- [x] 6.3 In the "Entrypoints layer facade" requirement: confirm the lazy-public follow-up sentence drops `aiida_plugin.py` (migrating in THIS change) and adds an explicit statement that the AiiDA plugin is NOT re-exported by the facade (discovered via the entry-point registry). Confirm the new scenario "AiiDA plugin is not re-exported by the entrypoints facade" is present.
- [x] 6.4 In the "Outside-layer-set exemptions" requirement: confirm the `yascheduler.aiida_plugin` bullet is removed (no shim, no longer at root). Confirm `yascheduler.db` is NOT listed in this requirement's enumeration (it never was here — only `aiida_plugin` is removed). Confirm the "Outside-set modules not flagged for layer direction" scenario enumeration drops `yascheduler.aiida_plugin` only.
- [x] 6.5 In the "Public API stability" requirement: confirm the `yascheduler.aiida_plugin` bullet is rewritten to key on the entry-point *name* `yascheduler` + object path `yascheduler.entrypoints.aiida_plugin:YaScheduler`, notes `importlib.metadata.entry_points` discovery by name, and flags the BREAKING deep-path removal (no shim). Confirm the "AiiDA plugin still loads" scenario is reground on the new entry-point path. Confirm the new scenario "Old aiida_plugin module path is gone" (ModuleNotFoundError) is present.

## 7. Rewrite testing-unit spec delta (already drafted — verify + finalize)

- [x] 7.1 Confirm `openspec/changes/relocate-aiida-plugin/specs/testing-unit/spec.md` contains 1 MODIFIED requirement "Dependency injection factories" with exact header text matching the live spec. Confirm the `make_aiida raises NotImplementedError` bullet is dropped and the other 3 bullets (`CLIDeps`, `make_cli_deps`, `make_daemon`) are preserved. Confirm the single scenario "make_cli_deps returns CLIDeps with PostgresUnitOfWork factory" is preserved. (This delta was reviewed and frozen; this task is a final confirmation.)

## 8. Refresh docs/ARCHITECTURE.md (full refresh, not point edits)

- [x] 8.1 §1 diagram: add a PRESENTATION (`yascheduler.entrypoints`) block at the top listing `client.py` (public Python client — Yascheduler facade) and `aiida_plugin.py` (AiiDA scheduler integration via SSH transport) as residents, with `depends on: infra, application, domain, shared`. Add a `shared/` block at the bottom (shared kernel: typing shims, async-to-sync bridge, path constants; depends on: stdlib only). In the Composition Root / outside-layer-set block, drop the `make_aiida()` line and the stale "ENTRY POINTS & LEGACY WRAPPERS" box; redistribute its contents: real residents → entrypoints; `client.py` shim → outside-set; `daemon_*.py` → outside-set (deferred); `config/` → outside-set (utility).
- [x] 8.2 §2 Component Reference table: fix the `client.py` row — it is now a compat shim re-exporting `Yascheduler` from `yascheduler.entrypoints.client` (NOT the facade; the real implementation lives in `entrypoints/client.py`). Fix the `aiida_plugin.py` row — now at `entrypoints/aiida_plugin.py`, uses SSH transport, does NOT use the `Yascheduler` client (the prior "uses Yascheduler client" claim was factually wrong — the plugin imports zero yascheduler modules, verified). Fix the `di.py` row — drop `make_aiida` from "Composition root: `make_daemon()`, `make_cli_deps()`".
- [x] 8.3 §2.8 Composition Root: delete the `make_aiida(config)` bullet (L242).
- [x] 8.4 §2.9 "Public API & Legacy Wrappers": rewrite to reflect that the public API lives in `entrypoints/client.py`; `client.py` at root is an explicit compat shim; `aiida_plugin.py` lives in `entrypoints/`. Drop the false "uses Yascheduler client" claim. Drop the "Legacy Wrappers" framing (the only remaining legacy wrappers are the daemon launchers, which are deferred).
- [x] 8.5 §3.7 Public API Stability: reground the AiiDA line to the new entry-point path `yascheduler.entrypoints.aiida_plugin:YaScheduler` (entry-point name `yascheduler` unchanged). Fix "class Yascheduler in `client.py` remains the public Python facade" → the facade is `entrypoints/client.py`; `client.py` is a shim re-export.
- [x] 8.6 §3.8 `class Yascheduler` (Public API): reground to `entrypoints/client.py` as the real home; `client.py` is the shim. The submit/query routing prose stays (it already describes `make_cli_deps` + `query_tasks` use case + UoW).
- [x] 8.7 §4 Project Structure tree: redraw. Show `entrypoints/` subtree containing `__init__.py`, `client.py`, `aiida_plugin.py`. Show `shared/` subtree (`async_utils.py`, `variables.py` — confirm against actual `yascheduler/shared/` contents). `client.py` remains at root as shim. Drop `aiida_plugin.py` from root. Expand `infra/` one nested level (`persistence/`, `ssh/`, `cloud/`, `cli/`, `notifier/`) without files (directories only). Show `config/`, `domain/`, `application/` as directories (their internal files are not enumerated below the top level — consistent with the "directories only below top level" decision). `di.py` stays at root.
- [x] 8.8 §6.2 (`make_aiida()` implementation): DELETE the entire subsection.
- [x] 8.9 §6.3 (`client.py` query methods via use cases): DELETE the entire subsection (resolved by `remove-legacy-db`; the content is already in §3.8 prose).
- [x] 8.10 §7 Open Questions table: DELETE the entire section (stale / cancelled per user instruction). The header note at the top of ARCHITECTURE.md (canonical source = knowledge graph; update this document when the two diverge) stays.

## 9. Verification

- [x] 9.1 `uv run pytest -m unit` — confirm all unit tests pass (especially `tests/unit/test_di.py` after `TestMakeAiida` deletion and the `make_aiida` import removal; `tests/unit/test_characterization.py`, `tests/unit/test_client_query.py` should be unaffected — they import `Yascheduler` via the package facade / entrypoints, not via `aiida_plugin`).
- [x] 9.2 `uv run lint-imports` — confirm the `layers` contract passes (entrypoints is layer 1; the relocated plugin imports only `aiida.*` + stdlib, trivially R3-compliant) and the `forbidden: shared → config` contract still passes.
- [x] 9.3 `uv run ruff check .` — confirm no lint errors.
- [x] 9.4 `uv run ruff format --check .` — confirm no formatting violations.
- [x] 9.5 `uv run zuban check` — confirm static analysis passes.
- [x] 9.6 `python3 scripts/grace_check.py` — confirm knowledge-graph XML + source MODULE_CONTRACT checks pass (including the updated `M-AIIDA` path, the `M-DI` annotation/purpose cleanup, the moved `aiida_plugin.py` file headers, and the updated `di.py` / `test_di.py` / `entrypoints/__init__.py` contracts).
- [x] 9.7 `openspec validate --all --json` — confirm all specs validate (the deltas in `openspec/changes/relocate-aiida-plugin/specs/` and the live specs in `openspec/specs/`).
- [x] 9.8 AiiDA entry-point smoke check: `python -c "import importlib.metadata as m; eps=m.entry_points(); e=[e for e in eps.select(group='aiida.schedulers') if e.name=='yascheduler'][0]; assert e.value=='yascheduler.entrypoints.aiida_plugin:YaScheduler', e.value; print('entry-point OK:', e.value)"` — confirm the new object path is registered (requires reinstall: `uv pip install -e .` or `uv sync` first so the new entry-point path is in the installed metadata).
- [x] 9.9 Import smoke check: `python -c "from yascheduler.entrypoints.aiida_plugin import YaScheduler, YaschedJobResource; print('import OK')"` — confirm the new module path loads. Also confirm `from yascheduler import Yascheduler` still resolves (unaffected). NOTE: `from yascheduler import Yascheduler` resolves ✓; `from yascheduler.entrypoints.aiida_plugin import …` module path is correct but the import requires the external `aiida` package (declared via the entry-point group, not a hard pyproject dependency — provided by the AiiDA runtime environment), which is not installed in this dev environment. The entry-point object path is verified resolvable by 9.8. Not a code defect.
- [x] 9.10 Stale-reference sweep: `rg -n "yascheduler\.aiida_plugin\b" --glob '!openspec/changes/archive/**' --glob '!*.egg-info/**'` — confirm zero references to the old deep path in non-archived, non-egg-info files (the only acceptable hit is inside archived OpenSpec change proposals, which are historical). NOTE: zero references in code/non-OpenSpec files; remaining hits are the change's own propositional artifacts (describing the old→new transition) and the pre-sync live `openspec/specs/package-facades/spec.md` (the delta replaces these at sync/archive time per OpenSpec workflow).