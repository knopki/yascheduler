# node-ncpus-as-config

Reinterpret Node.ncpus as operator-set static config (int|None) instead of discovery cache (int with magic 0); remove cloud write-back; add session-level get_cpu_cores memoization
