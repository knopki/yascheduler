## Common rules for every code-touching task

Every code-touching task below obeys these invariants. They exist because a
prior attempt at a similar change was discarded specifically for violating
them.

- **GRACE fields are a closed set.** Allowed fields: `PURPOSE`, `SCOPE`,
  `INVARIANTS`, `USECASES`, `DEPENDENCIES`, `RATIONALE`, `KEYWORDS`,
  `REQUIRES`, `ENSURES`. No invented fields. Specifically: no `SHALL NOT:`
  pseudo-field, no `EFFECTS:`, no `EXAMPLES:`, no `NOTES:`, no `RAISES:`,
  no free-form labels. The spec's removed `SHALL NOT` sentences do NOT become
  a `SHALL NOT:` contract field — they become an `INVARIANTS` entry stating
  the positive contract, or a `RATIONALE` Q/A if the rationale is the
  valuable part, or are simply cut when neither invariant nor rationale is
  the valuable part (pure absence enumeration).
- **`RATIONALE` is Q/A format only**, answering "why is this entity shaped
  this way?". It is NOT a junk drawer for arbitrary prose, NOT a place to
  restate `PURPOSE`, NOT a place to dump the trimmed spec text. One Q and one
  A per item, multi-item allowed when there are distinct reasons.
- **`PURPOSE` answers WHY, not WHAT.** "Re-export Yascheduler and the path
  constants from the package root" is WHAT and fails. "Let external consumers
  do `from yascheduler import Yascheduler` without knowing the layered
  module layout" is WHY and passes.
- **Every `CLASS_*` / `FUNC_*` / `METHOD_*` region encloses the FULL entity.**
  For a class: the `@dataclass(...)` decorator (if any), the `class` line, the
  docstring, every field, every `__init__` line, every `self.<attr>` assignment,
  through the trailing blank line before the next region marker. For a
  function: the decorator (if any), the `def`/`async def` line, the entire
  body, the trailing blank line. A region that closes before its entity ends
  (e.g. wrapping only the contract comment) is a defect. Nesting is allowed:
  `METHOD_*` and inner `BLOCK_*` regions live INSIDE the enclosing `CLASS_*`
  region; the `CLASS_*` `# endregion` comes after the last nested `# endregion`.
- **Comment-only diff.** No code logic, signature, decorator choice, docstring
  semantics, or import changes. Edits are `# region`/`# endregion` marker
  insertion and contract-field enrichment inside the marker block. No
  `__all__` list, no `from .xxx import yyy` re-export line, no `pyproject.toml`
  line, no method body, and no `__init__` signature is rewritten.

## 1. Apply the package-facades spec delta

- [x] 1.1 Apply the 12 MODIFIED requirements from `openspec/changes/package-facades-spec-trim/specs/package-facades/spec.md` to `openspec/specs/package-facades/spec.md`, replacing each original requirement block in place. Preserve every requirement header text exactly (whitespace-insensitive match) so OpenSpec recognizes the MODIFIED operation. Headers to match (in spec order): `Layer direction (R3)`, `Within-package relative imports (R1)`, `Cross-package facade imports (R2)`, `Package facade as public surface (lazy publication)`, `Entrypoints layer facade`, `Compat shim for yascheduler.client`, `Outside-layer-set exemptions`, `Layers contract configuration`, `Domain package facade contents`, `Extended facade contents (lazy publication driven by consumers)`, `Public API stability`, `Yascheduler facade public contract`.
- [ ] 1.2 Confirm the trimmed main spec contains exactly 2 `SHALL NOT` instances — the 2 genuine system-rule prohibitions preserved verbatim: (a) `yascheduler.shared SHALL NOT import from any other layer in the project.` in R3 body, and (b) `SHALL NOT change; their public task_id/jobs parameters stay int / list[int]` in Yascheduler-facade-public-contract body. The 5 negative-space prohibition items removed from requirement bodies (R1 contrapositive "Absolute cross-package imports ... SHALL NOT appear"; R2 "cross-layer consumers SHALL NOT import from them directly" + "Direct imports of submodules ... SHALL NOT appear in any import"; Entrypoints-facade "direct imports of the entrypoints client module from outside the layer SHALL NOT appear"; Compat-shim "SHALL NOT re-export Config or contain any business logic") are gone. Confirm every observable behavioral scenario (`#### Scenario:` count) is preserved: pre 43 → post 43. Confirm the trimmed bodies no longer contain: the "yascheduler.infra.cli is liquidated" sentence, the contrapositive layer-permission restatement in R3 body, the "Subpackage facades are internal organization" paragraph in R2 body, the "`__all__ = ["Yascheduler"]`" literal in the Compat-shim body, the "and dev dependency import-linter >=2.5,<2.6" code-detail tail in Layers-contract body, the "No forbidden contract entry exists" sentence in Layers-contract body, the "applies identically whether Yascheduler is imported via ..." sentence prefix "is keyed on the resolvable symbol and" is removed from Yascheduler-facade body (the trailing "applies identically across [3 paths]" behavioral contract is PRESERVED — only the "keyed on the resolvable symbol" rationale moves to code), the "Yascheduler facade is the sole int/TaskId marshalling boundary" implementation-invariant paragraph in Yascheduler-facade body, and the "single extraction helper SHALL be the sole extraction site" sentence in Yascheduler-facade body.
- [ ] 1.3 `openspec validate --all --json` passes (exit 0) for `package-facades-spec-trim` AND for the main `package-facades` spec AND no other already-passing spec regresses (the currently-failing `postgres-persistence-spec-trim`, `ssh-infrastructure-spec-trim`, `use-cases-spec-trim` are unrelated in-progress changes; they are allowed to remain failing).

## 2. yascheduler/__init__.py — enrich MODULE_CONTRACT

Existing `MODULE_CONTRACT` carries `PURPOSE`/`SCOPE`/`KEYWORDS`. Only defined GRACE fields are used; `PURPOSE` already answers WHY (it states what the facade enables for external consumers) — leave it.

- [ ] 2.1 Add `INVARIANTS` to the existing `MODULE_CONTRACT`: "`__all__` exports exactly `Yascheduler`, `__version__`, `CONFIG_FILE`, `LOG_FILE`, `PID_FILE` — these five names are the resolvable public surface; nothing else is importable from the package root."
- [ ] 2.2 Add `RATIONALE` (Q/A format) to the existing `MODULE_CONTRACT`: Q: why is the public contract keyed on the resolvable symbol (`from yascheduler import Yascheduler`) rather than on the file path that defines it? A: the implementation file has moved over the project's lifetime (`yascheduler/client.py` → `yascheduler/entrypoints/client.py`) and the re-export name stayed stable; keying the public contract on the resolvable symbol means relocating the implementation behind the same name does not break downstream consumers.
- [ ] 2.3 Verify `uv run ruff check yascheduler/__init__.py` and `uv run ruff format --check yascheduler/__init__.py` pass; `uv run pytest -m unit` for any test importing the package root is green.

## 3. yascheduler/entrypoints/__init__.py — enrich MODULE_CONTRACT

Existing `MODULE_CONTRACT` carries `PURPOSE`/`SCOPE`/`KEYWORDS`. Only defined GRACE fields are used; `PURPOSE` already answers WHY — leave it.

- [ ] 3.1 Add `INVARIANTS` to the existing `MODULE_CONTRACT`: "`__all__` exports exactly `CONFIG_FILE`, `LOG_FILE`, `PID_FILE`, `CLIDeps`, `Config`, `Yascheduler`, `make_cli_deps`, `make_daemon` (8 names); cross-layer consumers import via this layer facade only, not via deep entrypoints submodules (`yascheduler.entrypoints.client`, `yascheduler.entrypoints.di`, etc.)."
- [ ] 3.2 Add `RATIONALE` (Q/A format) to the existing `MODULE_CONTRACT`: Q: why is the entrypoints layer facade the sole public surface, given that submodules like `client.py` and `di.py` define the public symbols? A: the layer facade decouples consumers from the internal submodule layout; the `client.py`/`di.py`/`paths.py` split is an implementation convenience that has changed over time (daemon launchers, aiida plugin, and config_parser have all been relocated within the layer), but the facade `__all__` is the stable surface consumers depend on.
- [ ] 3.3 Verify `uv run ruff check yascheduler/entrypoints/__init__.py` and `uv run ruff format --check yascheduler/entrypoints/__init__.py` pass; `uv run pytest -m unit` for any test importing the entrypoints facade is green.

## 4. yascheduler/entrypoints/client.py — wrap CLASS_Yascheduler + FUNC__task_to_dict, enrich MODULE_CONTRACT and METHOD_* regions

This is the largest task. The existing file has: a `MODULE_CONTRACT` (PURPOSE/SCOPE/KEYWORDS), a wrapped `FUNC_to_sync`, an UNWRAPPED `_task_to_dict` (lines 68–106), an UNWRAPPED `class Yascheduler:` (lines 109–217) with seven nested `METHOD_*` regions (`METHOD___init__`, `METHOD_queue_submit_task_async`, `METHOD_queue_submit_task`, `METHOD_queue_get_tasks_async`, `METHOD_queue_get_tasks`, `METHOD_queue_get_task_async`, `METHOD_queue_get_task`). The seven `METHOD_*` regions are orphaned — GRACE Python rule requires them to live inside an enclosing `CLASS_*` region.

Only defined GRACE fields are used; every `PURPOSE` answers WHY. The existing `FUNC_to_sync` region is untouched (it is not in the trim's scope — no spec prose is being relocated onto it).

- [ ] 4.1 Add `INVARIANTS` to the existing `MODULE_CONTRACT`: "`_task_to_dict` is the single extraction site for the public Mapping shape returned by the query methods — no other helper builds the `{task_id, label, status, metadata, node}` dict; `Yascheduler` is the sole int/`TaskId` marshalling boundary in both directions (input `int` → `TaskId`, output `TaskId.value` → `int`); the class does not accept a `Config` aggregate as a constructor parameter — it takes `config_path` and parses internally."
- [ ] 4.2 Add `RATIONALE` (Q/A format) to the existing `MODULE_CONTRACT`: Q: why does `Yascheduler` marshal `int` ↔ `TaskId` at the facade instead of letting the use cases accept/return bare `int`? A: the use cases and the domain `Task` entity are typed against `TaskId` (a `NewType`-style wrapper) so domain code is self-documenting and the type-checker catches raw-int mistakes; the facade is the boundary where the public `int` contract meets the domain `TaskId` contract, so the marshalling lives here and only here. Q: why is `_task_to_dict` a single helper rather than inlined into each query method? A: the three query methods (`queue_get_tasks_async`, `queue_get_task_async`, and through it `queue_get_task`) all produce the same Mapping shape; consolidating the projection into one helper guarantees the dict shape is identical across the list and single-task variants and gives the metadata None-omission-plus-extra-merge logic one source of truth.
- [ ] 4.3 Add `# region FUNC__task_to_dict` ... `# endregion FUNC__task_to_dict` enclosing the FULL function — the `def _task_to_dict(t: Task, nodes_by_id: dict[NodeId, Node]) -> Mapping[str, Any]:` line, the body (the metadata-accumulation block and the dict-return expression), and the trailing blank line. The region opens one line above the `def` and closes one line below the closing `}` of the return expression. `PURPOSE` (WHY: project a typed `Task` plus its allocated `Node` into the public Mapping shape so the three query methods return identical dict structures from one source of truth). `ENSURES` (returns a Mapping with EXACTLY the keys `{task_id, label, status, metadata, node}`; `task_id` is `t.task_id.value` — a bare `int`, NOT a `TaskId`; `status` is `t.status` — a `TaskStatus` enum member, NOT a plain `int`; `metadata` is built by starting with `{"engine": t.engine}`, then conditionally adding `remote_folder` / `local_folder` / `webhook_url` / `webhook_custom_params` / `error` when their values are non-None (and `webhook_custom_params` additionally when it is truthy), then merging `**t.extra` for keys not already present; `node` is `None` when `t.allocated_node_id` is falsy, else the nested `node.*` field dict). `INVARIANTS` (the metadata key `engine` is always present even when its value is `None` — only the other five typed fields are conditionally omitted; `extra` keys do NOT overwrite typed-field keys already set; `node.ip` is `node.hostname` in the current Node shape — both refer to the same attribute; the projection performs no I/O and no DB access — it operates purely on the passed `Task` and `nodes_by_id` map). `RATIONALE` (Q/A): Q: why are None-valued typed fields omitted from `metadata` while `engine` is always present? A: `engine` is the only typed field that is structurally required on every task (a task without an engine is invalid); the other five are optional and their absence is signaled by omission rather than by `null`, matching the original dict shape external consumers have come to rely on. Q: why does `extra` merge with `if k not in metadata` instead of overwriting? A: typed fields are authoritative — a downstream consumer who accidentally puts an `engine` key in `extra` should not stomp the real `task.engine`; the not-in check makes typed fields win over `extra`.
- [ ] 4.4 Add `# region CLASS_Yascheduler` ... `# endregion CLASS_Yascheduler` enclosing the FULL class — opens one line above `class Yascheduler:`, encloses the docstring `"""Yascheduler client."""`, every class-level attribute (`STATUS_TO_DO`, `STATUS_RUNNING`, `STATUS_DONE`, `config: Config`), every nested `METHOD_*` region (`METHOD___init__`, `METHOD_queue_submit_task_async`, `METHOD_queue_submit_task`, `METHOD_queue_get_tasks_async`, `METHOD_queue_get_tasks`, `METHOD_queue_get_task_async`, `METHOD_queue_get_task`), and the trailing blank line at end of file. The `CLASS_Yascheduler` `# endregion` comes AFTER the last nested `METHOD_queue_get_task` `# endregion`. `PURPOSE` (WHY: give external consumers a stable sync+async Python client that submits and queries tasks while preserving a public `int`-typed contract over a `TaskId`-typed domain). `INVARIANTS` (the class is the sole int/`TaskId` marshalling boundary — on input `queue_get_task(task_id: int)` and `queue_get_tasks(jobs: Sequence[int])` wrap to `TaskId` before crossing into the use case; on output `_task_to_dict` extracts `.value` so the public dict preserves the `int` shape; `STATUS_TO_DO` / `STATUS_RUNNING` / `STATUS_DONE` are bare `int` class attributes — they are the `IntEnum` `.value` of the corresponding `TaskStatus` members, NOT the enum members themselves; the constructor does NOT accept a `Config` aggregate — it accepts `config_path` and `deps_factory` (keyword-only) and parses internally so the entrypoints layer's `Config` aggregate type never crosses the public surface). `RATIONALE` (Q/A): Q: why does the constructor take `config_path: PurePath | str = CONFIG_FILE` plus a keyword-only `deps_factory` instead of a pre-built `Config` aggregate? A: `Config` is a composition-root data type owned by `yascheduler.entrypoints`; accepting it on the public `Yascheduler(...)` constructor would leak the entrypoints-layer aggregate across the public boundary and force every external caller to learn the `Config` shape — accepting a path keeps the public surface to "a path string plus an optional DI hook". Q: why are the `STATUS_*` class attributes bare `int`s instead of `TaskStatus` members? A: backward compatibility — early consumers compared `task["status"]` to `Yascheduler.STATUS_TO_DO` and the historical values were plain `int`; the IntEnum-based `TaskStatus` was introduced later and preserves the integer values so the class attributes still match. Q: why does the same `Yascheduler` public contract apply whether imported via `from yascheduler import Yascheduler`, `from yascheduler.entrypoints import Yascheduler`, or `from yascheduler.client import Yascheduler`? A: all three paths resolve to the same object via re-export chains (`yascheduler/__init__.py` ← `yascheduler/entrypoints/__init__.py` ← `yascheduler/entrypoints/client.py`; `yascheduler/client.py` is a thin shim re-exporting from `yascheduler.entrypoints`) — the symbol is identical, so the contract is identical regardless of which re-export path a consumer happens to use.
- [ ] 4.5 Enrich existing `METHOD___init__`: tighten `PURPOSE` to WHY if slipped (current "Initialize the Yascheduler client with config path and optional DI factory." is borderline WHAT — replace with WHY such as "Bootstrap a Yascheduler client from a config path so the caller does not need to construct a Config aggregate themselves; the optional `deps_factory` hook lets tests inject a fake DI container without monkeypatching module-level wiring."). Add `ENSURES` (`self.config` is a parsed `Config`; `self._deps_factory` is `deps_factory` when not `None`, else `make_cli_deps`; calling `self._deps_factory(self.config)` returns a `CLIDeps`).
- [ ] 4.6 Enrich existing `METHOD_queue_submit_task_async`: tighten `PURPOSE` to WHY if slipped (current text is borderline WHAT — tighten to WHY). Add `ENSURES` (on success returns `(await deps.submit(label, dict(metadata), engine_name)).value` — a bare `int`, NOT a `TaskId`; the `await deps.submit(...)` result's `.value` is unwrapped at this exact site so the public `int` contract is preserved; `deps = self._deps_factory(self.config)` is constructed per-call so a fresh DI container — and therefore a fresh Unit of Work — is used for each submission). Add `INVARIANTS` (the `_webhook_onsubmit` keyword-only parameter is an internal seam — the sync `queue_submit_task` passes it through under the leading-underscore name to signal "internal use" to the async variant; it is NOT part of the public contract).
- [ ] 4.7 Enrich existing `METHOD_queue_submit_task`: tighten `PURPOSE` to WHY if slipped. Add `INVARIANTS` (offloads `queue_submit_task_async` to a sync call via `to_sync`; `webhook_onsubmit` is the public keyword — the sync wrapper translates it to the async variant's `_webhook_onsubmit` internal seam). Add `ENSURES` (returns a bare `int`).
- [ ] 4.8 Enrich existing `METHOD_queue_get_tasks_async`: tighten `PURPOSE` to WHY if slipped. Add `REQUIRES` (`status` values are valid `TaskStatus` integer values — `TaskStatus(x)` is invoked per element and raises `ValueError` on unknown values). Add `ENSURES` (returns a `Sequence[Mapping[str, Any]]` whose every element is the output of `_task_to_dict(t, nodes_by_id)` — exactly the 5 keys `{task_id, label, status, metadata, node}`; the list is empty when no tasks match). Add `INVARIANTS` (the method is the input side of the int/`TaskId` marshalling boundary — `[TaskId(i) for i in jobs]` wraps each public `int` job id to a domain `TaskId` before crossing into `query_tasks`; `deps = self._deps_factory(self.config)` is constructed per-call so each query uses a fresh Unit of Work).
- [ ] 4.9 Enrich existing `METHOD_queue_get_tasks`: tighten `PURPOSE` to WHY if slipped. Add `INVARIANTS` (offloads `queue_get_tasks_async` to a sync call via `to_sync`). Add `ENSURES` (returns the same `Sequence[Mapping[str, Any]]` shape as the async variant).
- [ ] 4.10 Enrich existing `METHOD_queue_get_task_async`: tighten `PURPOSE` to WHY if slipped. Add `ENSURES` (returns the first Mapping from `queue_get_tasks_async(jobs=[task_id])` or `None` when the list is empty — i.e. an `Optional[Mapping[str, Any]]` with exactly the 5 keys when the task exists, `None` when missing).
- [ ] 4.11 Enrich existing `METHOD_queue_get_task`: tighten `PURPOSE` to WHY if slipped. Add `INVARIANTS` (offloads `queue_get_task_async` to a sync call via `to_sync`). Add `ENSURES` (returns the same `Optional[Mapping[str, Any]]` shape as the async variant).
- [ ] 4.12 Verify `uv run ruff check yascheduler/entrypoints/client.py` and `uv run ruff format --check yascheduler/entrypoints/client.py` pass; `uv run pytest -m unit tests/unit/test_di_no_casts.py tests/unit/test_application_no_adapter_imports.py tests/unit/test_log_scope_discipline.py` is green; `uv run pytest -m unit` for any test importing `yascheduler.entrypoints.client` or instantiating `Yascheduler` is green.

## 5. yascheduler/entrypoints/di.py — enrich MODULE_CONTRACT

Existing `MODULE_CONTRACT` carries `PURPOSE`/`SCOPE`/`KEYWORDS`. The four existing entity regions (`CLASS_CLIDeps`, `FUNC__setup_domain_events`, `FUNC_make_daemon`, `FUNC_make_cli_deps`) are properly enclosed and untouched. Only defined GRACE fields are used; `PURPOSE` already answers WHY — leave it.

- [ ] 5.1 Add `RATIONALE` (Q/A format) to the existing `MODULE_CONTRACT`: Q: why does the composition root live in the entrypoints layer rather than in its own dedicated top-level package or in the application layer? A: hexagonal architecture places the composition root in the outermost layer (the driving-adapter layer) alongside the other entry points; the entrypoints layer is layer 1 in the `import-linter` `layers` contract, so its imports flow `entrypoints → infra → application → domain` — which is layer-legal. Placing the root here keeps wiring next to the entry points that consume it and avoids inventing a sixth architectural layer outside the contract.
- [ ] 5.2 Verify `uv run ruff check yascheduler/entrypoints/di.py` and `uv run ruff format --check yascheduler/entrypoints/di.py` pass; `uv run pytest -m unit` for any test importing `yascheduler.entrypoints.di` is green.

## 6. yascheduler/client.py — enrich MODULE_CONTRACT

Existing `MODULE_CONTRACT` carries `PURPOSE`/`SCOPE`/`KEYWORDS`. Only defined GRACE fields are used; `PURPOSE` already answers WHY — leave it.

- [ ] 6.1 Add `INVARIANTS` to the existing `MODULE_CONTRACT`: "`__all__` is exactly `["Yascheduler"]` — no other symbol is re-exported; the shim imports `Yascheduler` from `yascheduler.entrypoints` and adds nothing else; the module contains no business logic (no functions, no classes, no statements beyond the import and `__all__`); `from yascheduler.client import Config` raises `ImportError` because `Config` is not re-exported here."
- [ ] 6.2 Add `RATIONALE` (Q/A format) to the existing `MODULE_CONTRACT`: Q: why keep a compat shim at `yascheduler/client.py` when the implementation has moved to `yascheduler/entrypoints/client.py`? A: the deep import path `from yascheduler.client import Yascheduler` is part of the historical public API documented in the Public API stability spec; downstream consumers (notably the AiiDA scheduler plugin's early integration and external scripts) import via this path, and removing it would silently break them — the shim preserves the path while the actual implementation lives in the layered module.
- [ ] 6.3 Verify `uv run ruff check yascheduler/client.py` and `uv run ruff format --check yascheduler/client.py` pass; `uv run pytest -m unit` for any test importing `yascheduler.client` is green.

## 7. yascheduler/shared/__init__.py — enrich MODULE_CONTRACT

Existing `MODULE_CONTRACT` carries `PURPOSE`/`SCOPE`/`KEYWORDS`. Only defined GRACE fields are used; `PURPOSE` already answers WHY — leave it.

- [ ] 7.1 Add `INVARIANTS` to the existing `MODULE_CONTRACT`: "this package contains only typing shims consumed by ≥2 architectural layers (`Self`, `StrEnum`, `Unpack` from `.compat`) plus the `LogFormatter` (from `.log`); it contains no domain entities, no use-case orchestration, and no I/O — the package is the bottom of the `import-linter` layers contract and imports nothing from any other `yascheduler` layer."
- [ ] 7.2 Verify `uv run ruff check yascheduler/shared/__init__.py` and `uv run ruff format --check yascheduler/shared/__init__.py` pass; `uv run pytest -m unit` for any test importing `yascheduler.shared` is green.

## 8. yascheduler/infra/__init__.py — enrich MODULE_CONTRACT

Existing `MODULE_CONTRACT` carries `PURPOSE`/`SCOPE`/`KEYWORDS`. Only defined GRACE fields are used; `PURPOSE` already answers WHY — leave it.

- [ ] 8.1 Add `INVARIANTS` to the existing `MODULE_CONTRACT`: "`__all__` exports exactly the cross-layer surface: `AllSSHRetryExc`, `CloudAdapter`, `CloudProvisionerImpl`, `OccupancyChecker`, `OutputDownloader`, `PostgresUnitOfWork`, `SFTPRetryExc`, `SSHMachineRepository`, `TaskDeployer`, `apply_migrations`, `apply_schema`, `list_private_keys`, `resolve_adapter`, `webhook_handler` (14 names); the subpackage facades (`yascheduler.infra.ssh`, `yascheduler.infra.cloud`, `yascheduler.infra.persistence`, `yascheduler.infra.notifier`) are internal organization — cross-layer consumers import via this layer facade only, not via subpackage facades or deep submodules."
- [ ] 8.2 Add `RATIONALE` (Q/A format) to the existing `MODULE_CONTRACT`: Q: why is the infra layer facade the sole public surface for cross-layer consumers, given that the SSH / cloud / persistence / notifier subpackages each have their own internal `__init__.py`? A: a single layer facade gives the application layer and the composition root one import boundary to depend on; if cross-layer consumers imported from subpackage facades directly, relocating a class within the infra layer (e.g. `yascheduler.infra.ssh.repository.SSHMachineRepository` → `yascheduler.infra.ssh.SSHMachineRepository`) would break them. The layer facade absorbs internal relocations and keeps the cross-layer surface stable.
- [ ] 8.3 Verify `uv run ruff check yascheduler/infra/__init__.py` and `uv run ruff format --check yascheduler/infra/__init__.py` pass; `uv run pytest -m unit` for any test importing `yascheduler.infra` is green; `uv run lint-imports` still passes (no contract changes).

## 9. yascheduler/application/__init__.py — enrich MODULE_CONTRACT

Existing `MODULE_CONTRACT` carries `PURPOSE`/`KEYWORDS` (no `SCOPE`). Only defined GRACE fields are used; `PURPOSE` already answers WHY — leave it.

- [ ] 9.1 Add `INVARIANTS` to the existing `MODULE_CONTRACT`: "`__all__` exports exactly the cross-layer surface: `AbstractUnitOfWork`, `AllocationTracker`, `MessageBus`, `Orchestrator`, `abandon_node`, `query_tasks`, `submit_task` (7 names); cross-layer consumers (the composition root in `yascheduler.entrypoints.di`) import via this layer facade only, not via deep application submodules."
- [ ] 9.2 Verify `uv run ruff check yascheduler/application/__init__.py` and `uv run ruff format --check yascheduler/application/__init__.py` pass; `uv run pytest -m unit` for any test importing `yascheduler.application` is green.

## 10. yascheduler/domain/__init__.py — no enrichment

- [ ] 10.1 Confirm `MODULE_CONTRACT` already tight and WHY-shaped (the existing `PURPOSE`/`SCOPE`/`INVARIANTS`/`KEYWORDS` already absorb the spec's "events, model, engine types, exceptions, ports" framing verbatim via the existing `INVARIANTS` "`__all__` lists every re-exported symbol"). No edits required. Move on.

## 11. End-to-end verify

- [ ] 11.1 Manual scan: every `# region CLASS_*`, `FUNC_*`, `METHOD_*`, `BLOCK_*`, and `MODULE_CONTRACT` in the nine touched files (`yascheduler/__init__.py`, `yascheduler/entrypoints/__init__.py`, `yascheduler/entrypoints/client.py`, `yascheduler/entrypoints/di.py`, `yascheduler/client.py`, `yascheduler/shared/__init__.py`, `yascheduler/infra/__init__.py`, `yascheduler/application/__init__.py`, `yascheduler/domain/__init__.py`) has a paired `# endregion` and wraps the entire entity (no orphaned trailing code outside the region; no region closes before its entity ends; the new `CLASS_Yascheduler` `# endregion` comes AFTER the last nested `METHOD_queue_get_task` `# endregion`; the new `FUNC__task_to_dict` region encloses the full helper body and the trailing blank line).
- [ ] 11.2 Manual scan: no invented GRACE field names anywhere in the touched files — only `PURPOSE` / `SCOPE` / `INVARIANTS` / `USECASES` / `DEPENDENCIES` / `RATIONALE` / `KEYWORDS` / `REQUIRES` / `ENSURES`. Specifically, NO `SHALL NOT:` field anywhere.
- [ ] 11.3 Manual scan: every `PURPOSE` field answers WHY, not WHAT. Spot-check the new `CLASS_Yascheduler` `PURPOSE` and the new `FUNC__task_to_dict` `PURPOSE` in `yascheduler/entrypoints/client.py`; spot-check the tightened `METHOD_*` `PURPOSE` lines in the same file.
- [ ] 11.4 Manual scan: every `RATIONALE` field is in Q/A format ("Q: ... A: ..."). No `RATIONALE` block contains free-form prose that should be in `PURPOSE` / `INVARIANTS` / `SCOPE`. Spot-check the `RATIONALE` blocks added to `yascheduler/__init__.py`, `yascheduler/entrypoints/__init__.py`, `yascheduler/entrypoints/client.py`, `yascheduler/entrypoints/di.py`, `yascheduler/client.py`, `yascheduler/infra/__init__.py`.
- [ ] 11.5 `openspec validate --all --json` passes (exit 0) for `package-facades-spec-trim` AND for the main `package-facades` spec.
- [ ] 11.6 `uv run ruff check .` and `uv run ruff format --check .` are green on the nine touched files.
- [ ] 11.7 `uv run lint-imports` passes (no `[tool.importlinter]` change in the diff — the `layers` contract must stay green; this is a regression guard confirming the new `CLASS_Yascheduler` / `FUNC__task_to_dict` regions did not accidentally break the layer-direction enforcement by moving imports around).
- [ ] 11.8 `uv run pytest -m unit` is green; spot-check the facade tests (`tests/unit/test_application_no_adapter_imports.py`, `tests/unit/test_di_no_casts.py`, `tests/unit/test_log_scope_discipline.py`) and any test that imports `Yascheduler` or `_task_to_dict` to confirm the comment-only diff did not change observable behavior.
