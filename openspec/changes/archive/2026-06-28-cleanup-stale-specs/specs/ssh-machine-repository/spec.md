## REMOVED Requirements

### Requirement: MachineRepository port
**Reason**: Merged into the new `ssh-infrastructure` capability's `MachineRepository port` requirement. Content preserved verbatim; only the hosting spec changes (one spec for the SSH subsystem instead of three).
**Migration**: Read `ssh-infrastructure` — `MachineRepository port` requirement with all scenarios.

### Requirement: SSHMachineRepository implements MachineRepository
**Reason**: Merged into `ssh-infrastructure`'s `SSHMachineRepository implements MachineRepository` requirement. Content preserved verbatim.
**Migration**: Read `ssh-infrastructure` — `SSHMachineRepository implements MachineRepository` requirement.

### Requirement: MachineOperations port
**Reason**: Merged into `ssh-infrastructure`'s `MachineOperations port` requirement. Content preserved verbatim.
**Migration**: Read `ssh-infrastructure` — `MachineOperations port` requirement.

### Requirement: SSHMachineOperations composition
**Reason**: Merged into `ssh-infrastructure`'s `SSHMachineOperations composition` requirement. Content preserved verbatim.
**Migration**: Read `ssh-infrastructure` — `SSHMachineOperations composition` requirement.