## Common rules for every code-touching task

Every code-touching task below obeys these invariants. They exist because a
prior attempt at a similar change was discarded specifically for violating
them.

- **GRACE fields are a closed set.** Allowed fields: `PURPOSE`, `SCOPE`,
  `INVARIANTS`, `USECASES`, `DEPENDENCIES`, `RATIONALE`, `KEYWORDS`,
  `REQUIRES`, `ENSURES`. No invented fields. Specifically: no `SHALL NOT:`
  pseudo-field, no `EFFECTS:`, no `EXAMPLES:`, no `NOTES:`, no `RAISES:`,
  no free-form labels. The spec's removed `SHALL NOT` sentences do NOT become
  a `SHALL NOT:` contract field — they become an `INVARIANTS` entry stating
  the positive contract, or a `RATIONALE` Q/A if the rationale is the
  valuable part.
- **`RATIONALE` is Q/A format only**, answering "why is this entity shaped
  this way?". It is NOT a junk drawer for arbitrary prose, NOT a place to
  restate `PURPOSE`, NOT a place to dump the trimmed spec text. One Q and one
  A per item, multi-item allowed when there are distinct reasons.
- **`PURPOSE` answers WHY, not WHAT.** "Concrete MachineSession entity
  handle owning an SSH connection" is WHAT and fails. "Hand callers a
  per-machine handle that owns its SSH connection, adapter, mutable machine
  snapshot, and monitor task so they can deploy, download, and probe the
  remote machine without re-keying by IP" is WHY and passes.
- **Every `CLASS_*` / `FUNC_*` / `METHOD_*` region encloses the FULL entity.**
  For a class: the `@dataclass(...)` decorator (if any), the `class` line, the
  docstring, every field, every `__init__` line, every `self.<attr>` assignment,
  through the trailing blank line before the next region marker. For a
  function/method: the decorator (if any), the `def`/`async def` line, the
  entire body, every nested `BLOCK_*` region, the trailing blank line. A
  region that closes before its entity ends (e.g. wrapping only the contract
  comment) is a defect. Nesting is allowed: `METHOD_*` and inner `BLOCK_*`
  regions live INSIDE the enclosing `CLASS_*`; the `CLASS_*` `# endregion`
  comes after the last nested `# endregion`.
- **Comment-only diff.** No code logic, signature, decorator choice, docstring
  semantics, or import changes. Edits are `# region`/`# endregion` marker
  insertion and contract-field enrichment inside the marker block. The
  existing long internal `# region BLOCK_*` commentary across the SSH
  package stays as-is (it is non-region narrative inside a region and explains
  non-obvious ordering invariants like pop-before-await, mark-closed-before-
  await, fresh-SFTP-per-file, etc.).

## 1. Apply the ssh-infrastructure spec delta

- [x] 1.1 Apply the 12 MODIFIED requirements from `openspec/changes/ssh-infrastructure-spec-trim/specs/ssh-infrastructure/spec.md` to `openspec/specs/ssh-infrastructure/spec.md`, replacing each original requirement block in place. Preserve requirement header text exactly (whitespace-insensitive match) so OpenSpec recognizes the MODIFIED operation. Headers to match (in spec order): `MachineRepository port`, `SSHMachineRepository implements MachineRepository`, `MachineSession port`, `SSHMachineSession implements MachineSession`, `Session is returned by repository and resolved per-tick by the orchestrator`, `download_outputs per-file SFTP isolation and retry`, `start_task_on_machine rolls back BUSY on failure`, `File-upload non-SFTP errors propagate`, `Retry and backoff policy`, `SSHMachineSession memoizes CPU core discovery`, `Occupancy monitoring`, `Module layout`.
- [x] 1.2 Confirm the trimmed main spec contains zero `SHALL NOT` / `shall not` / `do NOT` / `does NOT` instances in requirement bodies (the 18+ enumerated in `proposal.md` Why § 1 are gone — the `does NOT have _machines or _monitors` scenario THEN is rewritten positively as "as its only collection attribute", and the `does NOT await` sentence on `cancel_monitor` is gone — the not-awaiting behavior is now an `INVARIANT` on `METHOD_cancel_monitor` in code). Confirm every observable behavioral scenario (`#### Scenario:` count) is preserved: pre 22 → post 22. Confirm the duplicated orchestrator-side caching guidance ("The orchestrator SHALL resolve a session via `repository.get_session(...)` at each consumer tick", "The orchestrator SHALL NOT cache session references across await boundaries", "Use cases SHALL receive sessions as parameters ... they SHALL NOT cache sessions") is gone from the ssh-infrastructure spec — that guidance is owned by the orchestrator and use-cases specs (handled by `orchestrator-spec-trim` and friends).
- [x] 1.3 `openspec validate --all --json` passes (exit 0). The change validates AND the trimmed main spec validates AND no other spec regresses (currently 20 specs + in-flight trim changes).

## 2. repository.py — wrap CLASS_MySSHClient + FUNC__build_tunnel_options, enrich CLASS_SSHMachineRepository + METHOD_* + MODULE_CONTRACT

Each new region encloses the FULL entity per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 2.1 Add `# region CLASS_MySSHClient` ... `# endregion CLASS_MySSHClient` enclosing the FULL class block — the `class MySSHClient(SSHClient):` line, the docstring, the `validate_host_public_key` method (signature + docstring + `# NOTE:` comment + `return True`), and the trailing blank line. `PURPOSE` (WHY: accept any SSH host public key at connect time so first-contact to freshly-provisioned VMs succeeds without an out-of-band fingerprint exchange — the cloud-provisioned hosts we connect to are trusted by construction). `INVARIANTS` (subclass of `asyncssh.SSHClient`; every host key is accepted unconditionally — insecure for adversarial networks; the validate method's args are unused and `# noqa: ARG002`-suppressed). `RATIONALE` Q/A — Q: why trust all host keys instead of pinning? A: yascheduler connects to VMs it just created via cloud APIs and to operator-managed static nodes from `yascheduler_nodes`; either path implies the operator already trusts the host. Pinning would require an out-of-band fingerprint channel that does not exist in this deployment shape.
- [x] 2.2 Add `# region FUNC__build_tunnel_options` ... `# endregion FUNC__build_tunnel_options` enclosing the FULL function — the `def _build_tunnel_options(node, client_keys, connect_timeout) -> SSHClientConnectionOptions | None:` line, the body, and the trailing blank line. `PURPOSE` (WHY: build the asyncssh bastion-leg options from `node.jump_*` fields so `connect` can pass a single `tunnel=` argument (or `None`) without re-deriving the bastion identity at every call site). `ENSURES` (returns `None` when `node.jump_host` is falsy — direct connection; otherwise returns an `SSHClientConnectionOptions` carrying the bastion host/port/username and the caller's `client_keys`). `INVARIANTS` (reads `node.jump_host` / `node.jump_port` / `node.jump_username` — the jump identity is sourced solely from the `Node`; no separate `jump_host` / `jump_username` parameter on the function).
- [x] 2.3 Enrich existing `CLASS_SSHMachineRepository`: tighten `PURPOSE` to WHY if slipped (current text mentions WHY — keep, do not bloat). Add `SCOPE` (connected-machine collection lifecycle (`connect`, `disconnect`, `disconnect_all`) and queries (`list_free`, `list_connected`, `get_session`, `contains`, `__contains__`, `__len__`); NOT: single-machine operations, accessor getters, state-transition wrappers, monitor mechanism — those are `SSHMachineSession`). Add `INVARIANTS` (keyed by `NodeId`, not by IP — the transport address survives only as a field on `node` read inside `connect`; the instance owns exactly one collection attribute `_sessions: dict[NodeId, SSHMachineSession]]`; `disconnect(node_id)` pops the session and delegates teardown to `session._close()` — the repository does not know about monitors; `disconnect(node_id)` is scoped to the targeted node only — it has no reach into any other session's monitor). Add `RATIONALE` Q/A — Q: why is the collection keyed by `NodeId` instead of by IP? A: a `Node`'s IP can change across reconnects (cloud re-creation, DHCP), but `NodeId` is the stable domain identity; keying by `NodeId` lets `disconnect`/`get_session` survive a transport-address change without re-discovering the new IP. Q: why does `connect` read `username` / `port` / `jump_*` from `node` instead of taking them as parameters? A: the caller (cloud allocator or static-node loader) is the sole authority for those fields — they are stamped onto the `Node` once and read many times; threading them as separate parameters would create a second source of truth that drifts the moment a caller forgets to pass one.
- [x] 2.4 Enrich existing `METHOD__open_connection`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (on success returns `(SSHClientConnection, SSHClientConnectionOptions)`; on `(asyncssh.misc.Error, OSError)` after `my_backoff_exc` exhausts, the exception propagates — the `connect` wrapper translates it into `MachineConnectionError`). Add `INVARIANTS` (decorated with `@my_backoff_exc()` — the SSHRetryExc retry wraps the connect attempt; `conn_opts.host == hostname`, `conn_opts.port == port`, `conn_opts.username == username`, `conn_opts.tunnel == tunnel_opts`; the `logger.debug("CONNECT", ...)` marker precedes the `asyncssh.connection.connect(...)` call).
- [x] 2.5 Enrich existing `METHOD_connect`: tighten `PURPOSE` to WHY if slipped; add `REQUIRES` (the caller has stamped transport identity onto `node` — `node.hostname`, `node.username`, `node.port`, and (optionally) `node.jump_host` / `node.jump_port` / `node.jump_username`). Add `ENSURES` (on success returns the registered `MachineSession`; on `(asyncssh.misc.Error, OSError)` raises `MachineConnectionError(node.node_id, node.hostname, str(err))` — the address is what the operator recognizes). Add `INVARIANTS` (delegates to `_connect_impl` which carries the `my_backoff_exc` retry; translates only `(asyncssh.misc.Error, OSError)` — other exceptions propagate unchanged).
- [x] 2.6 Enrich existing `METHOD__connect_impl`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (on success: an `SSHMachineSession` is constructed with `hostname=node.hostname`, an open `SSHClientConnection`, `SSHClientConnectionOptions`, a `ConnectedMachine(node_id=node.node_id, platform=adapter.platform, state=FREE, free_since=time.monotonic())`, `adapter`, `platforms`, normalized `data_dir`/`engines_dir`/`tasks_dir`; the session's CPU-core cache is primed with the value read via `adapter.get_cpu_cores(...)`; the session is stored at `self._sessions[node.node_id]`; the session is returned). Add `INVARIANTS` (the `ConnectedMachine` snapshot carries `node_id` and `platform` only — it does NOT carry `hostname` or `ncpus`; `hostname` lives on the session, `ncpus` lives in the session cache; an info log line with the CPU count is emitted here at the discovery site — `SSHMachineSession.setup_node` does NOT emit a separate CPU-count log; the `logger.debug("CPUS", ...)` and `logger.info("connected to %s (%d CPUs)", ...)` markers fire here).
- [x] 2.7 Enrich existing `METHOD_disconnect`: tighten `PURPOSE` to WHY if slipped; keep the inline comment about pop-before-await; add `ENSURES` (the session for `node_id` is removed from `_sessions` (no-op if absent) and its `_close()` is awaited; pop happens BEFORE await so a re-entry race cannot re-insert the cancelled task). Add `INVARIANTS` (scoped to the targeted `node_id` — does not affect any other session's monitor; idempotent on absent `node_id`).
- [x] 2.8 Enrich existing `METHOD_disconnect_all`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (iterates a snapshot `list(self._sessions)` of `NodeId` keys and calls `disconnect(node_id)` per session; idempotent — a second call is a no-op once the registry is empty). Add `RATIONALE` Q/A — Q: why snapshot the keys before iteration? A: `disconnect` mutates `_sessions` (pops the key); iterating `self._sessions` directly would raise `RuntimeError: dictionary changed size during iteration`.
- [x] 2.9 Enrich existing `METHOD_list_free`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns a list of `MachineSession` whose `machine.state == FREE`, optionally filtered by `machine.platform in platforms` when `platforms is not None`, sorted ascending by `session.machine.free_since or 0.0` — oldest-first).
- [x] 2.10 Enrich existing `METHOD_list_connected`, `METHOD_get_session`: tighten each `PURPOSE` to WHY if slipped; `METHOD_get_session` add `ENSURES` (returns the live `MachineSession` for `node_id` or `None` after `disconnect`).
- [x] 2.11 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep). Add `INVARIANTS` capturing the layering rule: the repository owns no `_machines` / `_monitors` dicts — single-machine operations and monitor mechanism live on `SSHMachineSession`; the repository is the only owner of the `_sessions` registry. Add `DEPENDENCIES` capturing the layering rule already in the file: `USES API: asyncssh (SSHClient, connection, options), backoff (retry decorator), yascheduler.domain (ConnectedMachine, MachineSession, MachineState, Node, NodeId, MachineConnectionError), .platform (ADAPTERS, _detect_platform, _init_paths, make_run_fn), .session (SSHMachineSession, my_backoff_exc)`.
- [x] 2.12 Verify `uv run ruff check yascheduler/infra/ssh/repository.py` and `uv run ruff format --check yascheduler/infra/ssh/repository.py` pass; `uv run pytest -m unit tests/unit/test_ssh_gateway.py tests/unit/test_ssh_gateway_connect.py tests/unit/test_ssh_gateway_machine_queries.py tests/unit/test_cloud_alloc_session_lifecycle.py` is green.

## 3. session.py — wrap METHOD__prime_ncpus_cache + 6 base-primitive METHOD_*s, enrich CLASS_SSHMachineSession + METHOD_* + MODULE_CONTRACT

Each new region encloses the FULL entity per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY. Trivial one-line property accessors (`hostname`, `machine`, `is_closed`, `adapter`, `platforms`, `data_dir`, `engines_dir`, `tasks_dir`, `path`, `quote`) stay unwrapped per the GRACE proportional rule.

- [x] 3.1 Add `# region METHOD__prime_ncpus_cache` ... `# endregion METHOD__prime_ncpus_cache` enclosing the FULL method (the `def _prime_ncpus_cache(self, ncpus: int) -> None:` line, docstring, body, trailing blank). `PURPOSE` (WHY: let `SSHMachineRepository.connect` seed the session's CPU-core cache with the value it already discovered, so the first `get_cpu_cores()` call on the new session returns without re-invoking the adapter). `INVARIANTS` (called only by `SSHMachineRepository.connect` — never by the session itself; unconditionally overwrites `_cached_ncpus` with the passed value; no retry decorator).
- [x] 3.2 Add `# region METHOD_run` ... `# endregion METHOD_run` enclosing the FULL method (the `async def run(self, cmd: str) -> ProcessResult:` line, docstring, body, trailing blank). `PURPOSE` (WHY: run a command and return a structured `ProcessResult` so callers branching on `exit_code` / `stdout` / `stderr` do not touch the raw asyncssh process object). `ENSURES` (returns a `ProcessResult` with `exit_code` derived from `proc.returncode` (`-1` when `None`), `stdout`/`stderr` coerced to `str`).
- [x] 3.3 Add `# region METHOD_run_bg` ... `# endregion METHOD_run_bg` enclosing the FULL method. `PURPOSE` (WHY: spawn a background process on the remote machine — single attempt, no retry — so daemon-style processes stay running after the SSH call returns). `INVARIANTS` (single attempt — spawn is non-idempotent; a successful remote side-effect followed by a lost client confirmation would produce a duplicate on retry; delegates to `self._adapter.run_bg(self._conn, self._adapter.quote, cmd, cwd=cwd)` — no hostname-keyed lookup, no call into the repository).
- [x] 3.4 Add `# region METHOD_upload` ... `# endregion METHOD_upload` enclosing the FULL method. `PURPOSE` (WHY: upload a local file to a remote path via SFTP — single attempt, no retry — so a partial upload followed by a retry does not produce a corrupt-or-duplicate remote file). `INVARIANTS` (single attempt — `sftp.put` is non-idempotent; opens a fresh SFTP client via `self._conn.start_sftp_client()` per call).
- [x] 3.5 Add `# region METHOD_open_sftp` ... `# endregion METHOD_open_sftp` enclosing the FULL method. `PURPOSE` (WHY: hand callers an async-context-manager SFTP client bound to this session's connection so per-file operations can be batched without each re-opening a channel). `ENSURES` (yields an `SFTPClient` from `self._conn.start_sftp_client()`).
- [x] 3.6 Add `# region METHOD_pgrep` ... `# endregion METHOD_pgrep` enclosing the FULL method. `PURPOSE` (WHY: yield remote processes whose name or command line matches a pattern so occupancy checks can detect if a calculation is still running). `INVARIANTS` (delegates to `self._adapter.pgrep(self._conn, self._adapter.quote, pattern, full=full)`; `pattern` accepts `str` or `re.Pattern`).
- [x] 3.7 Add `# region METHOD_list_processes` ... `# endregion METHOD_list_processes` enclosing the FULL method. `PURPOSE` (WHY: yield every running process on the remote machine so an operator can inspect what is consuming a node). `INVARIANTS` (delegates to `self._adapter.list_processes(self._conn, None)`).
- [x] 3.8 Enrich existing `CLASS_SSHMachineSession`: tighten `PURPOSE` to WHY if slipped. Add `INVARIANTS` (`hostname` is sourced from the `Node` parameter passed to `SSHMachineRepository.connect` — NOT from `ConnectedMachine.hostname` (the snapshot no longer carries `hostname`); base primitives (`run`, `run_full`, `run_bg`, `upload`, `open_sftp`, `pgrep`, `list_processes`, `setup_node`) use `self._conn` and `self._adapter` directly — no hostname-keyed lookup, no call into the repository; the session owns its own teardown — `_close()` is invoked only by `SSHMachineRepository.disconnect`; the session owns its own `_monitor_task` and `_cached_ncpus` — neither is registered in any repository-level dict). Add `RATIONALE` Q/A — Q: why does `SSHMachineSession` carry connect-time config (`adapter`, `platforms`, `data_dir`, `engines_dir`, `tasks_dir`) as read-only properties instead of re-deriving them per call? A: they are resolved once at `connect` time from the running connection's detected platform and the caller's paths; re-deriving per call would either re-run platform detection or thread the config through every call site — both are worse than a one-time capture.
- [x] 3.9 Enrich existing `METHOD_occupy` / `METHOD_release` / `METHOD_update`: tighten `PURPOSE` to WHY if slipped. `METHOD_occupy` add `REQUIRES` (`session.machine.state == FREE` — the read-modify-write transitions FREE → BUSY; calling on a BUSY machine is a programmer error). `METHOD_release` add `ENSURES` (`session.machine.state == FREE` with `free_since = time.monotonic()`). `METHOD_update` add `ENSURES` (`session.machine` is replaced wholesale with the passed `ConnectedMachine` — used by rollback paths that need to restore a prior snapshot).
- [x] 3.10 Enrich existing `METHOD_run_full`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns the raw `SSHCompletedProcess` from `self._adapter.run(self._conn, self._adapter.quote, cmd)`); add `INVARIANTS` (decorated with `@my_backoff_exc()` — retries on `SSHRetryExc` with fibonacci backoff, `max_time=60`; the retry is safe because `run_full` is a read — the caller awaits the result and the remote process exits before the function returns).
- [x] 3.11 Enrich existing `METHOD_get_cpu_cores`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns the cached CPU count on a cache hit without invoking the adapter; on a cache miss invokes `self._adapter.get_cpu_cores(make_run_fn(self._conn, self._adapter))` exactly once and stores the result in `self._cached_ncpus`). Add `INVARIANTS` (decorated with `@my_backoff_exc()` — the retry applies only on a cache miss (the first call in a session lifetime); once cached, the adapter is never re-invoked; the cache lives for the session's lifetime only — a reconnected session starts with an empty cache and re-discovers once). Add `RATIONALE` Q/A — Q: why is the cache per-session and not per-process? A: CPU count is invariant for the lifetime of one SSH connection, so one discovery per session is sufficient; per-process caching would survive a reconnect and serve a stale value for a re-provisioned VM. Q: why does `SSHMachineRepository.connect` prime the cache instead of letting the first `get_cpu_cores()` call discover lazily? A: `connect` already runs `adapter.get_cpu_cores(...)` for the info-log line at the discovery site — re-using that value primes the cache for free and avoids a redundant SSH exec on the first consumer tick. Q: what about CPU hot-add during a live session? A: hot-add goes unobserved until reconnect; an operator who needs the scheduler to see added CPUs without reconnecting SHALL set `ncpus` explicitly via `yasetnode ~N` (the value is read from the DB row, not the session cache).
- [x] 3.12 Enrich existing `METHOD_setup_node`: tighten `PURPOSE` to WHY if slipped; add `REQUIRES` (`engines: EngineRepository` filtered by `self._platforms` — `setup_node` calls `engines.filter_platforms(self._platforms)` before delegating). Add `ENSURES` (on success the engine dependencies are installed on the remote node via `self._adapter.setup_node(conn=self._conn, run=make_run_fn(self._conn, self._adapter), quote=self._adapter.quote, engines=..., engines_dir=self._engines_dir)`). Add `INVARIANTS` (decorated with a fresh `my_backoff_exc(exception=AllSSHRetryExc)` — retries on the wider SSH+SFTP retry tuple; does NOT log the CPU count — the CPU-count log is owned by `SSHMachineRepository.connect` at the discovery site).
- [x] 3.13 Enrich existing `METHOD_install_monitor`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (on a closed session: no-op; otherwise: a new `asyncio.Task` running `_checker` is stored on `self._monitor_task`, the prior monitor (if any) is cancelled first, and a done-callback clears the slot only if it still points at the new task). Add `INVARIANTS` (generic over `Callable[[], Awaitable[bool]]` and `Callable[[], None]` — does NOT reference `Engine`; re-installing on a session that already has a live monitor cancels the prior monitor before installing the new one; idempotent on a closed session; the monitor's `on_free` is invoked from inside the `_checker` task when `check_factory()` returns `False`).
- [x] 3.14 Enrich existing `METHOD_cancel_monitor`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (pops `self._monitor_task` and calls `task.cancel()` — does NOT await the cancellation; safe to call on a session with no live monitor). Add `INVARIANTS` (sync — the cancellation is fire-and-forget; the awaited cancellation lives in `METHOD__close`).
- [x] 3.15 Enrich existing `METHOD__close`: tighten `PURPOSE` to WHY if slipped; keep `ENSURES` (`self._closed` set True synchronously BEFORE any await — disconnect-scope isolation invariant); extend `ENSURES` (idempotent — returns immediately if `self._closed` is already `True`; otherwise sets `self._closed` synchronously, pops and cancels `self._monitor_task` (awaiting its cancellation under `suppress(asyncio.CancelledError)`), and closes `self._conn` via `close()` + `await wait_closed()` when the transport is still alive). Add `INVARIANTS` (called only by `SSHMachineRepository.disconnect`; the synchronous `_closed = True` BEFORE any await is the disconnect-scope isolation invariant — a re-entry race that pops the session from `_sessions` and then re-runs `disconnect` sees `is_closed` True and short-circuits).
- [x] 3.16 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep). Add `INVARIANTS` (the `my_backoff_exc = partial(backoff.on_exception, wait_gen=backoff.fibo, max_time=60, exception=SSHRetryExc)` canonical partial lives here — both `repository.py` and `session.py` import it; `SSHMachineSession`'s `__init__` stores all 10 connect-time fields as private attributes — none are exposed mutably except via the `occupy`/`release`/`update` mutators). Add `DEPENDENCIES` (`USES API: asyncssh (SSHClientConnection, SFTPClient, process types), backoff (retry decorator), yascheduler.domain (ConnectedMachine, ProcessResult, EngineRepository TYPE_CHECKING), .platform (make_run_fn), .platform.protocol (AllSSHRetryExc, SSHRetryExc)`).
- [x] 3.17 Verify `uv run ruff check yascheduler/infra/ssh/session.py` and `uv run ruff format --check yascheduler/infra/ssh/session.py` pass; `uv run pytest -m unit tests/unit/test_ssh_gateway.py tests/unit/test_ssh_gateway_connect.py tests/unit/test_ssh_gateway_bg_tasks.py tests/unit/test_cloud_alloc_session_lifecycle.py tests/unit/test_cloud_provisioner_ncpus.py` is green.

## 4. operations/deployment.py — enrich CLASS_TaskDeployer + METHOD_* + FUNC_* + MODULE_CONTRACT

Existing regions are kept (including the long internal `BLOCK_rollback_busy` commentary — leave the inner narrative intact). Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 4.1 Enrich existing `FUNC__safe_b64decode`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (lenient on padding — auto-appends `=` to satisfy `len % 4 == 0`; normalizes whitespace and newlines before decoding; accepts `str` or `bytes` input — `bytes` is decoded first).
- [x] 4.2 Enrich existing `FUNC__write_remote_file`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (writes `data` to `path` via `sftp.open(path, mode)` + `f.write(data)`; on `asyncssh.misc.Error` logs at `exception` level with `path` / `err.reason` / `err.code` and re-raises). Add `INVARIANTS` (SFTP errors are logged with structured `code` and `reason` fields THEN re-raised — they are NOT swallowed; non-SFTP exceptions propagate immediately — they are the abort signal for `start_task_on_machine`; `mode="wb"` for binary, `mode="w"` for text).
- [x] 4.3 Enrich existing `CLASS_TaskDeployer`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (stateless — `__init__` takes no arguments and stores nothing; per-call `(session, ...)` arguments; rolls back the session BUSY marking under `except BaseException` on any deploy/spawn failure — the rollback governs the session-level occupancy marker only, the DB task status and orchestrator's in-memory `mark_running()` are owned by the caller and unaffected).
- [x] 4.4 Enrich existing `METHOD__upload_task_data`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (creates `remote_dir` via `sftp.makedirs(..., exist_ok=True)`, writes every `input_file` from `task.extra` via `_write_remote_file` (binary for `fort.9` via `_safe_b64decode`, text otherwise); returns `True` on success). Add `INVARIANTS` (SFTP errors from `makedirs` are logged with `task_id` then re-raised; the `fort.9` special case decodes from base64 because the legacy submit API carries it encoded).
- [x] 4.5 Enrich existing `METHOD__exec_spawn_command`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (formats `engine.spawn` with `engine_path` / `task_path` / `ncpus` and calls `session.run_bg(run_cmd, cwd=str(task_dir))`); add `INVARIANTS` (any exception is logged at `exception` level and re-raised — spawn is the abort signal for `start_task_on_machine`).
- [x] 4.6 Enrich existing `METHOD_start_task_on_machine`: tighten `PURPOSE` to WHY if slipped; keep `REQUIRES` (`task.remote_folder is not None` — asserted before `session.occupy`); extend `ENSURES` (on success returns `True` and the session is BUSY; on any exception (including `CancelledError`) escaping the deploy/spawn steps, the `except BaseException` handler rolls back by calling `session.update(session.machine.release())` — equivalent to `session.release()` but defensive against concurrent state changes — and re-raises the original exception; the rollback SHALL run under `except BaseException`). Add `INVARIANTS` (the rollback is defensive: if `session.is_closed` is `True`, logs a warning and re-raises WITHOUT rollback (the session is gone); if `session.machine.state != BUSY`, logs a warning, still calls `session.release()`-via-`update`, and re-raises; otherwise logs an info line and re-raises; the rollback governs the session-level occupancy marker only — the DB task status and orchestrator's in-memory `mark_running()` are owned by the caller). Add `RATIONALE` Q/A — Q: why use `session.update(session.machine.release())` instead of `session.release()` directly? A: `release()` does a read-modify-write on the current snapshot; going through `update(machine.release())` makes the rollback defensive against a concurrent `update` from another path that swapped the snapshot out from under us — the released snapshot is the one we just read, not whatever a race installed.
- [x] 4.7 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep).
- [x] 4.8 Verify `uv run ruff check yascheduler/infra/ssh/operations/deployment.py` and `uv run ruff format --check yascheduler/infra/ssh/operations/deployment.py` pass; `uv run pytest -m unit tests/unit/test_allocate_task_node_pairing.py tests/unit/test_consume_task.py` is green.

## 5. operations/download.py — enrich CLASS_OutputDownloader + METHOD_download_outputs + MODULE_CONTRACT

Existing region kept; the long internal `BLOCK_download_outputs` / `BLOCK_per_file_download` / `BLOCK_rmtree_gate` commentary stays as-is. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 5.1 Enrich existing `CLASS_OutputDownloader`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (stateless — `__init__` is implicit; per-call `(session, ...)` arguments; opens a FRESH SFTP client per file so a dropped channel on file N invalidates only N's retries; classifies per-file `(OSError, SFTPError)` exceptions into `transient_errors` (subclass of `SFTPRetryExc`) or `permanent_errors` (everything else); removes the remote dir tree ONCE only on full success (both error lists empty) — any error preserves the remote dir for retry / debugging; the catch-all on the outer try classifies whole-session failures (open_sftp raising, or a non-(OSError|SFTPError) escape) as transient — the remote dir is preserved). Add `RATIONALE` Q/A — Q: why a fresh SFTP client per file instead of one client for the whole loop? A: a dropped SFTP channel on file N would invalidate the rest of the loop's retries under a shared client; per-file isolation bounds the dead-connection blast radius to one file. Q: why classify `SFTPRetryExc` as transient and everything else as permanent? A: `SFTPRetryExc` is the tuple of asyncssh SFTP errors that are safe to retry (connection-lost, EOF, failure, etc.) — they surface as transient so a downstream retry of `download_outputs` re-tries the file; `OSError` and non-retryable SFTP errors are permanent so the operator sees them surfaced, not silently re-tried forever.
- [x] 5.2 Enrich existing `METHOD_download_outputs`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns `(local_folder=str(local_dir), remote_folder=remote_dir, transient_errors, permanent_errors)`; on a per-file `(OSError, SFTPError)`, classifies into the appropriate list and continues the loop; on a whole-session failure (outer try catch-all), appends `(remote_dir, err)` to `transient_errors`; removes the remote dir tree via a fresh SFTP client ONLY when both error lists are empty; the method does NOT raise). Add `INVARIANTS` (per-file retry via `my_backoff_sftp()` (fibonacci, `max_time=60`) wraps each `sftp.get` — non-idempotent operations are NOT retried as a whole, only the per-file `get` is retried because a partial `get` followed by a retry re-downloads the whole file safely under `preserve=True`).
- [x] 5.3 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep). Add `INVARIANTS` (the `my_backoff_sftp = partial(backoff.on_exception, wait_gen=backoff.fibo, max_time=60, exception=SFTPRetryExc)` canonical partial lives here — `download_outputs` is the sole consumer).
- [x] 5.4 Verify `uv run ruff check yascheduler/infra/ssh/operations/download.py` and `uv run ruff format --check yascheduler/infra/ssh/operations/download.py` pass; `uv run pytest -m unit tests/unit/test_ssh_gateway_download_outputs.py` is green.

## 6. operations/occupancy.py — enrich CLASS_OccupancyChecker + METHOD_* + MODULE_CONTRACT

Existing regions kept. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 6.1 Enrich existing `CLASS_OccupancyChecker`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (stateless — `__init__` is implicit; per-call `(session, ...)` arguments; the monitor mechanism (`asyncio.Task`, sleep/cancel) lives on `SSHMachineSession.install_monitor` — `OccupancyChecker` only composes the check_factory and on_free callbacks; the safe-default is "busy" — when SSH fails, `_occupancy_by_pgrep` / `_occupancy_by_cmd` return `True` so a flaky check does NOT release a machine that still has a running task). Add `RATIONALE` Q/A — Q: why does `OccupancyChecker` NOT own the monitor task? A: the monitor mechanism is generic (Engine-agnostic, lives on `SSHMachineSession.install_monitor`); `OccupancyChecker` is the Engine-aware composition layer that wires `occupancy_check` as `check_factory` and `session.release` as `on_free` — keeping the two apart means a future non-Engine occupancy source (e.g. a cloud-idle probe) can re-use the same session monitor without re-implementing the asyncio plumbing.
- [x] 6.2 Enrich existing `METHOD__occupancy_by_pgrep`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns `True` (busy) on the first process matching `pattern`; returns `False` (free) when the pgrep yields zero processes; returns `True` (busy) when `SSHRetryExc` is raised — safe default). Add `INVARIANTS` (logs `PGREP` on first match and `PGREP_FREE` on no match; the `SSHRetryExc` catch logs at warning level and assumes busy).
- [x] 6.3 Enrich existing `METHOD__occupancy_by_cmd`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns `True` (busy) when `proc.returncode == expected_code`; returns `False` otherwise; returns `True` (busy) when `SSHRetryExc` is raised — safe default). Add `INVARIANTS` (logs `CHECK_CMD` with `cmd` / `exit_code` / `expected`).
- [x] 6.4 Enrich existing `METHOD_occupancy_check`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (dispatches to `_occupancy_by_pgrep` when `config.check_pname` is set; else to `_occupancy_by_cmd` when `config.check_cmd` is set; else returns `False` (free) — an engine with no check configured is presumed free so the monitor releases it on the first tick). Add `INVARIANTS` (logs `NO_CHECK` when neither `check_pname` nor `check_cmd` is configured).
- [x] 6.5 Enrich existing `METHOD_start_occupancy_check`: tighten `PURPOSE` to WHY if slipped; add `REQUIRES` (`session.machine.state` is `FREE` or `BUSY` — `occupy()` is called ONLY if `FREE` (idempotent — `start_task_on_machine` already occupied it on the deploy path)). Add `ENSURES` (calls `session.occupy()` when `session.machine.state == FREE`; installs a monitor on `session` with `interval=config.sleep_interval`, `check_factory` wrapping `self.occupancy_check(session, config)` (catch-all returns `True` on failure — safe default), `on_free=session.release`). Add `INVARIANTS` (no `asyncio.wait_for` around `occupancy_check` — on Python <3.12 it swallows the monitor task's cancellation, so `SSHMachineSession._close()` hangs on `task.cancel()`; the check_factory's try/except returns `True` on any non-SSH exception so a buggy check does not wedge the monitor).
- [x] 6.6 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep).
- [x] 6.7 Verify `uv run ruff check yascheduler/infra/ssh/operations/occupancy.py` and `uv run ruff format --check yascheduler/infra/ssh/operations/occupancy.py` pass; `uv run pytest -m unit tests/unit/test_ssh_gateway.py` is green.

## 7. operations/__init__.py + keys.py + __init__.py — tighten MODULE_CONTRACT PURPOSE

- [x] 7.1 Tighten `operations/__init__.py` `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep). Add `INVARIANTS` (the three collaborators are re-exported, not constructed here; each is stateless and takes a `session` per call).
- [x] 7.2 Enrich existing `FUNC_list_private_keys` in `keys.py`: tighten `PURPOSE` to WHY if slipped; keep `ENSURES`. Add `RATIONALE` Q/A — Q: why call `iterdir()` directly on `keys_dir` rather than `Path(keys_dir).iterdir()`? A: tests pass a `MagicMock` whose `__fspath__` is intentionally absent, so `Path(keys_dir)` would raise `TypeError`; `keys_dir` is expected to be a `Path` in production.
- [x] 7.3 Tighten `yascheduler/infra/ssh/__init__.py` `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep). Add `INVARIANTS` (the SSH subpackage facade re-exports the connected-machine repository, session, operations collaborators, and retry exception aliases — no behavior defined here). Add `DEPENDENCIES` (`READS: yascheduler.domain (MachineRepository), .operations (OccupancyChecker, OutputDownloader, TaskDeployer), .platform.protocol (AllSSHRetryExc, SFTPRetryExc), .repository (SSHMachineRepository), .session (SSHMachineSession)`).
- [x] 7.4 Verify `uv run ruff check yascheduler/infra/ssh/operations/__init__.py yascheduler/infra/ssh/keys.py yascheduler/infra/ssh/__init__.py` and `uv run ruff format --check` on all three pass; `uv run pytest -m unit` for any test importing these modules is green.

## 8. platform/protocol.py — wrap CLASS_ProcessInfo + 6 callable Protocol CLASS_*s, enrich MODULE_CONTRACT

Each new region encloses the FULL class block per the Common rules. Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 8.1 Add `# region CLASS_ProcessInfo` ... `# endregion CLASS_ProcessInfo` enclosing the FULL dataclass block — the `@dataclass(frozen=True)` decorator, `class ProcessInfo:`, the docstring, the three fields (`pid: int`, `name: str`, `command: str`), and the trailing blank line. `PURPOSE` (WHY: carry a single remote process's identity (pid, name, command line) out of `pgrep` / `list_processes` so occupancy checks and process listings branch on structured fields instead of re-parsing `ps` / `Get-CimInstance` output). `INVARIANTS` (frozen stdlib dataclass; `pid: int` (callers filter out non-int pids before yielding); `name: str`; `command: str` — on Windows, when `CommandLine` is empty, the yielder copies `Name` into `command` so consumers always see a non-empty command field).
- [x] 8.2 Add `# region CLASS_RunCallable` ... `# endregion CLASS_RunCallable` enclosing the FULL Protocol block (`class RunCallable(Protocol):`, docstring, `@abstractmethod`, `def __call__(...)` signature + docstring + `raise NotImplementedError` equivalent / `...`). `PURPOSE` (WHY: type every platform-specific synchronous-SSH command-execution callable against a single structural contract so adapter wiring stays platform-agnostic and the type-checker rejects run callables that drop the `quote` parameter). `INVARIANTS` (`Protocol` with `@abstractmethod` advisory — structural matching per PEP 544 still applies; signature: `(conn, quote, command, *args, cwd=None, **kwargs) -> Coroutine[..., SSHCompletedProcess]`).
- [x] 8.3 Add `# region CLASS_RunBgCallable` ... `# endregion CLASS_RunBgCallable` enclosing the FULL Protocol block. `PURPOSE` (WHY: type every platform-specific background-process spawn callable against a single structural contract so the session's `run_bg` can delegate without per-platform branching). `INVARIANTS` (signature: `(conn, quote, command, *args, cwd=None, **kwargs) -> Coroutine[..., SSHClientProcess]`; `stdin=DEVNULL, stdout=DEVNULL, stderr=DEVNULL` is the caller's responsibility — the protocol only types the callable).
- [x] 8.4 Add `# region CLASS_OuterRunCallable` ... `# endregion CLASS_OuterRunCallable` enclosing the FULL Protocol block. `PURPOSE` (WHY: type the closure that `make_run_fn` produces so adapter methods that need a run callable (`get_cpu_cores`, `setup_node`) accept a single typed callable instead of `(conn, quote)` plus a free function). `INVARIANTS` (signature: `(*args, cwd=None, **kwargs) -> Coroutine[..., SSHCompletedProcess]` — `conn` and `quote` are pre-bound by `make_run_fn`).
- [x] 8.5 Add `# region CLASS_ListProcessesCallable` ... `# endregion CLASS_ListProcessesCallable` enclosing the FULL Protocol block. `PURPOSE` (WHY: type every platform-specific process-listing callable so `SSHMachineSession.list_processes` delegates without per-platform branching). `INVARIANTS` (signature: `(conn, query: str | None = None) -> AsyncGenerator[ProcessInfo, None]`; the `query` parameter is platform-specific (Linux: a `ps`-pipe filter, Windows: a PowerShell `Where-Object` filter)).
- [x] 8.6 Add `# region CLASS_PgrepCallable` ... `# endregion CLASS_PgrepCallable` enclosing the FULL Protocol block. `PURPOSE` (WHY: type every platform-specific pattern-matching process callable so `SSHMachineSession.pgrep` delegates without per-platform branching). `INVARIANTS` (signature: `(conn, quote, pattern: str | Pattern[str], *, full: bool = True) -> AsyncGenerator[ProcessInfo, None]`; `full=True` matches against name OR full command line; `full=False` matches against name only).
- [x] 8.7 Add `# region CLASS_SetupNodeCallable` ... `# endregion CLASS_SetupNodeCallable` enclosing the FULL Protocol block. `PURPOSE` (WHY: type every platform-specific node-setup callable so `SSHMachineSession.setup_node` delegates without per-platform branching). `INVARIANTS` (signature: `(conn, run: OuterRunCallable, quote, engines: EngineRepository, engines_dir: PurePath) -> Coroutine[..., None]`; the callable owns the per-platform package-install + engine-deploy sequence).
- [x] 8.8 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep). Add `INVARIANTS` (the retry-exception tuples `SFTPRetryExc`, `SSHRetryExc`, `AllSSHRetryExc = SSHRetryExc + SFTPRetryExc` are defined here — `session.py`'s `my_backoff_exc` and `download.py`'s `my_backoff_sftp` import them; `ProcessInfo` is defined here — `linux.py` and `windows.py` import it; the 6 callable `Protocol`s are structural — `@abstractmethod` is advisory under `Protocol`, PEP 544 structural matching applies). Add `DEPENDENCIES` (`USES API: asyncssh.connection (SSHClientConnection), asyncssh.misc (retry-eligible exceptions), asyncssh.process (SSHClientProcess, SSHCompletedProcess), asyncssh.sftp (retry-eligible SFTP exceptions), yascheduler.domain (EngineRepository TYPE_CHECKING)`).
- [x] 8.9 Verify `uv run ruff check yascheduler/infra/ssh/platform/protocol.py` and `uv run ruff format --check yascheduler/infra/ssh/platform/protocol.py` pass; `uv run pytest -m unit` for any test importing the module is green.

## 9. platform/adapters.py — wrap CLASS_RemoteMachineAdapter, enrich MODULE_CONTRACT

- [x] 9.1 Add `# region CLASS_RemoteMachineAdapter` ... `# endregion CLASS_RemoteMachineAdapter` enclosing the FULL dataclass block — the `@dataclass(frozen=True)` decorator, `class RemoteMachineAdapter:`, the docstring, every field (`platform`, `path`, `quote`, `run`, `run_bg`, `get_cpu_cores`, `list_processes`, `pgrep`, `setup_node`, `checks`), and the trailing blank line. `PURPOSE` (WHY: bundle a platform's SSH-callables + check sequence into a single frozen value so platform detection can return one adapter carrying everything the session needs to operate on the remote machine). `INVARIANTS` (frozen stdlib dataclass; the `checks` tuple is the ordered sequence of `SSHCheck` callables — `detect._detect_platform` runs them under `asyncstdlib.all` to decide if this adapter matches the connected host; the per-platform adapter instances below are module-level frozen constants built via `RemoteMachineAdapter(...)` + `dataclasses.replace(...)` — they are NOT constructed per call). Add `RATIONALE` Q/A — Q: why are the per-platform adapter instances built via `replace(...)` from a parent (e.g. `debian_10_adapter = replace(debian_adapter, platform="debian-10", checks=(*debian_adapter.checks, check_is_debian_10))`) instead of via fresh `RemoteMachineAdapter(...)` calls? A: `replace(...)` inherits every field from the parent and overrides only the platform-specific ones — adding a new field to the dataclass does not require updating every per-platform constructor, only the root ones (`linux_adapter`, `windows_adapter`); the per-version adapters (Debian 10–15, Windows 7–12) just stack an extra check on top of the parent's checks tuple. Q: why are the adapters ordered most-specific-first in `registry.ADAPTERS`? A: `_detect_platform` returns the FIRST adapter whose `checks` all pass — ordering most-specific-first (debian-10 before debian before debian-like before linux before darwin) ensures the most specific platform tag wins when multiple match.
- [x] 9.2 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep). Add `INVARIANTS` (every per-platform adapter instance is a module-level frozen `RemoteMachineAdapter` constant; the `debian_*_adapter` / `windows*_adapter` variants are built via `dataclasses.replace(...)` from their parent (`debian_adapter` / `windows_adapter`) — no per-call construction; `darwin_adapter` reuses `linux_adapter`'s runnables and only swaps the platform tag and check tuple).
- [x] 9.3 Verify `uv run ruff check yascheduler/infra/ssh/platform/adapters.py` and `uv run ruff format --check yascheduler/infra/ssh/platform/adapters.py` pass; `uv run pytest -m unit` for any test importing the module is green.

## 10. platform/exceptions.py — wrap CLASS_PlatformGuessFailedError, enrich MODULE_CONTRACT

- [x] 10.1 Add `# region CLASS_PlatformGuessFailedError` ... `# endregion CLASS_PlatformGuessFailedError` enclosing the FULL class block (`class PlatformGuessFailedError(Exception):`, docstring, trailing blank). `PURPOSE` (WHY: signal that no `RemoteMachineAdapter` in `registry.ADAPTERS` matched the connected host so the caller can surface "unsupported platform" instead of silently proceeding with a `None` adapter). `INVARIANTS` (subclass of `Exception`; raised by `detect._detect_platform` when no adapter's `checks` tuple fully passes).
- [x] 10.2 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep).
- [x] 10.3 Verify `uv run ruff check yascheduler/infra/ssh/platform/exceptions.py` and `uv run ruff format --check yascheduler/infra/ssh/platform/exceptions.py` pass; `uv run pytest -m unit` for any test importing the module is green.

## 11. platform/windows.py — wrap CLASS_MyPureWindowsPath (enclosing nested METHOD), enrich FUNC_* + MODULE_CONTRACT

The nested `METHOD__parse_args` region already exists — it stays INSIDE the new `CLASS_MyPureWindowsPath` (closing order: inner `METHOD` `# endregion` first, then the outer `CLASS` `# endregion`). Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 11.1 Add `# region CLASS_MyPureWindowsPath` ... `# endregion CLASS_MyPureWindowsPath` enclosing the FULL class block — `class MyPureWindowsPath(PureWindowsPath):`, the docstring, the nested `METHOD__parse_args` region (already exists — leave its content untouched), and the trailing blank line. `PURPOSE` (WHY: subclass `PureWindowsPath` so SSH SFTP paths returned by a Windows remote arrive at the session without a spurious leading backslash that breaks SFTP makedirs / file placement). `INVARIANTS` (subclass of `PureWindowsPath`; the overridden `_parse_args` strips a leading `\` from UNC-style returns so paths like `\C:\Users\user` become `C:\Users\user`; the second guard re-introduces a `\\` root when parsing a `PurePath` instance whose first part equals its drive, restoring the path semantics `PureWindowsPath` would otherwise eat). Add `RATIONALE` Q/A — Q: why subclass `PureWindowsPath` instead of using `PureWindowsPath` directly? A: `asyncssh`'s SFTP `realpath` on Windows returns paths with a leading `\` (the SFTP protocol's POSIX-like prefix); `PureWindowsPath("\\C:\\Users")` parses the leading `\` as a UNC root and produces an unusable `\C:\Users` form — the subclass rewrites the parse to drop the leading `\` when the next part looks like a drive letter.
- [x] 11.2 Enrich existing `FUNC_windows_quote`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (wraps the input in single quotes and escapes embedded single quotes by doubling them — PowerShell's verbatim-string quoting rule; output is `'<input with '' for each '>'`).
- [x] 11.3 Enrich existing `FUNC_windows_get_cpu_cores`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns the integer from `[environment]::ProcessorCount` stdout; on parse failure returns `1` — the safe default so a misconfigured Windows host is still allocated exactly one CPU). Add `INVARIANTS` (delegates to the passed `OuterRunCallable` — no direct `conn` access).
- [x] 11.4 Enrich existing `FUNC_windows_list_processes`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (yields one `ProcessInfo` per line of stdout from `Get-CimInstance Win32_Process | ConvertTo-Json -compress`); on a JSON parse failure, the line is skipped (`continue`); lines whose `pid` is not `int`, `name` is not `str`, or `command` is not `str` are skipped; the `powershell.exe` self-process (whose command contains `Get-CimInstance Win32_Process`) is skipped. Add `INVARIANTS` (the inline `data["command"] = data["name"]` fallback when `command` is empty ensures consumers always see a non-empty command field).
- [x] 11.5 Enrich existing `FUNC_windows_pgrep`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (builds a PowerShell `Where-Object` filter matching `Name` (always) OR `CommandLine` (when `full=True`) against the pattern; delegates to `windows_list_processes(conn, query=where_expr)`).
- [x] 11.6 Enrich existing `FUNC_deploy_local_files` / `FUNC_deploy_local_archive` / `FUNC_deploy_remote_archive` (Windows variants): tighten each `PURPOSE` to WHY if slipped; add `INVARIANTS` mirroring the Linux variants with Windows specifics (`deploy_local_files` uses `asyncio.gather` over per-file `sftp.put` for concurrency; `deploy_local_archive` uses `Expand-Archive` instead of `tar xf`; `deploy_remote_archive` uses `Invoke-WebRequest` instead of `wget`).
- [x] 11.7 Enrich existing `FUNC_windows_deploy_engines`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (iterates `engines.values()`, builds `engine_dir = PureWindowsPath((await sftp.realpath(engines_dir / engine.name))[1:])` — strips the leading `\` that asyncssh's SFTP `realpath` prepends on Windows; calls `sftp.makedirs(PurePath(engine_dir), exist_ok=True)` because `sftp.makedirs` is broken for `PureWindowsPath` directly; dispatches each `engine.deployable` entry to the matching Windows deploy helper).
- [x] 11.8 Enrich existing `FUNC_windows_setup_node`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (opens a fresh SFTP client via `conn.start_sftp_client()` and delegates to `windows_deploy_engines(run, quote, sftp, engines, engines_dir)`; no package-install step — Windows engines are archive-only).
- [x] 11.9 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep). Add `DEPENDENCIES` (`USES API: asyncssh (SSHClientConnection, SFTPClient), yascheduler.domain (EngineRepository, LocalArchiveDeploy, LocalFilesDeploy, RemoteArchiveDeploy), .protocol (OuterRunCallable, ProcessInfo, QuoteCallable)`).
- [x] 11.10 Verify `uv run ruff check yascheduler/infra/ssh/platform/windows.py` and `uv run ruff format --check yascheduler/infra/ssh/platform/windows.py` pass; `uv run pytest -m unit` for any test importing the module is green.

## 12. platform/linux.py — enrich existing FUNC_* + MODULE_CONTRACT

- [x] 12.1 Enrich existing `FUNC_linux_get_cpu_cores`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns the integer from `getconf NPROCESSORS_ONLN 2> /dev/null || getconf _NPROCESSORS_ONLN` stdout; on parse failure returns `1` — the safe default). Add `INVARIANTS` (delegates to the passed `OuterRunCallable` — no direct `conn` access).
- [x] 12.2 Enrich existing `FUNC_linux_list_processes`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (yields one `ProcessInfo` per non-broken line of `ps -eo pid:255,comm:255,args:255` stdout; lines with fewer than 3 whitespace-split parts are skipped as broken; the bash subprocess running the `ps_cmd` itself is skipped). Add `INVARIANTS` (logs `LIST_PROCESSES` with the cmd, `BROKEN_LINE` per skipped line, `YIELD` per yielded process, and `DONE` with `lines` / `broken` / `self_skip` totals; the `ps` column format `pid:255,comm:255,args:255` truncates each field to 255 chars and uses 10-space padding as the column delimiter so `split(" * 10)` re-separates them).
- [x] 12.3 Enrich existing `FUNC_linux_pgrep`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (builds a `pgrep [-f] <pattern>` query and delegates to `linux_list_processes(conn, query=pgrep_query)`; `pattern` accepts `str` or `re.Pattern` — the latter's `.pattern` is unwrapped before quoting).
- [x] 12.4 Enrich existing `FUNC_deploy_local_files` / `FUNC_deploy_local_archive` / `FUNC_deploy_remote_archive` (Linux variants): tighten each `PURPOSE` to WHY if slipped; add `INVARIANTS` (`deploy_local_files` uses `sftp.put(lpaths, engine_dir, preserve=True)`; `deploy_local_archive` uploads then `tar xfv` extracts then `sftp.remove`s the archive; `deploy_remote_archive` `wget`s then `tar xfv` extracts then `sftp.remove`s).
- [x] 12.5 Enrich existing `FUNC_linux_deploy_engines`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (iterates `engines.values()`, creates `engine_dir = engines_dir / engine.name` via `sftp.makedirs(..., exist_ok=True)`, dispatches each `engine.deployable` entry to the matching Linux deploy helper; logs `Setup %s engine...` and `Setup of %s engine is done...` at info level).
- [x] 12.6 Enrich existing `FUNC_log_mpi_version`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (runs `mpirun --allow-run-as-root -V` with `check=True`; logs `VERSION` with the first stdout line on success; silently no-ops on non-zero exit — MPI may not be installed).
- [x] 12.7 Enrich existing `FUNC_linux_setup_node`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (opens a fresh SFTP client via `conn.start_sftp_client()` and delegates to `linux_deploy_engines(run, quote, sftp, engines, engines_dir)`; no package-install step — generic Linux engines are archive-only).
- [x] 12.8 Enrich existing `FUNC_linux_setup_deb_node`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (reads `conn.get_extra_info("username")` to decide `sudo` prefix; runs `apt-get -o DPkg::Lock::Timeout=600 -y update` + `upgrade` + per-engine `install <pkgs>`; if any package contains "mpi", logs the MPI version via `log_mpi_version`; opens a fresh SFTP client and delegates to `linux_deploy_engines`).
- [x] 12.9 Tighten the existing `MODULE_CONTRACT` `PURPOSE` to WHY if slipped (current text mentions WHY — keep). Add `DEPENDENCIES` (`USES API: asyncssh (SSHClientConnection, SFTPClient), yascheduler.domain (EngineRepository, LocalArchiveDeploy, LocalFilesDeploy, RemoteArchiveDeploy), .protocol (OuterRunCallable, ProcessInfo, QuoteCallable)`).
- [x] 12.10 Verify `uv run ruff check yascheduler/infra/ssh/platform/linux.py` and `uv run ruff format --check yascheduler/infra/ssh/platform/linux.py` pass; `uv run pytest -m unit` for any test importing the module is green.

## 13. platform/detect.py + common.py + paths.py + run_fn.py + checks.py + registry.py + __init__.py — enrich existing FUNC_* / MODULE_CONTRACT

Only defined GRACE fields are used; every `PURPOSE` answers WHY.

- [x] 13.1 Enrich existing `FUNC__detect_platform` in `detect.py`: tighten `PURPOSE` to WHY if slipped; keep `ENSURES`. Add `INVARIANTS` (uses `asyncstdlib.all` + `asyncstdlib.map` to run each adapter's `checks` tuple concurrently under a `Semaphore(MAX_SESSIONS)` — `MAX_SESSIONS=10` is the default OpenSSH server `MaxSessions`; the first adapter whose checks all pass is the returned `adapter`; the `platforms` list collects every matching adapter's platform tag (a Debian-12 host matches `linux`, `debian-like`, `debian`, `debian-12` — all four tags are returned so the orchestrator's engine filter sees them). Add `RATIONALE` Q/A — Q: why run every adapter's checks even after the first match? A: the `platforms` list (not just the chosen `adapter.platform`) is what the orchestrator's engine-filter reads — early exit would lose the multi-platform tags that let a Debian-12 host match a `linux`-only engine AND a `debian-12`-only engine. Q: why a `Semaphore(MAX_SESSIONS)`? A: the adapter checks open SSH channels concurrently; OpenSSH's default `MaxSessions=10` caps concurrent channels per connection — exceeding it surfaces as `ChannelOpenError` (a retryable `SSHRetryExc`); the semaphore caps the concurrency at the source.
- [x] 13.2 Enrich existing `MODULE_CONTRACT` of `detect.py`: tighten `PURPOSE` to WHY if slipped. Add `INVARIANTS` (`MAX_SESSIONS=10` is the default OpenSSH server `MaxSessions` — the semaphore cap matches the protocol's natural limit). Add `DEPENDENCIES` (`USES API: asyncstdlib (aall, amap), asyncio.locks (Semaphore), .exceptions (PlatformGuessFailedError), .adapters (RemoteMachineAdapter TYPE_CHECKING), .protocol (SSHCheck TYPE_CHECKING)`).
- [x] 13.3 Enrich existing `FUNC_run` in `common.py`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (prepends `cd <cwd>; ` when `cwd` is truthy; pops `timeout` from `kwargs` and coerces non-`float` to `None`; passes `check=kwargs.pop("check", False)` to asyncssh's `conn.run` — caller's responsibility to opt in). Add `RATIONALE` Q/A — Q: why coerce non-float `timeout` to `None`? A: asyncssh's `conn.run(timeout=...)` accepts only `float | None`; callers threading a non-float (e.g. an `int`) would raise `TypeError` — the coercion is a defensive guard.
- [x] 13.4 Enrich existing `FUNC_run_bg` in `common.py`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (prepends `cd <cwd>; ` when `cwd` is truthy; calls `conn.create_process(command, *args, stdin=DEVNULL, stdout=DEVNULL, stderr=DEVNULL, **kwargs)` — the spawned process is fully detached from the SSH channel's stdio).
- [x] 13.5 Enrich existing `MODULE_CONTRACT` of `common.py`: tighten `PURPOSE` to WHY if slipped (current text mentions WHY — keep).
- [x] 13.6 Enrich existing `FUNC__init_paths` in `paths.py`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns `(data_dir, engines_dir, tasks_dir)` as instances of `adapter.path` — the path-type matching the remote OS; falls back to `./data`, `<data_dir>/engines`, `<data_dir>/tasks` when any input is `None` or already an instance of `adapter.path`). Add `INVARIANTS` (pure — no I/O, no logging; the `isinstance(data_dir, path_cls)` check skips already-normalized paths so a caller passing a `PureWindowsPath` to the Windows adapter pays no re-construction cost).
- [x] 13.7 Enrich existing `MODULE_CONTRACT` of `paths.py`: tighten `PURPOSE` to WHY if slipped (current text mentions WHY — keep).
- [x] 13.8 Enrich existing `FUNC_make_run_fn` in `run_fn.py`: tighten `PURPOSE` to WHY if slipped; add `ENSURES` (returns an `OuterRunCallable` whose body delegates to `adapter.run(conn, adapter.quote, str(args[0]) if args else "", *args[1:], cwd=cwd, **kwargs)` — `conn` and `adapter.quote` are pre-bound). Add `INVARIANTS` (pure closure — no side effects at construction; the returned `_run_fn` carries the pre-bound `conn` and `adapter` so adapter methods accepting `OuterRunCallable` (`get_cpu_cores`, `setup_node`) do not re-thread `conn` / `quote` per call).
- [x] 13.9 Enrich existing `MODULE_CONTRACT` of `run_fn.py`: tighten `PURPOSE` to WHY if slipped (current text mentions WHY — keep).
- [x] 13.10 Enrich existing `FUNC_check_is_linux` / `FUNC_check_is_darwin` / `FUNC__get_os_release` / `FUNC_check_is_debian_like` / `FUNC_check_is_debian` / `FUNC__check_debian_version` / `FUNC_check_is_windows` / `FUNC_get_wmi_w32_os_caption` / `FUNC__check_is_windows_caption_version` in `checks.py`: tighten each `PURPOSE` to WHY if slipped; add `INVARIANTS` where useful (the `@lru_cache`-decorated checks (`check_is_linux`, `check_is_darwin`, `_get_os_release`, `check_is_windows`, `get_wmi_w32_os_caption`) cache on `conn` — repeated calls within one SSH session pay one round-trip; the `check_is_debian_*` and `check_is_windows_*` variants are `partial(_check_debian_version, "<ver>")` / `partial(_check_is_windows_caption_version, "<ver>")` — they re-use the cached `_get_os_release` / `get_wmi_w32_os_caption` round-trip).
- [x] 13.11 Tighten `MODULE_CONTRACT` of `checks.py`: tighten `PURPOSE` to WHY if slipped; add `DEPENDENCIES` (`USES API: asyncssh (SSHClientConnection.run), asyncstdlib (lru_cache)`).
- [x] 13.12 Enrich existing `MODULE_CONTRACT` of `registry.py`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (`ADAPTERS` is ordered most-specific-first: `debian_10..15` → `debian` → `debian_like` → `linux` → `darwin` → `windows10..12` → `windows7..8` → `windows`; `_detect_platform` returns the first whose `checks` all pass, so a Debian-12 host gets tagged `debian-12` (not `linux`); the multi-tag `platforms` list still collects every match — see `detect._detect_platform` RATIONALE).
- [x] 13.13 Enrich existing `MODULE_CONTRACT` of `platform/__init__.py`: tighten `PURPOSE` to WHY if slipped; add `INVARIANTS` (the platform subpackage facade re-exports the adapter instances, check functions, common runnables, detect + MAX_SESSIONS, exceptions, Linux/Windows deploy functions, path helper, protocol types, registry, and run_fn factory — no behavior defined here). Add `DEPENDENCIES` (lists the re-exports: `.adapters`, `.checks`, `.common`, `.detect`, `.exceptions`, `.linux`, `.paths`, `.protocol`, `.registry`, `.run_fn`, `.windows`).
- [x] 13.14 Verify `uv run ruff check yascheduler/infra/ssh/platform/` and `uv run ruff format --check yascheduler/infra/ssh/platform/` pass; `uv run pytest -m unit` for any test importing the platform modules is green.

## 14. End-to-end verify

- [x] 14.1 Manual scan: every `# region CLASS_*`, `FUNC_*`, `METHOD_*`, `BLOCK_*`, and `MODULE_CONTRACT` in `yascheduler/infra/ssh/**/*.py` has a paired `# endregion` and wraps the entire entity (no orphaned trailing code outside the region; no region closes before its entity ends; nested `METHOD_*` / `BLOCK_*` regions live INSIDE their enclosing `CLASS_*`; the `CLASS_*` `# endregion` comes after the last nested `# endregion`). Specifically: `CLASS_MyPureWindowsPath` encloses the nested `METHOD__parse_args`; `CLASS_SSHMachineRepository` encloses every `METHOD_*` and inner `BLOCK_*`; `CLASS_SSHMachineSession` encloses every `METHOD_*` (including the 7 new ones — `_prime_ncpus_cache`, `run`, `run_bg`, `upload`, `open_sftp`, `pgrep`, `list_processes`) and inner `BLOCK_*`.
- [x] 14.2 Manual scan: no invented GRACE field names anywhere in the touched files — only `PURPOSE` / `SCOPE` / `INVARIANTS` / `USECASES` / `DEPENDENCIES` / `RATIONALE` / `KEYWORDS` / `REQUIRES` / `ENSURES`. Specifically, NO `SHALL NOT:` field and NO `RAISES:` field anywhere (the spec's removed `SHALL NOT` sentences became `INVARIANTS` entries stating the positive contract, or `RATIONALE` Q/As).
- [x] 14.3 Manual scan: every `PURPOSE` field answers WHY, not WHAT. Spot-check every region in `repository.py`, `session.py`, `operations/deployment.py`, `operations/download.py`, `operations/occupancy.py`, `operations/__init__.py`, `keys.py`, `__init__.py`, `platform/protocol.py`, `platform/adapters.py`, `platform/exceptions.py`, `platform/windows.py`, `platform/linux.py`, `platform/detect.py`, `platform/common.py`, `platform/paths.py`, `platform/run_fn.py`, `platform/checks.py`, `platform/registry.py`, `platform/__init__.py`.
- [x] 14.4 Manual scan: every `RATIONALE` field is in Q/A format ("Q: ... A: ..."). No `RATIONALE` block contains free-form prose that should be in `PURPOSE` / `INVARIANTS` / `SCOPE`.
- [x] 14.5 `openspec validate --all --json` passes (exit 0); the trimmed `ssh-infrastructure` spec validates AND the change `ssh-infrastructure-spec-trim` validates AND no other spec regresses (still 20 specs + in-flight trim changes).
- [x] 14.6 `uv run pytest -m unit` — all unit tests pass (no behavior changed).
- [x] 14.7 `uv run pytest -m integration` — all integration tests pass (assume Docker running).
- [x] 14.8 `uv run ruff check .` and `uv run ruff format --check .` pass on all changed files.
- [x] 14.9 `uv run lint-imports` passes (no new imports introduced; markup-only edits).
- [x] 14.10 Confirm no public-surface change: no CLI command, console_script, INI config key, DB schema, public API, or log-format change in the diff. The diff is `# region`/`# endregion` markup + comment-field enrichment + spec text trim only.
