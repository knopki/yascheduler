## 1. Orchestrator class contract absorbs the relocated constructor-shape rationale

- [x] 1.1 Extend the `MODULE_CONTRACT` region at the top of `yascheduler/application/orchestrator.py` with `INVARIANTS` documenting: module-local logger is bound via `logging.getLogger(__name__)` (no injected logger); the module does not import `Config` from `yascheduler.entrypoints` at runtime or under `TYPE_CHECKING` (the layer direction is enforced by `import-linter`); orchestrator-level dependencies are typed against domain Protocols (`MachineRepository`, `CloudProvisioner`) and concrete collaborators (`TaskDeployer`, `OutputDownloader`, `OccupancyChecker`)
- [x] 1.2 Extend the `CLASS_Orchestrator` region in `yascheduler/application/orchestrator.py` with `INVARIANTS` and `RATIONALE` fields absorbing: the orchestrator does not accept a `log` parameter (enforced structurally by the static guard test in `tests/unit/test_log_scope_discipline.py`); the orchestrator does not accept a `config: Config` parameter (rationale: `Config` is a composition-root aggregate owned by `yascheduler.entrypoints`; `LocalSettings` and `RemoteDefaults` are the application-layer projections, passed as separate keyword arguments); the orchestrator SHALL NOT read `clouds.configs` and SHALL NOT hold `adapters`/`configs` dicts (provider selection delegated to `clouds.select_provider`)
- [x] 1.3 Verify outcome: `uv run pytest -m unit tests/unit/test_application_orchestrator.py tests/unit/test_log_scope_discipline.py tests/unit/test_application_no_adapter_imports.py tests/unit/test_di.py tests/unit/test_di_no_casts.py` is green; `uv run lint-imports` passes (the orchestrator module still respects the `layers` contract); no behavioral change in the orchestrator module

## 2. Consume loop contract absorbs the in-flight and occupancy-started invariants

- [x] 2.1 Add `METHOD_*` regions wrapping the consume producer and consume consumer methods inside the existing `CLASS_Orchestrator` region of `yascheduler/application/orchestrator.py` (the methods are currently bare — no METHOD region exists for them), then extend each region with `REQUIRES`/`ENSURES`/`INVARIANTS` absorbing: the orchestrator maintains an in-process set of in-flight consume task ids; the consume producer SHALL skip yielding a task whose id is in the in-flight set; the consume consumer SHALL add the task id to the in-flight set before awaiting `consume_task` and remove it in a `finally` block so a failed consume does not permanently block re-yield; the orchestrator maintains a separate in-process set of node_ids whose occupancy check has been started; the consumer SHALL add `task.allocated_node_id` to the occupancy-started set on the first tick and discard it on finalised consume
- [x] 2.2 Verify outcome: `uv run pytest -m unit tests/unit/test_application_orchestrator.py tests/unit/test_orchestrator_consumer_resilience.py tests/unit/test_consume_task.py` is green; the in-flight-guard and occupancy-started behaviors are now documented in the METHOD contracts and asserted by the existing scenarios in the surviving spec

## 3. Deallocate loop contract absorbs SSH-teardown ownership

- [x] 3.1 Extend the `FUNC_deallocate_node` region in `yascheduler/application/deallocate_nodes.py` with `REQUIRES`/`ENSURES`/`RATIONALE` absorbing: the helper performs SSH disconnect (`repository.contains(node.node_id)` + `repository.disconnect(node.node_id)`) BEFORE the `if node.cloud:` guard so teardown runs regardless of node type; the caller (orchestrator deallocate consumer) SHALL NOT duplicate the teardown — calling `repository.contains`/`repository.disconnect` directly is forbidden because the helper already owns it; the consumer wraps the helper call in `try/except Exception` that logs `node_id`, `hostname`, and the error and continues; rationale: SSH teardown is unconditional (runs for cloud AND static nodes), cloud deletion is conditional on `node.cloud`, the disable+remove bracket protects against allocator re-selection on failure
- [x] 3.2 Verify outcome: `uv run pytest -m unit tests/unit/test_application_orchestrator.py tests/unit/test_abandon_node.py` is green; the teardown-ownership contract is documented in the FUNC ENSURES and asserted by the surviving "Deallocator consumer does not duplicate SSH teardown" scenario

## 4. Connect-machine contract absorbs failure-timer, grace-window, and static/cloud-split rationale

- [x] 4.1 Add `METHOD_*` regions wrapping the connect-machine producer and connect-machine consumer methods inside the existing `CLASS_Orchestrator` region of `yascheduler/application/orchestrator.py` (the methods are currently bare — no METHOD region exists for them), then extend each region with `REQUIRES`/`ENSURES`/`INVARIANTS`/`RATIONALE` absorbing: the orchestrator maintains an in-memory per-node connect-failure timer (not persisted across daemon restarts — a restart resets the grace window); on a successful `repository.connect(...)` the orchestrator pops the node's `node_id` from the failure timer; for static nodes (`cloud is None`) the orchestrator emits a trace DEBUG record plus a `warning(...)` narrative record and returns early BEFORE the grace-check so static nodes retry indefinitely and NEVER reach `abandon_node` (rationale: a transient SSH outage after a daemon restart must not silently delete an operator's node row); for cloud nodes the orchestrator compares the elapsed monotonic age against the matching `CloudConfig`'s `connect_grace` (looked up from `active_clouds` by `prefix == node.cloud`); within grace retries; past grace triggers `abandon_node`; for nodes whose `node.cloud` does not match any `CloudConfig.prefix` in `active_clouds`, a conservative default `connect_grace` of 120 seconds applies (matches the slowest cloud default) so the abandon path still fires for misconfigured clouds; all connection identity comes from the `Node` itself (no `config.remote` lookup, no `CloudConfig` prefix-match loop for jump)
- [x] 4.2 Verify outcome: `uv run pytest -m unit tests/unit/test_orchestrator_start_task_on_machine.py tests/unit/test_connect_machine_consumer.py tests/unit/test_application_orchestrator.py` is green; the failure-timer + grace-window logic is documented in the METHOD contracts and asserted by the surviving connect-machine scenarios

## 5. Free-machine-selection gate contract absorbs DB-enabled-node rationale

- [x] 5.1 Extend the `FUNC_allocate_task` region in `yascheduler/application/allocate_task.py` and the `FUNC__find_free_machines` region in the same file with `REQUIRES`/`ENSURES`/`RATIONALE` absorbing: the use case reads `uow.nodes.list_enabled()` in the same Unit of Work it opens for `uow.tasks.list_by_status({RUNNING})`; the free-machine filter SHALL intersect `MachineRepository.list_free(platforms)` with the enabled-node set MINUS the busy-node set (the node_ids of RUNNING tasks' `allocated_node_id`); rationale: the gate restores the invariant that a machine is allocatable ONLY after its DB row is `enabled=TRUE` (the row is flipped from `enabled=FALSE` to `enabled=TRUE` by the persist step after `clouds.allocate` returns successfully — i.e. only after cloud-init, engine setup, and CPU detection complete); the gate lives in the use case, NOT in `MachineRepository`, because `MachineRepository` is an infrastructure-layer SSH-collection port that SHALL NOT be coupled to `NodeRepository` (a persistence port) — joining the two data sources is the use case's responsibility
- [x] 5.2 Verify outcome: `uv run pytest -m unit tests/unit/test_allocate_task_node_pairing.py tests/unit/test_allocate_task_failure_modes.py tests/unit/test_application_use_cases.py` is green; the DB-enabled-gate rationale is documented in the FUNC RATIONALE and asserted by the surviving "Setup-in-flight tmp-node is invisible to the allocator" / "Enabled node is allocatable after setup completes" / "Gate lives in the use case, not the repository" scenarios

## 6. Apply the spec delta to the main orchestrator spec and verify end-to-end

- [x] 6.1 Archive the change via `openspec archive 2026-07-17-orchestrator-spec-dehydrate`: this applies the MODIFIED-Requirements delta in `openspec/changes/2026-07-17-orchestrator-spec-dehydrate/specs/orchestrator/spec.md` to `openspec/specs/orchestrator/spec.md`, moving the change directory into `openspec/changes/archive/`
- [x] 6.2 Verify outcome: `openspec validate --all --json` passes (exit 0); the main `openspec/specs/orchestrator/spec.md` is reduced from 403 to ~284 lines; every observable scenario that was in the pre-change spec is present in the post-change spec (grep `#### Scenario:` counts pre/post — pre count MUST be ≤ post count, since two negative-space scenarios were intentionally dropped and their constraints are covered by the static guard test and the import-linter layers contract); `uv run pytest -m unit` is green across the orchestrator + use-case test files; `uv run ruff check .` and `uv run ruff format --check .` are green on changed files; `uv run lint-imports` passes
