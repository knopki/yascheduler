## REMOVED Requirements

### Requirement: CloudConfig structural Protocol
**Reason**: Merged into the new `cloud-config` capability's `CloudConfig structural Protocol` requirement. Content preserved verbatim; only the hosting spec changes (one spec for the cloud config subsystem instead of three).
**Migration**: Read `cloud-config` — `CloudConfig structural Protocol` requirement with all scenarios.

### Requirement: Cloud config DTOs relocated to infra
**Reason**: Merged into `cloud-config`'s `Cloud config DTOs relocated to infra` requirement. Content preserved verbatim.
**Migration**: Read `cloud-config` — `Cloud config DTOs relocated to infra` requirement.

### Requirement: Cloud config parser registry
**Reason**: Merged into `cloud-config`'s `Cloud config parser registry` requirement. Content preserved verbatim.
**Migration**: Read `cloud-config` — `Cloud config parser registry` requirement.

### Requirement: Cloud section parser functions
**Reason**: Merged into `cloud-config`'s `Cloud section parser functions` requirement. Content preserved verbatim.
**Migration**: Read `cloud-config` — `Cloud section parser functions` requirement.

### Requirement: Config.from_config_parser delegates cloud assembly
**Reason**: Merged into `cloud-config`'s `Config.from_config_parser delegates cloud assembly` requirement. Content preserved verbatim.
**Migration**: Read `cloud-config` — `Config.from_config_parser delegates cloud assembly` requirement.