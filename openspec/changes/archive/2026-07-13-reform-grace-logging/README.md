# reform-grace-logging

Reform GRACE-lite logging markers: split grace-debug traces from user-facing narrative, introduce YaLogger/trace(), LogFormatter, get_logger() factory, and M-ID canonical logger names.
