## 1. exceptions.py — wrap all 15 public exception classes in CLASS_* regions; absorb spec rationale into contract fields

Every `CLASS_*` region opens one line above the `class` line and closes one line below the class body, enclosing the FULL class — the `class` line, the docstring, every `__init__` line, every `self.<attr>` assignment — through the trailing blank line before the next region marker. No region closes before the class body ends. Only defined GRACE fields are used (`PURPOSE` / `INVARIANTS` / `ENSURES` / `RATIONALE` / `SCOPE`); no invented field names. Every `PURPOSE` states WHY, not WHAT.

- [x] 1.1 Add `# region CLASS_DomainError` ... `# endregion CLASS_DomainError` enclosing the full `class DomainError(Exception):` block (class line + docstring + trailing blank line). `PURPOSE`: give callers one typed root to catch so business failures are distinguished from system errors at every boundary. `INVARIANTS`: base of every domain exception; carries a stable human-readable message; catchable as `Exception`.
- [x] 1.2 Add `# region CLASS_ValidationError` ... `# endregion CLASS_ValidationError` enclosing the full `class ValidationError(DomainError):` block. `PURPOSE`: group input-rejection failures so callers can branch on validation separately from lifecycle and provider errors.
- [x] 1.3 Add `# region CLASS_UnsupportedEngineError` ... `# endregion CLASS_UnsupportedEngineError` enclosing the FULL block — `class` line, docstring, `__init__`, the `self.engine_name` assignment, and the `super().__init__` call, through the trailing blank line. `PURPOSE`: tell the caller which engine name was rejected so the submit path surfaces the unknown value rather than a generic error.
- [x] 1.4 Add `# region CLASS_MissingInputFileError` ... `# endregion CLASS_MissingInputFileError` enclosing the FULL block (class line, docstring, `__init__` with both `self.*` assignments and the `super().__init__` call, trailing blank line). `PURPOSE`: tell the caller which input file a given engine is missing so the submit path can name the gap to the operator.
- [x] 1.5 Add `# region CLASS_TaskError` ... `# endregion CLASS_TaskError` enclosing the full `class TaskError(DomainError):` block. `PURPOSE`: group task lifecycle guard failures so callers branch on lifecycle separately from validation and provider errors. `RATIONALE` Q/A: "Q: Why are there no `TaskAlreadyAllocatedError` / `TaskNotAllocatedError` exceptions? A: Allocation and the `TO_DO→RUNNING` transition are atomic — a single `task.run()` call guarded by `TaskNotTodoError` performs the transition, so neither an 'already allocated' nor a 'not allocated' guard can independently arise."
- [x] 1.6 Add `# region CLASS_TaskNotTodoError` ... `# endregion CLASS_TaskNotTodoError` enclosing the FULL block (class line, docstring, `__init__`, `self.task_id`, `super().__init__`, trailing blank line). `PURPOSE`: stop a non-TO_DO task from entering the allocation path so the `TO_DO→RUNNING` transition is guarded.
- [x] 1.7 Add `# region CLASS_TaskNotRunningError` ... `# endregion CLASS_TaskNotRunningError` enclosing the FULL block (class line, docstring, `__init__`, `self.task_id`, `super().__init__`, trailing blank line). `PURPOSE`: stop a non-RUNNING task from entering the consume/abandon path so only running tasks are finalised.
- [x] 1.8 Add `# region CLASS_MachineBusyError` ... `# endregion CLASS_MachineBusyError` enclosing the FULL block (class line, docstring, `__init__`, `self.node_id`, `super().__init__`, trailing blank line). `PURPOSE`: stop a second task from occupying an already-busy machine so allocation respects the per-machine occupancy invariant. `ENSURES`: stores `node_id` as an instance attribute; sets no `hostname` attribute; message is `machine ({node_id}) is busy` with the bare integer. `RATIONALE` Q/A: "Q: Why does this carry only `node_id` and not `hostname`? A: `node_id` is the stable identity; the machine already exists, so operators resolve its transport address via `yanodes` or the DB. `MachineConnectionError` is the contrast — it carries `hostname` because it is raised at the transport layer where the operator-recognizable address is the hostname."
- [x] 1.9 Add `# region CLASS_MachineConnectionError` ... `# endregion CLASS_MachineConnectionError` enclosing the FULL block (class line, docstring, `__init__`, all three `self.*` assignments, the `super().__init__` call, trailing blank line). `PURPOSE`: surface an SSH connect failure with the operator-recognizable address so the connection fault is diagnosable without a separate lookup. `ENSURES`: stores `node_id`, `hostname`, and `reason` as instance attributes; message is `cannot connect to machine ({node_id}) at {hostname}: {reason}`. `RATIONALE` Q/A: "Q: Why does this carry `hostname` when `MachineBusyError` does not? A: It is raised at the transport layer (SSH connect) before any session exists; `hostname` is the operator-recognizable address that makes the connection fault diagnosable without a separate node lookup."
- [x] 1.10 Add `# region CLASS_SchedulingError` ... `# endregion CLASS_SchedulingError` enclosing the full `class SchedulingError(DomainError):` block. `PURPOSE`: group allocation/placement failures so the allocator and its callers branch on scheduling separately from provider errors.
- [x] 1.11 Add `# region CLASS_NoCompatibleNodeError` ... `# endregion CLASS_NoCompatibleNodeError` enclosing the FULL block (class line, docstring, `__init__`, both `self.*` assignments, `super().__init__`, trailing blank line). `PURPOSE`: tell the allocator no node matched a task's platforms so it can decide to provision cloud capacity instead.
- [x] 1.12 Add `# region CLASS_CloudCapacityExhaustedError` ... `# endregion CLASS_CloudCapacityExhaustedError` enclosing the FULL block (class line, docstring, `__init__`, `self.task_id`, `super().__init__`, trailing blank line). `PURPOSE`: tell the allocator no provider can serve the request so it stops provisioning and surfaces the shortage. (The "why under `SchedulingError` and not `CloudError`" rationale stays at module level — it is already in `MODULE_CONTRACT RATIONALE`; do not duplicate it here.)
- [x] 1.13 Add `# region CLASS_CloudError` ... `# endregion CLASS_CloudError` enclosing the full block. Shorten the existing multi-line `CloudError` docstring to a one-line brief (`"""Operational cloud-provider failures."""`); relocate its content into the region fields. `PURPOSE`: group operational cloud-provider failures so callers branch on provider faults separately from scheduling decisions. `SCOPE`:
  - Operational cloud-provider failures: provider selection, VM creation, SSH/cloud-init/engine setup.
  - NOT: cloud capacity planning (that is `CloudCapacityExhaustedError` under `SchedulingError`).
- [x] 1.14 Add `# region CLASS_CloudAllocateError` ... `# endregion CLASS_CloudAllocateError` enclosing the full `class CloudAllocateError(CloudError):` block (class line + docstring + trailing blank line). `PURPOSE`: mark a failure in provider selection or VM creation so the allocator cleans up the tmp-node and surfaces a creation fault.
- [x] 1.15 Add `# region CLASS_CloudSetupError` ... `# endregion CLASS_CloudSetupError` enclosing the full `class CloudSetupError(CloudError):` block (class line + docstring + trailing blank line). `PURPOSE`: mark a failure in SSH/cloud-init/engine setup so the allocator cleans up the created VM and surfaces a setup fault.
- [x] 1.16 Verify every `CLASS_*` region encloses ALL content of its target: for base classes, the `class` line + docstring + trailing blank; for leaf classes with `__init__`, the `class` line + docstring + the entire `__init__` (every `self.<attr> = ...` line and the `super().__init__(...)` call) + trailing blank. No region closes before the class body ends. No invented contract field names appear — only `PURPOSE` / `INVARIANTS` / `ENSURES` / `RATIONALE` / `SCOPE`, each per its defined meaning. Every `PURPOSE` is a WHY, not a WHAT.
- [x] 1.17 Verify outcome: `uv run ruff check yascheduler/domain/exceptions.py` and `uv run ruff format --check yascheduler/domain/exceptions.py` pass; `uv run pytest -m unit tests/unit/test_domain_exceptions.py` is green (no behavior changed).

## 2. Apply the spec delta and verify end-to-end

- [x] 2.1 Apply the 4 MODIFIED requirements from `openspec/changes/domain-exceptions-spec-trim/specs/domain-exceptions/spec.md` to `openspec/specs/domain-exceptions/spec.md`, replacing each original requirement block in place. Preserve requirement header text exactly (whitespace-insensitive match) so OpenSpec recognizes the MODIFIED operation: `TaskError hierarchy`, `MachineBusyError`, `SchedulingError hierarchy`, `CloudError hierarchy`. The three untouched requirements (`DomainError base class`, `ValidationError hierarchy`, `MachineConnectionError`) stay verbatim.
- [x] 2.2 Confirm the trimmed main spec contains no removed prose: no "`TaskAlreadyAllocatedError` and `TaskNotAllocatedError` are not part of the hierarchy" paragraph; no "`MachineBusyError` ... SHALL NOT carry a `hostname` attribute" paragraph; no "`CloudCapacityExhaustedError` ... SHALL NOT be a subclass of `CloudError`" paragraph; no "`CloudError` covers operational cloud-provider failures ... capacity planning is NOT part of this hierarchy" paragraph. Every observable behavioral scenario (`#### Scenario:` count) MUST be present in the trimmed spec — pre/post counts compared (23 → 23).
- [x] 2.3 Run `openspec validate --all --json` — passes (exit 0). The change validates AND the main spec validates.
- [x] 2.4 Run `uv run pytest -m unit` — all unit tests pass (no behavior changed; `test_domain_exceptions.py` is the regression guard).
- [x] 2.5 Run `uv run ruff check .` and `uv run ruff format --check .` — pass on all changed files.
- [x] 2.6 Run `uv run lint-imports` — passes (no new imports introduced; markup-only edits).
- [x] 2.7 Confirm no public-surface change: no CLI, public API, INI config, DB schema, or log-format change in the diff. The diff is `# region`/`# endregion` markup + comment-field enrichment + one docstring shortening + spec text trim only.
