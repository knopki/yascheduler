## 1. New module: `entrypoints/cli/`

- [x] 1.1 Create `yascheduler/entrypoints/cli/__init__.py` with GRACE-lite `MODULE_CONTRACT` (PURPOSE: subpackage facade for the `init` CLI entry point; SCOPE: no re-exports — `init` is invoked by console_script, not imported across layers; DEPENDS: none; LINKS: M-ENTRYPOINTS-CLI-INIT), `MODULE_MAP` (none), `CHANGE_SUMMARY` (LAST_CHANGE: v1.0.0 - Initial subpackage facade for the relocated init command; init moved from infra/cli/ in relocate-init-command). Mirror the shape of `yascheduler/entrypoints/daemon/__init__.py`. No `__all__` (nothing re-exported).
- [x] 1.2 Create `yascheduler/entrypoints/cli/init.py` with the full GRACE-lite header: `FILE: yascheduler/entrypoints/cli/init.py`, `VERSION: 1.0.0`, `MODULE_CONTRACT` (PURPOSE: yainit CLI command — install service unit files and/or apply DB schema, with --schema/--daemon subset-selector flags; SCOPE: init command + argparse + systemd/sysv service install + DB schema application delegation; DEPENDS: M-PERSISTENCE-SCHEMA, M-CONFIG, M-SHARED; LINKS: M-ENTRYPOINTS-CLI-INIT, M-PERSISTENCE-SCHEMA), `MODULE_MAP` (init, _init_systemd, _init_sysv, _init_schema), `CHANGE_SUMMARY` (LAST_CHANGE: v1.0.0 - Reimplemented at entrypoints/cli/ in relocate-init-command: moved from infra/cli/init.py, added --schema/--daemon argparse flags, exit-code contract 0/1/2, overwrite-if-exists service files, /run/systemd/system detection, OSError handling). Do NOT carry the `# FIXME: split adapter and application layer` comment (design D7).

## 2. Implement `init()` with argparse

- [x] 2.1 In `entrypoints/cli/init.py`, add imports: `argparse`, `sys`, `os` (for chmod), `from pathlib import Path`, `from pg8000 import DatabaseError`, `from yascheduler.config import Config`, `from yascheduler.infra.persistence import apply_schema`, `from yascheduler.shared import CONFIG_FILE`. Note the layer direction: `entrypoints → infra.persistence`, `entrypoints → config` (outside-layer-set), `entrypoints → shared` — all allowed. (`DatabaseError` is imported once here; task 4.1 references it for the `except` clause.)
- [x] 2.2 Implement `init(argv: list[str] | None = None) -> None` with a `START_CONTRACT: init` block (PURPOSE: Parse --schema/--daemon flags, install service and/or apply schema per the selected subset, exit 0/1/2; INPUTS: { argv: list[str] | None - optional argv for argparse, None reads sys.argv (console_script default) }; OUTPUTS: None - no return value, calls sys.exit; SIDE_EFFECTS: Writes service unit files, creates DB tables, calls sys.exit; LINKS: M-ENTRYPOINTS-CLI-INIT, M-PERSISTENCE-SCHEMA). Body: construct `argparse.ArgumentParser(prog="yainit", description="Install yascheduler service and initialize the database schema")`, add `--schema` (`action="store_true"`, help "Apply only the database schema"), add `--daemon` (`action="store_true"`, help "Install only the service unit file"). Do NOT add a `mutually_exclusive_group` (design D2: subset selectors, both = default). Add a `START_BLOCK_VALIDATE_FLAGS` / `END_BLOCK_VALIDATE_FLAGS` anchor around `args = parser.parse_args(argv)`. The `argv=None` default means the console_script entrypoint (which calls `init()` with no args) reads `sys.argv` — the standard argparse convention; tests pass an explicit list.
- [x] 2.3 After `parse_args()`, compute `install_path = Path(__file__).parent.parent.parent` (the 3-level walk to `yascheduler/`; design D8: both old and new paths are 3 levels below `yascheduler/`, so this is invariant). Wrap the rest of `init()` in a `try/except SystemExit: raise` / `except Exception as e: print(f"Error: {e}", file=sys.stderr); sys.exit(1)` block (design D3: any unexpected exception maps to exit 1) with a `START_BLOCK_HANDLE_FAILURE` / `END_BLOCK_HANDLE_FAILURE` anchor around the try/except. Let `SystemExit` (from argparse exit 2, and from explicit `sys.exit` calls) propagate unchanged.
- [x] 2.4 Implement the dispatch: `run_daemon = not args.schema or args.daemon` and `run_schema = not args.daemon or args.schema` (so no flags = both True; either flag = only that one True; both flags = both True = default — design D2). Add a `START_BLOCK_DISPATCH` / `END_BLOCK_DISPATCH` anchor around this logic. Then `if run_daemon: ...` call service install, `if run_schema: ...` call `_init_schema()`. End with `sys.exit(0)` on the success path.

## 3. Implement service install (`_init_systemd`, `_init_sysv`)

- [x] 3.1 Implement systemd detection inside `init()` (or a small helper called from the `run_daemon` branch): `has_systemd = Path("/run/systemd/system").is_dir()` (design D5; replaces `not os.system("pidof systemd")`). Add a `START_BLOCK_DETECT_INIT_SYSTEM` / `END_BLOCK_DETECT_INIT_SYSTEM` anchor. Branch to `_init_systemd(install_path)` or `_init_sysv(install_path)`.
- [x] 3.2 Implement `_init_systemd(install_path: Path, unit_file: Path = Path("/etc/systemd/system/yascheduler.service")) -> None` with a `START_CONTRACT: _init_systemd` block (PURPOSE: Render and write the systemd unit file, overwriting if it exists; INPUTS: { install_path: Path - yascheduler/ package root, unit_file: Path - target unit path (default /etc/systemd/system/yascheduler.service, injectable for tests) }; OUTPUTS: None; SIDE_EFFECTS: Writes the unit file; raises SystemExit(1) on OSError; LINKS: M-ENTRYPOINTS-CLI-INIT). Body: `print("Installing systemd service")`; `src_unit_file = install_path / "data/yascheduler.service"`; `daemon_file = install_path / "entrypoints/daemon/daemon_systemd.py"`; `systemd_script = src_unit_file.read_text("utf-8").replace("%YASCHEDULER_DAEMON_FILE%", str(daemon_file))`. Then wrap the write in `try: unit_file.write_text(systemd_script, "utf-8") except OSError as e: print(f"Error: cannot write to {unit_file}: {e}"); sys.exit(1)` (design D4: overwrite-if-exists — no `if not unit_file.is_file()` guard; OSError → exit 1). The `unit_file` parameter has a production-default but is injectable so tests can pass a `tmp_path` (task 9.11/9.12/9.13).
- [x] 3.3 Implement `_init_sysv(install_path: Path, startup_file: Path = Path("/etc/init.d/yascheduler")) -> None` with a `START_CONTRACT: _init_sysv` block (mirrors `_init_systemd`; `startup_file` injectable). Body: `print("Installing SysV service")`; `src_startup_file = install_path / "data/yascheduler.sh"`; `daemon_file = install_path / "entrypoints/daemon/daemon_sysv.py"`; `sysv_script = src_startup_file.read_text("utf-8").replace("%YASCHEDULER_DAEMON_FILE%", str(daemon_file))`. Wrap the write + chmod in `try: startup_file.write_text(sysv_script, "utf-8"); os.chmod(startup_file, 0o755) except OSError as e: print(f"Error: cannot write to {startup_file}: {e}"); sys.exit(1)` (design D4: overwrite + chmod 0755 on every write; OSError → exit 1).

## 4. Implement schema apply (`_init_schema`)

- [x] 4.1 Implement `_init_schema() -> None` with a `START_CONTRACT: _init_schema` block (PURPOSE: Apply schema.sql via apply_schema adapter; INPUTS: None - reads config from CONFIG_FILE; OUTPUTS: None; SIDE_EFFECTS: Creates DB tables; raises SystemError on DatabaseError; LINKS: M-PERSISTENCE-SCHEMA). Body: `config = Config.from_config_parser(CONFIG_FILE)`; `apply_schema(config.db)`. Wrap in `try: ... except DatabaseError as e: print(f"Error: {e}", file=sys.stderr); sys.exit(1)` (design D6: rename from `_init_db`; the `DatabaseError` handler makes the exit-1 contract explicit at the call site — import `DatabaseError` from `pg8000` at the top of the file alongside the other imports). Do NOT add any "already exists" special-case (schema.sql is idempotent via IF NOT EXISTS; design D6 / brief schema-apply behavior). Add a `START_BLOCK_APPLY_SCHEMA` / `END_BLOCK_APPLY_SCHEMA` anchor around the `apply_schema(config.db)` call.

## 5. Remove the old module and update `infra/cli/`

- [x] 5.1 Delete `yascheduler/infra/cli/init.py`.
- [x] 5.2 Edit `yascheduler/infra/cli/__init__.py`: remove the `from .init import init` line, remove `"init"` from the `__all__` list, and update `MODULE_MAP` to drop the `init - Re-exported from .init` line. Update `CHANGE_SUMMARY` LAST_CHANGE: `v2.2.0 - Drop init re-export; init moved to entrypoints/cli/init.py in relocate-init-command. Bump VERSION to 2.2.0.` Do NOT add any compat shim or re-export of `init` from the new location (design D1: any infra→entrypoints re-export would invert the layer direction).

## 6. Update `pyproject.toml`

- [x] 6.1 In `pyproject.toml` `[project.scripts]`, change the `yainit` entry from `yainit = "yascheduler.infra.cli.init:init"` to `yainit = "yascheduler.entrypoints.cli.init:init"` (the line containing `yainit = "yascheduler.infra.cli.init:init"`). Do NOT touch `[tool.importlinter]` (layers contract stays green; entrypoints→infra is the allowed direction) or `[tool.setuptools.package-data]` (schema.sql stays in infra/persistence/sql/; service templates stay in yascheduler/data/) — both unchanged per design.

## 7. Specs

- [x] 7.1 Edit `openspec/specs/cli-commands/spec.md`: replace the existing `### Requirement: CLI commands call use cases via DI` block (the block starting with that header) with the MODIFIED version from `openspec/changes/relocate-init-command/specs/cli-commands/spec.md` (softens "init is an exception" to "init is a bootstrap entrypoint"; adds the `yainit is a bootstrap entrypoint without DI` scenario).
- [x] 7.2 Edit `openspec/specs/cli-commands/spec.md`: replace the existing `### Requirement: yainit uses apply_schema adapter` block with the MODIFIED version (expanded description covering service install + detection + overwrite; 13 scenarios: no-flags, --schema, --daemon, both, --help, --bogus→exit 2, idempotent, DatabaseError→exit 1, write fail→exit 1, overwrite systemd, overwrite sysv, systemd detect, non-systemd detect).
- [x] 7.3 Edit `openspec/specs/cli-commands/spec.md`: replace the existing `### Requirement: Entry points updated` block with the MODIFIED version (yainit resolves to `yascheduler.entrypoints.cli.init:init`; the other 5 commands unchanged).
- [x] 7.4 Edit `openspec/specs/package-facades/spec.md`: replace the existing `### Requirement: Within-package relative imports (R1)` block (including the `infra/cli/__init__.py uses relative imports` scenario) with the MODIFIED version (drops `init` from the infra/cli submodule list; adds "does NOT import init" clause; adds the new `entrypoints/cli/__init__.py uses relative imports` scenario). The other three scenarios in that requirement stay.

## 8. Knowledge graph

- [x] 8.1 Edit `docs/knowledge-graph.xml`: in the `M-CLI-COMMANDS` module element, delete the `<fn-init PURPOSE="Service initialization (infra/cli/init.py)" />` annotation. The other 5 `<fn-*>` annotations (`fn-submit`, `fn-check_status`, `fn-show_nodes`, `fn-manage_node`, `fn-daemonize`) stay.
- [x] 8.2 Edit `docs/knowledge-graph.xml`: delete the `<CrossLink from="M-CLI-COMMANDS" to="M-PERSISTENCE-SCHEMA" relation="init command calls apply_schema for DB schema initialization" />` — init no longer lives in `M-CLI-COMMANDS`, so this edge is stale.
- [x] 8.3 Add a new module node `M-ENTRYPOINTS-CLI-INIT` to `docs/knowledge-graph.xml` (place it near `M-DAEMON-SYSTEMD` / `M-DAEMON-SYSV`, mirroring their shape): `<M-ENTRYPOINTS-CLI-INIT NAME="yainit CLI entry point" TYPE="ENTRY_POINT" STATUS="implemented">` with `<purpose>Bootstrap CLI command — install service unit files and/or apply DB schema with --schema/--daemon subset flags.</purpose>`, `<path>yascheduler/entrypoints/cli/init.py</path>`, `<depends>M-PERSISTENCE-SCHEMA, M-CONFIG, M-SHARED</depends>`, `<annotations>` with `<fn-init PURPOSE="Service + schema initialization (--schema/--daemon subset selectors)" />`, `<fn-_init_systemd PURPOSE="Write systemd unit file (overwrite if exists)" />`, `<fn-_init_sysv PURPOSE="Write SysV init script (overwrite + chmod 0755)" />`, `<fn-_init_schema PURPOSE="Apply schema.sql via apply_schema adapter" />`.
- [x] 8.4 Add a new CrossLink to `docs/knowledge-graph.xml`: `<CrossLink from="M-ENTRYPOINTS-CLI-INIT" to="M-PERSISTENCE-SCHEMA" relation="calls apply_schema for DB schema initialization" />` (replaces the deleted `M-CLI-COMMANDS → M-PERSISTENCE-SCHEMA` edge with the correct originating module).

## 9. Tests

- [x] 9.1 Delete the `test_init_function_exists` method from `tests/unit/test_cli_smoke.py` (the method that imports `from yascheduler.infra.cli import init` and checks `_check_sync_function(init)`) — it only checks `init` exists and is sync, which is low-value smoke coverage replaced by real unit tests. Do NOT delete the other 5 command smoke tests.
- [x] 9.2 Create `tests/unit/test_cli_init.py` with a GRACE-lite `MODULE_CONTRACT` (PURPOSE: Unit tests for yainit init() flag parsing, dispatch, exit codes, and service overwrite behavior; SCOPE: init() and its helpers with mocked apply_schema + filesystem; DEPENDS: M-ENTRYPOINTS-CLI-INIT; LINKS: M-ENTRYPOINTS-CLI-INIT). Mark the file with `# pytestmark = pytest.mark.unit` (or equivalent, matching the project's unit-test marker convention from `openspec/specs/testing-unit`).
- [x] 9.3 Add `test_no_flags_runs_both` — mock `apply_schema` and `_init_systemd`/`_init_sysv` (patch them at `yascheduler.entrypoints.cli.init._init_systemd` etc.), invoke `init([])` (empty argv, no flags), assert both the service-install path and `apply_schema` were called, assert exit code 0 (use `pytest.raises(SystemExit) as exc` and check `exc.value.code == 0`).
- [x] 9.4 Add `test_schema_only_skips_service` — invoke `init(["--schema"])`, assert `_init_systemd`/`_init_sysv` NOT called, assert `apply_schema` called once, exit 0.
- [x] 9.5 Add `test_daemon_only_skips_schema` — invoke `init(["--daemon"])` with `/run/systemd/system` mocked to exist (or mock `Path.is_dir`), assert service install called, `apply_schema` NOT called, exit 0.
- [x] 9.6 Add `test_both_flags_runs_both` — invoke `init(["--schema", "--daemon"])`, assert both paths called, exit 0 (equivalent to no-flags default).
- [x] 9.7 Add `test_help_exits_zero` — invoke `init(["--help"])`, assert `pytest.raises(SystemExit) as exc` with `exc.value.code == 0`, and capture stdout to assert the help text mentions `--schema` and `--daemon`.
- [x] 9.8 Add `test_unknown_flag_exits_two` — invoke `init(["--bogus"])`, assert `SystemExit` code == 2, capture stderr to assert an argparse usage error is printed.
- [x] 9.9 Add `test_database_error_exits_one` — mock `apply_schema` to raise `DatabaseError` (import from `pg8000`), invoke `init(["--schema"])`, assert `SystemExit` code == 1, assert stderr contains the error message.
- [x] 9.10 Add `test_service_write_oserror_exits_one` — patch the service-file `write_text` to raise `OSError("Permission denied")`, invoke `init(["--daemon"])`, assert `SystemExit` code == 1, assert stderr/stdout contains `Error: cannot write to`.
- [x] 9.11 Add `test_overwrites_existing_systemd_unit` — create a `tmp_path / "yascheduler.service"` with stale content, call `_init_systemd(install_path, unit_file=tmp_path / "yascheduler.service")` directly (using the injectable `unit_file` param from task 3.2 — no monkeypatching of `/etc/...` needed), assert the file content was replaced with the freshly rendered template (overwrite, not skip). Verify the rendered content contains the substituted `%YASCHEDULER_DAEMON_FILE%` path.
- [x] 9.12 Add `test_overwrites_existing_sysv_script` — create a `tmp_path / "yascheduler"` with stale content, call `_init_sysv(install_path, startup_file=tmp_path / "yascheduler")` directly (injectable `startup_file` param from task 3.3), assert overwrite + `chmod 0755` preserved (check `stat.S_IMODE(os.stat(path).st_mode) == 0o755`).
- [x] 9.13 Add `test_missing_parent_dir_exits_one` — call `_init_systemd(install_path, unit_file=tmp_path / "nonexistent_dir" / "yascheduler.service")` (parent dir does not exist), assert `pytest.raises(SystemExit) as exc` with `exc.value.code == 1` (the `write_text` raises `OSError` / `FileNotFoundError` which is caught and mapped to exit 1). Verify the error message contains `Error: cannot write to`.
- [x] 9.14 Add `test_systemd_detection_via_run_dir` — patch `Path("/run/systemd/system").is_dir` to return True (e.g. via `monkeypatch.setattr` on the `Path` class or a dedicated check function), invoke `init(["--daemon"])`, assert `_init_systemd` called (not `_init_sysv`). Add a companion `test_non_systemd_detection` with `is_dir` returning False, assert `_init_sysv` called.
- [x] 9.15 Add `test_schema_idempotent_rerun` — mock `apply_schema` to succeed (no exception) on both calls, invoke `init(["--schema"])` twice against the same mocked `apply_schema`; assert both invocations exit 0 and `apply_schema` was called twice (covers the spec's "yainit initializes database idempotently" scenario — schema.sql is idempotent via `IF NOT EXISTS`, so re-applying is a no-op success, not an error).

## 10. Verification

- [x] 10.1 Run `uv run pytest -m unit` — all unit tests pass, including the new `tests/unit/test_cli_init.py` (14 tests: 9.3-9.13 single tests, 9.14 with two detection sub-tests, 9.15 idempotent re-run) and the updated `tests/unit/test_cli_smoke.py` (5 remaining command smoke tests).
- [x] 10.2 Run `uv run pytest -m integration` — integration tests pass; `tests/integration/conftest.py` calls `apply_schema` directly (not via `init`), so it is unaffected; verify no import-path breakage.
- [x] 10.3 Run `uv run lint-imports` — the `import-linter` `layers` contract passes: `yascheduler.entrypoints.cli.init` imports from `yascheduler.infra.persistence` (entrypoints→infra, allowed), `yascheduler.config` (outside-layer-set, allowed), `yascheduler.shared` (entrypoints→shared, allowed). No `ignore_imports` entry needed.
- [x] 10.4 Run `uv run zuban check` — no type errors in the new file.
- [x] 10.5 Run `uv run ruff check .` and `uv run ruff format --check .` — no lint/format violations in the new file.
- [x] 10.6 Run `python3 scripts/grace_check.py` — GRACE-lite validation passes: the new `entrypoints/cli/init.py` and `entrypoints/cli/__init__.py` carry valid `MODULE_CONTRACT`/`MODULE_MAP`/`CHANGE_SUMMARY`/function contracts/block anchors; `infra/cli/__init__.py` updated; `docs/knowledge-graph.xml` well-formed with the new `M-ENTRYPOINTS-CLI-INIT` node and the updated `M-CLI-COMMANDS` annotations + CrossLinks.
- [x] 10.7 Run `openspec validate --all --json` — spec validation passes; the delta specs at `openspec/changes/relocate-init-command/specs/*/spec.md` merge cleanly against the updated main specs at `openspec/specs/cli-commands/spec.md` and `openspec/specs/package-facades/spec.md` (headers match exactly).
- [x] 10.8 Manual smoke: install the package (`uv sync` or `pip install -e .`), run `yainit --help` — confirm the help screen lists `--schema` and `--daemon` with descriptions and exits 0. Run `yainit --bogus` — confirm exit code 2 with a stderr usage error.