## 1. events.py — add the missing CLASS_* regions and absorb construction-site detail

- [x] 1.1 Extend the existing `MODULE_CONTRACT` region in `yascheduler/domain/events.py` with an expanded `INVARIANTS` block absorbing the construction-site flow that leaves the spec: events are constructed only inside `Task` transition methods (`run`, `reject`, `complete`, `fail`, `abandon`) and inside `materialize_task` (for `TaskCreated`); `task_id` is passed as `self.task_id` (a `TaskId`) — no `.value` extraction at construction; `.value` is unwrapped only at external serialization boundaries (e.g. webhook payload construction). Keep `PURPOSE`/`SCOPE`/`KEYWORDS` as-is; do not touch the `NOT:` lines.
- [x] 1.2 Add `# region CLASS_DomainEvent` ... `# endregion CLASS_DomainEvent` enclosing the FULL `@dataclass(frozen=True)` block (the decorator line through the `webhook_custom_params` field and the trailing blank line) — region opens one line above the decorator and closes one line below the class body. `PURPOSE`: why the base exists (anchor the per-transition payload the UoW dispatches so handlers receive an immutable record carrying everything they need without re-querying aggregates). `INVARIANTS`: frozen; `task_id` is always a `TaskId`; `webhook_url` is `None` when no outbound delivery is configured for the originating task.
- [x] 1.3 Add `# region CLASS_TaskCreated` ... `# endregion CLASS_TaskCreated` enclosing the full dataclass block. `PURPOSE`: record that a task entered the system so the webhook handler can notify external systems of a new TO_DO task.
- [x] 1.4 Add `# region CLASS_TaskAllocated` ... `# endregion CLASS_TaskAllocated` enclosing the full dataclass block. `PURPOSE`: record that a TO_DO task was bound to a node and entered RUNNING so the webhook handler can notify external systems of the allocation.
- [x] 1.5 Add `# region CLASS_TaskCompleted` ... `# endregion CLASS_TaskCompleted` enclosing the full dataclass block. `PURPOSE`: record that a RUNNING task completed successfully so the webhook handler can notify external systems of the DONE outcome. `RATIONALE` Q/A: "Q: Why no `has_errors` field? A: Failure paths emit `TaskFailed` instead; `TaskCompleted` is the success-only signal, so a separate error flag would duplicate the event-type distinction."
- [x] 1.6 Add `# region CLASS_TaskFailed` ... `# endregion CLASS_TaskFailed` enclosing the full dataclass block. `PURPOSE`: record that a task ended in failure (rejected before running OR failed during run) so the webhook handler can notify external systems of the loss.
- [x] 1.7 Add `# region CLASS_TaskAbandoned` ... `# endregion CLASS_TaskAbandoned` enclosing the full dataclass block. `PURPOSE`: record that a RUNNING task was abandoned because its node disappeared so the webhook handler can notify external systems. `RATIONALE` Q/A: "Q: Why is `node_id` typed `NodeId` and not `NodeId | None`? A: The event is only constructed when `task.abandon(node_id)` is called with a non-`None` `node_id`; the double-abandon edge (node already deleted) emits no event, so a `None` `node_id` is unreachable here."
- [x] 1.8 Verify every `CLASS_*` region encloses ALL content of its target: the `@dataclass(frozen=True)` decorator line, the `class` line, the docstring, every field — through the trailing blank line before the next region marker. No region closes before the class body ends. No invented contract field names appear — only `PURPOSE` / `INVARIANTS` / `RATIONALE` (each per its defined meaning).
- [x] 1.9 Verify outcome: `uv run ruff check yascheduler/domain/events.py` and `uv run ruff format --check yascheduler/domain/events.py` pass; `uv run pytest -m unit tests/unit/test_domain_events.py` is green (no behavior changed).

## 2. model.py — extend CLASS_Task with the "no event primitives" rationale

- [x] 2.1 Extend the existing `CLASS_Task` region in `yascheduler/domain/model.py` by adding a new RATIONALE Q/A absorbing the "no `with_event`/`record_event`/`pull_events` primitives" rationale that leaves the spec: "Q: Why are there no `record_event`/`with_event`/`pull_events` primitives on `Task`? A: Event construction lives inside the transition methods so the source-status check and the event payload stay atomically bound — splitting them would let a caller record an event without performing (or after performing) the transition, breaking the invariant that events reflect actual state changes." Do NOT remove or alter the existing `allocated_node_id` RATIONALE Q/A.
- [x] 2.2 Verify outcome: `uv run ruff check yascheduler/domain/model.py` and `uv run ruff format --check yascheduler/domain/model.py` pass; `uv run pytest -m unit tests/unit/test_domain_model.py tests/unit/test_application_use_cases.py` is green.

## 3. uow.py — wrap AbstractUnitOfWork in a CLASS region; mark collect/publish methods

- [x] 3.1 Add `# region CLASS_AbstractUnitOfWork` ... `# endregion CLASS_AbstractUnitOfWork` enclosing the entire `@runtime_checkable class AbstractUnitOfWork(Protocol):` block (decorator line through `publish_events`'s `...` body and trailing blank line). `PURPOSE`: state the contract use cases depend on — transactional access to task/node repositories plus post-commit event dispatch — so use cases stay decoupled from any persistence implementation. `INVARIANTS`: Protocol only (no runtime implementation); `commit()` is the sole dispatch trigger; `rollback()` discards any collected events.
- [x] 3.2 Add `# region METHOD_collect_events` ... `# endregion METHOD_collect_events` wrapping the `async def collect_events(self) -> list[DomainEvent]:` stub. `PURPOSE`: declare that implementers pull domain events from saved aggregates so they can be dispatched after commit.
- [x] 3.3 Add `# region METHOD_publish_events` ... `# endregion METHOD_publish_events` wrapping the `async def publish_events(self) -> None:` stub. `PURPOSE`: declare that implementers collect and dispatch events via the message bus, called from `commit()` after the DB transaction succeeds.
- [x] 3.4 Verify the CLASS region encloses the entire Protocol body (all properties, dunder methods, and the four regular methods); the two new METHOD regions sit inside the CLASS region. No invented fields; only `PURPOSE` / `INVARIANTS`.
- [x] 3.5 Verify outcome: `uv run ruff check yascheduler/application/uow.py` and `uv run ruff format --check yascheduler/application/uow.py` pass; `uv run pytest -m unit tests/unit/test_persistence_adapter.py` is green (the Protocol is structurally typed — its implementer `PostgresUnitOfWork` carries the behavior tests, including `test_collect_events_preserves_shared_list`).

## 4. message_bus.py — add the functools.partial binding rationale to METHOD_register

- [x] 4.1 Extend the existing `METHOD_register` region in `yascheduler/application/message_bus.py` with a `RATIONALE` Q/A absorbing the functools.partial usage hint that leaves the spec: "Q: How do handlers obtain their non-event dependencies (http session, repositories)? A: Register a `functools.partial` that pre-binds the extra arguments; `dispatch()` calls the resulting callable with only the event, so the registry signature stays single-arg." Do NOT change `METHOD_dispatch`.
- [x] 4.2 Verify outcome: `uv run ruff check yascheduler/application/message_bus.py` and `uv run ruff format --check yascheduler/application/message_bus.py` pass; `uv run pytest -m unit tests/unit/test_message_bus.py` is green.

## 5. postgres_uow.py — extend METHOD_collect_events with the no-pull_events rationale

- [x] 5.1 Extend the existing `METHOD_collect_events` region in `yascheduler/infra/persistence/postgres_uow.py` with a `RATIONALE` Q/A absorbing the "SHALL NOT call `pull_events()` (no such method exists)" line that leaves the spec: "Q: Why does `collect_events` read `task.events` directly instead of calling a `pull_events()` method? A: The `events` field is public (`repr=True`) and immutable; the UoW reads it directly and clears its own save-tracking, so a `pull_events()` method on `Task` would duplicate the read with no mutation benefit (the aggregate is frozen — clearing happens on the UoW side, not the aggregate)." Keep the existing `PURPOSE`.
- [x] 5.2 Verify outcome: `uv run ruff check yascheduler/infra/persistence/postgres_uow.py` and `uv run ruff format --check yascheduler/infra/persistence/postgres_uow.py` pass; `uv run pytest -m unit tests/unit/test_persistence_adapter.py` is green (covers `PostgresUnitOfWork.collect_events` via `test_collect_events_preserves_shared_list`).

## 6. webhook.py — wrap WebhookPayload; enrich webhook_handler with ENSURES + RATIONALE

- [x] 6.1 Add `# region CLASS_WebhookPayload` ... `# endregion CLASS_WebhookPayload` enclosing the full `@dataclass(frozen=True) class WebhookPayload:` block (decorator line through the `custom_params` field and trailing blank line). `PURPOSE`: fix the outbound wire shape `{"task_id": int, "status": int, "custom_params": ...}` so `dataclasses.asdict` serializes a flat JSON-ready dict. `INVARIANTS`: frozen; `task_id` is a bare `int` (the `TaskId.value`), never a `TaskId` instance; `custom_params` defaults to an empty mapping.
- [x] 6.2 Extend the existing `FUNC_webhook_handler` region with `ENSURES` and `RATIONALE` fields absorbing the construction-site detail that leaves the spec. `ENSURES`: when `webhook_url is None`, returns without an HTTP request; otherwise builds `WebhookPayload(task_id=event.task_id.value, status=<status>.value, custom_params=event.webhook_custom_params)` (does NOT read `node_id`); exceptions from `_send_webhook` are caught and logged, never re-raised. `RATIONALE` Q/A: "Q: Why is `.value` extracted at the `WebhookPayload` construction site instead of passing `event.task_id`? A: `dataclasses.asdict` recurses into nested dataclasses, so passing `task_id=event.task_id` (a `TaskId`) would serialize to `{\"task_id\": {\"value\": 42}, ...}` instead of the required `{\"task_id\": 42, ...}`. `WebhookPayload.task_id` is therefore typed `int`."
- [x] 6.3 Verify the CLASS region encloses the full dataclass (decorator + class + docstring + all 3 fields); the FUNC region still encloses the entire `webhook_handler` body. No invented fields; `ENSURES` and `RATIONALE` used per their defined meaning.
- [x] 6.4 Verify outcome: `uv run ruff check yascheduler/infra/notifier/webhook.py` and `uv run ruff format --check yascheduler/infra/notifier/webhook.py` pass; `uv run pytest -m unit tests/unit/test_webhook_handler.py` is green.

## 7. Apply the spec delta and verify end-to-end

- [x] 7.1 Apply the 7 MODIFIED requirements from `openspec/changes/domain-events-spec-trim/specs/domain-events-and-dispatch/spec.md` to `openspec/specs/domain-events-and-dispatch/spec.md`, replacing each original requirement block in place. Preserve requirement header text exactly (whitespace-insensitive match) so OpenSpec recognizes the MODIFIED operation.
- [x] 7.2 Confirm the trimmed main spec contains no implementation prose: no construction-site `.value` flow; no "no `with_event` or `record_event` primitives exist"; no "no use case constructs `DomainEvent` subclasses directly or calls `record_event`/`with_event`"; no "SHALL NOT call a `pull_events()` method (no such method exists)"; no `webhook_handler` line-level "builds WebhookPayload(...)" narration; no `dataclasses.asdict` recursion explanation. Every observable behavioral scenario (`#### Scenario:` count) MUST be present in the trimmed spec — pre/post counts compared.
- [x] 7.3 Run `openspec validate --all --json` — passes (exit 0). The change validates AND the main spec validates.
- [x] 7.4 Run `uv run pytest -m unit` — all unit tests pass (no behavior changed across events, message_bus, webhook, uow, postgres_uow, model, and use-case tests).
- [x] 7.5 Run `uv run ruff check .` and `uv run ruff format --check .` — pass on all changed files.
- [x] 7.6 Run `uv run lint-imports` — passes (no new imports introduced; markup-only edits).
- [x] 7.7 Confirm no public-surface change: no CLI, public API, INI config, DB schema, or log-format change in the diff. The diff is `# region`/`# endregion` markup + comment-field enrichment + spec text trim only.
