## 1. Domain types — `TaskId`, `NewTask`, `Task`

- [x] 1.1 In `yascheduler/domain/model.py`, add `TaskId` as `@dataclass(frozen=True)` with field `value: int`; `__post_init__` that raises `ValueError` when `value <= 0`; `__str__` returning `str(self.value)`. Add GRACE `START_CONTRACT: TaskId` block (PURPOSE/INPUTS/OUTPUTS/SIDE_EFFECTS/LINKS). Place it before the `Task` dataclass. Verify: `TaskId(5)` ok; `TaskId(0)` and `TaskId(-1)` raise `ValueError`; `str(TaskId(5)) == "5"`; `TaskId(5) == 5` is `False`; `hash(TaskId(5))` works; `TaskId(5)` is usable as a dict key.
- [x] 1.2 In `yascheduler/domain/model.py`, add `NewTask` as `@dataclass(frozen=True)` with fields `label: str`, `context: TaskContext`, `status: TaskStatus = TaskStatus.TO_DO`, `allocated_ip: str | None = None` (mirrors `Task`'s non-`task_id`/non-`_events` fields with identical defaults; NO `task_id`, NO `_events`, NO lifecycle methods). Add GRACE `START_CONTRACT: NewTask` (PURPOSE: pre-persistence task record, pure data carrier). Verify: `NewTask(label="x", context=ctx)` constructs with defaults; no `task_id` attribute; no `_events` attribute; no `allocate_to`/`mark_running`/etc. methods.
- [x] 1.3 In `yascheduler/domain/model.py`, change the existing `Task` dataclass: replace `task_id: int` with `task_id: TaskId` as the FIRST field (before `label`); keep `label: str`, `context: TaskContext`, `status: TaskStatus = TaskStatus.TO_DO`, `allocated_ip: str | None = None`, `_events: tuple[DomainEvent, ...] = field(default=(), repr=False)`. Field order is dataclass-valid (`task_id`, `label`, `context` have no defaults; the rest follow with defaults). Update the `START_CONTRACT: Task` block to note `task_id: TaskId` (post-persistence, always carries identity). Verify: `Task(task_id=TaskId(1), label="x", context=ctx)` constructs; `task.task_id` is a `TaskId`; `Task(task_id=0, ...)` raises (`TaskId(0)` raises in `__post_init__`); existing lifecycle methods unchanged in behavior.
- [x] 1.4 In `Task.with_event` (model.py:345), the `task_id=self.task_id` construction already passes a `TaskId` after 1.3 — no `.value` extraction needed. Confirm the `START_BLOCK_DROP_BASE_FIELDS`/`START_BLOCK_CONSTRUCT_AND_RECORD` blocks still work (they pass `self.task_id` to the event constructor). Verify: `task.with_event(TaskCreated, engine_name=ctx.engine)` produces an event with `event.task_id == task.task_id` (a `TaskId`).
- [x] 1.5 Export `NewTask` and `TaskId` from `yascheduler/domain/__init__.py` (and ensure they are importable from `yascheduler.domain.model`). Update the domain facade's re-export list and any `__all__`. Update `START_MODULE_MAP`/`START_CHANGE_SUMMARY` in `model.py` (LAST_CHANGE: add TaskId/NewTask, Task.task_id: int -> TaskId / add-task-id-identity). Verify: `from yascheduler.domain import NewTask, TaskId` and `from yascheduler.domain.model import NewTask, TaskId` both resolve.

## 2. Domain events — `task_id: TaskId`

- [x] 2.1 In `yascheduler/domain/events.py`, change `DomainEvent.task_id: int` → `task_id: TaskId` (line 33). Import `TaskId` from `.` (model) under `TYPE_CHECKING` if not at runtime — but since `TaskId` is a frozen dataclass used as a field annotation, it must be importable at runtime for dataclass field construction; import it normally (NOT under TYPE_CHECKING) — confirm whether `from __future__ import annotations` is present (it is, line 25), so annotations are strings and `TaskId` can be imported under `TYPE_CHECKING`. Decide: if `events.py` has `from __future__ import annotations`, import `TaskId` under `TYPE_CHECKING`; the 5 subclasses inherit the field. Verify: `TaskCreated(task_id=TaskId(42), ...)` constructs; `event.task_id` is a `TaskId`.
- [x] 2.2 Update the `START_MODULE_MAP`/`START_CHANGE_SUMMARY` in `events.py` (LAST_CHANGE: DomainEvent.task_id: int -> TaskId / add-task-id-identity; note the Python 3.9 compat — keep `typing.Union` for the `Event` alias, no PEP 604). Verify: module imports cleanly; `uv run pytest -m unit` for domain-events tests passes after updating literals (see task 9).

## 3. Domain exceptions — `task_id: TaskId`

- [x] 3.1 In `yascheduler/domain/exceptions.py`, change the `__init__(self, task_id: int)` → `__init__(self, task_id: TaskId)` for: `TaskAlreadyAllocatedError`, `TaskNotAllocatedError`, `TaskNotTodoError`, `TaskNotRunningError`, `NoCompatibleNodeError`, `CloudCapacityExhaustedError`. NOTE: this file does NOT have `from __future__ import annotations`, so the `task_id: TaskId` annotation is runtime-evaluated — import `TaskId` at runtime with `from yascheduler.domain.model import TaskId` (NOT under `TYPE_CHECKING`, which would cause `NameError`). The `f"task {task_id} ..."` messages render via `TaskId.__str__` → bare integer (unchanged message text). Verify: `TaskAlreadyAllocatedError(TaskId(42))` constructs; `str(e)` contains `"42"` not `"TaskId(value=42)"`; `e.task_id == TaskId(42)`.
- [x] 3.2 In `yascheduler/infra/persistence/exceptions.py`, change `TaskRowNotFoundError.__init__(self, task_id: int)` → `task_id: TaskId` (line 39). NOTE: this file does NOT have `from __future__ import annotations`, so import `TaskId` at runtime (`from yascheduler.domain.model import TaskId`), NOT under `TYPE_CHECKING`. The `f"task row not found for task_id={task_id}"` message renders via `__str__`. Verify: `TaskRowNotFoundError(TaskId(42))` constructs; `str(e)` contains `"42"`.
- [x] 3.3 Update `START_CHANGE_SUMMARY` in both exception files. Verify: `uv run pytest -m unit` for domain-exceptions tests passes after updating literals (see task 9).

## 4. `TaskRepository` port

- [x] 4.1 In `yascheduler/domain/ports.py` `TaskRepository` Protocol (line 48): change `insert(task: Task) -> Task` → `insert(new_task: NewTask) -> Task`; `get(task_id: int) -> Task | None` → `get(task_id: TaskId) -> Task | None`; `update_status(task_id: int, status: TaskStatus) -> None` → `update_status(task_id: TaskId, status: TaskStatus) -> None`; `list_ids_by_ip_and_status(...) -> list[int]` → `-> list[TaskId]`; `list_by_jobs(job_ids: list[int]) -> list[Task]` → `list_by_jobs(job_ids: list[TaskId]) -> list[Task]`. Keep `save(task: Task)`, `list_by_status`, `count_by_status` unchanged. Import `NewTask`, `TaskId` (TYPE_CHECKING — the module has `from __future__ import annotations`). Update `START_MODULE_MAP`/`START_CHANGE_SUMMARY`. Verify: Protocol imports cleanly; `uv run pytest -m unit` for domain-ports tests passes.

## 5. `PostgresTaskRepository` impl

- [x] 5.1 In `yascheduler/infra/persistence/postgres.py`, change `insert(self, task: Task) -> Task` → `insert(self, new_task: NewTask) -> Task` (line 164). The body already runs `task/insert.sql ... RETURNING task_id` and returns `self._row_to_task(rows[0])` — keep it; `_row_to_task` (updated in 5.4) now wraps `TaskId`. The `new_task.task_id` no longer exists (NewTask has none); the DB generates the id. The `metadata`/`label`/`ip`/`status` come from `new_task.context`/`new_task.label`/`new_task.allocated_ip`/`new_task.status`. Update `START_CONTRACT: insert` INPUTS (`new_task: NewTask` not `task: Task`; task_id is absent, generated by DB). Verify: `insert(NewTask(label="x", context=ctx))` returns a `Task` carrying a `TaskId`.
- [x] 5.2 Change `get(self, task_id: int)` → `get(self, task_id: TaskId)` (line 90): pass `task_id=task_id.value` to `load_query("task/get_by_id")` (pg8000 can't adapt a dataclass). Update `START_CONTRACT: get` INPUTS. Verify: `get(TaskId(42))` passes `42` as the SQL param.
- [x] 5.3 Change `update_status(self, task_id: int, status)` → `update_status(self, task_id: TaskId, status)` (line 129): pass `task_id=task_id.value`. In `save(self, task)` (line 104): pass `task_id=task.task_id.value` to `update_by_id.sql` (was `task.task_id`). Update `START_CONTRACT: save`/`update_status` INPUTS. Verify: `update_status(TaskId(42), RUNNING)` passes `42`; `save(task)` passes `task.task_id.value`.
- [x] 5.4 Update `_row_to_task` (line 226): wrap `TaskId(int(row["task_id"]))` — `Task(task_id=TaskId(int(row["task_id"])), label=row.get("label", ""), context=ctx, status=TaskStatus(row["status"]), allocated_ip=row.get("ip") or None)`. Update `START_CONTRACT: _row_to_task` OUTPUTS (`Task` carrying `TaskId`). Verify: a row `{"task_id": 7, ...}` maps to `Task(task_id=TaskId(7), ...)`.
- [x] 5.5 Change `list_ids_by_ip_and_status` (line 148): return `[TaskId(int(row["task_id"])) for row in rows]` (was `[row["task_id"] for row in rows]`). Update `START_CONTRACT` OUTPUTS (`list[TaskId]`). Verify: returns `[TaskId(1), TaskId(2)]` not `[1, 2]`.
- [x] 5.6 Change `list_by_jobs(self, job_ids: list[int])` → `list_by_jobs(self, job_ids: list[TaskId])` (line 201): pass `[tid.value for tid in job_ids]` as the `task_ids` SQL param (was `job_ids` directly); return `[self._row_to_task(r) for r in rows]` (unchanged). Update `START_CONTRACT: list_by_jobs` INPUTS/OUTPUTS. Verify: `list_by_jobs([TaskId(1), TaskId(2)])` passes `[1, 2]` to SQL.
- [x] 5.7 Update `START_CHANGE_SUMMARY` in `postgres.py` (LAST_CHANGE: TaskRepository methods take/return TaskId; insert takes NewTask; _row_to_task wraps TaskId / add-task-id-identity). Verify: `uv run pytest -m unit` for persistence-adapter tests passes after updating test literals (see task 9).

## 6. Use cases

- [x] 6.1 In `yascheduler/application/submit_task.py`: change return type `int -> TaskId` (line 67); change the construction at line 81 from `Task(task_id=0, label=label, context=context, status=TaskStatus.TO_DO)` → `NewTask(label=label, context=context)` (status/allocated_ip default). The rest of the flow (`insert` → `with_context` → `with_event` → `save` → `return task.task_id`) now returns a `TaskId`. Update `START_CONTRACT: submit_task` OUTPUTS (`TaskId` not `int`). Update imports (`NewTask` from `yascheduler.domain`; drop `Task` if no longer used — it still is, for the `task` variable after insert; keep `Task` import). Update `START_CHANGE_SUMMARY`. Verify: `submit_task(...)` returns a `TaskId`; `str(TaskId)` renders the bare int for the CLI.
- [x] 6.2 In `yascheduler/application/allocate_task.py`: change the entry `task_id: int` → `task_id: TaskId` (line 467 and the contract INPUTS). Logging `"task_id=%s"` renders via `__str__` (no change). `uow.tasks.get(task_id)` now takes `TaskId`. `tracker.add`/`discard(task_id)` keys become `TaskId`. Update `START_CONTRACT: allocate_task` INPUTS. Update `START_CHANGE_SUMMARY`. Verify: the orchestrator calls `allocate_task(task.task_id, ...)` with a `TaskId`; `uv run pytest -m unit` for allocate_task tests passes after updating literals.
- [x] 6.3 In `yascheduler/application/consume_task.py`: change the entry `task_id: int` → `task_id: TaskId` (line 216 and the contract INPUTS). `uow.tasks.get(task_id)` takes `TaskId`. `download_outputs(..., task_id=task.task_id)` passes a `TaskId` — SSH ops uses it for logging/folder naming; `TaskId.__str__` handles rendering (no `.value` needed at this site). `tracker.discard(task.task_id)` (line 195) and `tracker.discard(task_id)` (line 230) keys become `TaskId`. Update `START_CONTRACT: consume_task` INPUTS. Update `START_CHANGE_SUMMARY`. Verify: `uv run pytest -m unit` for consume_task tests passes.
- [x] 6.4 In `yascheduler/application/query_tasks.py`: change `jobs: Sequence[int] | None` → `jobs: Sequence[TaskId] | None` (line 46/47 and the contract INPUTS). `list_by_jobs(list(jobs))` now takes `list[TaskId]`. Update `START_CONTRACT: query_tasks` INPUTS. Update `START_CHANGE_SUMMARY`. Verify: `uv run pytest -m unit` for query_tasks tests passes after updating literals.
- [x] 6.5 In `yascheduler/application/allocation_tracker.py`: change the internal `self._on_tasks: set[int]` → `set[TaskId]` (line 35); `add(self, task_id: int) -> bool` → `add(self, task_id: TaskId) -> bool` (line 44); `discard(self, task_id: int) -> None` → `discard(self, task_id: TaskId) -> None` (line 58); `__contains__(self, task_id: int) -> bool` → `__contains__(self, task_id: TaskId) -> bool` (line 61). NOTE: this file does NOT have `from __future__ import annotations`, so the annotations are runtime-evaluated — import `TaskId` at runtime (`from yascheduler.domain.model import TaskId`), NOT under `TYPE_CHECKING`. Update the `START_CONTRACT` INPUTS (`task_id: TaskId`) and `START_CHANGE_SUMMARY`. Verify: `tracker.add(TaskId(42))` works; `TaskId(42) in tracker` works; `hash(TaskId(42))` works (frozen dataclass).

## 7. Webhook handler — `.value` extraction

- [x] 7.1 In `yascheduler/infra/notifier/webhook.py`, change line 86 `task_id=event.task_id` → `task_id=event.task_id.value` (the `asdict`-recursion wire-shape fix; `WebhookPayload.task_id: int` at line 48 stays `int`). Update `START_CONTRACT: webhook_handler` INPUTS note (event.task_id is now a TaskId; extract .value at the WebhookPayload construction boundary). Update `START_CHANGE_SUMMARY`. Verify: `webhook_handler(TaskCreated(task_id=TaskId(42), ...), http)` builds `WebhookPayload(task_id=42, ...)`; `asdict(payload)` produces `{"task_id": 42, ...}` NOT `{"task_id": {"value": 42}, ...}`. (The handler is wired at `entrypoints/di.py:120` for all 5 event types — no change there.)

## 8. Public `Yascheduler` facade — sole int/TaskId boundary

- [x] 8.1 In `yascheduler/entrypoints/client.py` `_task_to_dict(t: Task)` (line 89): change `"task_id": t.task_id` → `"task_id": t.task_id.value` (the public dict keeps `int`). Update `START_CONTRACT: _task_to_dict` OUTPUTS note. Verify: the returned dict has `"task_id": <int>`.
- [x] 8.2 In `queue_submit_task` (and any sync/async variant): the return is `int`; wrap `submit_task` (now returns `TaskId`) with `.value` — `return (await submit_task(...)).value` (or the sync equivalent via `to_sync`). Verify: `queue_submit_task(...) -> int` returns a bare `int`.
- [x] 8.3 In `queue_get_task(task_id: int)` / `queue_get_task_async(task_id: int)` (lines ~207/212/224): keep the public signature `int`; internally wrap `TaskId(task_id)` before calling `uow.tasks.get(TaskId(...))` (or `list_by_jobs([TaskId(task_id)])`). Verify: `queue_get_task(42)` works; the public signature stays `int`.
- [x] 8.4 In `queue_get_tasks(jobs, status)` / `queue_get_tasks_async(jobs, status)`: keep `jobs: list[int]` public; internally wrap `[TaskId(i) for i in jobs]` before calling `query_tasks(jobs=[TaskId(...)], ...)`. Verify: `queue_get_tasks_async(jobs=[1,2,3])` works; public signature stays `list[int]`.
- [x] 8.5 Update `START_CHANGE_SUMMARY` in `client.py`. Verify: `uv run pytest -m unit` for client/facade tests passes; the public contract (6-key dict, `task_id` is `int`) is preserved.

## 9. CLIs

- [x] 9.1 `yasubmit` (`yascheduler/entrypoints/cli/submit.py`): line 173 `print(str(task_id))` — `task_id` is now the `TaskId` returned by `submit_task` (via DI/CLIDeps); `__str__` renders the bare integer. NO change to the print line. Verify: `yasubmit` prints the bare integer.
- [x] 9.2 `yastatus` (`yascheduler/entrypoints/cli/check_status.py`): in `_render_json` (line 181), change `"task_id": task.task_id` → `"task_id": task.task_id.value` (REQUIRED — `json.dumps` would raise `TypeError` on a `TaskId`). In `_render_default` (line 145, f-string) and `_render_info` (line 159, `.format`) — NO change (render via `__str__`). In `_query_tasks` (line 127), change `await uow.tasks.list_by_jobs(job_ids=args.jobs)` → wrap `[TaskId(j) for j in args.jobs]` (CLI-internal int→TaskId wrap, same pattern as the facade). Update `START_CHANGE_SUMMARY`. Verify: `yastatus --json` emits `"task_id": <int>` without `TypeError`; `yastatus` (text) renders the bare int; `uv run pytest -m unit` for check_status passes.
- [x] 9.3 `yanodes` (`yascheduler/entrypoints/cli/show_nodes.py`): narrow `_NodeView.task_id: int | None` → `task_id: TaskId | None` (line 56); `_fetch_nodes_view` keeps `task_id=task.task_id if task else None` (line 152, now carries a `TaskId`); table renderer `str(row.task_id)` (line 205) unchanged via `__str__`; JSON renderer (line 241) `{"task_id": r.task_id, ...}` → `{"task_id": r.task_id.value, ...}` (REQUIRED — `json.dumps`). Import `TaskId` from `yascheduler.domain`. Update `START_CHANGE_SUMMARY`. Verify: `yanodes --json` emits `"task_id": <int>`; `yanodes` (table) renders the bare int; `uv run pytest -m unit` for show_nodes passes.

## 10. Tests

- [x] 10.1 Unit: `tests/unit/test_domain_exceptions.py` — update exception-construction scenarios from `task_id=42` (int) → `TaskId(42)`; assert `e.task_id == TaskId(42)`; assert `str(e)` contains `"42"`. Add `TaskRowNotFoundError(TaskId(42))` test. Verify: `uv run pytest -m unit` passes.
- [x] 10.2 Unit: `tests/unit/test_domain_events.py` — update event-construction scenarios from `task_id=42` → `TaskId(42)`; assert `event.task_id == TaskId(42)`. Verify: `uv run pytest -m unit` passes.
- [x] 10.3 Unit: add/extend a domain-model test for `TaskId` (value-object behavior: `__post_init__` rejects ≤0; `__str__` renders bare int; `== int` is False; hashable) and `NewTask` (no `task_id`/`_events` attributes; no lifecycle methods; defaults). Verify: `uv run pytest -m unit` passes.
- [x] 10.4 Unit: `tests/unit/test_persistence_adapter.py` — update `Task` constructions: replace `Task(task_id=0, ...)` → `NewTask(label=..., context=..., status=...)` for insert inputs; update `insert` assertions to expect a returned `Task` carrying a `TaskId`; update `get`/`update_status`/`list_by_jobs`/`list_ids_by_ip_and_status` calls to pass/expect `TaskId`. Verify: `uv run pytest -m unit` passes.
- [x] 10.5 Integration: `tests/integration/test_persistence_adapter.py` and `tests/integration/test_db_integration.py` — replace the ~30+ `Task(task_id=0, ...)` constructions with `NewTask(...)`; update `insert` calls to `insert(NewTask(...))` and assert the returned `Task` carries a `TaskId`; update `get(TaskId(n))`, `update_status(TaskId(n), ...)`, `list_by_jobs([TaskId(n)])`, `list_ids_by_ip_and_status` assertions to expect `TaskId`. Verify: `uv run pytest -m integration` passes (testcontainer Postgres; no migration needed — task_id PK already exists).
- [x] 10.6 Integration: `tests/integration/test_task_row_not_found.py` — update `TaskRowNotFoundError` assertions: `save(task)` with a non-existent `task.task_id: TaskId` raises; `update_status(TaskId(99999), ...)` raises; assert `e.task_id == TaskId(99999)`. Verify: `uv run pytest -m integration` passes.
- [x] 10.7 Unit: `tests/unit/test_consume_task.py`, `tests/unit/test_allocate_task.py`, `tests/unit/test_abandon_node.py`, `tests/unit/test_allocate_task_failure_modes.py` — update `allocate_task`/`consume_task` entry `task_id` args from `int` → `TaskId(...)`; update `tracker.add`/`discard` calls to use `TaskId`; update event assertions to `event.task_id == TaskId(...)`. Verify: `uv run pytest -m unit` passes.
- [x] 10.8 Unit: `tests/unit/test_application_events.py`, `tests/unit/test_di.py` — update event/`with_event` assertions to expect `event.task_id: TaskId`. Verify: `uv run pytest -m unit` passes.
- [x] 10.9 Unit: `tests/unit/test_webhook_handler.py` — update event constructions to `TaskId(42)`; assert `payload.task_id == 42` (bare int, the `.value`); assert `asdict(payload)["task_id"] == 42` (NOT `{"value": 42}`). Verify: `uv run pytest -m unit` passes.
- [x] 10.10 Unit: `tests/unit/test_domain_ports.py` — update `TaskRepository` Protocol structural-subtype assertions for the new signatures (`insert(NewTask) -> Task`, `get(TaskId)`, etc.). Verify: `uv run pytest -m unit` passes.
- [x] 10.11 E2E: `tests/e2e/` — update any e2e that submits tasks / reads task status / asserts webhook payloads: `submit_task` returns `TaskId` (the facade `queue_submit_task` still returns `int`, so e2e through the facade is unaffected; but direct-use-case e2e needs `TaskId`). Verify: `uv run pytest -m e2e` passes.
- [x] 10.12 Audit `tests/` for any remaining `Task(task_id=0` or `task_id=42` (bare int in event/exception/tracker/repo contexts) and update to `NewTask(...)` / `TaskId(42)` as appropriate. Verify: `rg -n "task_id=0|task_id=42" tests/` returns only legit int-dict-assertion sites (e.g., `{"task_id": 42}` in facade-dict assertions).

## 11. GRACE-lite knowledge graph + final validation

- [x] 11.1 Update `docs/knowledge-graph.xml`: in `M-DOMAIN-MODEL` `<annotations>` add `type-TaskId PURPOSE="Task primary-key value object (frozen dataclass wrapping int)"` and `type-NewTask PURPOSE="Pre-persistence task record (no task_id)"`; update the existing `class-Task` annotation PURPOSE to mention it now carries `task_id: TaskId`. In `M-DOMAIN-EVENTS` update `class-DomainEvent` (task_id: TaskId). In `M-DOMAIN-EXCEPTIONS` update the task-keyed exception annotations if they reference `task_id`. In `M-DOMAIN-PORTS` update `TaskRepository` annotations (insert takes NewTask; get/update_status take TaskId). Verify: `python3 scripts/grace_check.py` passes.
- [x] 11.2 Run static checks: `uv run zuban check`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run lint-imports`. Verify: all pass.
- [x] 11.3 Run spec validation: `openspec validate --all --json`. Verify: exit 0.
- [x] 11.4 Run GRACE-lite validation: `python3 scripts/grace_check.py --json`. Verify: exit 0.
- [x] 11.5 Run the test matrix: `uv run pytest -m unit`, `uv run pytest -m integration`, `uv run pytest -m e2e`. Verify: all pass.