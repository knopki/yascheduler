## 1. Create domain/engine.py (merge + relocate)

- [x] 1.1 Create `yascheduler/domain/engine.py` with GRACE-lite MODULE_CONTRACT, MODULE_MAP, CHANGE_SUMMARY (DEPENDS: M-SHARED; LINKS: M-DOMAIN-MODEL, M-PLATFORM-LINUX, M-PLATFORM-WINDOWS, M-SSH-GATEWAY, M-CLOUD-PROVISIONER, M-APPLICATION-*, M-ENTRYPOINTS-DI)
- [x] 1.2 Define frozen `@dataclass(frozen=True)` `LocalFilesDeploy(files: tuple[PurePath, ...] = ())`, `LocalArchiveDeploy(file: PurePath)`, `RemoteArchiveDeploy(url: str)` — copy field shape from `config/engine.py:87-106`; `Deploy = Union[LocalFilesDeploy, LocalArchiveDeploy, RemoteArchiveDeploy]`
- [x] 1.3 Define frozen `@dataclass(frozen=True)` `Engine` with the 11 fields from design D1: `name: str`, `spawn: str`, `input_files: tuple[str, ...] = ()`, `output_files: tuple[str, ...] = ()`, `platforms: tuple[str, ...] = ()`, `check_cmd: str | None = None`, `check_pname: str | None = None`, `deployable: tuple[Deploy, ...] = ()`, `platform_packages: tuple[str, ...] = ()`, `check_cmd_code: int = 0`, `sleep_interval: int = 10`. No `from_config_parser_section`, no `get_valid_config_parser_fields`. Use `from __future__ import annotations` + `TYPE_CHECKING` guard for `TaskContext` import (avoid circular: `Engine.validate_inputs` references `TaskContext` from `domain/model.py`).
- [x] 1.4 Migrate `Engine.validate_inputs(ctx: TaskContext) -> None` from `domain/model.py:175-179` (raises `MissingInputFileError` from `domain/exceptions.py`)
- [x] 1.5 Define frozen `@dataclass(frozen=True)` `EngineRepository` with single field `data: Mapping[str, Engine] = field(default_factory=dict)` and the 7 methods from design D3 (`get`, `__getitem__`, `__contains__`, `values`, `filter`, `filter_platforms`, `get_platform_packages`). No `UserDict` inheritance, no `__hash__`, no `engines_dir`. `filter` returns `EngineRepository(data={k: v for k, v in self.data.items() if fn(v)})`.
- [x] 1.6 Update `yascheduler/domain/model.py`: remove the old 7-field `Engine` class (lines 155-179); replace with `from .engine import Engine, EngineRepository, LocalFilesDeploy, LocalArchiveDeploy, RemoteArchiveDeploy, Deploy` re-export so `from yascheduler.domain.model import Engine` and existing `test_domain_model.py` / `test_domain_services.py` imports continue to resolve. Add `EngineRepository` and `Deploy*` to the re-export. Update MODULE_MAP (note re-export from `M-DOMAIN-ENGINE`) and CHANGE_SUMMARY.
- [x] 1.7 Update `yascheduler/domain/__init__.py` `__all__` and imports: add `EngineRepository`, `LocalFilesDeploy`, `LocalArchiveDeploy`, `RemoteArchiveDeploy`, `Deploy` to `__all__`; add `from .engine import (...)` block. `Engine` is already re-exported via `.model`. Update MODULE_MAP and CHANGE_SUMMARY.
- [x] 1.8 Verify `uv run python -c "from yascheduler.domain import Engine, EngineRepository, Deploy, LocalFilesDeploy, LocalArchiveDeploy, RemoteArchiveDeploy"` and `from yascheduler.domain.model import Engine, EngineRepository` both resolve.

## 2. Create entrypoints/config_parser.py (parser extraction)

- [x] 2.1 Create `yascheduler/entrypoints/config_parser.py` with GRACE-lite MODULE_CONTRACT, MODULE_MAP, CHANGE_SUMMARY (DEPENDS: M-DOMAIN-ENGINE, M-CONFIG-UTILS; LINKS: M-ENTRYPOINTS-DI, M-CONFIG)
- [x] 2.2 Move `_check_spawn`, `_check_check_`, `_check_at_least_one_elem` validators from `config/engine.py:43-84` into `config_parser.py` as module-private functions (prefix `_`); adapt signatures to take `Engine` instance + value (same logic)
- [x] 2.3 Implement `parse_engine_section(sec: SectionProxy, engines_dir: PurePath) -> Engine`: copy logic from `config/engine.py:186-229` (warn_unknown_fields, gettuple helper, deploy alias resolution, validators called inline raising ValueError). Returns a frozen `Engine`.
- [x] 2.4 Implement `engine_valid_fields() -> Sequence[str]`: copy logic from `config/engine.py:164-177` (exclude `name`/`deployable`, add deploy aliases). Use `dataclasses.fields(Engine)` instead of `attrs.fields`.
- [x] 2.5 Implement `parse_engines(cfg: ConfigParser, engines_dir: PurePath) -> EngineRepository`: iterate `engine.*` sections, call `parse_engine_section`, build `EngineRepository(data={...})`. No `engines_dir` on the result.
- [x] 2.6 Import `warn_unknown_fields` from `yascheduler.config.utils` (stays there per design D2). Do NOT delete `make_default_field` from `config/utils.py` (consumers remain in remote/db/cloud).
- [x] 2.7 Verify `uv run python -c "from yascheduler.entrypoints.config_parser import parse_engine_section, parse_engines, engine_valid_fields"` resolves.

## 3. Delete config engine modules + update config facade

- [x] 3.1 Delete `yascheduler/config/engine.py`
- [x] 3.2 Delete `yascheduler/config/engine_repository.py`
- [x] 3.3 Update `yascheduler/config/__init__.py`: remove `Engine`, `EngineRepository`, `Deploy`, `LocalFilesDeploy`, `LocalArchiveDeploy`, `RemoteArchiveDeploy` from `__all__` and the `from .engine import (...)` / `from .engine_repository import (...)` blocks. Keep `AzureImageReference`, `Config`, `ConfigDb`, `ConfigLocal`, `ConfigRemote`, `ConfigCloud*` re-exports.
- [x] 3.4 Update `yascheduler/config/config.py` `Config.from_config_parser` engine assembly: replace `EngineRepository.from_config_parser(cfg, engines_dir)` call with `parse_engines(cfg, engines_dir)` imported from `yascheduler.entrypoints.config_parser`. Note: `entrypoints` importing into `config` is R3-illegal at module level — use a lazy import inside `from_config_parser` method OR move the engine-assembly call to `entrypoints/di.py` (preferred — di.py is in entrypoints layer). Decide based on whether `Config` aggregate needs `engines` populated at construction (it does, per existing contract) — if so, the lazy import inside `from_config_parser` is the pragmatic choice with a TODO for P4 (when `Config` moves to entrypoints and the import becomes intra-package). Document the decision in CHANGE_SUMMARY.
- [x] 3.5 Update `CHANGE_SUMMARY` in `config/config.py` and `config/__init__.py` with the engine-to-domain migration note.

## 4. Delete PEngine/PEngineRepository Protocols + update infra imports

- [x] 4.1 Update `yascheduler/infra/ssh/platform/protocol.py`: delete `PEngine` class (lines 111-120) and `PEngineRepository` class (lines 123-134); change `from yascheduler.config import (LocalArchiveDeploy, LocalFilesDeploy, RemoteArchiveDeploy)` to `from yascheduler.domain import (...)`. Update `SetupNodeCallable.__call__` (line 215) which references `PEngineRepository` in its signature — replace with `EngineRepository` imported under `TYPE_CHECKING` from `yascheduler.domain` (add a `TYPE_CHECKING` import block if not present). Update MODULE_CONTRACT DEPENDS (drop M-CONFIG-ENGINE, add M-DOMAIN-ENGINE) and MODULE_MAP (remove PEngine/PEngineRepository entries).
- [x] 4.2 Update `yascheduler/infra/ssh/platform/linux.py:40,43`: switch `from yascheduler.config import LocalArchiveDeploy, LocalFilesDeploy, RemoteArchiveDeploy` → `from yascheduler.domain import LocalArchiveDeploy, LocalFilesDeploy, RemoteArchiveDeploy`; switch `from .protocol import ... PEngineRepository ...` → `from yascheduler.domain import EngineRepository` (TYPE_CHECKING-safe if protocol.py re-exports it, but direct domain import is cleaner). Change all `engines: PEngineRepository` type hints (linux.py:250,307,327) to `engines: EngineRepository`. Update GRACE-lite function contracts that mention `PEngineRepository` to `EngineRepository`. Update MODULE_CONTRACT DEPENDS/LINKS.
- [x] 4.3 Update `yascheduler/infra/ssh/platform/windows.py:42,45`: same import + type-hint migration as 4.2 (windows.py:286,329 use `PEngineRepository`).
- [x] 4.4 Update `yascheduler/infra/ssh/platform/__init__.py`: remove `PEngine` and `PEngineRepository` from the `.protocol` re-export block (lines 75-76, 149-150 reference these names). Verify no other re-exports depend on the deleted Protocols.
- [x] 4.5 Update `yascheduler/infra/ssh/gateway.py:828`: `engines.filter_platforms(...)` call is unchanged (method preserved); verify any `PEngineRepository` type hint in the file switches to `EngineRepository` from `yascheduler.domain` (grep for `PEngineRepository` in gateway.py).
- [x] 4.6 Update `yascheduler/infra/cloud/manager.py:52`: TYPE_CHECKING import `EngineRepository` from `yascheduler.domain` (currently `from yascheduler.config import ... EngineRepository`). Update MODULE_CONTRACT DEPENDS/LINKS.

## 5. Update application + entrypoints imports

- [x] 5.1 Update `yascheduler/application/allocate_task.py:48`: TYPE_CHECKING `from yascheduler.config import Engine, EngineRepository` → `from yascheduler.domain import Engine, EngineRepository`. Update MODULE_CONTRACT DEPENDS (drop M-CONFIG-ENGINE-REPO, add M-DOMAIN-ENGINE).
- [x] 5.2 Update `yascheduler/application/consume_task.py:34`: TYPE_CHECKING `EngineRepository` → `from yascheduler.domain import EngineRepository`. Update MODULE_CONTRACT.
- [x] 5.3 Update `yascheduler/application/submit_task.py:38`: TYPE_CHECKING `EngineRepository` → `from yascheduler.domain import EngineRepository`. Update MODULE_CONTRACT.
- [x] 5.4 Update `yascheduler/application/orchestrator.py:54`: TYPE_CHECKING — split: `Config, ConfigCloud` stay from `yascheduler.config`; `EngineRepository` → `from yascheduler.domain import EngineRepository`. Update MODULE_CONTRACT.
- [x] 5.5 Update `yascheduler/entrypoints/di.py:59`: TYPE_CHECKING — `Config, ConfigCloud` stay from `yascheduler.config`; `EngineRepository` → `from yascheduler.domain import EngineRepository`. Update MODULE_CONTRACT.
- [x] 5.6 Update `yascheduler/entrypoints/cli/submit.py:36`: runtime import `from yascheduler.config import Config, Engine` → `from yascheduler.config import Config` + `from yascheduler.domain import Engine`. Update MODULE_CONTRACT.

## 6. Update knowledge graph

- [x] 6.1 Add `M-DOMAIN-ENGINE` element to `docs/knowledge-graph.xml` (TYPE=CORE_LOGIC, STATUS=implemented): `<purpose>Engine value object, EngineRepository collection, Deploy strategies</purpose>`, `<path>src/yascheduler/domain/engine.py</path>`, `<depends>M-SHARED</depends>`, annotations `fn-validate_inputs`, `class-Engine`, `class-EngineRepository`, `type-Deploy`, `class-LocalFilesDeploy`, `class-LocalArchiveDeploy`, `class-RemoteArchiveDeploy`.
- [x] 6.2 Remove `M-CONFIG-ENGINE` and `M-CONFIG-ENGINE-REPO` elements from `docs/knowledge-graph.xml`.
- [x] 6.3 Update CrossLinks: repoint edges from `M-PLATFORM-LINUX`, `M-PLATFORM-WINDOWS`, `M-SSH-GATEWAY`, `M-CLOUD-PROVISIONER`, `M-APPLICATION-ALLOCATE-TASK`, `M-APPLICATION-CONSUME-TASK`, `M-APPLICATION-SUBMIT-TASK`, `M-APPLICATION-ORCHESTRATOR`, `M-ENTRYPOINTS-DI` that previously targeted `M-CONFIG-ENGINE`/`M-CONFIG-ENGINE-REPO` → now target `M-DOMAIN-ENGINE`.
- [x] 6.4 Update `M-DOMAIN-MODEL` annotation: `Engine` PURPOSE unchanged but note re-export from `M-DOMAIN-ENGINE`; add `EngineRepository` re-export note.

## 7. Migrate tests — config.Engine mutation → domain.Engine constructor

- [x] 7.1 `tests/unit/test_cli_behavioral.py:47-51`: replace `engine.name = "g09"; engine.spawn = "run.sh"; engine.input_files = ("input",); engine.output_files = ("OUTPUT",); engine.platforms = ("linux",)` with `engine = Engine(name="g09", spawn="run.sh", input_files=("input",), output_files=("OUTPUT",), platforms=("linux",))`. Change import `from yascheduler.config import Engine, EngineRepository` → `from yascheduler.domain import Engine, EngineRepository`.
- [x] 7.2 `tests/unit/test_cli_submit.py:52-56`: same migration as 7.1. Import switch.
- [x] 7.3 `tests/unit/test_cli_show_nodes.py:52-56`: same migration as 7.1. Import switch.
- [x] 7.4 `tests/unit/test_cli_check_status.py:76-77`: `engine.name = "g09"; engine.spawn = "run.sh"` → `engine = Engine(name="g09", spawn="run.sh", check_cmd=..., check_pname=..., input_files=..., output_files=..., platforms=...)` (check the full field set the test needs). Import switch.
- [x] 7.5 `tests/unit/test_cli_manage_node.py:50`: `engine.name = "g09"` → `engine = Engine(name="g09", spawn=..., ...)` (audit what fields downstream test assertions access). Import switch.
- [x] 7.6 `tests/unit/test_ssh_gateway.py:42,233,234`: remove `PEngine` import (line 42); replace `MagicMock(spec=PEngine)` (line 233) with `Engine(name="test_engine", spawn=..., ...)` or `MagicMock(spec=Engine)`; replace `engine.name = "test_engine"` (line 234) with full `Engine(...)` constructor. Import switch: add `Engine` from `yascheduler.domain`.
- [x] 7.7 `tests/integration/test_ssh_gateway.py:38,300-302`: remove `PEngine` import (line 38); replace `MagicMock(spec=PEngine)` (line 301) with `Engine(name="test_engine", ...)` or `MagicMock(spec=Engine)`; replace `engine.name = "test_engine"` (line 302) with full `Engine(...)` constructor. Import switch.
- [x] 7.8 `tests/unit/test_cli_submit.py:354, 365`: `engine.input_files = (...)` → use `dataclasses.replace(engine, input_files=(...))` or full constructor.
- [x] 7.9 `tests/unit/conftest.py:48`: verify the `Engine(...)` factory fixture uses a full constructor compatible with the merged 11-field class (the 4 added fields have defaults, so existing kwargs-only construction should work — verify, fix only if needed). Import switch.

## 8. Migrate tests — PEngineRepository → EngineRepository + spec audit

- [x] 8.1 `tests/unit/test_ssh_gateway.py:908, 969`: `MagicMock(spec=PEngineRepository)` → `MagicMock(spec=EngineRepository)` imported from `yascheduler.domain`. Update import: drop `PEngineRepository` from `infra.ssh.platform.protocol`, add `EngineRepository` from `yascheduler.domain`.
- [x] 8.2 Grep all ~15 `MagicMock(spec=EngineRepository)` sites in `tests/unit/` for `UserDict`-inherited method usage: search for `.items(`, `.keys(`, `len(`, `for ... in <engine_repo_mock_var>` on those mock variables. Any hit → either add the method to `EngineRepository` (if production needs it) or migrate the test to use `.values()` / explicit dict access. Document findings in the change review.
- [x] 8.3 `tests/unit/test_config.py`: multi-site migration:
  - `Engine(...)` constructor calls (lines 338, 356, 364, 388, 397, 421) using the OLD config.Engine — verify they work with the merged domain.Engine (11 fields, 4 with defaults). Import switch from `yascheduler.config.engine` → `yascheduler.domain`.
  - `test_engine_empty_input_files` (line 335): this test currently asserts `Engine(name=..., input_files=())` raises `ValueError` via the attrs `_check_at_least_one_elem` validator. After P2 the domain `Engine` has NO parser-side validation, so direct construction succeeds silently. Migrate this test to assert `parse_engine_section(cfg["engine.x"], engines_dir)` raises `ValueError` for an `[engine.x]` section with `input_files` unset or empty (parser-side `_check_at_least_one_elem`).
  - `EngineRepository(data=..., engines_dir=...)` constructor calls (lines 372, 406, 429): drop the `engines_dir` kwarg (does not exist on the domain `EngineRepository`); use `EngineRepository(data={"a": e1, "b": e2})`.
  - `test_engine_repository_immutable` (line 419): currently asserts `repo["b"] = e1` and `del repo["a"]` raise `NotImplementedError` (from the neutralized `UserDict.__setitem__`/`__delitem__`). After P2 there is no `__setitem__`/`__delitem__` (no `UserDict` inheritance) — these raise `TypeError: 'EngineRepository' object does not support item assignment/deletion`. Update the `pytest.raises(...)` assertion from `NotImplementedError` to `TypeError`.
  - Any `Engine.from_config_parser_section(...)` or `EngineRepository.from_config_parser(...)` direct calls migrate to `parse_engine_section(...)` / `parse_engines(...)` from `yascheduler.entrypoints.config_parser`.
- [x] 8.4 `tests/unit/test_domain_services.py`: verify `Engine(name="fleur", spawn="fleur_MPI", platforms=("linux",))` still constructs (4 added fields default). Verify `filter_platforms` returns a new `EngineRepository` (now frozen dataclass, not UserDict) — adjust assertions if they checked `isinstance(filtered, EngineRepository)` (still True) or `UserDict` (now False — remove that assertion if present).
- [x] 8.5 `tests/unit/test_domain_model.py:247, 252, 260`: verify `Engine(name=..., spawn=..., input_files=...)` constructions still work with the merged class. Verify `validate_inputs` behavior unchanged.

## 9. Verification

- [x] 9.1 Run `uv run pytest -m unit` — all unit tests pass (target: zero failures, zero errors). Pay attention to `test_config.py`, `test_domain_services.py`, `test_domain_model.py`, `test_application_*`, `test_cli_*`, `test_ssh_gateway.py`, `test_allocate_task_failure_modes.py`.
- [x] 9.2 Run `uv run ruff check .` — clean.
- [x] 9.3 Run `uv run ruff format --check .` — clean.
- [x] 9.4 Run `uv run lint-imports` — layers contract passes. Verify no new `application → infra` or `application → config` runtime edges introduced; the TYPE_CHECKING imports of `EngineRepository` from `yascheduler.domain` are excluded from the contract (`exclude_type_checking_imports = true`).
- [x] 9.5 Run `python3 scripts/grace_check.py` — XML + source checks pass (new `M-DOMAIN-ENGINE` element valid; `M-CONFIG-ENGINE`/`M-CONFIG-ENGINE-REPO` removed cleanly; all CrossLinks point at existing M-IDs).
- [x] 9.6 Run `openspec validate --all --json` — spec validation passes (delta specs match MODIFIED requirement headings; new `domain-engine-types` capability created).
- [x] 9.7 Grep `from yascheduler.config import.*Engine\|from yascheduler.config.engine\|from yascheduler.config.engine_repository` in `yascheduler/` and `tests/` — zero matches (all migrated to `yascheduler.domain` or `yascheduler.entrypoints.config_parser`).
- [x] 9.8 Grep `PEngine\|PEngineRepository` in `yascheduler/` AND `tests/` — zero matches (Protocols deleted, all references migrated).
- [x] 9.9 Grep `from_config_parser_section\|get_valid_config_parser_fields` in `yascheduler/domain/` — zero matches (parser methods removed from value objects).
- [x] 9.10 Grep `attrs` in `yascheduler/config/engine.py` and `yascheduler/config/engine_repository.py` — both files deleted, so grep returns "No such file" (confirm via `ls` that the files are gone).