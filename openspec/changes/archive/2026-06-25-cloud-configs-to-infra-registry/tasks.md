## 1. Create infra/cloud/cloud_configs.py (DTO relocation)

- [x] 1.1 Create `yascheduler/infra/cloud/cloud_configs.py` with GRACE-lite MODULE_CONTRACT (PURPOSE: Cloud provider config DTOs + ConfigCloud union; SCOPE: ConfigCloudAzure, ConfigCloudHetzner, ConfigCloudUpcloud, ConfigCloudVastAI, AzureImageReference; DEPENDS: M-SHARED; LINKS: M-CLOUD-PROTOCOLS, M-ENTRYPOINTS-CONFIG-PARSER), MODULE_MAP, CHANGE_SUMMARY (LAST_CHANGE: v1.0.0 - Relocate cloud config DTOs from yascheduler/config/cloud.py; frozen stdlib dataclasses; no parser methods (cloud-configs-to-infra-registry)).
- [x] 1.2 Define `@dataclass(frozen=True) AzureImageReference` with fields `publisher: str = "Debian"`, `offer: str = "debian-11-daily"`, `sku: str = "11-backports-gen2"`, `version: str = "latest"`. Retain the `from_urn(cls, urn: str) -> Self` classmethod (pure URN parser, no INI dependency). Import `Self` from `yascheduler.shared`.
- [x] 1.3 Define `@dataclass(frozen=True) ConfigCloudAzure` with the 17 fields from `config/cloud.py:78-98` (tenant_id, client_id, client_secret, subscription_id, resource_group, location, vnet, subnet, nsg, vm_image, vm_size, max_nodes, username, priority, idle_tolerance, jump_username, jump_host). Class attr `prefix = "az"`. Drop `from_config_parser_section`, `get_valid_config_parser_fields`. Drop all attrs validators/`make_default_field` — plain field annotations with defaults. Validation moves to the parser (task group 2).
- [x] 1.4 Define `@dataclass(frozen=True) ConfigCloudHetzner` with the 12 fields from `config/cloud.py:168-181`. Class attr `prefix = "hetzner"`. Drop parser methods.
- [x] 1.5 Define `@dataclass(frozen=True) ConfigCloudUpcloud` with the 8 fields from `config/cloud.py:238-249`. Class attr `prefix = "upcloud"`. Drop parser methods.
- [x] 1.6 Define `@dataclass(frozen=True) ConfigCloudVastAI` with the 15 fields from `config/cloud.py:304-326`. Class attr `prefix = "vastai"`. Drop parser methods.
- [x] 1.7 Define `ConfigCloud = Union[ConfigCloudAzure, ConfigCloudHetzner, ConfigCloudUpcloud, ConfigCloudVastAI]`.
- [x] 1.8 Verify `uv run python -c "from yascheduler.infra.cloud.cloud_configs import ConfigCloud, ConfigCloudAzure, ConfigCloudHetzner, ConfigCloudUpcloud, ConfigCloudVastAI, AzureImageReference"` resolves and all are `@dataclass(frozen=True)` (check `dataclasses.is_dataclass` and `getattr(cls, "__dataclass_fields__")`).

## 2. Add cloud parsers + registry to entrypoints/config_parser.py

- [x] 2.1 If `entrypoints/config_parser.py` does not exist (P2 not yet implemented), create it with GRACE-lite MODULE_CONTRACT (PURPOSE: INI config parsing — adapter layer between ConfigParser and domain/infra types; SCOPE: parse_engine_section, parse_engines, engine_valid_fields (P2), parse_cloud_section, parse_clouds, cloud_valid_fields, CLOUD_CONFIG_PARSERS (P3); DEPENDS: M-DOMAIN-ENGINE, M-CLOUD-CONFIGS, M-CONFIG-UTILS; LINKS: M-ENTRYPOINTS-DI, M-CONFIG). If P2 already created it, extend it — do not duplicate the MODULE_CONTRACT.
- [x] 2.2 Move `_check_az_user(instance, value)` validator from `config/cloud.py:40-42` into `config_parser.py` as a module-private function (signature adapted: takes the would-be DTO fields, raises `ValueError` if `username == "root"`; the parser calls it before constructing `ConfigCloudAzure`).
- [x] 2.3 Move `_fmt_key(prefix, name)` helper from `config/cloud.py:45-46` into `config_parser.py` as a module-private function.
- [x] 2.4 Implement `_parse_azure_section(sec: SectionProxy) -> ConfigCloudAzure`: copy logic from `config/cloud.py:126-164` (warn_unknown_fields across all 4 providers' valid fields, `_fmt_key` prefixing, `AzureImageReference.from_urn` for `az_image`, `_check_az_user` validation on the username, construct frozen dataclass). Call `warn_unknown_fields([...all 4 valid field lists...], sec)`.
- [x] 2.5 Implement `_parse_hetzner_section(sec: SectionProxy) -> ConfigCloudHetzner`: copy logic from `config/cloud.py:209-234`. Validators (`validators.ge(0)` for max_nodes, `validators.ge(1)` for idle_tolerance) run as inline `if value < N: raise ValueError(...)` checks before construction.
- [x] 2.6 Implement `_parse_upcloud_section(sec: SectionProxy) -> ConfigCloudUpcloud`: copy logic from `config/cloud.py:277-300`.
- [x] 2.7 Implement `_parse_vastai_section(sec: SectionProxy) -> ConfigCloudVastAI`: copy logic from `config/cloud.py:353-384`. Validators (`ge(1)` for disk_gb/min_vram_mb/num_gpus, `ge(0)` for max_price_per_hr/max_nodes, `ge(1)` for idle_tolerance) run as inline checks.
- [x] 2.8 Implement `cloud_valid_fields(prefix: str) -> Sequence[str]`: dispatch on prefix to the per-provider field list (exclude `prefix`/`username`/`jump_username` + provider-specific excludes; include `user`/`jump_user` aliases + provider-specific includes like `image`/`size` for Azure). Use `dataclasses.fields(ConfigCloudX)` instead of `attrs.fields(cls)`.
- [x] 2.9 Define `CLOUD_CONFIG_PARSERS: dict[str, Callable[[SectionProxy], CloudConfig]] = {"az": _parse_azure_section, "hetzner": _parse_hetzner_section, "upcloud": _parse_upcloud_section, "vastai": _parse_vastai_section}`. Import `CloudConfig` from `yascheduler.domain` for the type annotation.
- [x] 2.10 Implement `parse_cloud_section(sec: SectionProxy, prefix: str) -> CloudConfig`: dispatch via `CLOUD_CONFIG_PARSERS[prefix](sec)`; raise `KeyError` on unknown prefix (the registry is the source of truth).
- [x] 2.11 Implement `parse_clouds(cfg: ConfigParser, remote: ConfigRemote) -> list[CloudConfig]`: derive `cloud_prefixes` from `[clouds]` options (split each option name on `_`, take first segment); inherit `remote.username` into `[clouds]` for any prefix whose `{prefix}_user` is absent; for each prefix in `cloud_prefixes` & `CLOUD_CONFIG_PARSERS.keys()`, call `CLOUD_CONFIG_PARSERS[prefix](cfg["clouds"])`; return the list. Import `ConfigRemote` from `yascheduler.config` under TYPE_CHECKING.
- [x] 2.12 Import `warn_unknown_fields` from `yascheduler.config.utils` (stays there until P4). Do NOT delete `make_default_field` / `opt_str_val` from `config/utils.py` (consumers remain in remote/db/local until P4).
- [x] 2.13 Verify `uv run python -c "from yascheduler.entrypoints.config_parser import parse_cloud_section, parse_clouds, cloud_valid_fields, CLOUD_CONFIG_PARSERS"` resolves.

## 3. Add CloudConfig Protocol to domain/ports.py

- [x] 3.1 Add `@runtime_checkable class CloudConfig(Protocol)` to `yascheduler/domain/ports.py` with fields `prefix: str`, `max_nodes: int`, `idle_tolerance: int`, `username: str`, `jump_username: str | None`, `jump_host: str | None`. Add a `START_CONTRACT: CloudConfig` block (PURPOSE: Structural contract for cloud provider config — the 6-field surface application consumers read; LINKS: M-DOMAIN-PORTS, M-CLOUD-CONFIGS, M-APPLICATION-DEALLOCATE, M-APPLICATION-ORCHESTRATOR). Precedent: `OccupancyConfig` and `TaskExecutionEngine` already in the file.
- [x] 3.2 Update `yascheduler/domain/__init__.py` `__all__` and imports to include `CloudConfig` from `.ports`. Update MODULE_MAP and CHANGE_SUMMARY.
- [x] 3.3 Verify `uv run python -c "from yascheduler.domain import CloudConfig; from yascheduler.infra.cloud.cloud_configs import ConfigCloudAzure; assert isinstance(ConfigCloudAzure(tenant_id='t', client_id='c', client_secret='s', subscription_id='s'), CloudConfig)"` passes (structural satisfaction).

## 4. Update Config.from_config_parser cloud assembly (lazy import)

- [x] 4.1 Update `yascheduler/config/config.py`: delete the `cloud_variants` tuple (lines 85-90), the `cloud_variants_match` filter (92-94), the `clouds = map(...)` construction (96-99), and the username-inheritance loop (78-83). Replace with a lazy-imported `parse_clouds(cfg, remote)` call: `from yascheduler.entrypoints.config_parser import parse_clouds` (inside `from_config_parser` method body), `clouds = parse_clouds(cfg, remote)`. Add a TODO comment: "lazy import; P4 moves Config to entrypoints and this becomes intra-package".
- [x] 4.2 Update the `from .cloud import (...)` block in `config/config.py` (lines 32-38) — it is deleted entirely. Add replacement import `from yascheduler.infra.cloud import ConfigCloud` for the `Config.clouds: Sequence[ConfigCloud]` field annotation. Note in a comment: "config is outside-layer-set exempt until P4; this entrypoints→infra edge is legal under the current exemption".
- [x] 4.3 Update MODULE_CONTRACT DEPENDS (drop M-CONFIG-CLOUD, add M-CLOUD-CONFIGS, M-ENTRYPOINTS-CONFIG-PARSER) and CHANGE_SUMMARY (LAST_CHANGE: cloud assembly delegated to parse_clouds via lazy import; cloud_variants tuple removed; cloud-configs-to-infra-registry).
- [x] 4.4 Verify `uv run python -c "from yascheduler.config import Config; c = Config.from_config_parser('tests/fixtures/test_config.ini'); assert all(hasattr(x, 'prefix') for x in c.clouds)"` runs (the fixture path may need adjustment — use any existing test fixture or a minimal inline INI).

## 5. Delete config/cloud.py + update config facade

- [x] 5.1 Delete `yascheduler/config/cloud.py`.
- [x] 5.2 Update `yascheduler/config/__init__.py`: remove `AzureImageReference`, `ConfigCloud`, `ConfigCloudAzure`, `ConfigCloudHetzner`, `ConfigCloudUpcloud`, `ConfigCloudVastAI` from the `from .cloud import (...)` block (delete the block) and from `__all__`. Keep `Config`, `ConfigDb`, `ConfigLocal`, `ConfigRemote` re-exports (P4 moves them). If P2 is already implemented, `Engine`/`EngineRepository`/`Deploy*` are already gone; if not, they remain (P2 removes them).
- [x] 5.3 Update MODULE_CONTRACT SCOPE, MODULE_MAP, and CHANGE_SUMMARY in `config/__init__.py` to drop the cloud DTO re-exports. Update DEPENDS (drop M-CONFIG-CLOUD).

## 6. Update infra/cloud/__init__.py facade re-exports

- [x] 6.1 Update `yascheduler/infra/cloud/__init__.py`: add `from .cloud_configs import (AzureImageReference, ConfigCloud, ConfigCloudAzure, ConfigCloudHetzner, ConfigCloudUpcloud, ConfigCloudVastAI)` and add all six to `__all__`.
- [x] 6.2 Update MODULE_CONTRACT SCOPE, MODULE_MAP, and CHANGE_SUMMARY to include the cloud config DTO re-exports. Update DEPENDS (add M-CLOUD-CONFIGS).

## 7. Switch infra/cloud imports to intra-package / facade

- [x] 7.1 Update `yascheduler/infra/cloud/protocols.py:37`: `from yascheduler.config import ConfigCloud` → `from .cloud_configs import ConfigCloud` (intra-package, relative). The three `TypeVar(bound=ConfigCloud)` declarations (lines 41-45) are unchanged. Update MODULE_CONTRACT DEPENDS (drop M-CONFIG-CLOUD, add M-CLOUD-CONFIGS).
- [x] 7.2 Update `yascheduler/infra/cloud/adapters.py:40` (TYPE_CHECKING): `from yascheduler.config import ConfigCloud` → `from .cloud_configs import ConfigCloud` (intra-package, since adapters.py sits in `infra/cloud/`).
- [x] 7.3 Update `yascheduler/infra/cloud/provider_selection.py:27` (TYPE_CHECKING): `from yascheduler.config import ConfigCloud` → `from .cloud_configs import ConfigCloud`.
- [x] 7.4 Update `yascheduler/infra/cloud/manager.py:49-54` (TYPE_CHECKING): split the import — `ConfigCloud` → `from .cloud_configs import ConfigCloud`; `ConfigLocal`, `ConfigRemote` stay from `yascheduler.config` (P4 moves them); `EngineRepository` → if P2 done, `from yascheduler.domain import EngineRepository`; if P2 not done, stays from `yascheduler.config` (no-op for P3). Update MODULE_CONTRACT DEPENDS/LINKS accordingly.
- [x] 7.5 Update `yascheduler/infra/cloud/providers/az.py:84-86` (TYPE_CHECKING): `from yascheduler.config import AzureImageReference, ConfigCloudAzure` → `from yascheduler.infra.cloud import AzureImageReference, ConfigCloudAzure` (R2 facade — parent-traversal relative `from ..cloud_configs` is banned by R1, deep path violates R2).
- [x] 7.6 Update `yascheduler/infra/cloud/providers/hetzner.py:53` (TYPE_CHECKING): `from yascheduler.config import ConfigCloudHetzner` → `from yascheduler.infra.cloud import ConfigCloudHetzner`.
- [x] 7.7 Update `yascheduler/infra/cloud/providers/upcloud.py:48` (TYPE_CHECKING): `from yascheduler.config import ConfigCloudUpcloud` → `from yascheduler.infra.cloud import ConfigCloudUpcloud`.
- [x] 7.8 Update `yascheduler/infra/cloud/providers/vastai.py:48` (TYPE_CHECKING): `from yascheduler.config import ConfigCloudVastAI` → `from yascheduler.infra.cloud import ConfigCloudVastAI`.
- [x] 7.9 (Optional, design D7) Update `yascheduler/infra/cloud/manager.py::_connect_to_vm`: replace `getattr(config, "jump_host", None) or None` / `getattr(config, "jump_username", None) or None` with `config.jump_host or None` / `config.jump_username or None` (direct attribute access; all DTOs declare the fields). Update the function contract INPUTS note if it mentions the getattr fallback.

## 8. Switch application + entrypoints imports

- [x] 8.1 Update `yascheduler/application/deallocate_nodes.py:31` (TYPE_CHECKING): `from yascheduler.config import ConfigCloud` → `from yascheduler.domain import CloudConfig`. Update the `config_clouds: Sequence[ConfigCloud]` parameter type to `config_clouds: Sequence[CloudConfig]`. Update the `deallocate_nodes` contract INPUTS note and MODULE_CONTRACT DEPENDS (drop M-CONFIG-CLOUD, add M-DOMAIN-PORTS).
- [x] 8.2 Update `yascheduler/application/orchestrator.py:54` (TYPE_CHECKING): split — `ConfigCloud` → `from yascheduler.domain import CloudConfig`; `Config` stays from `yascheduler.config` (P4); `EngineRepository` → if P2 done, `from yascheduler.domain import EngineRepository`; if not, stays. Update `config_clouds: Sequence[ConfigCloud]` → `config_clouds: Sequence[CloudConfig]` and `active_clouds: Sequence[ConfigCloud]` → `active_clouds: Sequence[CloudConfig]` in the `__init__` signature and the contract INPUTS note.
- [x] 8.3 Update `yascheduler/entrypoints/di.py:61` (TYPE_CHECKING): split — `ConfigCloud` → `from yascheduler.domain import CloudConfig` (for `active_clouds: list[ConfigCloud]` typing → `list[CloudConfig]`); `Config` stays; `EngineRepository` → P2 path. Update the `make_daemon` contract INPUTS note. Update MODULE_CONTRACT DEPENDS: drop `M-CONFIG-CLOUD`, add `M-DOMAIN-PORTS` (the `CloudConfig` Protocol source).
- [x] 8.4 Grep `yascheduler/` and `tests/` for `from yascheduler.config import.*ConfigCloud\|from yascheduler.config.cloud import` — zero matches expected after this step (all migrated to `yascheduler.infra.cloud` or `yascheduler.domain`).

## 9. Update knowledge graph

- [x] 9.1 Add `M-CLOUD-CONFIGS` element to `docs/knowledge-graph.xml` (TYPE=DATA_LAYER, STATUS=implemented): `<purpose>Cloud provider config DTOs (Azure, Hetzner, UpCloud, VastAI) and the ConfigCloud union</purpose>`, `<path>src/yascheduler/infra/cloud/cloud_configs.py</path>`, `<depends>M-CLOUD-PROTOCOLS, M-SHARED</depends>`, annotations `class-ConfigCloudAzure`, `class-ConfigCloudHetzner`, `class-ConfigCloudUpcloud`, `class-ConfigCloudVastAI`, `class-AzureImageReference`, `type-ConfigCloud`.
- [x] 9.2 Remove `M-CONFIG-CLOUD` element from `docs/knowledge-graph.xml`.
- [x] 9.3 Add `protocol-CloudConfig PURPOSE="Structural contract for cloud provider config (prefix, max_nodes, idle_tolerance, username, jump_*)."` annotation to `M-DOMAIN-PORTS`.
- [x] 9.4 Update `M-ENTRYPOINTS-CONFIG-PARSER` (created by P2, or create if P2 not done): add annotations `const-CLOUD_CONFIG_PARSERS`, `fn-parse_cloud_section`, `fn-parse_clouds`, `fn-cloud_valid_fields`. Update `<depends>` to include `M-CLOUD-CONFIGS`.
- [x] 9.5 Repoint CrossLinks that targeted `M-CONFIG-CLOUD`: from `M-CLOUD-PROVISIONER`, `M-CLOUD-ADAPTERS-NEW`, `M-CLOUD-PROVIDER-SELECTION`, `M-CLOUD-PROVIDERS-AZ`, `M-CLOUD-PROVIDERS-HETZNER`, `M-CLOUD-PROVIDERS-UPCLOUD`, `M-CLOUD-PROVIDERS-VASTAI` → target `M-CLOUD-CONFIGS`. From `M-APPLICATION-DEALLOCATE`, `M-APPLICATION-ORCHESTRATOR`, `M-ENTRYPOINTS-DI` (where they reference cloud config typing) → target `M-DOMAIN-PORTS` (for the `CloudConfig` Protocol).
- [x] 9.6 Update `M-CONFIG` (`config/config.py`) DEPENDS: drop `M-CONFIG-CLOUD`; the cloud-assembly seam is now a lazy import from `M-ENTRYPOINTS-CONFIG-PARSER` (note in the module's LINKS).
- [x] 9.7 Audit and update `<depends>` elements of modules that previously depended on `M-CONFIG-CLOUD`: `M-CLOUD-PROTOCOLS`, `M-CLOUD-PROVISIONER`, `M-CLOUD-ADAPTERS-NEW`, `M-CLOUD-PROVIDERS-AZ`, `M-CLOUD-PROVIDERS-HETZNER`, `M-CLOUD-PROVIDERS-UPCLOUD`, `M-CLOUD-PROVIDERS-VASTAI`, `M-CONFIG-HUB`, `M-APPLICATION-DEALLOCATE`, `M-APPLICATION-ORCHESTRATOR` — replace `M-CONFIG-CLOUD` with `M-CLOUD-CONFIGS` (for infra DTO consumers) or `M-DOMAIN-PORTS` (for application Protocol consumers). Verify no `<depends>` list still references the removed `M-CONFIG-CLOUD`.

## 10. Migrate tests

- [x] 10.1 `tests/unit/test_config.py:46-58`: change `from yascheduler.config.cloud import (ConfigCloudAzure, ConfigCloudHetzner, ConfigCloudUpcloud, ConfigCloudVastAI, AzureImageReference)` → `from yascheduler.infra.cloud import (...)`. Keep `from yascheduler.config.config import Config`, `from yascheduler.config.db import ConfigDb`, `from yascheduler.config.local import ConfigLocal`, `from yascheduler.config.remote import ConfigRemote` (P4 moves them).
- [x] 10.2 `tests/unit/test_config.py` cloud-parsing tests: migrate `ConfigCloudX.from_config_parser_section(...)` direct calls to `parse_cloud_section(sec, "prefix")` or `parse_clouds(cfg, remote)` from `yascheduler.entrypoints.config_parser`. Assert the DTOs are `@dataclass(frozen=True)` with no `from_config_parser_section` attribute.
- [x] 10.3 `tests/unit/test_provider_selection.py:32`: `from yascheduler.config.cloud import ConfigCloud` → `from yascheduler.infra.cloud import ConfigCloud`.
- [x] 10.4 `tests/unit/test_application_use_cases.py:52`: `from yascheduler.config.cloud import ConfigCloudAzure` → `from yascheduler.infra.cloud import ConfigCloudAzure`.
- [x] 10.5 `tests/unit/test_di.py:31`: `from yascheduler.config import (...)` including `ConfigCloud` → split: `ConfigCloud` → `from yascheduler.infra.cloud import ConfigCloud`; other config symbols (`Config`, `ConfigDb`, `ConfigLocal`, `ConfigRemote`) stay from `yascheduler.config` until P4.
- [x] 10.6 `tests/unit/test_application_orchestrator.py:54`: `from yascheduler.config import (...)` including `ConfigCloud` → `ConfigCloud` → `from yascheduler.domain import CloudConfig` (TYPE_CHECKING). Audit `config_clouds`/`active_clouds` mock lists — they can stay as concrete DTOs (which satisfy `CloudConfig` structurally) or migrate to `MagicMock(spec=CloudConfig)`; prefer concrete DTOs for realistic behavior.
- [x] 10.7 Grep `tests/` for `MagicMock(spec=ConfigCloud)` and `MagicMock(spec=ConfigCloudAzure)` etc. — audit for reliance on `from_config_parser_section` or `get_valid_config_parser_fields` being on the spec. Any hit migrates to the parser functions. Document findings inline in the test file comments.
- [x] 10.8 Grep `tests/` for `from yascheduler.config import.*ConfigCloud\|from yascheduler.config.cloud` — zero matches expected (all migrated).

## 11. Verification

- [x] 11.1 Run `uv run pytest -m unit` — all unit tests pass (target: zero failures, zero errors). Pay attention to `test_config.py`, `test_provider_selection.py`, `test_application_use_cases.py`, `test_di.py`, `test_application_orchestrator.py`, `test_cloud_provisioner_impl.py`.
- [x] 11.2 Run `uv run pytest -m integration` — integration tests pass (the `Config.from_config_parser` path is exercised by `test_persistence_adapter.py`, `test_postgres_schema.py`, `test_client_query_integration.py`).
- [x] 11.3 Run `uv run ruff check .` — clean.
- [x] 11.4 Run `uv run ruff format --check .` — clean.
- [x] 11.5 Run `uv run lint-imports` — layers contract passes. Verify no new `application → infra` runtime edges; the TYPE_CHECKING `CloudConfig` import from `yascheduler.domain` is excluded (`exclude_type_checking_imports = true`). Verify the `config/config.py → yascheduler.infra.cloud.ConfigCloud` runtime edge is legal under the outside-layer-set exemption for `yascheduler.config` (still exempt until P4).
- [x] 11.6 Run `python3 scripts/grace_check.py` — XML + source checks pass (new `M-CLOUD-CONFIGS` element valid; `M-CONFIG-CLOUD` removed cleanly; all CrossLinks point at existing M-IDs; `protocol-CloudConfig` annotation on `M-DOMAIN-PORTS`).
- [x] 11.7 Run `openspec validate --all --json` — spec validation passes (delta specs match MODIFIED requirement headings; 3 new capability specs created; `cloud-config-dtos`, `cloud-config-protocol`, `cloud-config-parsers`).
- [x] 11.8 Grep `from yascheduler.config import.*ConfigCloud\|from yascheduler.config.cloud\|from yascheduler.config import.*AzureImageReference` in `yascheduler/` and `tests/` — zero matches (all migrated to `yascheduler.infra.cloud` or `yascheduler.domain`).
- [x] 11.9 Grep `from_config_parser_section\|get_valid_config_parser_fields` on `ConfigCloud*` in `yascheduler/` and `tests/` — zero matches on cloud DTOs (parser methods removed; only `parse_cloud_section`/`parse_clouds`/`cloud_valid_fields` remain, in `entrypoints/config_parser.py`).
- [x] 11.10 Grep `attrs` in `yascheduler/config/cloud.py` — file deleted, grep returns "No such file" (confirm via `ls yascheduler/config/` that `cloud.py` is gone).
- [x] 11.11 Verify `uv run python -c "from yascheduler.infra.cloud import ConfigCloud, ConfigCloudAzure, ConfigCloudHetzner, ConfigCloudUpcloud, ConfigCloudVastAI, AzureImageReference; from yascheduler.domain import CloudConfig; assert isinstance(ConfigCloudAzure(tenant_id='t', client_id='c', client_secret='s', subscription_id='s'), CloudConfig)"` passes.